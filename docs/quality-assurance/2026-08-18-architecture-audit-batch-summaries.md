## F1 (20건)
범위: src/app 14파일 552줄 + src/entities 26슬라이스 85파일 2,954줄(테스트 10개 포함) 전수 열람. 참조로 shared/api/bindings.ts·unwrap-result.ts·query-key.ts·i18n.ts·use-tauri-event.ts·eslint.config.js, 소비자 확인용으로 widgets 일부, 마커 수명 확인용으로 src-tauri/src/domain/agent 를 조회.

기계 검증 축 재확인 결과 0: FSD 역방향 import(eslint.config.js:6-11 의 no-restricted-imports 로 레이어별 강제)·barrel index.ts·any(no-explicit-any error)·enum·function 키워드·useCallback/useMemo(no-restricted-syntax) — 배치 범위 내 위반 0건.

렌즈별 총평.
L-FSD: 레이어 방향 자체는 깨끗하나 entities 의 성격이 '데이터 layer' 를 넘어섰다. commands 6종이 toast·i18next·widget 브리지를 들고 있고(주요 발견 5), use-search-run.ts 는 useState/useRef/toast 를 가진 오케스트레이션 훅, reveal-registry 는 editor.focus() 로 UI 를 직접 조작한다 — 셋 다 shared 브리지를 경유해 상향 의존을 우회하는 같은 뿌리다. 슬라이스 26개 중 데이터 전용(ipc+query)은 19개로 다수는 정상.
L-TYPE: bindings 파생 여지 전수 확인 결과, entities 의 수동 타입은 9개뿐이고 그중 7개는 레지스트리 로컬 클로저 타입(Listener·FlushFn 등)이라 파생 대상이 아니다. 실제 재작성은 SearchMatchRowData(발견 8)와 emptySettingsPatch 54필드(발견 6) 두 건이며, search.type.ts:3 은 이미 Required<Omit<>> 로 올바르게 유도하고 있어 규약 자체는 인지돼 있다. app.type.ts 는 파생이 아니라 dead 재export.
L-QUERY: queryOptions 팩토리·QUERY_KEY 중앙관리·키 계층은 전 슬라이스 준수(인라인 배열 키 0건). 판단이 필요한 무효화 책임 쪽에서 4건이 나왔다 — 과잉 무효화(3), 부분 정리(4), 캐시를 클라이언트 스토어로 전용(10·11), 쿼리 밖 읽기(2·18). setQueryData 사용은 대부분 '서버가 확정 상태를 반환' 하는 정당 케이스(layout·tree·sync·agent, agent.query.ts:56-60 에 acknowledge 근거 기재)로 확인해 지적에서 제외.
L-BRIDGE: 범위 내 모듈 스코프 레지스트리 9개. 등록/해제가 짝을 이루는 것은 editor-instance·mirror-flush·untitled 3개뿐이고, agent-wait-marker(1)·reveal(15)·open-with/claude-diff(16)는 정리 경로가 부분적이거나 없다. 창 간 탭 이동(useMoveTabToWindow)이 도입된 뒤에도 realm 로컬 Map 소유권이 그대로라는 점이 발견 1의 근본 원인.
L-EFFECT: useEffect 8개 전수 확인 — 파생 값 계산이나 이벤트 대체 용도는 0건. 유일한 문제는 effect 안 데이터 fetch(발견 2). useLayoutEffect 4개는 DOM/테마 동기화로 정당.
L-CODE: 매직넘버는 전부 상수화돼 있고(LOG_PAGE_SIZE·TREE_PAGE_LIMIT·REMOTE_STATUS_REFETCH_MS 등) 삼항 중첩 0건. 주석 규율은 JSDoc 위주로 잘 지켜졌고 예외 1건(발견 12).
L-A11Y: app/entities 에 실질 JSX 가 StatusErrorBanner 렌더 2곳뿐이라 판단 대상 거의 없음 — 위반 0.
L-I18N: 하드코딩 문자열은 locale-provider.tsx:8 의 fallback 상수 1건(의도적, 정당)과 발견 13의 누락 1건.

압축으로 제외한 저심각 항목 계수(총 9건): settings.query.ts:13,14,25 의 invalidateQueries 앞 void 누락 3건(다른 27곳은 void 사용, eslint 에 no-floating-promises 미설정이라 기계 검출 불가) / theme-provider.tsx:28,46 의 console.error 2건(common.md §11 로깅 경로 미정의) / plugin-grammar.ts:25 의 as 단언과 :28 의 delete 로 인한 파괴적 수정 2건 / remote.query.ts:22,34 가 set·clear 양쪽에 같은 메시지 키(remote.passwordSessionsRevoked)를 쓰고 clear 에만 onError 가 없는 비대칭 1건 / bootstrap-snippets.ts:23 의 QueryObserver.subscribe 가 해제되지 않는 앱 수명 구독 1건(문서화된 의도적 설계라 지적 제외).

## F2 (14건)
범위: src/features 15슬라이스 94파일(비테스트 88, 총 6,932줄) 전량 열람. HEAD=5f15a84.

기계 검증 축 재확인 결과 0: FSD 역방향 import(@widgets/@app/@pages) 0, barrel index.ts 0, any/enum 0, useCallback/useMemo 0, function 키워드 0, @ts-ignore·eslint-disable·@ts-expect-error 0, 비 JSDoc 코드 주석 0.

렌즈별 총평.
- L-FSD(최중요): 레이어 순수성은 대체로 지켜지나 이탈이 4갈래로 뚜렷하다 — (1) settings 3파일의 query/mutation 소유, (2) use-zen-mode 오케스트레이션 훅, (3) hwp-preview 의 전역 패치·wasm 싱글턴, (4) React 무관 순수 모듈 5종의 features 잔류(2종은 소비처가 widgets 뿐). 88파일 중 실제 이탈은 10파일로, 나머지 78파일은 props+콜백 계약을 정확히 지킨다(remote-section 17 props·lsp-server-status-list·keybinding-row 가 모범). 시드 9건 판정: settings 3건·use-zen-mode 1건 = widgets 승격 필요(위 findings 1·2), code-editor.tsx:8·ai-inline-edit.ts:8-9 의 entities .ipc 직접 호출 5건 = 2026-08-16-wave-g-ai-contract.md:171 이 '기존 선례와 동일한 FSD 합법 방향'으로 이미 기각한 사안이므로 재론하지 않음(단 ai-inline-edit.ts 372줄이 features 최대 파일이라는 사실만 부기).
- L-SFC: 1파일 1 export 기준 충족. 내부 헬퍼 컴포넌트를 둔 파일 5건(agent-hooks-project-list 3·lsp-server-status-list 3·theme-picker 2·search-match-row 2·zen-mode-hint 2)은 모두 export 1개라 규약 내. 다중 export 5건 중 dead 는 font-picker 1건뿐(finding 9). FC<Props> 패턴은 전 컴포넌트 준수.
- L-EFFECT: useEffect 사용 8파일 전부 외부 시스템 동기화(monaco·xterm·pdfjs·wasm·window 리스너·타이머)로 정당하며 cleanup 누락 0(terminal-view.tsx:274-293, diff-view.tsx:44-47, hwp-preview.tsx:104-106 의 URL.revokeObjectURL 까지 확인). 파생값 effect·이벤트 로직 effect 0건.
- L-A11Y: 최대 결함군. role='button' 행 4종 중 status-row-item 만 키보드 핸들러 전무(major), 나머지 3종+shared 1종은 Enter 만 처리(minor), 수제 color-picker 슬라이더 2개는 role/키보드 없음(minor). 반면 aria-label·aria-pressed·시맨틱 태그(ul/li/nav/button) 사용률은 높고, file-tree-row 의 role=treeitem 은 widgets/explorer/file-tree.tsx:277-280 의 role=tree+tabIndex+onKeyDown 컨테이너가 받쳐주므로 정상이다.
- L-I18N: 하드코딩 UI 문자열 0. 전 컴포넌트가 t() 경유이며, 예외는 브랜드명 'TAIDE'(welcome-screen.tsx:26), 코드 샘플 리터럴(theme-live-preview), aria-label 이 붙은 글리프 '×'(ai-inline-edit.ts:100-102), 표시용 URL 상수(sync-section.tsx:8) 뿐이다.
- L-CODE: 삼항 중첩·dead code·주석 위반 0. 남은 것은 매직넘버와 소규모 중복.
- L-TYPE: bindings 파생 타입 사용은 양호(GitStashEntry·ThemeSummary·LspServerDetection 등 직접 소비). 유일한 실질 이탈이 에이전트 로스터 수기 중복(finding 5).

압축으로 findings 에 싣지 않은 저심각 잔여 7건: (a) theme-live-preview.tsx:24 toCssVariables(...) as CSSProperties 캐스트 1건, (b) terminal-view.tsx:207,222 event as InputEvent 캐스트 2건(DOM 리스너 경계), (c) useTranslation 을 useState 보다 위에 두어 frontend.md §3.2 본문 순서와 어긋나는 파일 다수(약 20건 — 코드베이스 전역 관행이라 features 단독 이슈 아님), (d) audio-preview/video-preview 의 <track> 자막 트랙 부재 2건, (e) markdown-preview.tsx:33 dangerouslySetInnerHTML 은 상류에서 DOMPurify 통과(widgets/editor-pane/editor-pane.tsx:1042 renderMarkdownToSafeHtml)라 안전하나 prop 명 `html` 이 그 계약을 드러내지 않음, (f) code-editor.tsx:99-108·terminal-view.tsx:90-99 의 props→ref 미러링 effect(코드베이스에 useEffectEvent 채택 파일 10개와 ref 미러링 17건이 공존 — features 단독 이탈 아님), (g) spreadsheet-preview.tsx:31 렌더 중 동기 워크북 파싱(React Compiler 활성 확인 — vite.config.ts:10-20 — 이므로 재계산은 방지됨).

## F3 (18건)
배치 F3 범위: 9개 위젯 슬라이스 21파일 약 4,680 LOC(테스트 3파일 386 LOC 포함)를 전수 열람. 규모 편중이 심함 — editor-pane 슬라이스 2,585 LOC(그중 editor-pane.tsx 1,050 + lsp-session-registry.ts 505), editor-area 1,227 LOC 가 전체의 81%이고 나머지 7슬라이스는 각 48~196 LOC.

기계 검증 축(FSD 역방향 import·barrel·any/enum·useCallback/useMemo·function 키워드) 재확인 결과 범위 내 0건.

렌즈별 총평 — L-SFC: 소형 위젯(preview·commit-file-diff·diff-pane·app-file-pane)은 단일 책임·FC<Props>·본문 순서 모두 양호하나, editor-pane.tsx 가 8개 관심사를 한 본문에 누적한 상태이고 그 결합이 이번 critical 2건의 직접 원인. tab-context-menu/sortable-tab 은 순수 프롭 컴포넌트인데 widgets 에 남아 features 형제(TabItem)와 배치가 어긋남. L-EFFECT: 대부분의 useEffect 는 monaco 구독·IPC 등 외부 동기화로 정당하나, 타이머 3종(auto-save·preview·mirror) 중 auto-save·preview 만 path 를 deps 로 갖지 않아 경로 전환 경계를 넘김(critical 1건). L-QUERY: QUERY_KEY 중앙관리·queryOptions 팩토리 사용은 전반적으로 준수. 문제는 무효화 책임 배치(위젯 3곳)와 캐시 저수준 접근 2건이며, 미러 setQueryData 는 wave-b-hardening 계약 승인 사항이라 감사 대상에서 제외했다. L-BRIDGE: 이 배치가 만지는 모듈 스코프 싱글톤 7종(editor-instance·model·mirror-flush·reveal·open-with·untitled·lsp-session) 중 editor-instance-registry 가 키(tabId)와 등록 트리거(monaco 생성/파기)가 어긋나 실사용 결함을 만들고, lsp-session-registry 는 레이어 오배치로 우회 레지스트리 2개를 파생시켰다. 다중 창은 Rust 측 `find_reusable_entry`/owner 채널이 세션을 재사용하므로 LSP 프로세스 중복 문제는 없음(확인 완료, 무발견). L-TYPE: bindings 163타입 자체를 손으로 다시 적은 사례는 범위 내 없음. 남은 이슈는 `string → 리터럴 유니온` 단언 9곳(3파일 반복)뿐.

압축으로 제외한 저심각 관측 10건(계수만): editor-pane 의 draftRef 초기화가 렌더 단계 블록(173-186)과 이펙트(573-575)로 이원화되어 순서 의존 1건 / 타이머 언마운트 정리 이펙트 2개 분리(621·623) 1건 / editor-area 의 dnd-kit `data.current` 무검증 단언 4곳 / blame 푸터의 ref 직접 textContent 기입(826-832, 근거 doc 없음) 1건 / search-editor-pane 의 옵션 로컬 상태가 탭 kind 로 되돌아가지 않음 1건 / diff-pane 의 `fileQueryOptions(compareWith=null)` 비활성 쿼리 키가 빈 문자열로 등록 1건 / claude-diff-pane 이 미해결 상태로 탭이 닫힐 때 pending 레지스트리 항목 잔존 1건.

수정은 일절 하지 않았고 앱 실행도 없음(코드 정독·grep·Rust 소스 대조만).

## F4 (22건)
범위: src/widgets 셸·패널 계열 14 슬라이스 33파일 약 4,490줄(테스트 4파일 227줄 포함). 전 파일 정독. 기계 축은 재확인 결과 0 — FSD 역방향 import 0, barrel(index.ts) 0, any/enum 0, useCallback/useMemo 0, function 키워드 0(범위 내 grep). 렌즈별 총평 — L-BRIDGE/멀티윈도우: 브리지 자체(Set 리스너 16파일 동형 보일러플레이트)는 창별 모듈 스코프라 격리는 성립하나, 전역 IPC 구독의 소유권이 조건부 렌더되는 표시 위젯(StatusBarContent·AppSidebar)에 얹혀 있는 것이 이 배치 최대 결함이며 critical 1건이 여기서 나왔다. 보조 창 분기(app.tsx)는 계약상 deferred 항목이라 재론하지 않았다. L-EFFECT: effect 는 대체로 외부 동기화로 한정되어 있고 JSDoc 근거도 충실하나, LSP 심볼 로딩 2벌·terminal-pane 의 ref 미러 2건이 규약 밖. 시드 확인 결과 keydown 리스너 통합은 사실상 완료 — 잔여 2곳(use-zen-mode 의 bubble Escape, command-palette 의 runsViaCommand 캡처)은 모두 계약 문서에 근거가 명시된 의도적 예외라 지적하지 않았다. terminal-pane pty 구독 수명도 attach/detach 대칭과 stale 구독 방지가 성립해 결함 없음(체이닝 스타일만 지적). L-QUERY: queryOptions 팩토리·QUERY_KEY 계층은 잘 지켜지나 위젯이 mutation·캐시 쓰기를 직접 소유하는 경로 4건. L-SFC: 순수 프레젠테이션 위젯 4종의 레이어 강등, 그리고 300~400줄 3파일(git-panel·command-palette·explorer-container)의 비대화. L-CODE/L-A11Y/L-I18N: shared 유틸 재구현 4종이 가장 실질적이고, 나머지는 국소적. 압축으로 제외한 저심각 항목 6건 — outline-panel 의 index 포함 key, system-usage-modal 의 단위 문자열 하드코딩(%·MB), commit-detail-panel 의 컴포넌트 외 추가 export(테스트용), title-bar-content 와 auxiliary-title-bar-content 의 파생 로직 근사 중복, search-panel-container 의 useId 템플릿 리터럴 구성, git-panel 의 useTranslation 배치 순서 불일치.

## F5 (19건)
범위: src/widgets 설정·확장 계열 5개 디렉터리 15개 파일 2,629 LOC(settings-view 927 / snippet-editor 675[테스트 150 포함] / plugin-manager 490 / keybindings-editor 304 / theme-editor 233). 전 파일 실제 열람, 범위 밖(features/settings 25종·features/theme 7종·entities/theme·settings·snippet·remote query, app/app.tsx, shared/lib/keybindings-bridge·monaco-keybinding-runtime·keymap-context, src-tauri/domain/settings·locale)은 근거 확인용으로만 참조.

기계 검증 축 재확인 결과 0: FSD 역방향 import(@app/@pages) 0, barrel index.ts 0, any/enum 0, useCallback/useMemo 0, function 키워드 0, 코드 라인주석 0(모든 블록 주석은 JSDoc). 타입 단언은 스코프 전체에 1건(settings-view.tsx:214)뿐이며 그 1건을 major 로 보고했다. i18n 은 정적 t() 키 195개 전수를 locale 정본(src-tauri/src/domain/locale/service.rs)과 대조해 en/ko/ja 3종 모두 존재함을 확인했고, 동적 키(themeEditor.ns.*, PLUGIN_ERROR_MESSAGE_KEY, THEME_IMPORT_ITEM_FAILURE_KEY, prompts.*, aiProvider*)도 전수 확인해 누락 0 — L-I18N 결함은 하드코딩 2건(네이티브 대화상자 필터명, /bin/zsh)뿐이다.

렌즈별 총평 — L-SFC: 1파일 1컴포넌트·FC<Props> 는 전부 준수하나 settings-view 단일 컴포넌트 비대(927 LOC·12섹션·31훅·서브뷰 스위치 겸업)가 최대 부채. L-EFFECT: settings-view·plugin-manager·snippet-editor 는 useEffect 0(파생값 렌더 중 계산·렌더 단계 state 동기화의 올바른 패턴), theme-editor 2건·keybindings-editor 5건 전부 외부 시스템 동기화 용도로 목적 자체는 정당하나, 전역 부수효과(monaco 오버라이드 적용)를 다이얼로그가 소유한 점이 보조 창 미적용이라는 실버그로 이어진다(critical 1건). theme-editor 의 identity 의존 effect 는 babel-plugin-react-compiler 로 실제 컴파일해 현재는 메모이즈되어 정상임을 확인한 뒤 잠재 위험으로만 보고했다. L-CODE: 매직넘버는 대부분 상수화돼 있으나 기본값·범위 상수가 shared/Rust 정본과 별개로 재선언돼 드리프트(폰트 범위 8~32 vs 6~48 는 실제 값 절삭). L-TYPE: 열거형 설정이 Rust~bindings 단에서 String 으로 열려 있어 화면단 단언·비전수 배열로 보완되는 구조.

압축으로 누락한 저심각(minor 미만) 항목 10건 계수: (1) 스니펫 파일 전환 시 미저장 draft 무경고 폐기(snippet-editor.tsx:52-54) (2) 테마 소스 변경 시 동일 패턴(theme-editor.tsx:72-74) (3) persistCandidates 루프 중간 실패 시 부분 저장 잔존(vsix-import-dialog.tsx:74-92) (4) 단순 위임 핸들러 3개(settings-view.tsx:231,305,306) (5) `h-[50vh]` 스페이서 매직값(settings-view.tsx:914) (6) SETTINGS_TOC_ITEMS 만 as const 누락(148) (7) 비활성 시 빈 문자열 센티넬 쿼리키(theme-editor.tsx:67) (8) setKeymapCapturing 전역을 두 effect 가 나눠 소유(keybindings-editor.tsx:161-165) (9) 스니펫 저장 후 서버 정규화 결과와 draft 불일치 가능(snippet-editor.tsx:88-91) (10) plugin-list-body 가 컴포넌트와 공유 타입을 함께 export(18행).

## F6 (21건)
범위 규모: src/shared/lib 루트 107파일(구현 61 + 테스트 46) + 하위 6개 디렉토리 30파일(theme-convert 12·shiki 6·remote 6·monaco 4·ai 1·pdf 1). lsp/ 는 타 배치라 참조만. 최대 파일은 monaco-actions.ts 828L, theme-convert/mapping-tables.ts 612L, keymap.ts 397L, command-registry.ts 301L.

기계 검증 축 재확인 결과 0: shared→상위(entities/widgets/features/app) 역방향 import 0(주석 언급 1건뿐), barrel index.ts 0, any/enum 0(‘enum’ 은 JSDoc 산문 1회), function 키워드 0, useCallback/useMemo 0, 코드 라인 주석 0(전부 JSDoc). 다만 JSDoc 본문 다수가 한국어 혼용(예: keymap-chord-store.ts:75, keymap-context.ts:5) — comments.md §2.2 는 영어 기본이고 다국어 시 target lang 을 프로젝트에 명시하라고 요구하나 docs 어디에도 그 기록이 없다(HANDOFF.md:123 은 오히려 “영어 JSDoc”이라 적혀 있음). 규모가 커 개별 발견으로 세지 않고 여기 계수만 남긴다.

렌즈별 총평 — L-BRIDGE(핵심): 브리지/레지스트리 13종은 전부 모듈 스코프 싱글톤이며 멀티 윈도우는 창마다 별도 webview(index.html?projectId&windowSlot, window-context.ts:24)라 격리 자체는 성립한다. 진짜 리스크는 격리의 부작용 쪽으로, (a) 보조 윈도우에서 발신되는 검색·파일히스토리·탐색기 요청이 구독자 부재로 무성 소실되고(2건만 getWindowContext 로 게이트됨), (b) 구독자 0 정책이 브리지마다 제각각(무시 10 / 보류큐 1 / false 반환 1)이며, (c) 원격 세션은 고정 라벨 'remote' 탓에 동시 접속 시 Rust LSP 채널 소유권이 충돌한다. 정리(dispose) 누수는 대체로 정상이나 terminal-write 큐와 shiki 재초기화 경로에 구멍이 있다. L-FSD: 역참조는 0이지만 브리지가 의존 방향을 세탁해 shared 안에 앱 오케스트레이션(DEFAULT_COMMANDS)·팔레트 파서·LSP 시맨틱 맵이 들어와 있고, 루트 평면 배치로 monaco/keymap/bridge 계열이 디렉토리와 루트로 이원화. L-TYPE: bindings 미유도 수동 타입 2건(SyntaxStyle 섀도잉, ANSI 토큰 3중 복제)과 as 단언 1건이 실질 위험. L-CODE: 확장자 추출 4중·fileNameOf 6중·레인 색 상수 3중 등 “2회 이상 룰” 위반이 반복. L-I18N: 양방향 하드코딩(blame 영어, remote 한국어). L-SFC/L-EFFECT/L-A11Y: 범위에 .tsx 0개라 해당 없음(전 파일 .ts).

압축으로 제외한 저심각 10건(계수만): color.ts 의 세그먼트 리터럴 6 상수화 누락, file-icon.ts 가 Tailwind 클래스명을 로직 모듈에 내장, resolveFolderIcon 이 특수 폴더에서 expanded 무시, remote-json isRecord 가 배열도 통과, mapping-tables 의 GRAPH_LANE_ANSI_ORDER 불필요 export, mergeVscodeThemeChain 의 초기값 없는 reduce(현재 호출자는 항상 1개 이상), theme-draft resetToken 이 base 키 존재를 전제(noUncheckedIndexedAccess 미설정), keymap-when 의 무제한 표현식 캐시(현재 입력은 유한), ai 캐시 키의 djb2 32bit 충돌 가능성, monaco-keybinding 의 하드코딩 KeyCode 테이블이 monaco 업그레이드 시 검증 없이 드리프트.

## F7 (18건)
범위 규모: 실제 감사 대상 63파일 — src/shared/lib/lsp 53(프로덕션 31 + 테스트 22, 프로덕션 약 2,700줄; 지정 경로 src/shared/lsp 는 미존재라 실경로로 대체) + shared/ui 17 + constants 13 + hooks 8 + api 3 + icons 3 + i18n 1 + scroll 2. 기계 축 재확인 결과 0: FSD 역방향 import 0, barrel(index.ts) 0, useCallback/useMemo 0, `any`·`enum` 0(유일한 any 3건은 생성물 bindings.ts:1638·1644·1645 — Tauri Specta 산출물이라 수정 대상 아님), `function` 키워드는 shadcn 벤더 14파일 + 생성 bindings 로 한정.

렌즈별 총평. L-TYPE: protocol.ts 301줄 수동 타입은 정당 — package.json 에 vscode-languageserver-protocol 계열 의존이 없고 LSP 와이어 스펙에는 파생 원본이 없다(`as const`+union, `Pick`/`Omit`, `(typeof X)[keyof typeof X]` 를 이미 일관 사용). 대신 진짜 이중 관리는 타입이 아니라 *술어·서수 테이블* 쪽에 있다(발견 3·11). L-SFC(shadcn 예외 판정): shared/ui 의 14개 shadcn 벤더 파일이 upstream 의 `function`+다중 export 를 유지하는 것은 예외로 **유효** — fsd.md §2.1 이 shadcn 산출물을 shared/ui 로 배정하고, docs/architecture.md:154 가 `ui(shadcn vendored)` 로 명문화했으며, upstream 형상 유지가 재생성 가능성을 지킨다. 자작 3종(icon-button·file-group-header·status-error-banner)은 FC<Props>·1 export 를 정확히 지킨다. 다만 dialog.tsx 는 i18n 적용, command.tsx 는 영어 기본값 유지로 벤더 파일 간 로컬 수정 정도가 갈린다. L-CODE: 매직넘버는 constants 로 잘 분리(스크롤바·터미널·미러 등), 삼항 중첩·주석 규율 위반 없음(주석은 전부 JSDoc 영문). 문제는 복제 — NOOP_DISPOSABLE 16회, location 변환 3중, capability 술어 25회. L-BRIDGE: 모듈 스코프 가변 싱글톤 6개(diagnostics rawDiagnosticsByOwnerUri, peek preloadedModelPaths/Timers, model-dirty-tracker, registry handlers/codeLensRefreshListeners, use-monaco-markers) — 이 중 semantic-tokens·peek·model-dirty-tracker 는 스코프·정리 근거가 문서화돼 양호하나, diagnostics 와 codeLens refresh 는 세션 격리가 미완이다. LSP 계층 분리(client/adapters/registry): client 는 JSON-RPC·문서버전·capability 게이트, adapters 는 monaco 변환, registry 는 서버→클라 요청 — 큰 축은 깨끗하고 DI(WorkspaceEditApplierDeps·MonacoCommandExecutor·LspCommandExecutor)도 일관적이다. 균열은 (a) capability 지식이 client 와 adapters 에 이중으로 있고 (b) adapters 계약이 타입으로 없어 상위 registry 가 특수 케이스를 흡수하는 두 지점이다. 보안: 서버 주도 편집 경로의 루트 스코프가 문자열 prefix 검사라 `..` 로 우회 가능(발견 1, monaco uri.js 소스로 검증)하고 무루트 fallback 창이 실재(발견 2)한다 — 이 배치 최상위 리스크.

압축으로 누락한 저심각 7건(계수만): (1) client.ts:119 `isJsonRpcErrorResponse` 로 이미 좁혀진 값에 불필요한 `as JsonRpcErrorResponse`. (2) client.ts:97-107 요청 타임아웃 부재 — 무응답 서버의 pending 항목이 세션 dispose 까지 잔존. (3) peek-model-preload.ts:111 `resetPeekModelPreloadStateForTests` 가 프로덕션 모듈에 노출된 테스트 전용 seam. (4) shared/ui/command.tsx:21-22 `CommandDialog` 가 앱 어디에서도 미사용이면서 영어 기본 문자열 보유(벤더 미사용 export). (5) constants/platform.ts:1 deprecated `navigator.platform` 의존. (6) i18n/i18n.ts:7 모듈 평가 시 `init` 부작용(단일 인스턴스라 실害는 낮음). (7) hooks/use-overlay-scrollbar.ts:24-185 단일 useEffect 160줄에 리스너 10종·옵저버 2종이 응집 — 동작은 정확하나 분해 후보.

## R1 (20건)
R1 범위 실측: 셸 코어 8파일 1,475 LOC(lib.rs 778·state.rs 222·events.rs 213·paths.rs 108·ids.rs 62·constants.rs 49·error.rs 38·main.rs 5) + domain/app 277·window 317·layout 2,923(layout/service.rs 2,116 중 실코드 976·테스트 1,140) = 합계 약 4,992 LOC. 범위 내 전 파일을 열어 감사했고, 스코프 밖 파일(project/git/theme/snippet/file/remote/dispatch)은 주장 검증용으로만 조회했다.

렌즈별 총평. L-RUST-STATE: 가장 무거운 축. 레이아웃 상태의 소유권이 state.rs(필드 pub)·layout/commands(18중 복제 프로토콜)·window/service(순서 다른 두 번째 구현)·lib.rs(유일한 저장자)로 4분할돼 있고, 그 틈에서 실제 데이터 유실 경로 1건(project_close)과 경합 1건(dirty 표시-공개 순서)이 나왔다. 전역 begin_mutation 은 lsp_stop 축소 이후에도 git push/pull 이 락 안에서 동기 네트워크 IO 를 수행해 architecture.md §2.1 을 어긴다. L-RUST-LAYER: architecture.md 의 'command 얇게·로직은 service' 규정이 window 도메인에서 역전(service 가 스폰·락·emit)됐고, 반대로 lib.rs 는 도메인 로직 5종을 흡수했다. 이벤트 등록은 수기 목록 3개에 파리티 테스트 부재(커맨드 축에는 선례 존재). L-RUST-BOUND: 도메인 간 직접 import 34엣지·순환 4쌍(스코프의 layout↔window 포함) — 정찰이 잡은 'service 기준 14엣지' 보다 실제 표면이 넓다. L-RUST-ERR: prod 경로 unwrap/expect/panic 재확인 결과 스코프 내 위험 0(lib.rs:521·586·755 부팅 실패 즉시 종료 3건, app/service.rs:37 번들 상수 직렬화 1건뿐, layout/service.rs 실코드 0건). 대신 AppError 설계 자체가 문제로, 5변종 자유문자열이 UI 토스트에 직송되며 문구가 한국어 101/영어 268 로 갈린다. L-TYPE: ids.rs 의 검증 없는 newtype 이 경로 안전을 호출부 관습에 떠넘겼고, 같은 원인이 스코프 밖에서 실제 취약점(theme_delete 임의 .json 삭제, 원격 노출)으로 실현돼 있어 critical 로 보고했다(theme 파일은 R5 와 겹치므로 중복 조정 필요).

acknowledge 기결 사항은 재론하지 않았다: 보조 창 닫기 미차단·핫엑싯 핸드셰이크 설계·window-state 라벨 접기·AppFile 경로 은닉·레이아웃 v2 마이그레이션 방식·apply_settings_file 별도 진입점은 계약 문서에 근거가 있어 제외했다.

압축으로 누락한 저심각 8건(계수만): events.rs 의 타입 참조 표기 혼재(use 임포트 4 vs 필드 인라인 fully-qualified 9), AppReady 만 serde rename_all 누락, layout_move_tab 의 u32→usize 캐스트 관례, ids.rs 튜플 필드 pub 직접 노출, window/types.rs 창 크기 4상수의 도메인 위치 적정성, lib.rs BINDINGS_PATH 상대경로 하드코딩, HOT_EXIT_FLUSH_TIMEOUT_MS 의 u64→f64 이벤트 캐스트, restore_state 경고 문자열 포맷 산개. 앱 실행·수정은 하지 않았고 cargo 빌드/테스트도 수행하지 않았다(정적 조회만).

## R2 (19건)
범위: src-tauri/src/infra 13파일 전량 정독(mod.rs 포함 14, 총 2,659줄 — 테스트 제외 실코드 약 1,100줄). 전 파일에 #[cfg(test)] 모듈이 붙어 있고 커버리지 자체는 균질하나, 대부분 성공 경로/순수 함수에 몰려 있어 실패·정리·종료 경로가 비어 있는 것이 이번 배치 결함의 공통 배경이다.

렌즈별 총평.
L-RUST-LAYER(주 렌즈): infra 의 '외부 자원 어댑터' 정의가 세 방향에서 침식돼 있다. (a) infra→domain 역참조 4건(language/pty/root_guard/watcher) — architecture.md:75 의 단방향 전제 위반. (b) 두 모듈(root_guard 의 프로젝트 소유권 판정, language 전체)은 IO 가 없는 순수 정책이라 애초에 infra 가 아니다. (c) 추상화가 실제 사용 형태를 못 담아 우회를 유발하는 사례가 http.rs 다 — 스스로 '유일한 egress' 를 선언했지만 프로덕션 LSP 다운로드는 무타임아웃 bare client 로 우회하며, 60초 총 데드라인이라 우회하지 않을 수도 없다. 반례로 secret.rs 의 SecretStore trait + InMemory 테스트 대역은 문서가 말하는 격리를 유일하게 제대로 구현한 모듈이다.
L-RUST-ERR: 무결성 결함 2건이 핵심이다 — lsp_proc 프레이밍의 Content-Length 상한 부재(rustc -O 로 패닉 실측 확인)와 persist::write_atomic 의 실패 시 temp 미정리(같은 파일 안에서 with_mode 판만 정리하는 비대칭). 그 밖에 unwrap_or_default 폴백, stderr 폐기, atomic_install 의 비원자성이 같은 계열이다.
보안: root_guard 의 경계 로직 자체는 견고하다(canonicalize 후 컴포넌트 단위 starts_with, canonicalize_lenient 의 종료성·`..` 거부, Forbidden/InvalidArgument 구분 근거 문서화). 실질 약점은 경계 판정이 아니라 소유자 선택의 비결정성(중첩 루트)이었다. shell_quote::posix_quote 는 fish 의 홑따옴표 내부 이스케이프까지 커버하는 사실상 완전한 구현이고 회귀 테스트도 정확하다 — 추가 지적 없음. crypto::constant_time_eq 는 구현 자체는 옳고 문제는 사본이 셋이라는 SSOT 붕괴다. shell_integration 은 설계·문서화 수준이 높으나 셸이 source 할 스크립트를 공용 /tmp 에 기본 umask 로 쓰는 하드닝 공백이 남는다.
시드 판단(extract_zip 이원화): 정당하다. domain/vsix/service.rs:425-434 의 근거(LSP 아카이브는 lsp/commands.rs:690-694 에서 sha256 이 '필수'라 미게시 시 설치 자체를 거부 → 하드닝 불필요 / 플러그인 zip 은 출처 검증이 없어 하드닝 필수)가 실코드와 일치함을 확인했다. 사용자 공급 zip 경로(plugin/service.rs:277)는 실제로 extract_hardened_zip 만 쓴다. tar 경로도 안전 — tar-0.4.46 은 preserve_permissions=false 에서 mode 를 0o777 마스킹 + umask 적용(entry.rs:814-815)하므로 setuid 승계가 없다. extract_zip 의 잔여 위험(unix_mode 원문 적용, 크기·엔트리 상한 없음)은 sha256 게이트가 실질적으로 봉쇄하며, 비특권 사용자 소유 파일의 setuid 는 권한 상승으로 이어지지 않는다. 따라서 별도 findings 로 올리지 않았다.

기계 검증 0 축(FSD 역방향 import·barrel·any/enum·useCallback/useMemo·function 키워드): 이번 배치는 Rust 범위라 해당 없음 — 재확인 결과 0.

압축으로 누락한 저심각 8건(계수만): (1) extract_zip 이 zip 심볼릭 링크 엔트리를 링크 대상 텍스트가 담긴 일반 파일로 기록, (2) download_to_file 에 최대 크기 상한 없음(Content-Length 미활용), (3) AppError 에 Cancelled 변형이 없어 사용자 취소를 Internal 로 표현(lsp_install.rs:235), (4) SecretStoreState 의 tuple 필드가 pub 이라 스토어 교체가 무제약, (5) write_atomic 이 rename 후 부모 디렉토리를 fsync 하지 않아 크래시 내구성은 미보장, (6) shell_integration::prepare 가 macos/linux/windows 외 타깃에서 미정의, (7) compare_version_segments 가 프리릴리스 우선순위(1.0.0-rc < 1.0.0) 미처리, (8) secret 값 String 에 zeroize 부재.

## R3 (15건)
범위: src-tauri/src/domain/remote 9파일 3,866줄 전수 정독(dispatch 1179·commands 854·server 484·service 462·serving 235·ws 249·login_page 318·types 77·mod 8). 범위 밖은 참조만 — lib.rs(RAW_CHANNEL_COMMANDS·이벤트 팬아웃), terminal/lsp/ai/sync/app commands, infra/secret, docs/ipc-contract·architecture, src/shared/lib/remote/*, src/entities/remote.

렌즈별 총평.
L-CONTRACT: 거부 16종(코드 실측 = 문서 표 16종 일치)·strip 3중 방어(settings_update patch 3필드 / app_file_write 전체 대치 3필드 / sync strip_non_syncable 4필드, remote_access_enabled 제외는 문서화된 의도)는 필드 수준에서 정합한다. 무너지는 곳은 값이 아니라 검증 구조다 — 파리티 테스트가 match arm 을 보지 않고(F#2), 거부 테스트는 헬퍼 동어반복(F#3)이라 16종 계약을 지키는 기계 장치가 사실상 없다. 게이팅 완전성에서는 키링 5계정 중 1계정만 거부되는 비대칭(F#7)이 유일하게 결정이 필요한 실질 공백이다.
보안: Host/Origin 이중 게이트, 와일드카드 레이블 매칭, nonce 오라클 차단, 잠금 2축 분리, constant_time 비교, 토큰 원문 미로깅은 실코드에서 확인했고 테스트도 두껍다. 남은 실공백은 nonce 쿠키 Secure 누락(F#4)과 /__taide/file 응답의 CSP/nosniff 부재(F#5) 둘.
L-RUST-LAYER: service.rs 는 tauri 무결합 순수 로직, commands.rs 의 스토어 배치는 architecture.md:69-71 가 명시 승인한 패턴이라 위반 아님. 다만 도메인 간 직접 호출 금지 규칙과 dispatch 의 20도메인 의존이 문서상 미해결(F#14).
L-RUST-ERR: 프로덕션 경로 unwrap/expect/panic 재확인 결과 0 — 검출 21건 전부 #[cfg(test)] 모듈 내부. AppError 중앙화(err/respond/json_ok)도 일관.
L-RUST-STATE/L-BRIDGE: parking_lot 가드를 await 넘어 들고 있는 지점 0(전부 문장 스코프 clone). 실질 결함은 소켓 sink 소유권 — sink 가 실패를 Ok 로 위장해 도메인 프루닝 불변식이 원격에만 깨지고 writer/client_count 가 누수된다(F#1, 이 배치 최중대).
기계 검증 0 축(FSD 역방향 import·barrel·any/enum·useCallback/useMemo·function 키워드)은 이번 배치에 렌더러 파일이 없어 해당 없음.

압축으로 누락한 저심각 10건(계수만): extract_link_token 퍼센트 미디코딩(F#8 로 흡수), 로그인 라우트당 settings.read() 3회 반복 취득, parse_urlencoded_body 의 중복 키 last-wins, serve_embedded 가 미존재 경로에 404 대신 index.html 200, BROWSER_CSP 의 `connect-src ws: wss:` 와일드카드, login_page::message() 의 누락 키 무음 빈문자열, escape_html 이 작은따옴표 미이스케이프(현재 전 속성 큰따옴표라 무해), dispatch 의 arg! 매크로가 `?` 제어흐름을 은닉, 와이어 상수(0x01/0x02/4001/로그인 경로)의 Rust↔TS 수기 이중화(코드 주석에 사유 명시·4001 은 테스트 존재), RemoteStatus.port 가 u16 포트를 u32 로 보유.

## R4 (22건)
범위·규모: Rust 4개 도메인 6,230줄(git 3,877 = service 2,935[프로덕션 1,726 + 테스트 1,209]·commands 660·types 194·watch 84 / file 1,121 / project 724 / tree 508)을 전량 정독하고, 짝이 되는 데이터 계층(src/entities/{git,tree,file,project})·소비 지점(ipc-sync-provider, git-panel, editor-pane)·정본 문서(architecture.md, ipc-contract.md, data-model.md, ADR-0006, features/git.md)를 대조했다. 참조만 한 범위 밖 파일: state.rs, error.rs, constants.rs, infra/{persist,root_guard,language}.rs, remote/dispatch.rs, layout/commands.rs.

기계 검증 재확인 = 0: FSD 역방향 import 0(shared·entities → features/widgets 0건), barrel index.ts 0, `any` 0(유일 grep 히트는 문서 주석의 영어 단어), `enum` 0, useCallback/useMemo 0, `function` 키워드는 vendored shadcn(shared/ui)에만. Rust 프로덕션 코드(테스트 모듈 제외) 전 범위에서 unwrap/expect/panic!/todo! 0건 — L-RUST-ERR 의 패닉 축은 깨끗하고, 대신 오류 '분류' 축에서 문제가 나왔다(#15).

시드(to_repo_relative 전수) 결과: 경로를 받는 public 함수 15종(stage·unstage·discard·show_file·diff_file·gutter·discard_hunk·conflict_sides·resolve_conflict·stage_hunk·unstage_hunk·stage_lines·unstage_lines·file_log·blame_range) 모두 to_repo_relative 를 거치는 것을 확인했다 — 적용 누락은 없다. 대신 그 게이트 '내부'와 '바깥'에서 두 건이 나왔다: 함수 자체의 백슬래시 무조건 치환·CurDir 미필터(#20), 그리고 git 도메인이 file 도메인의 root_guard(프로젝트 루트)와 다른 경계(레포 루트)를 쓰면서 반환 경로 규약까지 어긋나 깃 패널의 '파일 열기'가 깨지는 건(#2, 이번 배치 최대 결함).

렌즈별 총평 — L-RUST-BOUND: plugin 결합 4엣지(git/file 의 commands·types)와 그로 인한 infra→domain 역의존이 실재하고(#6), project/commands.rs 는 4개 도메인을 직접 호출하는 사실상의 조립부다(#5). architecture.md:77 의 금지 조항이 이 배치에서 가장 자주 깨진다. L-RUST-STATE: 전역 mutation guard 1개가 네트워크 git 작업까지 삼키고(#3, 이미 file/commands.rs 주석이 위험을 문서화한 채 우회만 함), tree 스토어는 read-clone-write 로 lost update 를 낸다(#1). 도메인 스토어의 프로젝트 종료 정리 누락(#7)은 LSP 에서 한 번 나온 패턴의 재발이다. L-RUST-LAYER: spawn_blocking 이 41종 중 14종에만 걸려 있고, 미적용 27종에 가장 무거운 것들(status 전체 스캔·push/pull/fetch)이 몰려 있다(#3에 병합). query 의 부수효과 금지도 깨져 있다(#10). L-RUST-ERR: 중앙 AppError 는 잘 서 있으나 git 경로가 전부 Internal 로 접혀 프론트가 분기 불가(#15). L-QUERY: entities/git 은 queryOptions·QUERY_KEY·공용 무효화까지 규약을 모범적으로 지키고 있고(지적 없음), 문제는 tree 쪽 — 가짜 페이지네이션(#12)과 fsChanged 의 전체 트리 N+1 왕복(#13). L-TYPE: 프론트는 생성 bindings 만 쓰고 수동 타입 중복 0 — 대신 Rust 영속 스키마의 버전 규약이 비어 있다(#8). 비대 분해(git/service.rs 2,935): 프로덕션은 1,726줄이고 hunk/patch 재합성 블록(688~998, 약 310줄)이 명확한 응집 단위라 '지금 당장 쪼개야 할' 비대는 아니라고 판단해 별도 발견으로 올리지 않았다 — 다만 그 블록만 patch 서브모듈로 떼면 파일이 절반 아래로 내려간다.

압축으로 생략한 저심각(8건, 계수만): (1) find_existing_project_id 가 프로젝트 열 때마다 projects/ 전 디렉토리의 JSON 을 선형 읽기, (2) backup_corrupted 가 기존 .bak 을 말없이 덮어씀, (3) log/file_log 의 push_glob·push_head 반환값을 `let _` 로 버려 ref 없는 저장소와 실패가 구분되지 않음, (4) blame_range 가 min/max_line 밖 hunk 라인까지 그대로 밀어 넣을 여지, (5) entry_order 가 비교마다 to_lowercase 로 String 2개 할당, (6) 미러 파일명 FNV-1a 64bit 충돌 시 대응 없음, (7) copy_dir_recursive 재귀 깊이 무제한, (8) restore_expanded 가 expand 실패를 전량 무시. 전부 minor 미만이거나 실증 조건이 좁아 상위 22건에서 제외했다. acknowledge 기각·승인분(settings→remote 와일드카드, ContextKeyExpr/emmet 딥임포트, monaco-editor-core dev 의존, deferred 4건)과 architecture.md 가 이미 기록한 이탈(infra/repo.rs 미생성 = ADR-0006 결정 3항)은 재론하지 않았다.

## R5 (16건)
범위 규모: Rust 8,169줄(domain/locale 4,144 · theme 1,509 · settings 1,482 · sync 1,034) + 프론트 약 3,950줄(entities/{locale,settings,theme,sync} 435 · features/settings 1,668 · features/theme 594 · widgets/settings-view 927 · widgets/theme-editor 233 · app/providers/{locale,theme}-provider 97). 총 약 12,100줄을 직접 열람했다.

기계 검증이 이미 0인 축 재확인 결과 0: FSD 역방향 import 0 · barrel(index.ts) 0 · any/enum 0 · useCallback/useMemo 0 · function 키워드 0 · 1파일 다중 컴포넌트 export 0 · 범위 내 프로덕션 경로 unwrap/expect/panic 0(적중 전량이 #[cfg(test)] 내부).

렌즈별 총평.
L-RUST-BOUND(주 렌즈): sync 는 ai·locale·settings·theme 4개 도메인을, settings 는 agent·ide·remote·theme 4개 도메인을 직접 호출해 앱에서 가장 엣지가 많은 두 허브다. architecture.md:77 의 "도메인 간 직접 호출 금지 + 이벤트 버스/상위 조립부" 규칙은 이 배치뿐 아니라 전체 14개 도메인 약 30개 엣지에서 지켜지지 않는다. sync 의 허브성은 기능상 불가피한 면이 있으나(집계 도메인), settings→{agent,ide,remote} 는 SettingsChanged 이벤트 버스가 같은 함수 안에 이미 있는데도 명령형 호출을 택한 것이라 성격이 다르다. 규칙을 코드에 맞출지 코드를 규칙에 맞출지 정본 결정이 필요하다.
L-RUST-STATE(주 렌즈): 시드로 지목된 locale/service.rs 4,081줄 단독 분해는 "정당한 리터럴 데이터"가 아니라고 판단한다 — 근거는 취향이 아니라 같은 저장소의 대조군이다. architecture.md:67 이 "테마와 완전히 같은 구조"라고 선언한 theme 도메인이 동일 성격 데이터를 resources/themes/*.json + include_str! 로 이미 분리했고, locale 만 키를 4중 중복 선언한 채 인라인으로 남았다. 락 관련해서는 await-across-lock 은 없으나(parking_lot 가드가 전부 문 단위 임시값), sync 커맨드가 전역 mutation mutex 를 GitHub 왕복 내내 점유하는 구조적 결합이 남아 있다.
L-RUST-LAYER: commands 는 전반적으로 얇고(파라미터→service→이벤트) service 는 tauri 비결합이라 계층은 대체로 지켜진다. 예외는 sync_upload/sync_disconnect 가 정본 문서가 선언한 단일 설정 쓰기 경로를 우회하는 것.
L-TYPE: 이 배치의 최대 부채. Settings 필드는 Rust 4곳 + TS 1곳에 손으로 미러링되고, 테마 토큰 약 200개도 Rust/TS 손 미러이며, Rust 기본값은 TS `??` 폴백으로 53회 재선언되어 이미 2개 필드에서 값이 어긋났다. ADR-0011 의 "생성으로 드리프트 차단" 원칙이 타입에는 적용되고 상수·기본값·키 목록에는 적용되지 않은 것이 공통 원인이다.
L-QUERY: queryOptions 팩토리·QUERY_KEY 계층·mutation onSuccess 무효화 책임은 전반적으로 규약을 따른다. 유일한 이탈이 테마 프리뷰의 캐시 직접 주입.
L-EFFECT/L-A11Y/L-I18N/L-SFC: useEffect 는 전부 외부 시스템 동기화(i18next 주입·CSS 변수 적용·시스템 테마 구독·shiki 초기화)로 정당. div onClick 0건, 하드코딩 UI 문자열 0건(t('...') 미등록 키도 0건 — TS 사용 키 595개 전량이 로케일에 존재), 파일당 export 컴포넌트 1개 준수. 단 settings-view 한 파일의 비대화는 남는다.

압축으로 누락한 저심각 5건(계수만): (1) theme-provider 가 테마 적용 외에 플러그인 문법 등록·shiki 초기화까지 겸함(단일 책임 흐림) (2) apply_integration_toggles 의 서버 기동 실패가 log::warn 만 남기고 사용자 피드백 없음 (3) list_locales/list_themes 가 손상된 사용자 JSON 을 무음 스킵(경고 미노출) (4) LocaleProvider 의 로드 실패 배너 문구만 영어 하드코딩(로케일 로드 실패 상황이라 불가피하나 근거 미기록) (5) settings-view 가 ThemeEditor/SnippetEditor 를 조기 return 전체 화면 교체로 라우팅.

acknowledge 기각/승인 사항은 재론하지 않았다: settings→remote 와일드카드 상수 엣지, civil_from_days 자체 구현 및 날짜 크레이트 미도입(2026-08-11-github-sync §3), sync_gist_id/sync_last_synced_at 을 SettingsPatch 에서 제외한 결정(같은 문서 §2), 테마/로케일 다운로드의 "실패분만 건너뛰기"(§4).

## R6 (21건)
배치 R6 범위 = src-tauri/src/domain/{ai,agent,ide} 전 20파일 7,589줄(테스트 2,567줄 = 33.8% 제외 시 프로덕션 약 5,022줄). 전 파일 실독했다. 기계 검증 0 축(FSD 역방향 import·barrel·any/enum·useCallback/useMemo·function 키워드)은 프론트 전용이라 이 배치에 해당 코드 없음 — 재확인 결과 0. 프로덕션 경로 unwrap/expect 는 ai/prompt.rs 의 include_str! 번들 리소스 파싱 3건뿐이며 전용 테스트가 이를 커버해 지적 제외. acknowledge 기결 항목(ide/server.rs auth_callback 의 #[allow(clippy::result_large_err)] — 2026-08-07-qa-batch-decisions.md:52, 프롬프트 템플릿 파일 3분할 — 2026-08-16-wave-g §2-3)은 재론하지 않았다.

렌즈별 총평 — L-RUST-BOUND: ide 가 명백한 허브다. 외향 엣지 4개(layout·plugin·project·file) 중 2개가 상대 도메인의 commands 모듈이며, MCP WS 핸들러가 #[tauri::command] 를 직접 호출해 원격 디스패치 테이블과 무관한 제2 커맨드 진입점을 만든다(architecture.md:77 위반). agent 의 외향 엣지는 terminal::commands::TerminalStore 1개로 얕고, ai 는 crate::domain 외향 의존 0으로 가장 깨끗하다. L-RUST-LAYER: 세 도메인 모두 service.rs 는 tauri 무의존 순수 로직으로 잘 지켜졌으나(ai/service·agent/service·ide/service), 각 도메인의 "네 번째 파일"(agent/hooks.rs·ide/server.rs)이 TCP/WS 전송을 domain 안에서 직접 수행하며 AppHandle 에 결합하고, 그 IO/상태 헬퍼가 commands.rs 에 얹혀 전송↔IPC 양방향 의존이 생겼다. agent/commands.rs(717줄)는 IPC·fs·ps 서브프로세스·osascript 특권 실행이 한 파일에 겹친 최대 혼합층이다. 외부 프로세스/프롬프트 주입면: prompt.rs 의 단일 패스 치환(render_placeholders)과 basename 축약, providers/mod.rs 의 bearer/장문 토큰 마스킹은 근거·테스트가 촘촘해 주입·유출면에서 결함을 찾지 못했다 — 대신 결함은 "적용 범위의 비대칭"에 몰려 있다(auto-tab 만 상한·클램프 없음, codex 만 절단 검출 없음). 시드 확인: instruct/complete 이원화의 중복은 실재하며 ollama↔omlx 는 complete 본문·complete_chat·chat 파이프라인이 문자 단위로 두 벌이다. 마스킹 자체는 세 provider 전부 동일 경로로 일관되지만, 마스킹된 메시지가 전부 AppError::Internal 문자열로 평탄화되어 상태코드 구분이 소실된다.

압축으로 제외한 저심각 6건(계수만): ①ide/server.rs:115 encode() 의 unwrap_or_default 로 직렬화 실패가 빈 WS 프레임이 됨 ②hooks.rs:173-181 parse_query_param 의 퍼센트 디코딩 부재(현 토큰이 hex uuid 라 무해) ③agent/service.rs:282-286 is_cwd_within_root 의 문자열 접두사 비교(정규화 없음, 배지 귀속 용도라 영향 한정) ④ide/server.rs:342-349 openDiff 의 pending 삽입-후-emit 순서(emit 실패 시 10분 잔류) ⑤ide/lockfile.rs:97-105 is_pid_alive 의 pid 재사용 취약(잔여 lockfile 보존 방향이라 안전측) ⑥ai/providers/*.rs 에서 masked body 를 바깥 format! 로 감싸 최종 메시지 길이가 MAX_PROVIDER_ERROR_MESSAGE_LEN 을 초과할 수 있음.

## R7 (24건)
범위: src-tauri/src/domain/{lsp,plugin,vsix,search} 19파일 + 짝을 이루는 infra(lsp_install 553·lsp_proc 276·root_guard) 합계 약 4,900 Rust 라인, 그리고 계약 확인을 위해 참조한 렌더러 접점(entities/lsp·entities/search·widgets/editor-pane/lsp-session-registry·widgets/search-panel·features/search)과 remote/dispatch.rs 의 해당 커맨드 분기. 전부 실제로 열어 확인했다.

렌즈별 총평. L-RUST-BOUND: 가장 심각한 면이다 — plugin↔vsix 가 양방향으로 직접 의존해 순환을 이루고(하드닝 zip 해제가 도메인 안에 산다), 경로 봉쇄 원시함수가 root_guard 와 plugin::service 에 두 벌 존재하며, 일반 파일시스템 헬퍼(atomic_install)가 LSP 전용 infra 모듈 이름 아래에서 plugin·vsix 에 공유된다. 원격 dispatch 정책도 설치 계열에서 lsp_install 만 비대칭으로 뚫려 있다. L-RUST-ERR: 중앙 AppError 자체는 잘 지켜지나 취소를 Internal 로 표현해 정상 조작이 에러 토스트가 되고, 커맨드 경로에서 도달하는 manifest::servers() 의 expect 가 유일한 prod 패닉 지점이다(나머지 unwrap/expect 는 전부 #[cfg(test)] 안이다 — 프로덕션 경로 unwrap 잔여 0). L-RUST-STATE: 시드로 지목된 lsp/commands.rs 가드 재구성 자체는 견고하다(store 언링크 후 비가드 shutdown, stopping 플래그로 재사용 배제, Arc 동일성 기반 install/search 스토어 정리까지 테스트로 고정). 남은 경합면은 락이 아니라 수명 쪽이다 — 종료 시 kill 이 50ms 폴링에 의존해 서버가 고아로 남을 수 있고, 설치 스토어에 RAII 해제가 없으며, 인바운드 메시지마다 전역 세션 맵을 잠근다. infra/lsp_* 와의 책임 분담은 대체로 옳다(프레이밍·다운로드·압축 해제는 infra, 정책·이벤트는 domain). owner 스코프 세션(Wave I)은 lsp 에서만 도입되어 검색은 여전히 창 구분이 없고, 반대로 lsp 쪽은 owner 조건이 너무 강해 channels 다중 구독이 구조적으로 도달 불가가 되면서 ipc-contract.md:693 서술과 모순된다. 백엔드 멀티루트 설계는 렌더러 레지스트리 키(projectId::serverId)가 root 를 빼는 바람에 통째로 우회된다. 경계 인코딩(바이트 오프셋 → UTF-16 컬럼)과 뮤테이션 가드 누락(search_replace)은 구조 결함이 그대로 실버그로 드러난 두 사례다.

기계 검증 축 재확인 결과 0: 범위 내 프런트 접점(entities/lsp·entities/search·features/search·widgets/search-panel·widgets/search-editor)에서 FSD 역방향 import 0, barrel index.ts 0, any/enum 0, useCallback/useMemo 0, function 키워드 0.

압축으로 제외한 저심각 9건(전부 minor 미만): ① apply_grammar_conflicts 가 load_plugin_dir 에서 이미 파싱한 grammar 파일을 플러그인마다 다시 읽고 재파싱(plugin/service.rs:60) ② disabled_plugin 의 매직 리터럴 "0.0.0"(plugin/service.rs:118) ③ vsix 의 strip_json_comments 가 테마 JSON 전체를 Vec<char> 로 복제(vsix/service.rs:272) ④ package.nls.<locale>.json 미지원으로 로케일별 표시명 누락(vsix/service.rs:23) ⑤ import_vsix_as_plugin 이 contributes.themes 를 항상 빈 Vec 으로 버림(vsix/service.rs:590-594) ⑥ search_run 의 on_match.send 실패를 무시해 닫힌 창의 검색이 트리 전체를 끝까지 순회(search/commands.rs:83) ⑦ LspServerId 의 PartialEq<str>/<&str> 수동 impl(lsp/types.rs:39-49) ⑧ shutdown 타임아웃이 shutdown·exit 두 번 적용되어 최악 4초(lsp/commands.rs:383·387, 문서화됨) ⑨ context_lines: u32 → usize 반복 캐스팅(search/service.rs:222). acknowledge 로 이미 확정된 결정(하드닝 zip 신설·gitignore parents 스코프·vsix_extract_themes 원격 거부 등)은 재론하지 않았다.

## R8 (18건)
범위 규모: Rust 5도메인 2,253 LOC(terminal 673 / system 729 / task 428 / snippet 364 / font 59, 이 중 약 1,050 이 인라인 테스트) + 수명주기 추적을 위해 infra/pty.rs(384)·infra/shell_integration.rs 일부·remote/ws.rs·dispatch.rs 의 pty arm·프론트 대응부(entities/terminal·task·snippet·system·font, widgets/terminal-pane 2파일)를 참조 감사. 기계 검증 0축(FSD 역방향 import·barrel·any/enum·useCallback/useMemo·function 키워드)은 참조한 프론트 파일 전체 재확인 결과 0.

렌즈별 총평 — L-RUST-ERR: 실질 clean. 프로덕션 경로 unwrap/expect/panic 은 task/service.rs:100 의 정적 리터럴 정규식 `expect` 1건뿐이고 나머지는 전부 테스트, 에러는 전부 AppError 중앙 타입으로 흐른다(다만 메시지 언어 혼재는 발견 15). L-RUST-LAYER: task·snippet·font 는 commands 얇게/service 순수 규율을 지키지만, terminal(커맨드 415줄에 구독자 다중화·IDE 대기 로직 상주)과 system(순수 URL 검증 + sysinfo 어댑터 상주)이 규율에서 이탈했고 spawn_blocking 적용도 도메인마다 갈린다. L-RUST-BOUND: 스코프 내 도메인 간 직접 엣지 4개 확인(system→terminal/lsp/agent, terminal→ide) — 전부 architecture.md:77 금지 대상, 순환은 없음. PTY/프로세스 수명: 가장 취약한 축. Wave I 다중 구독자 전환으로 attach/detach 는 정합해졌으나 **세션 자체의 회수 경로가 통째로 비어 있고**(pty_kill 호출자 0), Drop 안전망 부재·pause 소유권 분열·원격 sink 무조건 Ok 로 인해 프로세스/스레드/메모리/임시파일이 모두 단조 증가한다. 링버퍼는 이름과 달리 O(n) drain 이고, 전역 스토어 락을 잡은 채 블로킹 write/채널 send 를 수행하는 구조가 앱 전체 프리즈 경로를 만든다.

압축으로 제외한 저심각 관찰 6건(계수만): ①`task.query.ts:8-9` 의 `projectId ?? ''` 센티널(enabled 로 무해하나 타입 좁히기로 대체 가능) ②`shared/lib/terminal-write-bridge.ts` 의 `pendingWritesByTabId` 무제한 누적(탭이 끝내 등록하지 않으면 영구 잔류) ③`widgets/terminal-pane/terminal-link-opener.ts:28` catch 블록 내 설명 주석(comments.md 위반, F배치 소관) ④`system_open_app_data_path` 가 조회성 이름으로 디렉터리를 생성하는 부수효과 ⑤`SystemUsageStore::new` 와 `Default` 중복 정의 ⑥`terminal_sessions`/`foreground_pids` 가 전 세션 맵을 락 안에서 선형 순회(현 규모에선 무해). 검토 deferred 4건 및 acknowledge 기결 사항(raw 채널 3종 specta 제외, 원격 pty=RCE 전제, settings 스트립 정책)은 재론하지 않았다.

## X1 (18건)
X1(계약 정합 전용) 결과. 4중 대조는 기계 검증했고 커맨드 축은 완전 정합, 이벤트 축에서 결함이 몰려 나왔다.

4중 대조 실측(HEAD=5f15a84). lib.rs collect_commands! **179** ↔ bindings.ts `__TAURI_INVOKE` **179** ↔ dispatch.rs IMPLEMENTED_JSON_COMMANDS **179** — 세 집합의 양방향 차집합이 전부 공집합이고 중복도 0. RAW_CHANNEL_COMMANDS 3종(pty_spawn·pty_attach·file_read_raw)은 세 목록 밖에서 dispatch/dispatch_raw 전용 arm 으로 처리되며, 프론트 raw invoke 3곳(file.raw.ts:18, terminal.ipc.ts:16·19)과 정확히 1:1. 원격 명시 거부는 코드 실측 16종(dispatch.rs:553,580,624(2종),958,959,960,961,962,1000,1001,1006,1008(2종),1010,1011)으로 문서 표 16종과 완전 일치. 부분 스트립 2종(settings_update 3필드 / app_file_write Settings 3필드)도 일치. 도메인별 카운트도 문서와 맞다(git 41 / agent 9 / ai 8 / ide 9 / remote 7). 유일한 수치 불일치는 문서 머리말 178·181(발견 14) — 같은 문서 §원격 dispatch 정책의 179 가 맞다.

이벤트 25종 전수 매핑(이번 배치의 핵심 산출). events.rs 25 = collect_events! 25 = bindings makeEvent 25, fanout_remote_events! 24(HotExitFlushRequested 제외 — QA6 의도). 발행 0: app:ready·terminal:cwd-changed 2종. 구독 0: 위 2종 + terminal:exited·lsp:session-status-changed·remote:state-changed·project:focus-kind-changed = 6종(프론트 실구독 19종). app:ready 는 기지 이슈로 재확인만 했고, terminal:cwd-changed 는 문서가 살아있는 흐름으로 단언한다는 점에서 새 발견으로 올렸다(발견 1). 그리고 이 축에는 파리티 테스트가 0건이다(발견 8) — 정찰이 지목한 "bindings↔dispatch 만 강제" 라는 공백은 확인됐을 뿐 아니라, 그 하나뿐인 테스트조차 생성 산출물 bindings.ts 를 기준선으로 삼아 collect_commands! 엣지는 무강제다(발견 9).

data-model.md ↔ types.rs 대조. ProjectLayout·PaneNode·Tab(TabKind 9종)·ClosedTab·AuxWindowLayout·ShellViewState·AppFileTarget·SearchQuery·SearchMatch·MirrorFile·UntitledMirrorFile·Task·git 신규 6종·AI 요청응답 4종·AuxiliaryWindowInfo 는 전부 실코드와 일치. 어긋난 곳은 4군데 — sync 페이로드 스키마 통째 누락 + 잘못된 상호참조(발견 5), 디스크 트리의 locales/·lsp/ 누락(발견 15), MirrorFile baseline 출처 주석 낡음(발견 16), ProjectRef.root 타입과 Project 구조체(발견 17). 설정 필드 파리티는 별도 검증했고 현재 드리프트 0 — Settings 56 / SettingsPatch 53 / emptySettingsPatch 53(양방향 차집합 공집합) / settings_to_sync_patch 는 `..Default` 없는 exhaustive 리터럴이라 컴파일러가 강제한다.

문서가 정본으로 기재했으나 실물이 없는 것 3건이 이번 배치의 실질 소득이다: viewState 영속(발견 4), fs:changed 의 echo 플래그(발견 10), revision 게이트(발견 11). 여기에 원격 경계 3건 — agent:external-open 팬아웃+drain 허용 조합(발견 3, QA6 가 file_flush_complete 에 대해 이미 고친 것과 동일 유형), asset 스코프 미회수(발견 7), sync 버전 게이트 무력화(발견 6).

압축으로 누락한 저심각 7건(계수만): projectGetActive 만 반환 타입이 `ProjectId | null` 아닌 `string | null`(specta transparent newtype 인라인 편차 — ProjectId 가 순수 alias 라 무해) / AppReady 만 serde rename_all 누락(R1 기보고) / layout_resize 인자는 `(number|null)[]` 인데 PaneNode.sizes 는 `f32[]` 인 비대칭 / tauri-internals-shim.ts:79 이 모든 `plugin:*` invoke 를 조용히 null 로 삼켜 원격에서 dialog 가 에러 없이 무응답 / ipc-contract §1 의 revision 규정이 25종 중 1종에만 적용된 상태로 일반 규칙처럼 서술 / SyncThemeEntry·SyncLocaleEntry 는 specta::Type 미파생(IPC 미노출이라 정상이나 SyncPayload 만 버전 필드를 가짐) / capabilities/main.json 은 opener 계열 0·dialog 2종만으로 §4 선언과 정합(위반 아님, 확인 결과만 기록).

기계 검증 0 축(FSD 역방향 import·barrel·any/enum·useCallback/useMemo·function 키워드)은 이번 배치 범위가 Rust 계약 표면과 생성 바인딩이라 해당 없음. acknowledge 기결 4건(settings→remote 와일드카드 엣지·ContextKeyExpr/emmet 딥임포트·monaco-editor-core dev 의존·검토 deferred)은 재론하지 않았다. 앱 실행·수정 없이 정적 조회와 bun 단발 스크립트만 사용했다.

