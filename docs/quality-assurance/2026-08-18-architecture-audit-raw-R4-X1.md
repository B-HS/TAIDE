## R4
요약: 범위·규모: Rust 4개 도메인 6,230줄(git 3,877 = service 2,935[프로덕션 1,726 + 테스트 1,209]·commands 660·types 194·watch 84 / file 1,121 / project 724 / tree 508)을 전량 정독하고, 짝이 되는 데이터 계층(src/entities/{git,tree,file,project})·소비 지점(ipc-sync-provider, git-panel, editor-pane)·정본 문서(architecture.md, ipc-contract.md, data-model.md, ADR-0006, features/git.md)를 대조했다. 참조만 한 범위 밖 파일: state.rs, error.rs, constants.rs, infra/{persist,root_guard,language}.rs, remote/dispatch.rs, layout/commands.rs.

기계 검증 재확인 = 0: FSD 역방향 import 0(shared·entities → features/widgets 0건), barrel index.ts 0, `any` 0(유일 grep 히트는 문서 주석의 영어 단어), `enum` 0, useCallback/useMemo 0, `function` 키워드는 vendored shadcn(shared/ui)에만. Rust 프로덕션 코드(테스트 모듈 제외) 전 범위에서 unwrap/expect/panic!/todo! 0건 — L-RUST-ERR 의 패닉 축은 깨끗하고, 대신 오류 '분류' 축에서 문제가 나왔다(#15).

시드(to_repo_relative 전수) 결과: 경로를 받는 public 함수 15종(stage·unstage·discard·show_file·diff_file·gutter·discard_hunk·conflict_sides·resolve_conflict·stage_hunk·unstage_hunk·stage_lines·unstage_lines·file_log·blame_range) 모두 to_repo_relative 를 거치는 것을 확인했다 — 적용 누락은 없다. 대신 그 게이트 '내부'와 '바깥'에서 두 건이 나왔다: 함수 자체의 백슬래시 무조건 치환·CurDir 미필터(#20), 그리고 git 도메인이 file 도메인의 root_guard(프로젝트 루트)와 다른 경계(레포 루트)를 쓰면서 반환 경로 규약까지 어긋나 깃 패널의 '파일 열기'가 깨지는 건(#2, 이번 배치 최대 결함).

렌즈별 총평 — L-RUST-BOUND: plugin 결합 4엣지(git/file 의 commands·types)와 그로 인한 infra→domain 역의존이 실재하고(#6), project/commands.rs 는 4개 도메인을 직접 호출하는 사실상의 조립부다(#5). architecture.md:77 의 금지 조항이 이 배치에서 가장 자주 깨진다. L-RUST-STATE: 전역 mutation guard 1개가 네트워크 git 작업까지 삼키고(#3, 이미 file/commands.rs 주석이 위험을 문서화한 채 우회만 함), tree 스토어는 read-clone-write 로 lost update 를 낸다(#1). 도메인 스토어의 프로젝트 종료 정리 누락(#7)은 LSP 에서 한 번 나온 패턴의 재발이다. L-RUST-LAYER: spawn_blocking 이 41종 중 14종에만 걸려 있고, 미적용 27종에 가장 무거운 것들(status 전체 스캔·push/pull/fetch)이 몰려 있다(#3에 병합). query 의 부수효과 금지도 깨져 있다(#10). L-RUST-ERR: 중앙 AppError 는 잘 서 있으나 git 경로가 전부 Internal 로 접혀 프론트가 분기 불가(#15). L-QUERY: entities/git 은 queryOptions·QUERY_KEY·공용 무효화까지 규약을 모범적으로 지키고 있고(지적 없음), 문제는 tree 쪽 — 가짜 페이지네이션(#12)과 fsChanged 의 전체 트리 N+1 왕복(#13). L-TYPE: 프론트는 생성 bindings 만 쓰고 수동 타입 중복 0 — 대신 Rust 영속 스키마의 버전 규약이 비어 있다(#8). 비대 분해(git/service.rs 2,935): 프로덕션은 1,726줄이고 hunk/patch 재합성 블록(688~998, 약 310줄)이 명확한 응집 단위라 '지금 당장 쪼개야 할' 비대는 아니라고 판단해 별도 발견으로 올리지 않았다 — 다만 그 블록만 patch 서브모듈로 떼면 파일이 절반 아래로 내려간다.

압축으로 생략한 저심각(8건, 계수만): (1) find_existing_project_id 가 프로젝트 열 때마다 projects/ 전 디렉토리의 JSON 을 선형 읽기, (2) backup_corrupted 가 기존 .bak 을 말없이 덮어씀, (3) log/file_log 의 push_glob·push_head 반환값을 `let _` 로 버려 ref 없는 저장소와 실패가 구분되지 않음, (4) blame_range 가 min/max_line 밖 hunk 라인까지 그대로 밀어 넣을 여지, (5) entry_order 가 비교마다 to_lowercase 로 String 2개 할당, (6) 미러 파일명 FNV-1a 64bit 충돌 시 대응 없음, (7) copy_dir_recursive 재귀 깊이 무제한, (8) restore_expanded 가 expand 실패를 전량 무시. 전부 minor 미만이거나 실증 조건이 좁아 상위 22건에서 제외했다. acknowledge 기각·승인분(settings→remote 와일드카드, ContextKeyExpr/emmet 딥임포트, monaco-editor-core dev 의존, deferred 4건)과 architecture.md 가 이미 기록한 이탈(infra/repo.rs 미생성 = ADR-0006 결정 3항)은 재론하지 않았다.
발견: [{"severity": "critical", "lens": "L-RUST-STATE", "title": "tree 커맨드 4종이 TreeStore 전체를 복제→통째 덮어쓰기 (lost update + 매 호출 전체 딥카피)", "file": "src-tauri/src/domain/tree/commands.rs", "line": 61, "claim": "tree_rows/tree_toggle/tree_reveal/tree_refresh 모두 `let mut trees = tree_store.0.read().clone()` 로 **모든 프로젝트의 TreeState(확장 집합 + 디렉토리별 자식 캐시 전체)** 를 복제한 뒤 `*tree_store.0.write() = trees` 로 맵 전체를 덮어쓴다. tree_rows 는 mutation guard 도 잡지 않으므로, 스크롤 중 tree_rows 와 다른 창/원격 세션의 tree_toggle 이 교차하면 뒤에 write 하는 쪽이 상대의 확장 상태를 통째로 되돌린다(silent lost update). 동시에 조회 전용 경로가 트리 규모에 비례하는 딥카피를 매 호출 수행한다.", "evidence": "tree/commands.rs:61-65(tree_rows: read().clone() → rows_page(읽기 전용) → write() 전체 대입), 77-81/94-98/111-115(나머지 3종 동일 패턴). TreeState 는 `expanded: BTreeSet<PathBuf>` + `cache: HashMap<PathBuf, Vec<Entry>>`(tree/service.rs:19-23) 로, 확장한 모든 디렉토리의 엔트리를 보유한다. tree_rows 만 `begin_mutation` 이 없어(commands.rs:54-66 vs 76/93/110) 상호 배제도 없다. 호출 빈도는 ipc-sync-provider.tsx:80-82 의 fsChanged 핸들러가 끌어올린다.", "suggestedDirection": "스토어를 `RwLock<HashMap<..>>` 그대로 두되 맵 전체 복제 대신 항목 단위로 잠가 in-place 변경(get_mut) 하고, 조회 전용 tree_rows 는 write 를 아예 하지 않도록(지연 생성은 별도 upsert 경로로) 분리한다. 트리 상태는 전역 mutation guard 가 아니라 TreeStore 자체 잠금으로 직렬화하는 것이 소유권상 맞다."}, {"severity": "critical", "lens": "L-RUST-BOUND", "title": "git_status 의 StatusRow.path 는 repo 상대 경로인데 file_*/layout 은 절대 경로만 받는다 — 깃 패널 '파일 열기'가 실패하거나 엉뚱한 파일을 연다", "file": "src-tauri/src/domain/git/service.rs", "line": 1560, "claim": "collect_status_rows 는 git2 의 repo-root 상대 경로(`src/main.rs`)를 그대로 StatusRow.path 로 내보낸다. 반면 file 도메인은 root_guard 로 절대 경로를 전제한다. 깃 패널이 이 값을 그대로 파일 탭으로 넘기면, 프로덕션(앱 번들 CWD=`/`)에서는 canonicalize_lenient 가 부모까지 거슬러 실패해 `유효하지 않은 경로입니다: `(빈 경로) 오류로 끝나고, dev(CWD=src-tauri)에서는 CWD 기준으로 해석돼 **같은 이름의 다른 파일이 열린다**(그대로 편집·저장하면 엉뚱한 파일을 덮어쓴다). ipc-contract.md 의 git 절에도 경로 규약(상대/절대)이 명시돼 있지 않다.", "evidence": "git/service.rs:1551-1565(entry.path() = repo 상대). 소비 경로: widgets/git-panel/git-panel.tsx:209·225(onOpenFile(row.path)) → git-panel-container.tsx:170(openTab kind:'file', path) → layout/commands.rs:100-108(검증·정규화 없이 그대로 Tab 저장) → entities/file/file.query.ts:10(openFile(path)) → file/commands.rs:18(root_guard::resolve_owning_project) → infra/root_guard.rs:57-70(canonicalize_lenient 는 상대 경로를 프로세스 CWD 기준으로 해석, 실패 시 부모 재귀 끝에 InvalidArgument). 같은 값이 onCopyPath·onRevealInExplorer(git-panel.tsx:227-228)로도 흘러간다. 반면 git_diff_file/git_show_file 은 to_repo_relative 가 상대·절대 모두 받아 정상 동작하므로 diff 탭만 우연히 성공한다.", "suggestedDirection": "경로 규약을 계약으로 고정한다 — StatusRow/CommitFile 에 표시용 상대 경로와 절대 경로를 함께 싣거나(권장), git 도메인이 절대 경로를 내보내고 내부에서만 to_repo_relative 로 낮춘다. 어느 쪽이든 ipc-contract.md 의 git 절에 '반환 경로 = repo 루트 상대/절대'를 명시하고, layout_open_tab 이 File 탭 경로의 절대성을 경계에서 거절하도록 한다."}, {"severity": "major", "lens": "L-RUST-STATE", "title": "git_push/pull/fetch/commit 이 전역 mutation guard 를 쥔 채 spawn_blocking 없이 네트워크 서브프로세스를 동기 대기", "file": "src-tauri/src/domain/git/commands.rs", "line": 256, "claim": "push/pull/fetch/commit 은 `begin_mutation()` 으로 앱 전역 단일 뮤텍스를 잡은 뒤 `run_git`(std::process::Command::output, 동기 블로킹)을 그대로 호출한다. 느린 네트워크 push 1회가 (a) tokio 워커 스레드 하나를 통째로 점유하고 (b) 파일 저장·탭 이동·레이아웃 변경 등 앱의 모든 mutation 커맨드를 그 시간만큼 큐에 세운다. 이 사실은 이미 코드 주석으로 인지돼 있으나 회피책(특정 커맨드만 락 면제)만 있고 근본 원인은 그대로다.", "evidence": "git/commands.rs:256-262(git_push: `let _guard = state.begin_mutation().await;` 뒤 service::push 동기 호출), 266-283(pull/fetch), 238-252(commit). service/git service.rs:189-203 → run_git(service.rs:1711-1725, Command::output 블로킹). state.rs:38·58-60(mutation_guard = 단일 tokio Mutex, 전 도메인 공용). file/commands.rs:86-89 주석: \"hot-exit 플러시가 긴 락 보유자(예: git_push) 뒤에서 대기해 HOT_EXIT_FLUSH_TIMEOUT_MS 를 넘기고 미저장 미러를 잃는다\" — 위험을 문서화하고 우회한 상태. 더불어 architecture.md §2.1(블로킹 작업은 spawn_blocking) 기준으로 git 커맨드 41종 중 27종이 spawn_blocking 없이 실행된다(git_status 전체 스캔·git_gutter·stash 계열 포함).", "suggestedDirection": "네트워크/서브프로세스 계열은 spawn_blocking 안으로 옮기고, 전역 mutation guard 는 AppState 를 실제로 건드리는 구간에만 잡도록 범위를 좁힌다(git 상태는 GitStore/레포 단위 잠금으로 충분). lsp_stop 이 이미 같은 방향으로 정리된 선례가 있다(ipc-contract.md 의 lsp 절)."}, {"severity": "major", "lens": "L-CODE", "title": "DiffOptions::pathspec 을 fnmatch 그대로 사용 — 파일명에 glob 메타문자가 있으면 gutter/hunk 스테이지가 조용히 빈 diff", "file": "src-tauri/src/domain/git/service.rs", "line": 457, "claim": "pathspec 은 git2 문서상 'paths/fnmatch patterns' 이며 exact match 로 바꾸는 `disable_pathspec_match` 가 따로 있는데, 레포 전체에서 한 번도 쓰이지 않는다. 그 결과 `app/[id]/page.tsx` 같은 Next.js 동적 라우트(대괄호)나 `*`·`?` 가 든 파일명은 자기 자신과 매치되지 않아 diff 가 비고, git_gutter 는 아무 표시 없이 성공하고 stage_hunk/stage_lines 는 '변경 사항이 없습니다' 로 실패한다. 같은 함수들이 `Patch::from_diff(diff, 0)` 으로 0번 델타가 요청 파일이라고 무검증 가정하는 것도 같은 축의 취약점이다.", "evidence": "service.rs:457·511·615·642·658·678·1099 이 pathspec 사용, 레포 전체 grep 상 disable_pathspec_match 0건. git2-0.21.0/src/diff.rs:773-775 \"If pathspecs are specified, this flag means that they should be applied as an exact match instead of a fnmatch pattern\", 동 파일 937 \"Add to the array of paths/fnmatch patterns\". 델타 0번 가정: service.rs:521-523(discard_hunk), 700-702(build_whole_hunk_patch), 743-745(build_partial_patch).", "suggestedDirection": "pathspec 을 거는 모든 DiffOptions 에 disable_pathspec_match(true) 를 함께 설정하고, Patch::from_diff 로 꺼낸 델타의 경로가 요청한 relative 와 일치하는지 확인한 뒤 진행한다(불일치 시 명시적 오류). 대괄호가 든 경로를 회귀 테스트로 고정한다."}, {"severity": "major", "lens": "L-RUST-BOUND", "title": "project/commands.rs 가 4개 타 도메인 모듈을 직접 호출 — architecture.md 의 '도메인 간 직접 호출 금지' 위반", "file": "src-tauri/src/domain/project/commands.rs", "line": 124, "claim": "project 도메인의 커맨드가 git·layout·ide·agent 네 도메인의 내부 모듈을 직접 import/호출해 프로젝트 열기/닫기의 사실상 조립부 역할을 겸한다. architecture.md 는 도메인 간 직접 호출을 금지하고 이벤트 버스 또는 상위 조립부를 거치라고 규정한다. 지금 구조에서는 새 도메인이 프로젝트 수명주기에 붙을 때마다 project/commands.rs 를 수정해야 하고, 반대로 project 를 테스트하려면 4개 도메인이 전부 링크된다.", "evidence": "project/commands.rs:9(`use crate::domain::git::watch as git_watch`), :10(layout::service), :124(`crate::domain::ide::commands::refresh_lockfile(&app)`), :128(`crate::domain::agent::hooks::reconcile_installed_hooks`), :157(project_close 에서 다시 ide::commands). architecture.md:77 \"도메인 간 직접 호출은 금지하고, 필요하면 이벤트 버스(내부 broadcast channel) 또는 상위 조립부를 거친다\". 같은 파일 8행은 file::types::FsChange 를 끌어와 watcher 콜백 타입으로 쓴다.", "suggestedDirection": "ProjectOpened/ProjectClosed 이벤트를 내부 구독 지점으로 만들어 각 도메인이 스스로 attach/detach 하게 하거나(architecture.md §3 의 ProjectCapability::attach/detach 가 원래 이 자리다), lib.rs 부트스트랩에 프로젝트 수명주기 조립부를 두고 project 도메인은 순수 상태만 담당하게 한다."}, {"severity": "major", "lens": "L-RUST-BOUND", "title": "git·file 이 plugin 도메인의 commands 모듈에 의존하고, infra/language 가 domain 타입을 역참조", "file": "src-tauri/src/domain/git/commands.rs", "line": 13, "claim": "git·file 커맨드가 plugin 도메인의 **commands 모듈**(IPC 경계 계층) 함수 `ensure_loaded` 와 `PluginStore` 를 직접 끌어 쓰고, 두 service 는 `plugin::types::LoadedPlugin` 을 파라미터로 받아 다시 infra 로 넘긴다. 그 결과 최하층이어야 할 infra/language.rs 가 domain::plugin 을 import 하는 역방향 의존이 생겼다(infra → domain). 언어 판정이라는 공통 관심사가 plugin 도메인 소유의 타입·저장소에 묶여, 도메인 경계와 계층 방향이 동시에 침식된다.", "evidence": "git/commands.rs:13, file/commands.rs:8(`use crate::domain::plugin::commands::{self as plugin_commands, PluginStore}` — 각각 git_diff_file:95, file_open:20 에서 ensure_loaded 호출). git/service.rs:10, file/service.rs:9(`use crate::domain::plugin::types::LoadedPlugin`). infra/language.rs:3(`use crate::domain::plugin::types::LoadedPlugin`) — architecture.md:53·75 는 infra 를 domain 아래의 외부 자원 어댑터로 규정한다.", "suggestedDirection": "언어 기여(languages contribution) 스냅샷을 plugin 타입이 아닌 infra 소유의 경량 타입(예: `LanguageOverlay { extensions, id }`)으로 좁혀 infra→domain 참조를 끊고, `ensure_loaded` 는 plugin::service 로 내려 commands 는 IPC 어댑터만 남긴다."}, {"severity": "major", "lens": "L-RUST-STATE", "title": "project_close 가 GitStore·TreeStore 를 비우지 않아 닫힌 프로젝트의 캐시가 잔존·재사용된다", "file": "src-tauri/src/domain/project/commands.rs", "line": 153, "claim": "project_close 는 layouts·watchers·git_watchers 는 정리하지만 도메인별 스토어(GitStore 의 projectId→repo_root 캐시, TreeStore 의 전체 트리 캐시)는 손대지 않는다. 프로젝트 식별자는 경로 기준으로 재사용되므로(find_existing_project_id), 같은 폴더를 닫았다 다시 열면 이전 세션의 TreeState(옛 디렉토리 목록)와 repo_root 가 그대로 되살아나고, 닫힌 프로젝트의 트리 캐시는 앱이 살아 있는 동안 메모리에 남는다.", "evidence": "project/commands.rs:148-155(layouts/watchers/git_watchers 만 remove). GitStore 는 git/commands.rs:20·41 에서만 채워지고 지워지는 곳은 git_init(:71) 뿐 — 레포 전체 grep 상 lib.rs:609 등록 외 참조 없음. TreeStore(tree/commands.rs:12)도 제거 경로 없음, ensure_entry(:40-45)는 기존 항목이 있으면 그대로 재사용. project/service.rs:236-258 이 같은 root 에 같은 ProjectId 를 되돌려 준다. 동일 유형의 누락이 ipc-contract.md 의 lsp 절에 'project_close 가 LspStore 를 건드리지 않는다'로 이미 기록돼 있다(프론트에서 우회 처리).", "suggestedDirection": "ProjectClosed 를 도메인 스토어 정리 훅으로 삼아 GitStore/TreeStore(및 향후 스토어)가 자기 항목을 스스로 비우게 한다. 위 '도메인 간 직접 호출' 정리와 같은 조립부에서 함께 처리하면 스토어가 늘어도 project 도메인을 고치지 않는다."}, {"severity": "major", "lens": "L-TYPE", "title": "project/session 영속 스키마에 버전 규약이 없다 — Project 에 version 필드 부재, migrate_session 은 version 무시", "file": "src-tauri/src/domain/project/service.rs", "line": 144, "claim": "data-model.md §5 는 '모든 영속 파일은 version 필드를 가지고, 로더는 현재 버전까지 순차 마이그레이션을 적용한다'를 규정하지만, Project(project.json)에는 version 필드 자체가 없고 migrate_session 은 이름과 달리 serde_json::from_value 한 줄로 version 을 전혀 보지 않는다. 상위 버전(새 앱이 쓴) 세션 파일도 그대로 역직렬화돼, 다음 저장에서 미지 필드가 조용히 소실된다. layout 도메인은 같은 문제에 대해 버전 비교와 다운그레이드 거절을 구현해 둔 상태라 두 도메인의 규약이 어긋난다.", "evidence": "project/service.rs:144-147(migrate_session 전문). project/types.rs:27-36(Project 에 version 없음), :7 `PROJECT_SCHEMA_VERSION` 은 정의만 있고 레포 전체 참조 0건(SESSION_SCHEMA_VERSION 은 Default 스탬프에만 사용). data-model.md:121-124. 대조군: ipc-contract.md 의 마이그레이션 절 — layout 은 `version <= LAYOUT_SCHEMA_VERSION` 검사 + 더 높은 버전만 거절하는 회귀 테스트 보유.", "suggestedDirection": "Project 에 version 필드를 추가하고(#[serde(default)] 로 기존 파일 흡수), load_session/load_project 를 layout 과 같은 '현재 버전 이하만 마이그레이션, 상위 버전은 백업 후 거절' 규칙으로 통일한다. 미사용 PROJECT_SCHEMA_VERSION 은 그때 실제 기준값으로 쓰이게 된다."}, {"severity": "major", "lens": "L-CODE", "title": "Project.capabilities 는 소비처가 0인 죽은 상태 — architecture.md 의 핵심 확장점이 스텁", "file": "src-tauri/src/domain/project/service.rs", "line": 71, "claim": "open_project 가 .git 유무로 Git, 무조건 Terminal 을 넣어 capabilities 를 만들고 project.json 과 IPC 타입까지 노출하지만, Rust·TS 어느 쪽에서도 읽는 코드가 없다(자체 단위 테스트만 검증). Lsp·AgentWatch 는 아예 생성되지 않고, restore_session 은 복원 시 재감지 없이 파일값을 그대로 쓰거나 빈 벡터를 넣으며, git_init 성공 후에도 갱신되지 않는다. architecture.md §3 이 '프로젝트 = 폴더 + capability 집합'을 미래 확장(RemoteControl 등)의 축으로 선언한 것과 실제 구현이 어긋난다.", "evidence": "project/service.rs:71-83(생성), :208-217(restore 시 capabilities 재감지 없음, 파일 부재 시 Vec::new()). 소비처 grep: src-tauri 전체에서 capabilities 읽는 곳 없음(project/service.rs 자체 테스트 :339-340 만), src/ 전체에서 `capabilities` 는 LSP 서버 capability 뿐(shared/lib/lsp/client.ts). bindings.ts:541·912 로 타입만 프론트에 노출. git/commands.rs:70(git_init)도 capabilities 를 갱신하지 않는다.", "suggestedDirection": "둘 중 하나로 정리한다 — (a) architecture.md §3 대로 CapabilityCtx/attach·detach 를 실제 부착 지점(워처·git·lsp)으로 승격시키고 open/restore/git_init 이 같은 감지 경로를 공유하게 하거나, (b) 확장점 도입을 미룬다면 Project 에서 capabilities 를 걷어내고 아키텍처 문서에 현재 상태를 반영한다."}, {"severity": "major", "lens": "L-CODE", "title": "file_save 가 원본 파일의 퍼미션을 잃는다 — 실행 비트가 저장할 때마다 소실", "file": "src-tauri/src/domain/file/service.rs", "line": 105, "claim": "save_file 은 persist::write_atomic 으로 임시 파일을 새로 만들어 rename 하는데, 임시 파일은 기본 umask(보통 0644)로 생성되고 원본 모드를 복원하지 않는다. 따라서 chmod +x 된 스크립트를 에디터에서 편집·저장하면 실행 비트가 조용히 사라진다(하드링크·확장속성도 함께 끊긴다). 같은 모듈에 모드를 지정하는 write_atomic_with_mode 가 이미 존재하는데 사용자 파일 저장 경로만 이를 쓰지 않는다.", "evidence": "file/service.rs:105-107(save_file → persist::write_atomic). infra/persist.rs:11-25(File::create 로 임시 파일 생성 → sync_all → rename, 모드 보존 없음) vs :27-58(write_atomic_with_mode, 유닉스 모드 지정 지원, 현재는 시크릿 파일 전용 :63-65).", "suggestedDirection": "save_file 경로에서 저장 전 원본 metadata().permissions() 를 읽어 write_atomic_with_mode 로 같은 모드를 적용한다(원본 부재 시에만 기본 모드). 실행 스크립트 저장 후 모드가 유지되는지 회귀 테스트로 고정한다."}, {"severity": "major", "lens": "L-RUST-LAYER", "title": "query 커맨드 git_status 가 인덱스를 쓴다(update_index) — 부수효과 금지 계약 위반, 동시 호출 시 index.lock 경합", "file": "src-tauri/src/domain/git/service.rs", "line": 1540, "claim": "collect_status_rows 가 StatusOptions 에 update_index(true) 를 켜 두어, 조회 전용으로 계약된 git_status 가 호출될 때마다 libgit2 가 인덱스의 stat 캐시를 디스크에 다시 쓴다. architecture.md 의 IPC 표에서 query 는 '부수효과 금지'로 못 박혀 있고, 이 커맨드는 워처 이벤트·멀티 윈도우·원격 세션에서 동시에 들어올 수 있어 index.lock 경합으로 실패할 여지도 생긴다(git_status 는 spawn_blocking 도 걸려 있지 않다).", "evidence": "git/service.rs:1531-1540(`.update_index(true)`), :66-80(status → collect_status_rows). architecture.md:119 \"query | invoke('git_status', ...) → 데이터 반환 | 상태 조회. 부수효과 금지\". git/commands.rs:77-82(git_status 는 spawn_blocking·guard 모두 없음). git2 의 GIT_STATUS_OPT_UPDATE_INDEX 는 인덱스 stat 캐시를 갱신·기록하는 옵션이다.", "suggestedDirection": "git_status 는 update_index 를 끄고(필요하면 NO_REFRESH 계열로 조회 전용화), 인덱스 stat 캐시 갱신이 정말 필요하면 mutation 계열(스테이지/커밋) 경로나 별도 유지보수 태스크에서 수행한다."}, {"severity": "major", "lens": "L-QUERY", "title": "트리 페이지네이션이 사실상 미사용 — 프론트는 limit 5000 한 장만 요청하고, mutation 은 전체 트리를 캐시에 덮어쓴다", "file": "src/entities/tree/tree.query.ts", "line": 6, "claim": "Rust 는 tree_rows(offset, limit)+total 로 페이지네이션 계약을 제공하지만 프론트는 항상 offset 0, limit 5000 한 번만 호출하고 total 을 쓰지 않는다. 즉 표시 행이 5000 을 넘는 프로젝트는 나머지 행이 아무 신호 없이 사라진다. 게다가 toggle/reveal/refresh 는 full_page(전량)를 반환해 setQueryData 로 캐시에 넣으므로, 같은 캐시 키가 '전량'과 '5000행 절단' 사이를 오가며 재조회 시 조용히 잘린다.", "evidence": "tree.query.ts:6(TREE_PAGE_LIMIT = 5_000), :12(offset: 0 고정, total 미사용), :18·26·34(setQueryData 로 full_page 주입). tree/commands.rs:80·97·114(full_page), tree/service.rs:175-192(rows_page vs full_page). 계약: docs/ipc-contract.md:106-109(query 는 offset/limit, mutation 3종은 갱신된 TreeRowPage 를 반환값으로 직접 돌려준다).", "suggestedDirection": "둘 중 하나로 통일한다 — 실제 가상 스크롤 페이지네이션을 붙여 total 기반으로 페이지를 이어 받거나, 페이지네이션을 계약에서 걷어내고 tree_rows 도 full_page 로 맞춘 뒤 대형 트리 대응은 다른 축(지연 확장)으로 푼다. 지금처럼 두 규약이 한 캐시 키를 공유하는 상태는 유지될 수 없다."}, {"severity": "major", "lens": "L-QUERY", "title": "fsChanged 핸들러가 디렉토리마다 전체 트리를 왕복시키고 그 응답을 버린 뒤 다시 invalidate 한다", "file": "src/app/providers/ipc-sync-provider.tsx", "line": 80, "claim": "파일 변경 이벤트마다 변경 경로의 부모 디렉토리 집합을 만들어 각각 refreshTreeDir 를 호출하는데, 이 커맨드는 전역 mutation guard 를 잡고 TreeStore 전체를 복제한 뒤 **전체 트리(full_page)** 를 직렬화해 돌려준다. 그런데 반환값은 쓰이지 않고, 곧바로 TREE.ROWS 를 invalidate 해 tree_rows 를 한 번 더 호출한다. 빌드/설치처럼 변경이 몰리는 상황에서 디렉토리 수 N에 대해 전체 트리 왕복이 N+1회 발생하고, 그동안 다른 mutation 은 전부 대기한다.", "evidence": "ipc-sync-provider.tsx:79-82(dirs 집합 → Promise.all(refreshTreeDir) → invalidateQueries(TREE.ROWS)). tree/commands.rs:104-117(tree_refresh: begin_mutation + 전체 맵 clone + full_page 반환). infra 워처는 debounce 300ms(constants.rs:17) 뿐이라 다중 디렉토리 배치가 그대로 들어온다.", "suggestedDirection": "refreshTreeDir 의 반환값을 setQueryData 로 그대로 쓰고 후속 invalidate 를 없애거나(왕복 1회), 아예 Rust 가 워처에서 트리 무효화를 스스로 처리하고 프론트에는 '변경됨' 신호만 보내 tree_rows 한 번으로 수렴시킨다(architecture.md §6-4 의 소형 페이로드 원칙과도 일치)."}, {"severity": "major", "lens": "L-CODE", "title": ".git 을 디렉토리로만 판정 — git worktree·submodule 에서 Git capability 미감지 + git 워처 미부착", "file": "src-tauri/src/domain/project/service.rs", "line": 72, "claim": "프로젝트 열기와 git 워처 부착이 모두 `<root>/.git` 이 **디렉토리**인지로만 판정한다. git worktree(`git worktree add`)와 submodule 에서 .git 은 `gitdir: ...` 를 담은 **파일**이므로 두 경로 모두 조용히 건너뛴다. 반면 실제 git 커맨드는 Repository::discover 로 정상 동작하므로, 워크트리에서는 git 기능이 동작은 하되 .git 변경을 감지하지 못해 상태/refs 가 자동 갱신되지 않는다(수동 mutation 후에만 갱신).", "evidence": "project/service.rs:72(`if canonical.join(GIT_DIR_NAME).is_dir()`), project/commands.rs:42-45(attach_git_watcher 가 같은 is_dir 검사로 조기 return). 대조: git/service.rs:58-64(discover 는 Repository::discover 로 .git 파일·상위 탐색 모두 처리). 워처가 감시하는 대상도 `<root>/.git` 경로 자체라 워크트리의 실제 gitdir(공용 저장소 하위)과 다르다.", "suggestedDirection": "두 지점 모두 `service::discover` 결과(또는 Repository::discover 로 얻은 실제 gitdir)를 단일 판정·감시 대상으로 삼는다. 감시 경로도 workdir 의 .git 이 아니라 discover 가 알려준 gitdir 을 쓰면 워크트리·submodule 이 함께 해결된다."}, {"severity": "minor", "lens": "L-RUST-ERR", "title": "map_git_err/run_git 이 모든 실패를 AppError::Internal 로 평탄화해 오류 분류가 사라진다", "file": "src-tauri/src/domain/git/service.rs", "line": 1707, "claim": "map_git_err 은 git2 에러를 코드 구분 없이 Internal 로 접고, run_git 도 CLI 실패 전부를 Internal 로 만든다. 그 결과 '브랜치 없음', '충돌', '원격 거절', '인증 실패' 가 프론트에서 동일한 코드로 도착해 UI 가 분기할 수 없다. AppError 는 NotFound/InvalidArgument/Forbidden 을 이미 갖추고 있고, checkout_ref·stash_apply 처럼 몇몇 지점만 개별적으로 Conflict 를 구분하는 비대칭 상태다.", "evidence": "git/service.rs:1707-1709(map_git_err), :1711-1725(run_git 의 `AppError::Internal(format!(\"git {command} 실패: {stderr}\"))`). 개별 분기 예: :1481-1487(Conflict → InvalidArgument), :1462-1468(stash_apply). error.rs:6-21(가용 변형 5종). ipc-contract 의 원격 dispatch·프론트 toast 는 error.message 문자열에 의존하게 된다.", "suggestedDirection": "git2::ErrorCode/ErrorClass 를 AppError 로 매핑하는 표를 map_git_err 안에 두고(NotFound·Conflict·Auth 최소 3분류), run_git 도 exit code + stderr 를 같은 표로 접는다 — ADR-0006 의 'CLI 출력은 exit code + stderr 를 구조화해 에러 UI 로 전달' 조항이 이 자리를 이미 지정하고 있다."}, {"severity": "minor", "lens": "L-CODE", "title": "git/types.rs 의 상수 2개가 데드이고, 그중 하나는 프론트에서 값이 재선언된다", "file": "src-tauri/src/domain/git/types.rs", "line": 4, "claim": "GRAPH_LANE_COLOR_COUNT 와 LOG_PAGE_SIZE 는 정의만 있고 Rust 어디서도 참조되지 않는다(pub 이라 dead_code 경고도 뜨지 않는다). 특히 LOG_PAGE_SIZE=100 은 프론트가 같은 값을 별도 상수로 다시 적어 쓰고 있어, 페이지 크기 규약이 두 곳에 갈라져 있다.", "evidence": "git/types.rs:4-5. 레포 전체 grep 결과 참조는 src/entities/git/git.query.ts:41(`const LOG_PAGE_SIZE = 100`)에서 독립 선언되어 :54·:194 에서 사용될 뿐, Rust 측 사용처는 0건.", "suggestedDirection": "실제로 페이지 크기를 Rust 가 정하는 값으로 삼을 것이면 tauri-specta 로 내보내 프론트가 그것을 쓰게 하고, 아니면 두 상수를 삭제하고 프론트 상수만 남긴다."}, {"severity": "minor", "lens": "L-CODE", "title": "ADR-0006 의 결과 조항(기동 시 git 설치 감지·안내)이 미구현", "file": "src-tauri/src/domain/git/service.rs", "line": 1711, "claim": "commit/push/pull/fetch/init 이 git CLI 서브프로세스에 의존하는데, ADR-0006 이 결과 조항으로 명시한 '기동 시 git --version 감지, 미설치면 commit/push UI 에 명확한 안내'가 어디에도 구현돼 있지 않다. git 이 없는 환경(예: macOS CLT 미설치)에서는 커밋 시도 시 'git 실행에 실패했습니다: No such file or directory' 만 뜬다.", "evidence": "레포 전체에서 `git --version` 감지 코드·`gitNotInstalled` 류 로케일 키 모두 0건. git/service.rs:1711-1717(실패 시 Internal 문자열). docs/adr/0006-git-backend.md '결과' 절 1항.", "suggestedDirection": "app 도메인의 기동 진단(또는 ide status 계열)에 git 가용성 판정을 추가하고, 미설치 시 commit/push/pull/fetch/init 진입을 UI 에서 사전 차단하거나 안내 문구를 띄운다."}, {"severity": "minor", "lens": "L-RUST-STATE", "title": "file_log 가 이력 전체를 커밋마다 tree-to-tree diff 하며 상한·취소가 없다", "file": "src-tauri/src/domain/git/service.rs", "line": 1048, "claim": "file_log 는 refs/heads/* 전체를 revwalk 하면서 각 커밋마다 first-parent 대비 pathspec diff 를 수행해 '이 커밋이 경로를 건드렸는지'를 판정한다. take 개를 채우지 못하면 저장소 전 이력을 끝까지 훑고, 상한도 취소 채널도 없다. 파일 히스토리 패널을 여는 것만으로 대형 저장소에서 수십 초~분 단위 blocking 태스크가 생긴다(spawn_blocking 안이라 UI 는 살아 있지만 작업은 끝까지 돌아간다).", "evidence": "git/service.rs:1048-1092(walk 전체 순회, break 는 take 충족 시에만), :1094-1105(commit_touches_path 가 커밋마다 diff_tree_to_tree). git/commands.rs:567-579(spawn_blocking, 취소 수단 없음). 대조: search 도메인은 sessionId 기반 취소를 갖는다(ipc-contract 의 search 절).", "suggestedDirection": "스캔 커밋 수 상한(예: 최대 N 커밋 검사 후 '더 있음' 표시)이나 search 와 같은 세션 취소 채널을 도입하고, 페이지 요청마다 처음부터 다시 훑지 않도록 커서(마지막 검사 oid)를 계약에 넣는다."}, {"severity": "minor", "lens": "L-RUST-LAYER", "title": "IPC 직렬화 타입 4개가 types.rs 가 아닌 service.rs 에 정의돼 모듈 분할 규약이 흐트러졌다", "file": "src-tauri/src/domain/git/service.rs", "line": 47, "claim": "architecture.md 는 도메인을 commands(IPC)/service(로직)/types(직렬화 타입)로 나누라고 규정하는데, 배치 범위에서만 4개의 Serialize+specta 타입이 service.rs 안에 산다. 그 결과 커맨드 시그니처에 `service::AheadBehind` 같은 참조가 섞이고, 타입 목록을 types.rs 한 곳에서 훑을 수 없다.", "evidence": "git/service.rs:47-52(AheadBehind, git/commands.rs:148 에서 `service::AheadBehind` 로 노출), file/service.rs:38-46(MirrorEntry)·55-61(UntitledMirrorEntry, file/commands.rs:6 에서 재수출), project/service.rs:18-23(ProjectOpenResult, project/commands.rs:107 반환 타입). architecture.md:73 \"각 domain 모듈은 commands.rs / service.rs / types.rs 로 나눈다\".", "suggestedDirection": "네 타입을 각 도메인 types.rs 로 옮기고 service/commands 는 그것을 참조만 하게 한다(생성 바인딩 이름은 바뀌지 않는다)."}, {"severity": "minor", "lens": "L-CODE", "title": "to_repo_relative 가 POSIX 에서도 백슬래시를 무조건 슬래시로 바꾸고 CurDir 을 걸러내지 않는다", "file": "src-tauri/src/domain/git/service.rs", "line": 1509, "claim": "git 도메인의 유일한 경로 정규화·검증 게이트인 to_repo_relative 가 첫 줄에서 `raw.replace('\\\\', \"/\")` 를 무조건 수행한다. POSIX 에서 백슬래시는 정상 파일명 문자이므로 `weird\\name.txt` 가 `weird/name.txt` 라는 존재하지 않는 하위 경로로 바뀌어 조용히 오동작한다. 또 `..`/절대 성분은 막지만 `.`(CurDir)은 통과시켜 `./src/a.rs` 같은 값이 그대로 git2 에 전달된다.", "evidence": "git/service.rs:1508-1528. 전수 확인 결과 경로를 받는 공개 함수 15종(stage/unstage/discard/show_file/diff_file/gutter/discard_hunk/conflict_sides/resolve_conflict/stage_hunk/unstage_hunk/stage_lines/unstage_lines/file_log/blame_range)이 모두 이 함수를 거치므로, 이 함수의 정규화 규칙이 곧 도메인 전체의 규칙이다. 필터는 :1520-1525 의 ParentDir|RootDir|Prefix 뿐.", "suggestedDirection": "백슬래시 치환은 `#[cfg(windows)]` 로 한정하고, Component::CurDir 은 걸러낸 뒤(또는 정규화 시 제거) 결과를 만들도록 좁힌다. 이 함수만 단위 테스트가 1건(:상위 탐색 거부)이므로 케이스를 함께 보강한다."}, {"severity": "minor", "lens": "L-RUST-STATE", "title": "git_stash_drop 만 이벤트를 발행하지 않아 다른 창·원격 세션의 스태시 목록이 갱신되지 않는다", "file": "src-tauri/src/domain/git/commands.rs", "line": 396, "claim": "git 도메인의 모든 mutation 커맨드가 GitStatusChanged 또는 GitRefsChanged 를 발행하는데 git_stash_drop 만 아무것도 발행하지 않는다. 호출한 창은 프론트 mutation 이 프로젝트 프리픽스를 invalidate 해 가려지지만, 다른 에디터 창이나 원격 세션은 이벤트가 유일한 무효화 채널이라 삭제된 스태시가 계속 보인다.", "evidence": "git/commands.rs:396-402(emit 없음) vs :366-378·380-394(stash_push/apply 는 emit_status_changed). 프론트 보정: src/entities/git/git.query.ts:108-118·160(useGitMutation 이 호출 창에서만 invalidate). 크로스 윈도우 채널: src/app/providers/ipc-sync-provider.tsx:46-52(gitStatusChanged/gitRefsChanged → invalidate).", "suggestedDirection": "git_stash_drop 도 형제 커맨드와 동일하게 emit_status_changed 를 호출한다(스태시 목록이 status 프리픽스로 무효화되므로 추가 이벤트 종류는 필요 없다)."}, {"severity": "minor", "lens": "L-RUST-STATE", "title": "TreeState 캐시가 축소되지 않고, rows_page 는 매 호출 전체 트리를 flatten 한다", "file": "src-tauri/src/domain/tree/service.rs", "line": 104, "claim": "collapse 는 expanded 집합에서만 지우고 cache 항목은 남겨, 한 번 펼친 디렉토리의 엔트리는 프로젝트가 닫힐 때까지 메모리에 누적된다. 또 rows_page 는 페이지 요청마다 flatten() 으로 전체 행(경로·이름 String 복제 포함)을 새로 만든 뒤 슬라이스만 반환해, 페이지네이션이 비용을 줄이지 못한다. 위의 '스토어 전체 복제' 문제와 곱해져 호출당 비용이 커진다.", "evidence": "tree/service.rs:104-106(collapse), :167-186(flatten 후 슬라이스), :147-165(push_children 이 행마다 String 2개 복제). 캐시 제거 경로는 invalidate(:116-125)의 대상 디렉토리 1건뿐.", "suggestedDirection": "collapse 시 하위 캐시를 함께 비우거나 LRU 상한을 두고(architecture.md §6-2 의 캐시 상한 원칙과 같은 취지), flatten 결과를 revision 과 함께 캐시해 변경이 없는 페이지 요청은 재계산 없이 슬라이스만 돌려준다."}]

## R5
요약: 범위 규모: Rust 8,169줄(domain/locale 4,144 · theme 1,509 · settings 1,482 · sync 1,034) + 프론트 약 3,950줄(entities/{locale,settings,theme,sync} 435 · features/settings 1,668 · features/theme 594 · widgets/settings-view 927 · widgets/theme-editor 233 · app/providers/{locale,theme}-provider 97). 총 약 12,100줄을 직접 열람했다.

기계 검증이 이미 0인 축 재확인 결과 0: FSD 역방향 import 0 · barrel(index.ts) 0 · any/enum 0 · useCallback/useMemo 0 · function 키워드 0 · 1파일 다중 컴포넌트 export 0 · 범위 내 프로덕션 경로 unwrap/expect/panic 0(적중 전량이 #[cfg(test)] 내부).

렌즈별 총평.
L-RUST-BOUND(주 렌즈): sync 는 ai·locale·settings·theme 4개 도메인을, settings 는 agent·ide·remote·theme 4개 도메인을 직접 호출해 앱에서 가장 엣지가 많은 두 허브다. architecture.md:77 의 "도메인 간 직접 호출 금지 + 이벤트 버스/상위 조립부" 규칙은 이 배치뿐 아니라 전체 14개 도메인 약 30개 엣지에서 지켜지지 않는다. sync 의 허브성은 기능상 불가피한 면이 있으나(집계 도메인), settings→{agent,ide,remote} 는 SettingsChanged 이벤트 버스가 같은 함수 안에 이미 있는데도 명령형 호출을 택한 것이라 성격이 다르다. 규칙을 코드에 맞출지 코드를 규칙에 맞출지 정본 결정이 필요하다.
L-RUST-STATE(주 렌즈): 시드로 지목된 locale/service.rs 4,081줄 단독 분해는 "정당한 리터럴 데이터"가 아니라고 판단한다 — 근거는 취향이 아니라 같은 저장소의 대조군이다. architecture.md:67 이 "테마와 완전히 같은 구조"라고 선언한 theme 도메인이 동일 성격 데이터를 resources/themes/*.json + include_str! 로 이미 분리했고, locale 만 키를 4중 중복 선언한 채 인라인으로 남았다. 락 관련해서는 await-across-lock 은 없으나(parking_lot 가드가 전부 문 단위 임시값), sync 커맨드가 전역 mutation mutex 를 GitHub 왕복 내내 점유하는 구조적 결합이 남아 있다.
L-RUST-LAYER: commands 는 전반적으로 얇고(파라미터→service→이벤트) service 는 tauri 비결합이라 계층은 대체로 지켜진다. 예외는 sync_upload/sync_disconnect 가 정본 문서가 선언한 단일 설정 쓰기 경로를 우회하는 것.
L-TYPE: 이 배치의 최대 부채. Settings 필드는 Rust 4곳 + TS 1곳에 손으로 미러링되고, 테마 토큰 약 200개도 Rust/TS 손 미러이며, Rust 기본값은 TS `??` 폴백으로 53회 재선언되어 이미 2개 필드에서 값이 어긋났다. ADR-0011 의 "생성으로 드리프트 차단" 원칙이 타입에는 적용되고 상수·기본값·키 목록에는 적용되지 않은 것이 공통 원인이다.
L-QUERY: queryOptions 팩토리·QUERY_KEY 계층·mutation onSuccess 무효화 책임은 전반적으로 규약을 따른다. 유일한 이탈이 테마 프리뷰의 캐시 직접 주입.
L-EFFECT/L-A11Y/L-I18N/L-SFC: useEffect 는 전부 외부 시스템 동기화(i18next 주입·CSS 변수 적용·시스템 테마 구독·shiki 초기화)로 정당. div onClick 0건, 하드코딩 UI 문자열 0건(t('...') 미등록 키도 0건 — TS 사용 키 595개 전량이 로케일에 존재), 파일당 export 컴포넌트 1개 준수. 단 settings-view 한 파일의 비대화는 남는다.

압축으로 누락한 저심각 5건(계수만): (1) theme-provider 가 테마 적용 외에 플러그인 문법 등록·shiki 초기화까지 겸함(단일 책임 흐림) (2) apply_integration_toggles 의 서버 기동 실패가 log::warn 만 남기고 사용자 피드백 없음 (3) list_locales/list_themes 가 손상된 사용자 JSON 을 무음 스킵(경고 미노출) (4) LocaleProvider 의 로드 실패 배너 문구만 영어 하드코딩(로케일 로드 실패 상황이라 불가피하나 근거 미기록) (5) settings-view 가 ThemeEditor/SnippetEditor 를 조기 return 전체 화면 교체로 라우팅.

acknowledge 기각/승인 사항은 재론하지 않았다: settings→remote 와일드카드 상수 엣지, civil_from_days 자체 구현 및 날짜 크레이트 미도입(2026-08-11-github-sync §3), sync_gist_id/sync_last_synced_at 을 SettingsPatch 에서 제외한 결정(같은 문서 §2), 테마/로케일 다운로드의 "실패분만 건너뛰기"(§4).
발견: [{"severity": "critical", "lens": "L-TYPE", "title": "SettingsPatch 병합이 \"미지정\"과 \"해제\"를 구분하지 못해 폰트·셸 되돌리기가 무동작", "file": "src-tauri/src/domain/settings/service.rs", "line": 314, "claim": "apply_patch 는 Option<String> 필드를 patch.x.or_else(|| settings.x) 로 병합한다. 즉 null 은 항상 \"값 유지\"로 해석되어, UI 가 제공하는 \"해제\" 조작이 백엔드에 도달할 수 없다. FontPicker 의 \"시스템 기본\" 항목과 셸 오버라이드 비우기는 이미 값이 설정된 뒤에는 아무 일도 하지 않는다.", "evidence": "settings/service.rs:314(shell_override)·319-321(editor/terminal/ui_font_family)·324(keymap_overrides)·364-365(ai_provider/ai_model) 전부 `.or_else(...)`. 반면 ai_omlx_base_url 만 merge_ai_omlx_base_url(268-274)로 빈 문자열=해제를 별도 구현. 프론트 경로: font-picker.tsx:71 `onSelect={() => handleSelect(null)}` → settings-view.tsx:539/745 `updateSettings({...emptySettingsPatch(), editorFontFamily})`, settings-view.tsx:752 `shellOverride: value.trim() || null`. emptySettingsPatch() 는 53필드 전부 null 이므로 전송 패치는 실질적으로 빈 패치다. docs/quality-assurance/2026-08-18-e2e-harness.md:187-195 는 같은 원인을 e2e teardown 한계로만 기록하고 제품 경로 영향은 다루지 않았다. Rust 측에 해제 시나리오 테스트 없음(settings/service.rs 테스트 418-1106).", "suggestedDirection": "patch 의 3상태(미지정/설정/해제)를 타입으로 표현한다. 예: Option<Option<T>> + `#[serde(default, deserialize_with = double_option)]`, 또는 ai_omlx_base_url 이 이미 쓰는 \"빈 문자열=해제\" 규약을 모든 Option 필드로 일반화하고 그 규약을 ipc-contract.md 에 명시. 어느 쪽이든 해제 왕복 테스트를 함께 건다."}, {"severity": "major", "lens": "L-RUST-LAYER", "title": "sync_upload·sync_disconnect 가 apply_and_broadcast 를 우회해 settings.json 을 직접 쓴다", "file": "src-tauri/src/domain/sync/commands.rs", "line": 130, "claim": "정본 문서와 코드 주석이 \"모든 설정 쓰기 진입점은 apply_and_broadcast 하나로 모인다\"고 선언하지만 실제로는 sync_upload·sync_disconnect 두 진입점이 save_settings + state 직접 대입으로 우회하고 SettingsChanged 를 발행하지 않는다. 그 결과 열려 있는 settings.json AppFile 탭이 갱신되지 않고, 그 탭을 저장하면 방금 기록된 syncGistId/syncLastSyncedAt 이 낡은 값으로 되돌아간다.", "evidence": "sync/commands.rs:130-135(upload)·91-96(disconnect) 는 settings_service::save_settings + `*state.settings.write()` 후 SyncStateChanged 만 emit. 계약 정본은 세 진입점만 열거한다: docs/ipc-contract.md:672-676, docs/data-model.md:432-436(settings_update·sync_download·app_file_write). settings/commands.rs:21-28 주석도 동일 주장. 프론트에서 settingsChanged 핸들러만 APP_FILE.CONTENT({kind:'settings'}) 를 무효화하고(ipc-sync-provider.tsx:54-63, 주석 56-61 이 정확히 이 덮어쓰기 위험을 설명) syncStateChanged 핸들러(65-70)는 무효화하지 않는다. app_file_write 는 버퍼 전체를 Settings 로 파싱해 적용한다(app/commands.rs:36-39).", "suggestedDirection": "sync_upload/sync_disconnect 의 설정 갱신도 apply_and_broadcast 로 통과시키거나(그러면 SettingsChanged 가 자동 발행), 통과가 부적절하면 ipc-contract.md·data-model.md 의 \"세 진입점\" 서술을 실제 다섯 진입점으로 고치고 syncStateChanged 핸들러에 APP_FILE 무효화를 추가한다."}, {"severity": "major", "lens": "L-TYPE", "title": "Settings 필드가 5곳에 손으로 미러링되고 Settings→SettingsPatch 방향만 컴파일 무보증", "file": "src-tauri/src/domain/settings/service.rs", "line": 13, "claim": "설정 한 필드를 추가하려면 Settings·Default impl·SettingsPatch·apply_patch·settings_to_sync_patch·emptySettingsPatch 6곳을 손으로 맞춰야 한다. 이 중 Settings→SettingsPatch 방향만 컴파일러가 강제하지 못해, 새 설정이 조용히 \"패치 불가·동기화 제외\" 상태로 남을 수 있다.", "evidence": "Settings 56필드(settings/types.rs:20-156) / SettingsPatch 53필드(settings/service.rs:13-69) / apply_patch 전 필드 수동 나열(308-386) / settings_to_sync_patch 전 필드 수동 나열(sync/service.rs:79-135) / emptySettingsPatch 53필드 수동 나열(src/entities/settings/settings.ipc.ts:5-58). apply_patch·Default 는 구조체 리터럴이라 누락 시 컴파일 실패하지만, Settings 에만 필드를 추가하고 apply_patch 에서 `settings.foo.clone()` 로 통과시키면 SettingsPatch 미등록이 탐지되지 않는다. 두 타입의 필드 집합을 비교하는 테스트는 없다(service.rs 테스트 418-1106). docs/acknowledge/2026-08-11-github-sync-domain-decisions.md §1 은 화이트리스트로 SettingsPatch 를 재사용한다는 결정만 기록한다.", "suggestedDirection": "필드 목록을 선언 매크로 한 곳에서 생성(Settings·SettingsPatch·apply_patch 동시 전개)하거나, 최소한 serde_json::to_value(Settings::default()) 의 키 집합과 SettingsPatch 키 집합 + 의도적 제외 목록(version·sync_*)을 비교하는 파리티 테스트를 추가한다."}, {"severity": "major", "lens": "L-CODE", "title": "에디터 폰트 크기의 유효 범위가 3곳에서 서로 다르고 Rust sanitize 는 아예 보정하지 않는다", "file": "src-tauri/src/domain/settings/service.rs", "line": 279, "claim": "sanitize 는 \"Settings 가 만들어지는 모든 출구에서 검증되지 않은 값이 Monaco/xterm 까지 흘러가는 것을 막는다\"고 선언하지만 editor_font_size·terminal_font_size·auto_save_delay_ms 는 클램프 대상이 아니다. 동시에 프론트의 두 컨트롤이 서로 다른 범위(8–32 vs 6–48)를 강제해 같은 필드에 세 가지 유효 범위가 존재한다.", "evidence": "settings/service.rs:276-306 sanitize 는 editor_tab_size(161-162)·terminal_scrollback(165-166)·resizer_thickness(167-168)만 clamp. 폰트 크기는 미보정. 설정 화면은 지역 상수 MIN_FONT_SIZE=8/MAX_FONT_SIZE=32(src/widgets/settings-view/settings-view.tsx:71-72)로, 상태바 확대/축소는 MIN_CODE_FONT_SIZE=6/MAX_CODE_FONT_SIZE=48(src/shared/constants/code-font-size.ts:1-3, 사용처 status-bar-content.tsx:36,140-143)로 클램프한다. 상태바로 6 까지 줄인 뒤 설정 화면 숫자 입력에서 blur 하면 8 로 조용히 올라간다. 또한 editor_font_size 는 settings_to_sync_patch(sync/service.rs:82)에 포함되어 손으로 편집한 gist 로도 유입 가능하다. settings-view 의 DEFAULT_FONT_SIZE=13(:73)은 shared/constants/code-font-size.ts:5 의 DEFAULT_CODE_FONT_SIZE 와 중복 정의다.", "suggestedDirection": "폰트 크기·자동저장 지연의 범위를 Rust sanitize 에 추가해 최종 방어선을 일치시키고, 프론트 범위 상수는 shared/constants/code-font-size.ts 하나로 통일한다(settings-view 지역 상수 제거)."}, {"severity": "major", "lens": "L-TYPE", "title": "Rust 기본값과 TS `??` 폴백이 이중 정의되어 이미 두 필드에서 값이 어긋나 있다", "file": "src/widgets/settings-view/settings-view.tsx", "line": 461, "claim": "specta 가 `#[serde(default)]` 때문에 Settings 의 모든 필드를 optional 로 생성하는 탓에 소비처마다 기본값을 손으로 다시 적는다. 그 사본이 이미 Rust 정본과, 그리고 소비처끼리도 불일치한다.", "evidence": "Rust 기본값은 default_true: agent_status_badge_enabled(settings/types.rs:54-55), ide_auto_open_diff(:60-61). TS 사본은 settings-view.tsx:461 `agentStatusBadgeEnabled ?? false`, :488 `ideAutoOpenDiff ?? false` 로 반대다. 같은 필드를 app-sidebar.tsx:40 은 `?? true` 로 읽어 소비처끼리도 다르다. settings-view 한 파일에만 이런 폴백이 53개 있다(:363~:902). 생성 타입은 전부 optional(src/shared/api/bindings.ts:1091-1160)이지만 런타임에서는 skip_serializing_if 가 없어 항상 채워져 나가므로 현재는 잠재 결함이다.", "suggestedDirection": "Serialize 측 optional 을 없애는 방향(specta 애트리뷰트로 Settings 를 필수 필드로 생성)으로 폴백 자체를 제거하거나, 불가능하면 Rust 기본값에서 유도한 SETTINGS_DEFAULTS 상수 하나를 shared 에 두고 모든 소비처가 그것만 참조하게 한다."}, {"severity": "major", "lens": "L-RUST-BOUND", "title": "settings 도메인이 agent·ide·remote 3개 도메인의 수명주기를 명령형으로 구동한다", "file": "src-tauri/src/domain/settings/commands.rs", "line": 50, "claim": "설정 도메인이 토글 변화를 감지해 IDE 서버 기동/정지, 에이전트 훅 설치/제거, 원격 서버 기동/정지를 직접 호출한다. 같은 함수가 바로 아래에서 SettingsChanged 를 발행하고 있어 architecture.md 가 규정한 이벤트 버스 경로가 이미 존재하는데도 쓰이지 않는다.", "evidence": "settings/commands.rs:50-81 apply_integration_toggles 가 ide_commands::ide_start/stop_server, hooks::reconcile_installed_hooks/uninstall_hooks_from_open_projects/stop_hooks_server, remote_commands::remote_start/stop_server 를 직접 호출(import 6-10). 규칙 정본은 docs/architecture.md:77 \"도메인 간 직접 호출은 금지하고, 필요하면 이벤트 버스 또는 상위 조립부를 거친다\". 이 배치 범위만 봐도 settings→{agent,ide,remote,theme}, sync→{ai,locale,settings,theme} 로 도메인당 4엣지이며, 전체 도메인 그래프는 14개 도메인에 걸쳐 약 30개 엣지다(domain/*/ 의 `use crate::domain::` 집계). 실패 시 log::warn 만 남고 사용자 피드백이 없다(:55,:75).", "suggestedDirection": "각 하위 시스템이 SettingsChanged 를 구독해 스스로 재조정하도록 뒤집거나(이벤트 버스는 이미 lib.rs 의 fanout 목록에 등록되어 있다), 그것이 과하면 lib.rs 부트스트랩에 \"조립부\" 계층을 두고 토글 반영을 그쪽으로 옮긴다. 어느 쪽도 택하지 않을 거라면 architecture.md:77 의 금지 규칙을 실제 계층 설계로 다시 쓴다."}, {"severity": "major", "lens": "L-RUST-STATE", "title": "sync 커맨드가 전역 mutation mutex 를 GitHub 왕복 내내(최대 60초) 점유한다", "file": "src-tauri/src/domain/sync/commands.rs", "line": 102, "claim": "sync_upload·sync_download 는 begin_mutation() 가드를 함수 첫 줄에서 잡고 네트워크 왕복이 끝난 뒤에야 놓는다. 그 사이 앱의 모든 mutation 커맨드(파일 저장·레이아웃 변경 등)가 직렬화되어 막힌다. 실제 상태 변경은 응답 수신 이후 마지막 몇 줄뿐이다.", "evidence": "sync/commands.rs:102(guard) → 119/122(update_gist/create_gist await) → 130-131(state 변경), sync_download 도 146(guard) → 159(fetch_gist await) → 175. 타임아웃은 요청 60초·연결 10초(infra/http.rs:3-4)이며 그 파일의 doc 주석이 \"several of those commands hold AppState::begin_mutation's guard while awaiting the response, which would also block every other mutation command for as long as the hang lasts\" 라고 이 결합을 이미 인지하고 있다(완화책은 타임아웃뿐).", "suggestedDirection": "네트워크 호출을 가드 밖에서 수행하고, 응답을 받은 뒤 가드를 잡아 settings 를 다시 읽어 병합·저장한다(락 안에서 IO 금지 — architecture.md §2.1)."}, {"severity": "major", "lens": "L-RUST-STATE", "title": "locale/service.rs 4081줄 중 3760줄이 메시지 리터럴 — 쌍둥이 도메인인 theme 은 같은 데이터를 이미 외부 JSON 으로 분리했다", "file": "src-tauri/src/domain/locale/service.rs", "line": 12, "claim": "로케일 데이터가 로직과 한 파일에 섞여 있고, 메시지 키 목록이 MESSAGE_NAMESPACES + en/ko/ja 3개 테이블에 총 4번 중복 선언된다. 같은 구조라고 문서가 선언한 theme 도메인은 동일 성격의 데이터를 resources/*.json + include_str! 로 이미 분리했으므로, 이는 정당화된 리터럴 데이터가 아니라 두 도메인 사이의 미해소 불일치다.", "evidence": "locale/service.rs 구성: MESSAGE_NAMESPACES 12-892(881줄), en_messages 905-1850, ko_messages 1852-2794, ja_messages 2796-3759, 실제 로직은 3761-3913(153줄)뿐. 대조군: theme/service.rs:11-75 는 번들 테마 36종을 `include_str!(\"../../../resources/themes/*.json\")` 로 참조한다. docs/architecture.md:67 은 \"locale 은 테마와 완전히 같은 구조\"라고 선언. 키 중복은 테스트로만 지켜진다(service.rs:3923-3945 두 테스트가 en=ko=ja 및 required_message_keys 포함 관계를 검사).", "suggestedDirection": "메시지를 resources/locales/{en,ko,ja}.json 으로 옮겨 include_str! 로 로드하고, MESSAGE_NAMESPACES 는 en 팩의 키에서 유도해 4중 중복을 1개 정본으로 줄인다(테마 도메인의 기존 패턴 그대로)."}, {"severity": "major", "lens": "L-TYPE", "title": "테마 토큰 목록 약 200개가 Rust 와 TS 에 손으로 미러링되어 있고 교차 검증이 없다", "file": "src/entities/theme/theme-tokens.ts", "line": 3, "claim": "테마 에디터가 쓰는 COLOR_NAMESPACES·SYNTAX_TOKENS·TERMINAL_TOKENS 가 Rust 의 required_* 키 목록과 별개로 손으로 유지된다. ADR-0011 이 세운 \"Rust 타입에서 TS 를 생성해 드리프트를 차단한다\"는 원칙 밖에 있고, 어긋나도 어떤 테스트도 잡지 못한다.", "evidence": "Rust: theme/service.rs:77-216(COLOR_NAMESPACES)·217-250(SYNTAX_TOKENS)·251-273(TERMINAL_ANSI_TOKENS) → required_color_keys/required_syntax_keys/required_terminal_keys(274-287). TS: src/entities/theme/theme-tokens.ts:3-107·111-143·147-168. 두 목록의 토큰 문자열을 추출·정렬 비교하면 현재는 완전히 일치하지만(차이 0), 일치를 강제하는 테스트나 생성기는 없다. 원칙 정본은 docs/architecture.md:15-16,125. 어긋나면 Rust 만 아는 토큰은 에디터에 노출되지 않고, TS 만 아는 토큰은 저장 후 base 채움 경고를 유발한다.", "suggestedDirection": "required_color_keys/required_syntax_keys/required_terminal_keys 를 IPC(또는 빌드 타임 생성 상수)로 노출해 TS 가 그것을 소비하게 하거나, 최소한 두 목록의 동치를 검사하는 테스트를 한쪽에 둔다."}, {"severity": "major", "lens": "L-QUERY", "title": "테마 프리뷰가 조작한 ResolvedTheme 를 서버 상태 캐시에 직접 써 넣는다", "file": "src/entities/theme/theme.query.ts", "line": 44, "claim": "useThemePreview 가 서버에서 온 적 없는 합성 ResolvedTheme 를 QUERY_KEY.THEME.CURRENT 캐시에 setQueryData 로 주입해 캐시를 UI 프리뷰 채널로 전용한다. 서버 캐시의 의미가 깨지고, 같은 키를 무효화하는 다른 구독자와 경합한다.", "evidence": "theme.query.ts:33-53 — toResolvedThemeFromDraft 로 draft 에서 만든 값(warnings: [] 로 위조)을 `queryClient.setQueryData(QUERY_KEY.THEME.CURRENT, ...)`, clearPreview 는 invalidateQueries. 소비처 theme-editor.tsx:106-109 은 draft 변경마다 setPreview. 한편 theme-provider.tsx:37 은 OS 테마 변경 구독에서 QUERY_KEY.THEME.ALL 을 무효화하므로 편집 중 프리뷰가 조용히 되돌아간다. query.md §5·§7 은 캐시 직접 조작 대신 무효화 후 재조회를 기본으로 규정한다.", "suggestedDirection": "프리뷰 오버라이드를 별도 채널(예: 프리뷰 전용 Context 또는 shared/store 의 UI 상태)로 분리하고, 테마 CSS 변수를 적용하는 지점에서 \"서버 테마 + 프리뷰 오버레이\"를 합성한다. 서버 캐시는 서버 응답만 담게 둔다."}, {"severity": "major", "lens": "L-SFC", "title": "settings-view.tsx 가 927줄·훅 25개·12섹션을 한 컴포넌트에 담고 있고 분해 기준이 일관되지 않다", "file": "src/widgets/settings-view/settings-view.tsx", "line": 176, "claim": "12개 설정 섹션 중 Sync·Remote·LSP 등 일부만 features 컴포넌트로 분리되고 나머지 8개 섹션은 위젯 본문에 인라인 JSX 로 남아 있다. 같은 파일이 쿼리 11개·뮤테이션 14개를 직접 소유해 한 섹션을 고칠 때마다 전체 파일을 건드린다.", "evidence": "src/widgets/settings-view/settings-view.tsx:185-212 에 useQuery 11개·useMutation 14개·useQueryClient. 섹션 정의 SETTINGS_SECTION_ID 12개(:133-146). 분리된 것: SyncSection(:879)·RemoteSection(:894)·LspServerStatusList(:795)·PluginManager(:875). 인라인: Appearance(:357-407)·Interface(:428-522)·Editor(:524-712)·Terminal(:730-783) 등. 또한 위젯이 ThemeEditor/SnippetEditor 를 조기 return 으로 전체 화면 교체해(:324-334) 라우터 역할까지 겸한다.", "suggestedDirection": "이미 존재하는 SyncSection/RemoteSection 패턴대로 나머지 섹션도 features 컴포넌트(값+onChange props)로 내리고, 위젯은 쿼리 조립과 섹션 배치만 남긴다. 전체 화면 교체는 상위(탭/라우팅) 책임으로 올린다."}, {"severity": "minor", "lens": "L-CODE", "title": "어디서도 참조되지 않는 로케일 키 39개가 3개 언어에 유지되고 있다", "file": "src-tauri/src/domain/locale/service.rs", "line": 894, "claim": "MESSAGE_NAMESPACES 에 등록되고 en/ko/ja 3벌 번역까지 있는 키 39개가 프론트·Rust·e2e 어디에서도 참조되지 않는다. 테스트는 \"en 의 키가 전부 MESSAGE_NAMESPACES 에 있는가\"만 검사하고 반대 방향(미사용)은 보지 않는다.", "evidence": "en_messages 의 키를 추출해 src·e2e·src-tauri 전체 문자열 참조와 대조한 결과 39개가 미참조(예: git.push·git.pull·git.tags·terminal.newTerminal·terminal.processExited·ide.startFailed·settings.themeImportButton·themeEditor.previewSidebarTitle·common.rename·common.reset·app.welcome). 동적 키(themeEditor.ns.*, labelKey 상수)와 Rust 원격 로그인 페이지 사용분(remote/login_page.rs:153)은 제외한 수치다. 검사 테스트는 locale/service.rs:3937-3945 한 방향뿐. 39키 × 3언어 ≈ 117개 리터럴이 유지 대상으로 남는다.", "suggestedDirection": "실제 참조를 스캔해 미사용 키를 정리하고, 반대 방향(등록되었으나 미사용) 검사를 CI 스크립트나 테스트로 추가할지 결정한다. 의도적으로 예약해 둔 키가 있으면 그 사실을 명시한다."}, {"severity": "minor", "lens": "L-RUST-LAYER", "title": "list_themes 는 중복 id 를 그대로 나열하고 list_locales 는 교체한다 — 같은 구조라는 선언과 어긋난다", "file": "src-tauri/src/domain/theme/service.rs", "line": 903, "claim": "사용자 테마 디렉토리에 내장/번들 id 와 같은 파일이 있으면 테마 목록에 같은 id 가 두 번 나타나고, load_theme 는 내장 쪽을 택하며, delete_theme 는 내장 id 라는 이유로 삭제를 거부해 그 파일이 앱에서 제거 불가능해진다. 로케일 쪽은 같은 상황에서 기존 항목을 교체한다.", "evidence": "theme/service.rs:903-911 은 무조건 `list.push(...)`; locale/service.rs:3845-3848 은 `find(...)` 후 교체. load_theme 은 builtin 우선(944-947), delete_theme 은 builtin id 거부(932-935). save_theme 의 내장 id 거부(920-922)는 저장 시점 기준이므로, 앱 업데이트로 번들 테마가 늘어나면 기존 사용자 파일이 뒤늦게 충돌 상태가 된다. 프론트는 `key={theme.id}` 로 렌더한다(features/settings/theme-picker.tsx:40).", "suggestedDirection": "두 도메인의 목록 병합 규칙을 하나로 맞추고(교체 또는 충돌 표시), 내장 id 와 충돌한 사용자 파일을 사용자가 처리할 수 있는 경로(경고 표시 또는 삭제 허용)를 둔다."}, {"severity": "minor", "lens": "L-RUST-BOUND", "title": "sync 가 시크릿 마스킹 유틸을 ai 도메인에서 직접 가져다 쓴다", "file": "src-tauri/src/domain/sync/github.rs", "line": 5, "claim": "도메인 무관한 공통 유틸(에러 메시지 토큰 마스킹)이 ai 도메인 안에 남은 채 sync 가 직접 참조한다. 2개 도메인이 쓰는 순간 공용 계층으로 승격되어야 할 코드가 도메인 경계를 넘는 엣지로 굳었다.", "evidence": "sync/github.rs:5 `use crate::domain::ai::providers::mask_provider_error;`(사용처 74,79,87,101,113,124). 정의는 ai/providers/mod.rs:17. 규칙 정본은 docs/architecture.md:77. docs/acknowledge/2026-08-11-github-sync-domain-decisions.md §6 은 재사용 자체는 정당화하지만(그 웨이브에서 ai/* 를 수정하지 않으려 함) 위치 승격은 미해결로 남겼다.", "suggestedDirection": "mask_provider_error 를 infra 또는 lib 의 공용 모듈로 옮기고 ai·sync 가 함께 참조하게 한다(동작 변경 없는 이동)."}, {"severity": "minor", "lens": "L-CODE", "title": "sync_status 가 같은 keyring 항목을 한 호출에서 두 번 읽는다", "file": "src-tauri/src/domain/sync/commands.rs", "line": 34, "claim": "OS 키체인 조회를 한 커맨드 안에서 중복 수행한다. 두 번째 조회는 첫 조회 결과를 재사용하면 제거된다.", "evidence": "sync/commands.rs:34 `let connected = secret.get(SecretAccount::GithubSync)?.is_some();` 직후 :38 `if let (true, Some(token), Some(gist_id)) = (connected, secret.get(SecretAccount::GithubSync)?, ...)` 로 동일 계정을 다시 조회한다.", "suggestedDirection": "첫 조회 결과를 `Option<String>` 으로 보관해 connected 판정과 토큰 사용에 함께 쓴다."}, {"severity": "minor", "lens": "L-CODE", "title": "bundled_themes 가 호출마다 36개 테마 JSON 을 다시 파싱하고 잉여 map 왕복이 남아 있다", "file": "src-tauri/src/domain/theme/service.rs", "line": 748, "claim": "컴파일 타임에 고정된 번들 테마를 매 호출 파싱한다. list_themes 는 호출당 36회 파싱하고, theme_exists/save_theme/delete_theme 이 부르는 builtin_by_id 도 매번 파싱한다. 같은 함수에 결과를 쓰지 않는 튜플 map 이 두 번 겹쳐 있다.", "evidence": "theme/service.rs:748-754 `filter_map(... .map(|theme| (id, theme))).map(|(_, theme)| theme)` — id 를 만들었다가 즉시 버린다. 호출처: list_themes(:897), builtin_by_id(:760) ← theme_exists(:961) ← settings::service::set_theme(settings/service.rs:399).", "suggestedDirection": "OnceLock 등으로 파싱 결과를 1회 캐시하고, 쓰이지 않는 튜플 map 단계를 제거한다."}]

## R6
요약: 배치 R6 범위 = src-tauri/src/domain/{ai,agent,ide} 전 20파일 7,589줄(테스트 2,567줄 = 33.8% 제외 시 프로덕션 약 5,022줄). 전 파일 실독했다. 기계 검증 0 축(FSD 역방향 import·barrel·any/enum·useCallback/useMemo·function 키워드)은 프론트 전용이라 이 배치에 해당 코드 없음 — 재확인 결과 0. 프로덕션 경로 unwrap/expect 는 ai/prompt.rs 의 include_str! 번들 리소스 파싱 3건뿐이며 전용 테스트가 이를 커버해 지적 제외. acknowledge 기결 항목(ide/server.rs auth_callback 의 #[allow(clippy::result_large_err)] — 2026-08-07-qa-batch-decisions.md:52, 프롬프트 템플릿 파일 3분할 — 2026-08-16-wave-g §2-3)은 재론하지 않았다.

렌즈별 총평 — L-RUST-BOUND: ide 가 명백한 허브다. 외향 엣지 4개(layout·plugin·project·file) 중 2개가 상대 도메인의 commands 모듈이며, MCP WS 핸들러가 #[tauri::command] 를 직접 호출해 원격 디스패치 테이블과 무관한 제2 커맨드 진입점을 만든다(architecture.md:77 위반). agent 의 외향 엣지는 terminal::commands::TerminalStore 1개로 얕고, ai 는 crate::domain 외향 의존 0으로 가장 깨끗하다. L-RUST-LAYER: 세 도메인 모두 service.rs 는 tauri 무의존 순수 로직으로 잘 지켜졌으나(ai/service·agent/service·ide/service), 각 도메인의 "네 번째 파일"(agent/hooks.rs·ide/server.rs)이 TCP/WS 전송을 domain 안에서 직접 수행하며 AppHandle 에 결합하고, 그 IO/상태 헬퍼가 commands.rs 에 얹혀 전송↔IPC 양방향 의존이 생겼다. agent/commands.rs(717줄)는 IPC·fs·ps 서브프로세스·osascript 특권 실행이 한 파일에 겹친 최대 혼합층이다. 외부 프로세스/프롬프트 주입면: prompt.rs 의 단일 패스 치환(render_placeholders)과 basename 축약, providers/mod.rs 의 bearer/장문 토큰 마스킹은 근거·테스트가 촘촘해 주입·유출면에서 결함을 찾지 못했다 — 대신 결함은 "적용 범위의 비대칭"에 몰려 있다(auto-tab 만 상한·클램프 없음, codex 만 절단 검출 없음). 시드 확인: instruct/complete 이원화의 중복은 실재하며 ollama↔omlx 는 complete 본문·complete_chat·chat 파이프라인이 문자 단위로 두 벌이다. 마스킹 자체는 세 provider 전부 동일 경로로 일관되지만, 마스킹된 메시지가 전부 AppError::Internal 문자열로 평탄화되어 상태코드 구분이 소실된다.

압축으로 제외한 저심각 6건(계수만): ①ide/server.rs:115 encode() 의 unwrap_or_default 로 직렬화 실패가 빈 WS 프레임이 됨 ②hooks.rs:173-181 parse_query_param 의 퍼센트 디코딩 부재(현 토큰이 hex uuid 라 무해) ③agent/service.rs:282-286 is_cwd_within_root 의 문자열 접두사 비교(정규화 없음, 배지 귀속 용도라 영향 한정) ④ide/server.rs:342-349 openDiff 의 pending 삽입-후-emit 순서(emit 실패 시 10분 잔류) ⑤ide/lockfile.rs:97-105 is_pid_alive 의 pid 재사용 취약(잔여 lockfile 보존 방향이라 안전측) ⑥ai/providers/*.rs 에서 masked body 를 바깥 format! 로 감싸 최종 메시지 길이가 MAX_PROVIDER_ERROR_MESSAGE_LEN 을 초과할 수 있음.
발견: [{"severity": "critical", "lens": "L-RUST-BOUND", "title": "agent_cli_install/uninstall·agent_hooks_install(User) 이 원격 세션에서 그대로 실행 — dispatch 의 자체 거부 분류를 두 항목 모두 위반", "file": "src-tauri/src/domain/agent/commands.rs", "line": 487, "claim": "remote/dispatch.rs 는 (a) 데스크톱 화면에 네이티브 창을 띄우는 커맨드와 (b) 프로젝트 root-guard 밖 로컬 파일시스템에 쓰는 커맨드를 원격에서 일괄 거부한다는 명시적 정책을 갖고 있는데, agent 도메인의 세 커맨드는 두 조건에 모두 해당하면서 거부 arm 없이 실핸들러로 디스패치된다.", "evidence": "dispatch.rs:636-637 `\"agent_cli_install\" => respond(agent::agent_cli_install().await)` / `\"agent_cli_uninstall\" => ...` 는 바로 위 624행 `\"plugin_install\" | \"plugin_uninstall\" => Err(deny_remote_plugin_install(name))` 와 나란히 있다. deny_remote_plugin_install 의 사유는 \"원격 세션이 데스크톱 로컬 파일시스템 밖에 읽고 쓰게 해선 안 된다\"(dispatch.rs:310-317), deny_remote_open_external_url/deny_remote_window_open 의 사유는 \"원격에서 볼 수도 쓸 수도 없는 창을 데스크톱에 띄우게 된다\"(dispatch.rs:345-357). agent_cli_install 은 commands.rs:487-497 → run_cli_osascript(commands.rs:365-376) 으로 `osascript -e \"do shell script ... with administrator privileges\"`(service.rs:139-149) 를 실행해 데스크톱에 **관리자 암호 프롬프트**를 띄우고 /usr/local/bin 에 심링크를 만든다. agent_hooks_install 의 User 스코프(commands.rs:600-621)는 `~/.codex/hooks.json`·`~/.gemini/settings.json`(service.rs:357-370)에 `\"taide-cli\" hook --url ...` command 훅을 주입한다 — 전부 프로젝트 root-guard 밖이며, 주입된 command 는 이후 에이전트 CLI 가 실행한다.", "suggestedDirection": "세 커맨드를 dispatch 의 기존 거부 분류에 편입한다(deny_remote_plugin_install 과 같은 계열의 deny arm 신설, IMPLEMENTED_JSON_COMMANDS 목록에는 남겨 parity 테스트 유지). agent_hooks_install 은 Project 스코프만 원격 허용하고 User 스코프를 거부하는 분기도 검토 대상."}, {"severity": "major", "lens": "L-RUST-LAYER", "title": "ide_resolve_diff 가 begin_mutation 없이 파일을 쓰고 file 도메인 service 를 직접 호출 — file_save 본문 복제", "file": "src-tauri/src/domain/ide/commands.rs", "line": 437, "claim": "레포의 모든 쓰기 커맨드가 취하는 앱 전역 직렬화 가드(AppState::begin_mutation)를 이 경로만 건너뛰고, 동시에 file 도메인의 service 를 직접 호출해 file_save 의 본문을 복제한다.", "evidence": "ide/commands.rs:420-451 `ide_resolve_diff` 는 `crate::domain::file::service::save_file(&resolved_path, &content)?; crate::domain::file::service::clear_mirror(...)` (437-438) 를 호출하는데 함수 전체에 `state.begin_mutation()` 이 없다. 동일 3줄을 수행하는 file/commands.rs:26-33 `file_save` 는 첫 줄이 `let _guard = state.begin_mutation().await;` 다. begin_mutation 은 file/tree/layout/settings/app/agent 커맨드 전반(grep 기준 30+ 지점)에서 취해지며 agent_release_marker(agent/commands.rs:466) 조차 취한다. ide_resolve_diff 는 원격 디스패치 대상(dispatch.rs:970)이기도 해서 desktop file_save·hot-exit flush 와 실제로 병행 진입 가능하다. 더해 architecture.md:77 \"도메인 간 직접 호출은 금지\" 도 함께 위반한다.", "suggestedDirection": "Saved 분기를 file 도메인의 기존 진입점(file_save 상당)으로 위임하거나, 최소한 begin_mutation 가드를 취한 뒤 저장하도록 맞춘다. 크로스도메인 호출은 상위 조립부/이벤트 경유로 정리."}, {"severity": "major", "lens": "L-RUST-BOUND", "title": "MCP WebSocket 서버가 layout/plugin 도메인의 #[tauri::command] 를 직접 호출 — 커맨드 표면의 제2 진입점", "file": "src-tauri/src/domain/ide/server.rs", "line": 271, "claim": "ide/server.rs 는 외부 프로세스(Claude Code)가 여는 WS 커넥션 핸들러에서 다른 도메인의 IPC 커맨드 함수를 직접 호출한다. 이는 architecture.md:77 의 도메인 간 직접 호출 금지 위반이자, 원격 디스패치 테이블이 가진 거부/보정 정책을 통과하지 않는 두 번째 커맨드 진입 경로를 만든다.", "evidence": "server.rs:22-25 가 `use crate::domain::layout::commands as layout_commands; use crate::domain::layout::service as layout_service; use crate::domain::plugin::commands::{self as plugin_commands, PluginStore};` 를 끌어온다. tool_open_file 은 server.rs:271-281 에서 `layout_commands::layout_open_tab(app.clone(), state, project_id, TabKind::File{..}, title, None, preview).await` 를 호출하는데 layout_open_tab 은 layout/commands.rs:83-85 의 `#[tauri::command] #[specta::specta] pub async fn` 이다. tool_close_tab/tool_close_all_diff_tabs 도 `layout_commands::close_tab_and_finish`(server.rs:509·528), tool_open_file/tool_get_open_editors 는 `plugin_commands::ensure_loaded`(server.rs:286·370)를 직접 부른다. ide 도메인의 외향 의존은 layout(commands·service·types)·plugin(commands·types)·project(types)·file(service) 4개로, 이 중 2개가 상대 도메인의 commands 모듈이다.", "suggestedDirection": "MCP 툴이 필요로 하는 동작을 layout/plugin 의 service 레벨 또는 상위 조립부가 제공하는 중립 API 로 노출하고, server.rs 는 그 API 만 부르게 한다. 최소한 commands 모듈 의존을 service 의존으로 낮춘다."}, {"severity": "major", "lens": "L-RUST-LAYER", "title": "ai_inline_complete 만 요청 크기 상한이 없다 — 같은 원격 허용목록에 있고 프롬프트 경로도 클램프하지 않는다", "file": "src-tauri/src/domain/ai/commands.rs", "line": 112, "claim": "ai/commands.rs 는 원격 디스패치 가능한 AI 커맨드에 바이트 상한을 두는 규약을 문서화해 두고 ai_inline_edit·ai_commit_message 에만 적용했는데, 같은 허용목록에 있는 ai_inline_complete 는 prefix/suffix 에 어떤 상한도 없고 auto-tab 프롬프트 렌더러도 클램프하지 않는다.", "evidence": "commands.rs:18-29 의 doc comment 는 \"Both commands are remotely dispatchable (domain/remote/dispatch.rs) … a raw IPC/remote call has nothing else stopping it from handing an arbitrarily large payload to the provider HTTP request\" 라고 근거를 적고 AI_INLINE_EDIT_SELECTION_MAX_BYTES 등 4개 상수를 정의한다. 그러나 dispatch.rs:174 는 `\"ai_inline_complete\"` 를 동일 허용목록에 올려 두고 989행에서 실핸들러로 보낸다. ai_inline_complete(commands.rs:112-140)에는 ensure_within_byte_limit 호출이 없고, 그 프롬프트 렌더 경로 prompt::render(prompt.rs:125-135)는 prefix/suffix 를 그대로 치환한다 — 반면 render_inline_edit(prompt.rs:137-151)는 clamp_prefix_context/clamp_suffix_context 로 2,000자씩 자른다.", "suggestedDirection": "AiInlineCompleteRequest 의 prefix/suffix(및 model/requestId)에도 ensure_within_byte_limit 을 적용하거나, auto-tab 렌더 경로에 inline-edit 과 동일한 컨텍스트 클램프를 두어 세 커맨드의 입력 계약을 맞춘다."}, {"severity": "major", "lens": "L-RUST-STATE", "title": "ensure_hooks_server_started 의 검사-후-바인드 경합 — 리스너 이중 기동·accept 태스크 유실·이미 기록된 훅 URL 토큰 무효화", "file": "src-tauri/src/domain/agent/hooks.rs", "line": 18, "claim": "server_info() 조회와 set_server() 사이에 await 지점(bind)이 있어 동시 호출 시 두 개의 리스너가 뜨고, 나중 호출이 앞선 accept 핸들을 abort 없이 덮어써 태스크·포트가 유실되며, 앞서 파일에 기록된 훅 URL 의 토큰이 더 이상 기대 토큰이 아니게 된다.", "evidence": "hooks.rs:18-44 는 `if let Some(info) = store.server_info() { return Ok(info); }` 로 검사한 뒤 `TcpListener::bind(\"127.0.0.1:0\").await` 를 거쳐 `store.set_server(info.clone(), accept_handle)` 를 호출한다. set_server(agent/commands.rs:134-138)는 기존 accept_handle 을 abort 하지 않고 `guard.accept_handle = Some(accept_handle)` 로 그대로 대체한다. 동시 진입 경로가 실제로 여럿이다: lib.rs:677(부팅 태스크), settings/commands.rs:64(설정 토글), project/commands.rs:128(프로젝트 열기), agent/commands.rs:589·607(agent_hooks_install 두 스코프). 예컨대 프로젝트 열기 중 훅 설치 버튼을 누르면 두 호출이 모두 None 을 보고 각자 bind 한다. 앞선 라운드에서 reconcile 이 settings.local.json 에 기록한 hook_url 의 token 은 두 번째 info 로 교체되므로, 이후 도착하는 훅 요청은 handle_connection(hooks.rs:250-254)의 constant_time_eq 에서 403 으로 조용히 떨어진다.", "suggestedDirection": "바인드까지 포함한 기동을 단일 진입으로 직렬화한다(예: 스토어에 \"기동 중\" 상태를 두고 락 안에서 선점, 또는 OnceCell/tokio::sync::Mutex 로 감싼 초기화). set_server 가 기존 핸들을 발견하면 abort 하도록도 보강."}, {"severity": "major", "lens": "L-RUST-ERR", "title": "Codex instruct 는 completed 이벤트 없이 스트림이 끝나면 잘린 텍스트를 정상 결과로 반환 — ollama/omlx 가 이미 막은 무경고 절단 재발", "file": "src-tauri/src/domain/ai/providers/codex.rs", "line": 202, "claim": "세 provider 중 ollama/omlx 만 instruct 경로에서 절단을 에러로 승격하고(fail_on_truncation), codex 는 SSE 스트림이 response.completed 없이 끝나도 누적 텍스트를 Ok(Some) 으로 돌려준다. Inline Edit 는 이 값을 선택 영역 치환에 그대로 쓰므로 반쪽 코드가 완성된 결과처럼 적용된다.", "evidence": "codex.rs:190-203 의 read_codex_completion 은 while 루프가 정상 종료되면 `Ok((!content.trim().is_empty()).then_some(content))` 를 반환하며, 이 동작은 테스트 `완성_이벤트_없이_스트림이_끝나도_누적된_텍스트를_반환한다`(codex.rs:457-466)로 고정돼 있다. codex.rs:258-260 의 instruct 는 complete 와 같은 send_responses_request 를 그대로 쓴다. 반면 ollama.rs:246-254·omlx.rs:347-355 의 extract_chat_text 는 `fail_on_truncation && done_reason==\"length\"` 를 AppError::Internal 로 올리고, ollama.rs:95-98·omlx.rs:191-194 가 instruct 에 대해서만 true 를 넘긴다. 이 분리 자체가 docs/acknowledge/2026-08-16-wave-g-ai-contract.md:152-153 의 \"[major] instruct 가 auto-tab 용 256 토큰 출력 캡을 상속 — Inline Edit 결과 무경고 절단\" 수정으로 도입된 것인데, codex 만 같은 보정을 받지 않았다.", "suggestedDirection": "read_codex_completion 에 \"completed 이벤트를 봤는가\" 플래그를 두고, instruct 호출 시 completed 없이 끝난 스트림을 에러로 승격한다(complete/auto-tab 경로는 현행 관용 유지)."}, {"severity": "major", "lens": "L-RUST-STATE", "title": "AI 커맨드마다 reqwest::Client 를 새로 만든다 — auto-tab 매 요청이 커넥션 풀·TLS 핸드셰이크를 버린다", "file": "src-tauri/src/domain/ai/commands.rs", "line": 127, "claim": "reqwest::Client 는 내부에 커넥션 풀을 소유해 재사용이 전제인데, ai 도메인의 5개 커맨드가 호출마다 새 Client 를 만들어 keep-alive 가 매번 폐기된다. 가장 잦은 호출자가 키 입력 디바운스마다 도는 auto-tab 이다.", "evidence": "commands.rs:88·106·127·176·223 이 각각 `let client = create_outbound_http_client();` 로 매 호출 새 Client 를 만든다(sync 도메인도 같은 패턴 4곳). 이 팩토리는 infra/http.rs:12-18 에서 `reqwest::Client::builder()...build()` 를 매번 수행한다. ai_inline_complete(commands.rs:112)는 auto-tab ghost text 경로이며, providers/ollama.rs:14 의 OLLAMA_NUM_PREDICT doc comment 는 \"this value must not change without re-validating auto-tab latency (contract §3.1 'auto-tab 회귀 0')\" 라고 이 경로의 지연 예산이 계약 대상임을 명시한다. 부수적으로 build 실패 시 `unwrap_or_default()`(infra/http.rs:17) 는 타임아웃이 전혀 없는 기본 Client 로 조용히 폴백해 같은 파일 doc comment 가 선언한 보증을 뒤집는다(범위 밖 파일, 참조).", "suggestedDirection": "Client 를 앱 상태로 한 번 만들어 관리(app.manage 또는 AppState 필드)하고 커맨드가 그것을 빌려 쓰게 한다. 팩토리는 부트스트랩 1회 호출로 축소."}, {"severity": "major", "lens": "L-RUST-LAYER", "title": "agent/commands.rs 가 IPC·infra(ps 서브프로세스·fs·심링크·osascript)·로직을 한 파일에 겹쳐 담고, hooks.rs 가 그 IO 를 역참조한다", "file": "src-tauri/src/domain/agent/commands.rs", "line": 409, "claim": "architecture.md 는 command 를 \"파라미터 검증 → service 호출 → 이벤트 발행\" 으로 얇게 두고 로직은 service, 외부 자원 어댑터는 infra 에 두라고 규정하는데, agent/commands.rs(717줄)는 훅 설정 파일 IO·프로세스 조회·osascript 실행을 직접 품고 있고 전송 계층인 hooks.rs 가 다시 그것을 끌어 쓴다.", "evidence": "commands.rs 안에 파일 IO 계층 전체가 있다: settings_local_path(378), read_json_file_rejecting_invalid(384), existing_file_mode(393), write_hooks_file_preserving_mode(398), read_settings_local(409)/write_settings_local(413)/read_user_level_hooks(417)/write_user_level_hooks(421), home_dir_env(425). 프로세스 조회는 `std::process::Command::new(\"ps\").args([\"-o\",\"comm=,state=,args=\",...])`(206-227), 특권 실행은 run_cli_osascript(365-376). 반면 순수 로직 파일 agent/service.rs 는 1~529행 전부 tauri/IO 무의존이라 대비가 뚜렷하다. hooks.rs:6 은 `use super::commands::{self, AgentHooksStore, AgentStore, HooksServerInfo};` 로 IPC 모듈을 역참조해 commands::read_settings_local/write_settings_local/read_user_level_hooks/write_user_level_hooks/home_dir_env/TAIDE_CLI_TARGET_PATH 를 호출한다(hooks.rs:70·77·90·98·106·119·127·136·144·151).", "suggestedDirection": "훅 설정 파일 IO 는 service(또는 infra) 로, ps/osascript 호출은 infra 어댑터로 내리고, commands 는 검증·호출·이벤트만 남긴다. hooks.rs 는 commands 대신 그 계층에 의존하게 한다."}, {"severity": "major", "lens": "L-RUST-BOUND", "title": "agent/service.rs 가 infra::crypto::constant_time_eq 를 쓰지 않고 동일 구현을 재작성 — 보안 프리미티브 단일 출처 이탈", "file": "src-tauri/src/domain/agent/service.rs", "line": 275, "claim": "상수시간 비교는 이미 infra/crypto.rs 에 공용으로 있고 ide·remote 도메인이 그것을 참조하는데, agent 도메인만 바이트 단위로 같은 함수를 별도 정의하고 테스트까지 복제했다.", "evidence": "infra/crypto.rs:1-6 과 agent/service.rs:275-280 은 본문이 완전히 동일하다(`if a.len() != b.len() { return false } a.iter().zip(b.iter()).fold(0u8, |acc,(x,y)| acc | (x^y)) == 0`). 다른 소비자는 공용을 쓴다: ide/service.rs:12 `pub use crate::infra::crypto::constant_time_eq;`, remote/service.rs:1, remote/commands.rs:17. agent 쪽 사본은 hooks.rs:250 의 훅 토큰 검증에 쓰이고, 같은 테스트 3건이 infra/crypto.rs:12-25 와 agent/service.rs:851-862 에 중복 존재한다. common.md \"2회 이상 룰\" 의 반대 방향 위반(이미 추출된 공용을 재구현).", "suggestedDirection": "agent/service.rs 의 사본과 그 중복 테스트를 제거하고 ide/service.rs 와 같은 방식으로 infra::crypto 를 재-export 하거나 직접 참조한다."}, {"severity": "major", "lens": "L-RUST-LAYER", "title": "ollama·omlx provider 가 complete 의 FIM-우선/chat-폴백 계약과 chat 파이프라인을 통째로 중복 구현 — instruct 추가 후 공통부가 회수되지 않았다", "file": "src-tauri/src/domain/ai/providers/omlx.rs", "line": 226, "claim": "trait 에 instruct 를 얹으면서 두 provider 의 chat 메커닉이 각자 send_chat_request/extract_chat_text 로 복제됐고, complete 본문과 complete_chat 은 문자 단위로 같은 코드가 두 벌 존재한다. 계약을 고치려면 항상 두 곳을 같이 고쳐야 한다.", "evidence": "ollama.rs:131-149 와 omlx.rs:226-244 의 `complete` 는 AiPromptVars 구성 → `if let Some(text) = self.complete_fim(...).await { return Ok(Some(text)) }` → `self.complete_chat(...)` 까지 동일하다. complete_chat(ollama.rs:185-196 / omlx.rs:290-301)도 동일 형태다. send_chat_request(ollama.rs:206-239 / omlx.rs:308-340)와 extract_chat_text(ollama.rs:246-260 / omlx.rs:347-361)는 엔드포인트 경로·필드명(done_reason vs finish_reason, num_predict vs max_tokens)만 다르고 구조·에러 처리·fail_on_truncation 분기가 같으며, 각 doc comment 가 서로를 \"mirrors the Ollama Cloud provider's ...\" 로 지목한다(omlx.rs:249-252·306-307). build_chat_body 도 두 파일에 같은 이름·같은 shape 로 각각 존재한다(ollama.rs:78 / omlx.rs:178).", "suggestedDirection": "AiProviderClient 에 complete_fim 을 포함시켜 complete 를 default method 로 올리고, chat 요청/파싱은 (엔드포인트·필드명만 주입받는) 공용 헬퍼로 모은다. codex 는 FIM 없음을 default 구현 오버라이드로 표현."}, {"severity": "major", "lens": "L-RUST-LAYER", "title": "ide 도메인의 commands.rs ↔ server.rs 상호 의존 — 런타임 상태 타입이 IPC 모듈 안에 산다", "file": "src-tauri/src/domain/ide/server.rs", "line": 16, "claim": "WS 전송 계층(server.rs)이 IPC 계층(commands.rs)에서 상태 타입과 헬퍼를 끌어오고, commands.rs 는 다시 server.rs 의 알림 빌더를 부르는 양방향 모듈 의존이 만들어져 있다. IdeStore·PendingDiff·PendingSave·IdeSelectionSnapshot 은 IPC 가 아니라 도메인 런타임 상태다.", "evidence": "server.rs:16 `use super::commands::{self, IdeSelectionSnapshot, IdeStore, PendingDiff, PendingSave};` 이고 server.rs:669·702 는 `commands::emit_status_changed(&app, client_count)` 를 호출한다. 반대로 commands.rs:10 `use super::server;` 이며 ide_set_selection(398)·ide_notify_at_mention(466)이 `server::selection_changed_notification`/`server::at_mentioned_notification` 을 부른다. 상태 정의는 전부 commands.rs 안이다: PendingDiff(21) PendingSave(27) IdeSelectionSnapshot(31) IdeStoreInner(43) ShutdownState(60) IdeStore(69-244). 더해 IdeSelectionSnapshot(commands.rs:31-41)은 types.rs:61-72 의 IdeSelectionInput 과 8개 필드가 완전히 같아 ide_set_selection(387-397)이 필드를 하나씩 옮겨 적는다.", "suggestedDirection": "IdeStore 와 pending/selection 타입을 중립 모듈(예: ide/store.rs)로 분리해 commands·server 가 같은 아래층을 보게 만든다. IdeSelectionSnapshot 은 IdeSelectionInput 에서 유도하거나 통합."}, {"severity": "major", "lens": "L-BRIDGE", "title": "IdeStore 의 선택영역·진단이 창·세션 구분 없는 전역 단일 슬롯 — 원격 세션이 데스크톱의 getCurrentSelection 을 덮어쓴다", "file": "src-tauri/src/domain/ide/commands.rs", "line": 207, "claim": "IdeStore 는 current_selection/latest_selection 을 프로세스 전역 한 칸으로 들고, 이를 쓰는 ide_set_selection 은 메인 창·보조 편집기 창·모든 인증된 원격 세션이 함께 호출할 수 있다. 외부 MCP 클라이언트가 보는 \"현재 선택\"은 마지막 작성자의 것이다.", "evidence": "commands.rs:54-55 `current_selection: Option<IdeSelectionSnapshot>, latest_selection: Option<IdeSelectionSnapshot>` 는 단일 값이며 set_selection(207-213)은 무조건 덮어쓴다. IdeSelectionSnapshot(commands.rs:31-41)에는 project_id 만 있고 창 label 이나 세션 식별자가 없다. ide_set_selection·ide_clear_selection 은 원격 허용목록에 있고(dispatch.rs:164-165, 967-968) 거부 arm 이 없다. server.rs:353-358 의 tool_get_current_selection 은 그 전역 값을 그대로 MCP 응답으로 낸다. 보조 편집기 창은 실제로 지원되며(ide/service.rs:89-93 doc: \"auxiliary window\"), 같은 파일이 is_active 를 메인 트리 기준으로만 판정한다고 명시해 다중 창 존재를 전제한다. 브로드캐스트 알림(commands.rs:400 → server.rs:126-138)도 모든 MCP 클라이언트에 무차별 전송된다.", "suggestedDirection": "선택 스냅샷에 창/세션 출처를 실어 저장하고, MCP 응답이 데스크톱 메인 창 출처만(또는 명시적 우선순위 규칙으로) 노출하도록 좁힌다. 원격 세션의 ide_set_selection 허용 여부도 dispatch 정책과 함께 재판정."}, {"severity": "minor", "lens": "L-RUST-LAYER", "title": "ai/service.rs 의 provider 3분기 match 가 list_models·complete·instruct 에 3벌 — provider 추가 비용이 선형으로 늘어난다", "file": "src-tauri/src/domain/ai/service.rs", "line": 140, "claim": "RPITIT trait 이라 dyn 디스패치가 불가한 것은 사실이나, 그 결과 provider 생성+자격증명 로드 코드가 세 함수에 그대로 반복되어 네 번째 provider 추가 시 손댈 지점이 3곳으로 늘어난다.", "evidence": "list_models(service.rs:140-150), complete(160-174), instruct(215-229) 이 각각 `AiProviderId::OllamaCloud => OllamaCloudProvider{ api_key: load_ollama_api_key(secret)? } ... / Codex => codex_provider(load_codex_credential(secret)?) ... / Omlx => omlx_provider(secret, omlx_base_url)? ...` 로 동일한 3-arm 을 반복한다. 같은 이유로 provider 별 파라미터인 omlx_base_url: Option<String> 이 token_status(43)·list_models(134)·complete(153)·instruct(206)·inline_edit(232)·commit_message(254) 6개 시그니처를 관통한다.", "suggestedDirection": "자격증명 로드+생성을 하나의 `resolve_provider(secret, provider, omlx_base_url)` 로 모으고, 반환을 provider enum 래퍼로 만들어 각 trait 메서드를 한 번의 match 로 위임한다."}, {"severity": "minor", "lens": "L-RUST-BOUND", "title": "provider/model 기본값 해석이 IPC 경계 양쪽에 갈라져 있다 — auto-tab 만 프론트가 해석", "file": "src-tauri/src/domain/ai/types.rs", "line": 31, "claim": "같은 Settings.ai_provider/ai_model 을 근거로 하는 해석이 inline-edit/commit-message 는 Rust(resolve_provider_and_model), auto-tab 은 프론트에서 각각 수행된다. 같은 도메인 안에서 요청 계약이 서로 다르다.", "evidence": "AiInlineCompleteRequest.provider/model(types.rs:31-32)은 필수 필드인 반면 AiInlineEditRequest(119-121)·AiCommitMessageRequest(142-144)는 `#[serde(default)] Option` 이고, ai/commands.rs:157-166·204-213 이 `service::resolve_provider_and_model(request.provider, request.model, settings.ai_provider, settings.ai_model)` 로 서버에서 폴백을 결정한다(service.rs:187-204, \"most call sites don't need to resolve 'the configured AI provider' themselves\"). auto-tab 은 프론트가 채운다 — src/shared/lib/ai/inline-completion.ts:142 `provider: config.provider`. 결과적으로 ai_inline_complete 를 원격/raw IPC 로 부르는 호출자는 provider 를 스스로 알아내야 하고, 설정 미구성 시의 에러 문구도 두 경로가 다르다.", "suggestedDirection": "AiInlineCompleteRequest 의 provider/model 도 Option 으로 낮추고 resolve_provider_and_model 을 공통 적용해 세 커맨드의 해석 지점을 Rust 한 곳으로 모은다."}, {"severity": "minor", "lens": "L-CODE", "title": "훅 JSON 조작 API 가 url 계열/command 계열 두 벌로 갈라져 이미 동작이 미세하게 어긋났다", "file": "src-tauri/src/domain/agent/service.rs", "line": 471, "claim": "handler 종류(http url vs command)만 다른 여섯 함수가 평행하게 존재하며, 그중 존재 판정 두 함수는 가드 조건이 이미 서로 달라졌다.", "evidence": "쌍이 여섯이다: remove_taide_hook_entries(447)/remove_taide_command_hook_entries(459), inject_taide_hook_entries(451)/inject_taide_command_hook_entries(463), has_hook_entries_for_url(471)/has_command_hook_entries_for_command(493), entry_has_url(485)/entry_has_command(508). entry_has_url 과 entry_has_command 는 조회 필드명(\"url\" vs \"command\")만 다른 같은 본문이다. has_command_hook_entries_for_command(497)는 `!events.is_empty() &&` 가드를 갖지만 has_hook_entries_for_url 에는 없다(MANAGED_HOOK_EVENTS 고정이라 현재는 무해하지만, 두 벌 유지에서 발생한 드리프트 그 자체다).", "suggestedDirection": "events 와 handler 필드 키를 파라미터로 받는 단일 함수군으로 합치고, Claude(http)/Codex·Gemini(command)는 인자로 구분한다."}, {"severity": "minor", "lens": "L-CODE", "title": "user_level_hook_command_timeout 하나가 초/밀리초 두 단위를 같은 u64 로 반환한다", "file": "src-tauri/src/domain/agent/service.rs", "line": 349, "claim": "에이전트별 훅 타임아웃 단위가 다른데 타입으로 구분되지 않아, 반환값이 어느 단위인지는 호출자가 agent_name 을 기억해야만 알 수 있다. 미지 에이전트 폴백값은 단위가 정의되지 않은 값이 된다.", "evidence": "service.rs:349-355 는 Codex 에 CODEX_HOOK_COMMAND_TIMEOUT_SECONDS(=5, types.rs:25), Gemini 에 GEMINI_HOOK_COMMAND_TIMEOUT_MS(=5,000, types.rs:26)를 같은 u64 로 반환하고, `_ => HOOKS_HTTP_TIMEOUT_SECONDS`(=5) 를 폴백한다. 반환값은 inject_taide_command_hook_entries(463-469)의 `\"timeout\": timeout` 으로 그대로 JSON 에 박힌다. 테스트도 두 단위를 그대로 쓴다(hooks.rs:398 `5`, hooks.rs:407 `5_000`).", "suggestedDirection": "단위를 타입으로 드러내거나(초/밀리초 newtype), 훅 핸들러 생성 지점에서 에이전트별 단위 변환을 명시적으로 수행한다."}, {"severity": "minor", "lens": "L-CODE", "title": "agent_hooks_install 이 동일한 설정 게이트를 두 match arm 에 복제", "file": "src-tauri/src/domain/agent/commands.rs", "line": 585, "claim": "두 스코프 분기의 첫 문장이 완전히 같은 게이트 검사인데 arm 안쪽에 각각 들어가 있다.", "evidence": "commands.rs:585-587 (Project arm) 과 601-603 (User arm) 이 모두 `if !state.settings.read().agent_hooks_enabled { return Err(AppError::InvalidArgument(\"agent hooks are disabled in settings\".to_string())); }` 로 문자열까지 동일하다.", "suggestedDirection": "match 진입 전으로 게이트를 끌어올린다."}, {"severity": "minor", "lens": "L-TYPE", "title": "AI 응답 타입 3개가 필드까지 동일 — 생성 TS 바인딩도 동형 타입 3개", "file": "src-tauri/src/domain/ai/types.rs", "line": 41, "claim": "세 AI 커맨드의 응답이 `{ requestId, text: Option<String> }` 로 완전히 같은데 각각 별도 struct 로 선언돼, specta 가 프론트에 동형 타입 3개를 생성한다.", "evidence": "AiInlineCompleteResponse(types.rs:41-44), AiInlineEditResponse(132-135), AiCommitMessageResponse(151-154)가 파생 매크로·serde 속성·필드가 전부 같다. 같은 파일의 AiInlineEditPromptTemplate(83-87)·AiCommitMessagePromptTemplate(103-107) 역시 `{version, system, user}` 로 동일하다(단 템플릿 파일 분리는 acknowledge 2026-08-16-wave-g §2-3 에서 합의된 사항이라 별건).", "suggestedDirection": "응답은 하나의 AiTextResponse 로 합치거나, 유지한다면 왜 별개 타입이어야 하는지를 doc comment 로 남긴다."}, {"severity": "minor", "lens": "L-RUST-STATE", "title": "MCP 커넥션이 요청당 태스크를 무제한 spawn 하고, 다음 메시지 도착 시점에만 정리한다", "file": "src-tauri/src/domain/ide/server.rs", "line": 671, "claim": "요청 핸들 벡터는 새 메시지를 받을 때만 완료분을 걷어내므로, 장시간 대기 툴을 연속 호출한 뒤 조용해진 커넥션은 태스크와 pending 엔트리를 그대로 물고 있는다.", "evidence": "server.rs:671-686 은 메시지마다 `request_handles.retain(|h| !h.inner().is_finished()); request_handles.push(spawn(...))` 를 수행한다. 상한이 없고, retain 은 다음 메시지가 와야 돈다. openDiff 는 IDE_DIFF_TIMEOUT_MS = 600_000ms(types.rs:13) 동안 대기하며 그동안 IdeStore.pending_diffs 에 항목을 유지한다(server.rs:323-330). pending_diffs 는 상한 없는 HashMap(commands.rs:52)이고, 정리는 커넥션 종료(server.rs:695-697)·서버 종료(commands.rs:280)·프로젝트 닫힘(commands.rs:191-205)에만 일어난다.", "suggestedDirection": "동시 처리 중 요청 수 상한을 두거나(세마포어), 완료 정리를 태스크 자체의 종료 시점에 수행하도록 바꾼다."}, {"severity": "minor", "lens": "L-BRIDGE", "title": "AiRequestStore 가 호출자 제공 requestId 만으로 전역 공유 — 데스크톱/원격 세션 구분이 없다", "file": "src-tauri/src/domain/ai/commands.rs", "line": 46, "claim": "in-flight AI 요청 맵의 키가 호출자가 준 문자열뿐이라 창·세션 소유권이 없다. ai_request_cancel 도 원격 허용목록에 있어, 한 세션이 다른 세션의 요청을 취소하거나 id 를 선점해 거부시킬 수 있다.", "evidence": "commands.rs:46 `pub struct AiRequestStore(Mutex<HashMap<String, oneshot::Sender<()>>>)` 이고 begin/finish/cancel(57-75)은 request_id 문자열만 본다. dispatch.rs:177·992 가 ai_request_cancel 을 원격에서 그대로 실핸들러로 보낸다. begin 은 중복 id 를 거부하므로(58-64) 선점된 id 로는 정상 요청이 InvalidArgument 로 떨어진다(commands.rs:118-123). 키 길이 제한도 없어 ai_inline_edit/commit 이 상한을 두는 다른 필드들과 대비된다.", "suggestedDirection": "엔트리에 소유 세션/창 식별자를 함께 저장하고 cancel 이 소유자 일치를 요구하도록 한다. requestId 길이 상한도 다른 필드와 같이 둔다."}, {"severity": "minor", "lens": "L-RUST-ERR", "title": "provider HTTP 실패가 전부 AppError::Internal 문자열로 평탄화 — 401 과 5xx 를 프론트가 구분할 수 없다", "file": "src-tauri/src/domain/ai/providers/ollama.rs", "line": 228, "claim": "상태 코드가 사람이 읽는 메시지 안에만 남고 에러 코드로 승격되지 않아, 재인증 유도와 재시도 유도를 UI 가 나눌 수 없다. AppError 에는 InvalidArgument·Forbidden 같은 구분 수단이 이미 있다.", "evidence": "ollama.rs:225-232(chat)·108-115(tags), omlx.rs:203-210·326-333, codex.rs:214-221·287-294 가 모두 `Err(AppError::Internal(format!(\"... request failed ({status}): {}\", mask_provider_error(&body))))` 형태다. 반면 같은 도메인의 자격증명 실패는 코드로 구분되어 있다 — service.rs:60 `AppError::InvalidArgument(\"Codex access token was rejected\")`, service.rs:105·112 \"is not configured\". error.rs:6-21 의 AppError 는 Io/NotFound/InvalidArgument/Forbidden/Internal 5종이며 serde tag=\"code\" 로 프론트에 코드가 전달된다.", "suggestedDirection": "401/403 은 InvalidArgument(또는 전용 변형)로, 그 외는 Internal 로 매핑하는 상태코드→AppError 변환을 provider 공용 헬퍼로 둔다."}]

## R7
요약: 범위: src-tauri/src/domain/{lsp,plugin,vsix,search} 19파일 + 짝을 이루는 infra(lsp_install 553·lsp_proc 276·root_guard) 합계 약 4,900 Rust 라인, 그리고 계약 확인을 위해 참조한 렌더러 접점(entities/lsp·entities/search·widgets/editor-pane/lsp-session-registry·widgets/search-panel·features/search)과 remote/dispatch.rs 의 해당 커맨드 분기. 전부 실제로 열어 확인했다.

렌즈별 총평. L-RUST-BOUND: 가장 심각한 면이다 — plugin↔vsix 가 양방향으로 직접 의존해 순환을 이루고(하드닝 zip 해제가 도메인 안에 산다), 경로 봉쇄 원시함수가 root_guard 와 plugin::service 에 두 벌 존재하며, 일반 파일시스템 헬퍼(atomic_install)가 LSP 전용 infra 모듈 이름 아래에서 plugin·vsix 에 공유된다. 원격 dispatch 정책도 설치 계열에서 lsp_install 만 비대칭으로 뚫려 있다. L-RUST-ERR: 중앙 AppError 자체는 잘 지켜지나 취소를 Internal 로 표현해 정상 조작이 에러 토스트가 되고, 커맨드 경로에서 도달하는 manifest::servers() 의 expect 가 유일한 prod 패닉 지점이다(나머지 unwrap/expect 는 전부 #[cfg(test)] 안이다 — 프로덕션 경로 unwrap 잔여 0). L-RUST-STATE: 시드로 지목된 lsp/commands.rs 가드 재구성 자체는 견고하다(store 언링크 후 비가드 shutdown, stopping 플래그로 재사용 배제, Arc 동일성 기반 install/search 스토어 정리까지 테스트로 고정). 남은 경합면은 락이 아니라 수명 쪽이다 — 종료 시 kill 이 50ms 폴링에 의존해 서버가 고아로 남을 수 있고, 설치 스토어에 RAII 해제가 없으며, 인바운드 메시지마다 전역 세션 맵을 잠근다. infra/lsp_* 와의 책임 분담은 대체로 옳다(프레이밍·다운로드·압축 해제는 infra, 정책·이벤트는 domain). owner 스코프 세션(Wave I)은 lsp 에서만 도입되어 검색은 여전히 창 구분이 없고, 반대로 lsp 쪽은 owner 조건이 너무 강해 channels 다중 구독이 구조적으로 도달 불가가 되면서 ipc-contract.md:693 서술과 모순된다. 백엔드 멀티루트 설계는 렌더러 레지스트리 키(projectId::serverId)가 root 를 빼는 바람에 통째로 우회된다. 경계 인코딩(바이트 오프셋 → UTF-16 컬럼)과 뮤테이션 가드 누락(search_replace)은 구조 결함이 그대로 실버그로 드러난 두 사례다.

기계 검증 축 재확인 결과 0: 범위 내 프런트 접점(entities/lsp·entities/search·features/search·widgets/search-panel·widgets/search-editor)에서 FSD 역방향 import 0, barrel index.ts 0, any/enum 0, useCallback/useMemo 0, function 키워드 0.

압축으로 제외한 저심각 9건(전부 minor 미만): ① apply_grammar_conflicts 가 load_plugin_dir 에서 이미 파싱한 grammar 파일을 플러그인마다 다시 읽고 재파싱(plugin/service.rs:60) ② disabled_plugin 의 매직 리터럴 "0.0.0"(plugin/service.rs:118) ③ vsix 의 strip_json_comments 가 테마 JSON 전체를 Vec<char> 로 복제(vsix/service.rs:272) ④ package.nls.<locale>.json 미지원으로 로케일별 표시명 누락(vsix/service.rs:23) ⑤ import_vsix_as_plugin 이 contributes.themes 를 항상 빈 Vec 으로 버림(vsix/service.rs:590-594) ⑥ search_run 의 on_match.send 실패를 무시해 닫힌 창의 검색이 트리 전체를 끝까지 순회(search/commands.rs:83) ⑦ LspServerId 의 PartialEq<str>/<&str> 수동 impl(lsp/types.rs:39-49) ⑧ shutdown 타임아웃이 shutdown·exit 두 번 적용되어 최악 4초(lsp/commands.rs:383·387, 문서화됨) ⑨ context_lines: u32 → usize 반복 캐스팅(search/service.rs:222). acknowledge 로 이미 확정된 결정(하드닝 zip 신설·gitignore parents 스코프·vsix_extract_themes 원격 거부 등)은 재론하지 않았다.
발견: [{"severity": "critical", "lens": "L-RUST-STATE", "title": "LSP 자동 재시작이 프로토콜 세션을 복구하지 않는다 (프로세스만 되살리고 Running 보고)", "file": "src-tauri/src/domain/lsp/commands.rs", "line": 334, "claim": "handle_process_exit 의 재시작 경로는 새 프로세스를 spawn 하고 상태를 Running 으로 올리지만, 새 프로세스는 initialize 핸드셰이크도 didOpen 도 받지 못한 빈 서버다. 렌더러 쪽에 이를 복구할 주체가 없어(lsp:session-status-changed 구독자 0) 서버가 한 번 죽으면 세션은 영구히 죽은 채 'Running' 으로 표시된다.", "evidence": "commands.rs:334-337 이 spawn_process 후 곧바로 LspSessionStatus::Running 을 emit 한다. 그러나 initialize 는 렌더러의 createSession 안에서 세션 생성 시 딱 한 번만 호출된다(src/widgets/editor-pane/lsp-session-registry.ts:273 `await client.initialize(...)`), 문서 열기도 acquireDocument 시 1회(같은 파일 490-494). `grep -rn \"lspSessionStatusChanged|LspSessionStatusChanged\" src` 결과 bindings.ts 외 소비자 0건 — 재시작을 감지해 재핸드셰이크할 코드가 존재하지 않는다. acquireLspSession 은 기존 record 를 그대로 재사용하므로(같은 파일 334-342) 탭을 닫았다 열어도 같은 죽은 client 가 반환된다.", "suggestedDirection": "재시작을 '프로세스 교체'가 아니라 '세션 재수립' 계약으로 승격 — 백엔드가 Starting→Running 전이 시 세션 세대(generation)를 이벤트에 실어 보내고, 렌더러 레지스트리가 이를 구독해 client 재생성 + initialize + 열린 문서 didOpen 재전송을 수행하도록 한다. 그 전까지는 자동 재시작 대신 Crashed 로 두고 사용자에게 명시적 재시작을 요구하는 편이 '살아있는 척하는 세션'보다 안전하다."}, {"severity": "critical", "lens": "L-RUST-BOUND", "title": "검색 결과 오프셋이 UTF-8 바이트인데 프런트는 UTF-16/1-기반 컬럼으로 소비한다", "file": "src-tauri/src/domain/search/service.rs", "line": 235, "claim": "SearchMatch 의 column·match_start·match_end 는 Rust 쪽 바이트 오프셋(0-기반)인데, 렌더러는 이를 JS 문자열 slice 인덱스(UTF-16)와 monaco 컬럼(1-기반 UTF-16)으로 그대로 사용한다. ASCII 줄에서도 커서가 1칸 어긋나고, 비ASCII(한글·이모지)가 앞에 있는 줄에서는 하이라이트 구간이 깨지고 점프 위치가 틀어진다.", "evidence": "service.rs:232-241 이 `column: match_start`(바이트 오프셋)와 build_preview 가 돌려준 바이트 인덱스(service.rs:123-126 `new_start = (match_start - window_start) + prefix.len()`)를 그대로 SearchMatch 에 싣는다. 소비 측: src/features/search/search-match-row.tsx:21-23 `match.preview.slice(match.matchStart, match.matchEnd)` (UTF-16 인덱스), src/entities/editor/reveal-registry.ts:15-16 `{ lineNumber: target.line, column: target.column }` → monaco 는 1-기반 UTF-16 컬럼을 기대한다(src/features/search/search-results-list.tsx:44 가 match.column 을 그대로 전달).", "suggestedDirection": "경계에서 인코딩을 확정한다 — 백엔드가 내보내는 오프셋을 UTF-16 코드유닛 기준으로 변환(`line[..start].encode_utf16().count()`)하고 column 은 +1 하여 monaco 계약에 맞추거나, 반대로 필드명을 byte_* 로 바꾸고 렌더러에서 일괄 변환한다. 어느 쪽이든 ipc-contract 에 '오프셋 단위'를 명시하고 한글 포함 회귀 테스트를 붙인다."}, {"severity": "critical", "lens": "L-RUST-STATE", "title": "search_replace 만 전역 뮤테이션 가드 없이 프로젝트 전역 파일을 덮어쓴다", "file": "src-tauri/src/domain/search/commands.rs", "line": 97, "claim": "search_replace 는 프로젝트 트리 전체를 순회하며 파일을 read→modify→write 하는데 begin_mutation 가드를 잡지 않는다. 같은 파일을 대상으로 하는 file_save 는 가드를 잡으므로, 저장과 일괄 치환이 겹치면 서로의 결과를 덮어써 사용자 편집이 소실될 수 있다.", "evidence": "commands.rs:97-110 의 search_replace 본문에는 begin_mutation 호출이 없다(같은 파일 search_run:75, search_cancel:115 는 잡는다). 대비: src-tauri/src/domain/file/commands.rs:27 file_save 는 `let _guard = state.begin_mutation().await;` 로 시작한다. 치환 자체도 비원자적 read→write 다 — service.rs:356 `std::fs::read(path)` 후 service.rs:384 `persist::write_atomic(path, ...)`.", "suggestedDirection": "search_replace 도 다른 쓰기 커맨드와 같은 뮤테이션 가드 아래 두되, 긴 블로킹 작업이 락을 오래 잡는 문제는 lsp_stop 이 쓴 방식(가드는 동기 구간만, 실제 I/O 는 밖) 대신 '치환 대상 파일 집합 확정 → 파일 단위로 가드 재획득' 형태로 분할하는 방향을 검토한다."}, {"severity": "major", "lens": "L-RUST-BOUND", "title": "plugin ↔ vsix 도메인 간 양방향(순환) 의존", "file": "src-tauri/src/domain/plugin/service.rs", "line": 277, "claim": "plugin 도메인이 vsix 도메인의 서비스 함수를 직접 호출하고, vsix 도메인은 다시 plugin 의 types·service·commands(Store)를 직접 import 한다. 도메인 간 직접 의존을 금지하는 계층 원칙(backend.md §1)에 정면으로 어긋나며, 두 도메인은 사실상 분리 불가능하게 얽혀 있다.", "evidence": "plugin/service.rs:277 `crate::domain::vsix::service::extract_hardened_zip(source, &temp_extract_dir)?`. 반대 방향: vsix/service.rs:14-16 이 `crate::domain::plugin::types::{PluginContributions, PluginLanguageContribution, PluginManifest, ...}` 를, vsix/commands.rs:7-9 가 `crate::domain::plugin::commands::PluginStore` 와 `plugin::service as plugin_service` 를 import 한다(vsix/commands.rs:28 에서 plugin_service::load_plugins 호출).", "suggestedDirection": "공유 조각을 도메인 밖으로 승격한다 — 하드닝 zip 해제는 성격상 infra(예: infra::archive)로, PluginManifest/Contributions 는 두 도메인이 함께 참조하는 타입 소유자를 하나 정한 뒤 한쪽 방향(vsix → plugin)만 남긴다. 최소한 plugin → vsix 엣지는 제거해 순환을 끊는다."}, {"severity": "major", "lens": "L-RUST-BOUND", "title": "경로 봉쇄(containment) 원시함수가 plugin 안에 중복 구현되어 있다", "file": "src-tauri/src/domain/plugin/service.rs", "line": 199, "claim": "plugin::service 가 root_guard::ensure_within_root 와 동일한 '루트 밖 경로 거부' 로직과 root_guard 의 private canonicalize_lenient 를 그대로 복제해 갖고 있다. 보안 임계 원시함수가 두 벌 존재하며 에러 변종(InvalidArgument vs Forbidden)까지 갈라져 있어, 한쪽만 강화되면 조용히 드리프트한다.", "evidence": "plugin/service.rs:199-209 resolve_contribution_path 는 canonical_candidate.starts_with(canonical_root) 검사로 root_guard.rs:30-42 ensure_within_root 와 같은 판정을 하고, plugin/service.rs:306-319 canonicalize_lenient 는 root_guard.rs:57-70 과 문자 단위로 동일한 구현이다. 같은 파일이 이미 root_guard 를 import 하고 있다(plugin/service.rs:7, 243·298 에서 ensure_safe_component 사용). 에러도 한쪽은 InvalidArgument(plugin/service.rs:207), 한쪽은 Forbidden(root_guard.rs:36)이다.", "suggestedDirection": "root_guard 에 lenient 변형(`ensure_within_root_lenient`)을 공개 API 로 추가하고 plugin 은 그것만 쓰도록 통합한다. canonicalize_lenient 사본은 삭제하고, 반환 에러 변종도 root_guard 기준(Forbidden)으로 일원화한다."}, {"severity": "major", "lens": "L-BRIDGE", "title": "LSP 세션의 channels 맵은 구조적으로 항상 1개 — 문서가 약속한 '여러 창 동시 구독'은 도달 불가", "file": "src-tauri/src/domain/lsp/commands.rs", "line": 183, "claim": "find_reusable_entry 가 '이미 그 owner 가 구독 중인' 엔트리만 재사용 후보로 내주므로, channels 에 새 owner 가 추가될 경로가 존재하지 않는다. 즉 HashMap·브로드캐스트·실패 구독자 pruning 과 그것을 검증하는 테스트 4건은 프로덕션에서 도달 불가능한 상태를 기술한다. 정본 문서의 서술과도 모순된다.", "evidence": "commands.rs:183 `&& entry.channels.lock().contains_key(owner)` 가 재사용 조건이고, 삽입 지점은 재사용 경로(commands.rs:444 `channels.lock().insert(owner, on_message)` — 이미 존재하는 키를 덮어씀)와 신규 생성(commands.rs:465 `HashMap::from([(owner, on_message)])`) 둘뿐이다. 반면 docs/ipc-contract.md:693-696 은 \"lsp_spawn(reuse 경로)과 pty_attach 는 세션당 구독자 목록을 유지해 여러 창이 같은 LSP 세션을 동시에 구독할 수 있다\" 라고 적고 있다. 테스트 broadcast_은_모든_구독자에게_전달된다 등(commands.rs:1043-1096)은 손으로 만든 2-키 맵으로만 성립한다.", "suggestedDirection": "둘 중 하나로 정합화한다 — (a) 창별 격리를 정책으로 확정하고 channels 를 `Option<Channel>`/단일 필드로 축소한 뒤 ipc-contract.md:693 의 LSP 부분을 pty 한정으로 정정하거나, (b) 진짜 다중 구독을 원한다면 창별 initialize/request-id 충돌을 백엔드 프록시(요청 id 리매핑)로 해결한 뒤 owner 조건을 푼다. 현 상태는 어느 쪽도 아니다."}, {"severity": "major", "lens": "L-FSD", "title": "렌더러 LSP 레지스트리 키에 root 가 빠져 있어 백엔드 멀티루트 설계가 통째로 우회된다", "file": "src/widgets/editor-pane/lsp-session-registry.ts", "line": 76, "claim": "세션 캐시 키가 `projectId::serverId` 뿐이라 파일마다 다르게 해석된 root(monorepo 의 서로 다른 package.json)가 두 번째부터 조용히 버려진다. 그 결과 백엔드가 갖춘 멀티루트 기구(roots 참조계수, workspace/didChangeWorkspaceFolders 송신, should_reuse_session)는 데스크톱 UI 경로에서 한 번도 실행되지 않는다.", "evidence": "lsp-session-registry.ts:76 `toSessionKey = (projectId, serverId) => \\`${projectId}::${serverId}\\``, 334-342 에서 기존 record 가 있으면 root 비교 없이 refCount 만 올리고 반환한다. 호출부는 파일별로 root 를 새로 해석한다 — src/widgets/editor-pane/use-lsp-session.ts:69-74 `resolveLspRoot(...).then((resolvedRoot) => acquireLspSession(projectId, serverId, resolvedRoot ?? projectRoot, ...))`. 따라서 lsp_spawn 은 (project, server) 당 창별 1회만 호출되고, 백엔드의 재사용/루트 추가 경로(commands.rs:422-454, roots·workspace_folders_notification)는 진입 불가다. 해제 시에도 첫 root 만 통보된다(lsp-session-registry.ts:312 `stopLspSession(session.sessionId, record.root)`).", "suggestedDirection": "레지스트리 키에 해석된 root 를 포함시키거나(서버가 shares_sessions=false 인 경우), shares_sessions=true 서버는 같은 record 를 유지하되 새 root 마다 lsp_spawn 을 다시 호출해 백엔드의 루트 참조계수·didChangeWorkspaceFolders 경로를 실제로 태운다. 어느 쪽이든 'root 가 다르면 어떻게 되는가'를 계약 문서에 명시한다."}, {"severity": "major", "lens": "L-BRIDGE", "title": "검색 세션 키가 창/원격 세션 단위로 스코프되지 않는다 (lsp 의 owner 와 불일치)", "file": "src/widgets/search-panel/search-panel-container.tsx", "line": 48, "claim": "백엔드 SearchStore 는 session_id 문자열만으로 '이전 실행 취소'를 판정하는데, 검색 패널의 session_id 는 렌더러 realm 내부에서만 유일한 useId 값이다. Wave I 에서 lsp_spawn 이 owner(창 라벨)를 도입해 해결한 것과 같은 문제가 검색에는 남아 있어, 두 번째 에디터 창이나 원격 세션의 검색이 메인 창의 진행 중 검색을 취소할 수 있다.", "evidence": "search-panel-container.tsx:48 `const sessionId = \\`search-panel-${useId()}\\`` — useId 는 realm 로컬 카운터라 동일한 트리를 렌더하는 다른 창에서 같은 값이 나온다. 백엔드는 commands.rs:39-48 begin_search 에서 `sessions.get(session_id)` 만 보고 이전 플래그를 true 로 만들고, search_cancel(commands.rs:114-120)도 owner 없이 session_id 만 받는다. Search Editor 쪽은 tabId 를 쓰는데(src/widgets/search-editor/search-editor-pane.tsx:49) 원격 세션은 메인 창 레이아웃을 미러링하므로 같은 tabId 가 두 클라이언트에서 동시에 존재할 수 있다. 대비: lsp_stop 은 owner 를 필수 인자로 받는다(lsp/commands.rs:539-546).", "suggestedDirection": "search_run/search_cancel 에도 owner(getCurrentWindow().label, 원격은 'remote')를 추가해 SearchStore 키를 (owner, session_id) 복합키로 만든다. 프런트에서는 useId 대신 창 라벨을 접두어로 붙이는 것만으로도 충돌은 사라진다."}, {"severity": "major", "lens": "L-RUST-STATE", "title": "앱 종료 시 LSP kill_all 이 비동기라 언어 서버가 고아로 남을 수 있다", "file": "src-tauri/src/infra/lsp_proc.rs", "line": 42, "claim": "LspProcHandle::kill() 은 AtomicBool 만 세우고, 실제 start_kill 은 별도 tokio 태스크가 50ms 주기로 폴링할 때 일어난다. RunEvent::Exit 에서 kill_all 을 부른 직후 프로세스가 내려가면 그 폴링이 돌지 않아 rust-analyzer·jdtls 같은 대형 서버가 살아남는다. 같은 자리에서 호출되는 터미널은 동기 kill 이라 대칭이 깨져 있다.", "evidence": "lsp_proc.rs:42-44 `pub fn kill(&self) { self.kill_requested.store(true, Ordering::SeqCst); }` 이고, 실제 시그널은 lsp_proc.rs:149-153 의 wait 루프가 `LSP_WAIT_POLL_MS`(=50, lsp_proc.rs:14) 간격으로 깨어나야 보낸다. 종료 훅은 src-tauri/src/lib.rs:759-760 에서 `TerminalStore::kill_all()` 다음 `LspStore::kill_all()` 를 부르고 곧바로 반환한다. 대비: TerminalStore::kill_all 은 domain/terminal/commands.rs:61-65 에서 `entry.pty.kill()` 로 즉시 시그널을 보낸다.", "suggestedDirection": "LspProcHandle 이 Child 를 공유 소유(Arc<Mutex<Child>>)하거나 pid 를 보관해 kill_all 에서 동기적으로 시그널(unix 는 프로세스 그룹까지, 이미 toolchain 설치 경로에 kill_toolchain_process_group 선례가 있다)을 보내도록 바꾼다. 최소한 종료 훅에서 exited 플래그를 짧게 대기하는 확인 절차를 둔다."}, {"severity": "major", "lens": "L-RUST-LAYER", "title": "플러그인/vsix 의 대용량 압축 해제가 전역 뮤테이션 가드를 쥔 채 async 런타임에서 블로킹된다", "file": "src-tauri/src/domain/plugin/commands.rs", "line": 56, "claim": "plugin_install 은 begin_mutation 가드를 잡은 상태로 최대 128MB 압축 해제(extract_hardened_zip)와 재귀 디렉토리 복사를 async 함수 본문에서 직접 수행한다. vsix_import_plugin·vsix_extract_themes 도 같은 패턴이다. 같은 파일의 LSP 설치 경로는 정확히 이 이유로 spawn_blocking 을 쓰라고 주석까지 달아 두었는데, 이웃 도메인은 그 규칙을 따르지 않아 tokio 워커와 앱 전체 뮤테이션 락이 동시에 묶인다.", "evidence": "plugin/commands.rs:56-63 은 `let _guard = state.begin_mutation().await;` 직후 install_from_archive → vsix/service.rs:430-465 extract_hardened_zip(예산 VSIX_ARCHIVE_MAX_TOTAL_BYTES = 128MB, vsix/service.rs:31)을 그대로 호출한다. vsix/commands.rs:24-28 도 동일. vsix/commands.rs:15-17 vsix_extract_themes 는 가드는 없지만 최대 64MB 해제를 async 스레드에서 돈다. 대비 근거: lsp/commands.rs:756-768 \"Archive extraction is blocking filesystem/CPU work ... would monopolize a tokio worker thread\" 주석과 `tokio::task::spawn_blocking`.", "suggestedDirection": "세 커맨드의 압축 해제·복사 구간을 spawn_blocking 으로 옮기고, 가드는 최종 atomic_install·스토어 갱신 같은 짧은 동기 구간에만 건다(lsp_stop 이 이미 쓴 가드 축소 패턴 재사용)."}, {"severity": "major", "lens": "L-RUST-BOUND", "title": "lsp_install 만 원격 dispatch 허용 — 이웃 설치 커맨드는 전부 명시 거부", "file": "src-tauri/src/domain/remote/dispatch.rs", "line": 666, "claim": "plugin_install·plugin_uninstall·vsix_import_plugin·vsix_extract_themes 는 '원격 세션이 데스크톱 로컬 파일시스템을 읽거나 쓰게 해서는 안 된다'는 근거로 모두 명시 거부됐는데, 같은 성격의 lsp_install 은 그대로 실행된다. 원격 클라이언트가 호스트에서 수백 MB 다운로드와 툴체인 프로세스 실행(go/gem/cs/ghcup)을 유발할 수 있다.", "evidence": "dispatch.rs:666 `\"lsp_install\" => respond(lsp::lsp_install(...).await)`. 이 호출은 lsp/commands.rs:933-940 을 통해 run_download_install(네트워크 다운로드 + 앱 데이터 디렉토리 쓰기) 또는 run_toolchain_install(lsp/commands.rs:849-858 `tokio::process::Command::new(binary)` 실제 프로세스 spawn)로 간다. 반면 dispatch.rs:624 `\"plugin_install\" | \"plugin_uninstall\" => Err(deny_remote_plugin_install(name))`, dispatch.rs:1001 vsix_import_plugin 거부, dispatch.rs:1000 vsix_extract_themes 거부이며 거부 사유는 dispatch.rs:309-313 에 명시돼 있다.", "suggestedDirection": "정책을 한 줄로 정한다 — '호스트에 바이너리를 설치하거나 호스트 프로세스를 실행시키는 커맨드는 원격 거부'. 그 기준이면 lsp_install/lsp_install_cancel 도 deny_* 함수로 옮기고, 허용하기로 한다면 그 판단 근거를 acknowledge 에 남겨 이웃 거부들과의 비대칭을 문서화한다."}, {"severity": "major", "lens": "L-CODE", "title": "손으로 만든 glob 매처가 UI 가 안내하는 패턴 문법과 어긋난다", "file": "src-tauri/src/domain/search/service.rs", "line": 133, "claim": "glob_match 는 `*` 하나만 지원하고 그 `*` 가 `/` 도 넘어간다. `**`·`?`·`{a,b}` 는 없다. 정작 앱은 입력창 placeholder 로 `**/*.test.ts` 를 예시로 제시하므로, 사용자가 안내대로 입력해도 루트 직하 파일은 제외되지 않고 `src/*.ts` 는 하위 디렉토리까지 잡는다.", "evidence": "service.rs:133-164 는 바이트 단위 백트래킹으로 `*` 만 처리한다(디렉토리 경계 특례 없음) — 그래서 `src/*.ts` 가 `src/a/b/c.ts` 에도 매칭된다. 반대로 `**/*.test.ts` 는 패턴에 `/` 리터럴이 남아 루트의 `foo.test.ts` 를 매칭하지 못한다. UI 안내 문구는 src-tauri/src/domain/locale/service.rs:988 `(\"search.excludeGlobPlaceholder\", \"Files to exclude (e.g. **/*.test.ts)\")`, 1935(한국어)에 동일하게 박혀 있고 입력창은 src/features/search/search-exclude-glob-input.tsx:16 이다.", "suggestedDirection": "이미 의존성 그래프에 들어와 있는 globset(Cargo.lock:1654, ignore 0.4.33 의 의존)을 직접 의존으로 올려 GlobBuilder 로 교체한다. 자체 구현을 유지한다면 최소한 `**` 와 디렉토리 경계 규칙을 지원하고 placeholder 문구를 실제 지원 문법에 맞춘다."}, {"severity": "major", "lens": "L-RUST-ERR", "title": "LSP 매니페스트를 호출마다 전량 재파싱하며 실패 시 expect 로 패닉한다", "file": "src-tauri/src/domain/lsp/manifest.rs", "line": 12, "claim": "servers() 는 19개 서버가 든 번들 JSON 을 매 호출마다 파싱+검증하고 Vec 을 새로 할당한다. find_spec 이 그 위에 얹혀 있어 파일을 열 때마다(언어 매칭 서버 수만큼) 반복된다. 게다가 실패 시 expect 로 프로세스를 죽인다 — IPC 핸들러 경로에서의 패닉이다.", "evidence": "manifest.rs:11-13 `pub fn servers() -> Vec<LanguageServerSpec> { parse_manifest(LSP_MANIFEST_SOURCE).expect(\"bundled lsp manifest must be valid\") }`, manifest.rs:15-17 find_spec 은 servers() 를 통째로 만든 뒤 find 한다. 호출 경로: lsp/commands.rs:419(lsp_spawn), 652(lsp_resolve_root — 렌더러가 파일 attach 마다 호출, src/widgets/editor-pane/use-lsp-session.ts:69), 926(lsp_install), service.rs:16·154(builtin_specs/detect_servers). 소스는 컴파일 타임 상수다(types.rs:9 `include_str!`).", "suggestedDirection": "`static SPECS: OnceLock<Vec<LanguageServerSpec>>` 로 1회 파싱해 캐시하고 find_spec 은 참조를 돌려주도록 좁힌다. 초기화 실패는 앱 부팅 시점에 한 번만 검사해(부팅 실패로) 처리하고, 커맨드 경로에서는 expect 를 없앤다."}, {"severity": "minor", "lens": "L-RUST-STATE", "title": "restart_count 가 정상 가동 후에도 리셋되지 않아 누적 3회로 자동 재시작이 영구 중단된다", "file": "src-tauri/src/domain/lsp/commands.rs", "line": 293, "claim": "restart_count 는 프로세스가 죽을 때마다 단조 증가하고, 세션이 오래 정상 동작해도 감소하거나 0 으로 돌아가지 않는다. 몇 시간 간격으로 세 번 죽은 서버는 네 번째부터 Crashed 로 고정되어 다시는 자동 복구되지 않는다.", "evidence": "commands.rs:293-305 `let restarts = entry.restart_count.fetch_add(1, ...) + 1; if restarts > RESTART_BACKOFF_LIMIT { ... Crashed ... return; }`. 0 으로 되돌리는 곳은 명시적 lsp_restart 뿐이다(commands.rs:615 `entry.restart_count.store(0, ...)`). 성공적으로 Running 이 된 뒤 카운터를 내리는 코드는 없다(commands.rs:334-338).", "suggestedDirection": "'연속 실패' 개념으로 바꾼다 — 재시작 후 일정 시간(예: 백오프 상한의 수 배) 이상 살아있으면 카운터를 0 으로 되돌리거나, 마지막 크래시 시각을 함께 저장해 시간 창 밖의 실패는 새 시퀀스로 센다."}, {"severity": "minor", "lens": "L-RUST-STATE", "title": "LSP 인바운드 메시지마다 전역 LspStore 락을 잡는다", "file": "src-tauri/src/domain/lsp/commands.rs", "line": 261, "claim": "프로세스 stdout 콜백이 메시지 한 건마다 find_entry 로 전역 세션 맵을 잠근다. 진단·시맨틱 토큰·완성 응답이 초당 수백 건 오가는 구간에서 모든 세션과 lsp_spawn/lsp_stop/lsp_sessions 가 같은 뮤텍스에서 직렬화된다.", "evidence": "commands.rs:257-265 의 on_message 클로저가 `let Ok(entry) = find_entry(&store, &message_session_id)` 를 호출하고, find_entry 는 commands.rs:149-156 에서 `store.0.lock()` 을 잡는다. 같은 락을 lsp_spawn(473·478), lsp_stop(577), lsp_sessions(628), kill_all(93), server_pids(105) 이 공유한다.", "suggestedDirection": "spawn_process 가 세션 엔트리의 `Weak<SessionEntry>`(또는 channels 맵의 Arc)를 클로저에 직접 캡처하게 해 메시지 경로에서 전역 맵 조회를 없앤다."}, {"severity": "minor", "lens": "L-RUST-ERR", "title": "설치 취소가 AppError::Internal 로 모델링되어 사용자에게 에러 토스트가 뜬다", "file": "src-tauri/src/domain/lsp/commands.rs", "line": 880, "claim": "사용자가 직접 누른 '설치 취소'가 Internal 에러로 반환되고, 프런트 mutation onError 가 그대로 toast.error 를 띄운다. 정상적인 사용자 조작이 실패로 보고되며, 프런트는 취소와 진짜 실패를 문자열 비교 없이는 구분할 수 없다.", "evidence": "commands.rs:880 `return Err(AppError::Internal(\"설치가 취소되었습니다\".to_string()));`(다운로드 경로도 동일: infra/lsp_install.rs:235). AppError 에는 취소를 표현할 변종이 없다(src-tauri/src/error.rs:6-21 Io/NotFound/InvalidArgument/Forbidden/Internal). 소비 측: src/widgets/settings-view/settings-view.tsx:226 `installLspServer(serverId, { onError: (error) => toast.error(error.message) })`.", "suggestedDirection": "취소를 에러가 아닌 결과로 승격하거나(예: lsp_install 이 완료/취소를 구분하는 값을 반환), AppError 에 Cancelled 변종을 추가해 프런트가 코드로 분기하도록 한다."}, {"severity": "minor", "lens": "L-RUST-STATE", "title": "LspInstallStore 엔트리에 해제 보장이 없어 설치가 영구 '진행 중'으로 잠길 수 있다", "file": "src-tauri/src/domain/lsp/commands.rs", "line": 942, "claim": "begin 으로 등록한 엔트리는 lsp_install 본문 끝의 finish 호출로만 제거된다. 다운로드에 타임아웃이 없어 무한 대기하거나 커맨드 future 가 드롭되면 엔트리가 남고, 이후 그 서버는 앱 재시작 전까지 '설치가 이미 진행 중입니다'로 거부된다.", "evidence": "commands.rs:929-943 — begin(929) 후 await 하는 설치 본문(934-935)을 지나 942 에서야 finish 가 불린다. 취소 플래그는 lsp_install_cancel 이 세울 수 있지만(commands.rs:948-953) 엔트리 제거는 여전히 원 호출부의 정상 복귀에 달려 있다. 다운로드 클라이언트에는 타임아웃 설정이 없다(commands.rs:696 `reqwest::Client::new()`).", "suggestedDirection": "finish 를 Drop 가드(RAII) 타입으로 감싸 어떤 경로로 빠져나가도 해제되게 하고, reqwest 클라이언트에 연결/전체 타임아웃을 준다."}, {"severity": "minor", "lens": "L-RUST-STATE", "title": "atomic_install 이 실행 중인 서버 디렉토리를 먼저 통째로 지운다", "file": "src-tauri/src/infra/lsp_install.rs", "line": 139, "claim": "동일 버전 재설치 시 최종 디렉토리를 remove_dir_all 한 뒤 rename 하는데, 그 디렉토리에서 언어 서버가 실행 중인지 확인하지 않는다. 세션이 살아 있는 상태에서 재설치하면 실행 중 프로세스의 파일이 사라진다.", "evidence": "lsp_install.rs:135-143 `if final_dir.exists() { std::fs::remove_dir_all(final_dir)?; } std::fs::rename(temp_dir, final_dir)`. 세션의 실행 파일은 이 디렉토리에서 해석된다(lsp/commands.rs:206-209 managed_dir_for → 220-233 resolve_spec_command). lsp_install 은 LspStore 를 보지 않는다(commands.rs:918-944).", "suggestedDirection": "설치 전에 LspStore 에서 해당 server_id 의 활성 세션 유무를 확인해 거부하거나, 기존 디렉토리를 삭제 대신 임시 이름으로 rename 해 두고 세션 종료 후 정리하는 방식으로 바꾼다."}, {"severity": "minor", "lens": "L-RUST-ERR", "title": "언어 서버 stderr 를 버려 크래시 원인을 남길 수 없다", "file": "src-tauri/src/infra/lsp_proc.rs", "line": 111, "claim": "LSP 자식 프로세스의 stderr 를 null 로 버린다. 서버가 죽어도 last_error 에 담을 수 있는 정보는 종료 코드뿐이라, 사용자에게도 로그에도 원인이 남지 않는다. 툴체인 설치 경로는 같은 파일군에서 stderr tail 을 수집하는 선례가 있다.", "evidence": "lsp_proc.rs:108-111 `.stderr(std::process::Stdio::null())`. 그래서 크래시 메시지는 lsp/commands.rs:300-303 의 \"마지막 종료 코드: {code:?}\" 가 전부다. 대비: lsp/commands.rs:810-826 capture_output_tail 이 설치 프로세스의 stdout/stderr 마지막 20줄을 모아 에러 메시지에 붙인다.", "suggestedDirection": "stderr 를 piped 로 열고 capture_output_tail 과 같은 링 버퍼로 마지막 N줄을 보관해, Crashed 상태의 last_error 에 붙여 준다."}, {"severity": "minor", "lens": "L-CODE", "title": "대소문자 무시 검색이 ASCII 만 접는다", "file": "src-tauri/src/domain/search/service.rs", "line": 37, "claim": "리터럴 검색의 case-insensitive 처리가 to_ascii_lowercase 라서 비ASCII 문자는 접히지 않는다. 정규식 경로는 RegexBuilder::case_insensitive 로 유니코드 폴딩을 하므로 같은 옵션이 모드에 따라 다르게 동작한다.", "evidence": "service.rs:34-43 `line.to_ascii_lowercase()` / `query.text.to_ascii_lowercase()`. 정규식 경로는 service.rs:65-70 `RegexBuilder::new(&query.text).case_insensitive(!query.case_sensitive)` 로 유니코드 규칙을 따른다.", "suggestedDirection": "바이트 오프셋 보존이 필요해 to_lowercase 를 쓸 수 없다면, 리터럴 검색도 regex(escape + case_insensitive) 경로로 합쳐 두 모드의 의미를 하나로 만든다."}, {"severity": "minor", "lens": "L-RUST-ERR", "title": "치환 대상 명시 경로 중 루트 밖 항목을 조용히 버린다", "file": "src-tauri/src/domain/search/service.rs", "line": 405, "claim": "paths 로 지정한 치환 대상 중 프로젝트 루트를 벗어난 경로는 filter_map(.ok())로 소리 없이 사라진다. 호출자는 일부만 치환됐다는 사실을 알 수 없고, 결과는 성공으로 보고된다.", "evidence": "service.rs:402-408 `Some(explicit) => explicit.iter().filter_map(|path| root_guard::ensure_within_root(root, path).ok()).collect::<Vec<_>>()`. 반환 SearchReplaceResult 에는 건너뛴 개수를 담을 필드가 없다(domain/search/types.rs:46-51).", "suggestedDirection": "루트 밖 경로는 Forbidden 으로 즉시 실패시키거나(파일 커맨드들의 기존 정책과 동일), 최소한 skipped 개수를 결과에 실어 UI 가 부분 실패를 알리게 한다."}, {"severity": "minor", "lens": "L-RUST-STATE", "title": "검색이 파일을 크기 제한 없이 통째로 메모리에 읽는다", "file": "src-tauri/src/domain/search/service.rs", "line": 210, "claim": "search_file/replace_in_file 이 파일 전체를 std::fs::read 로 올린다. SEARCH_MATCH_LIMIT 는 매치 수만 제한하므로, 프로젝트에 수 GB 짜리 로그·덤프가 있으면 그대로 메모리에 적재된다. 에디터 쪽에는 FileSizeTier 게이트가 있는데 검색에는 대응물이 없다.", "evidence": "service.rs:210-219 `let Ok(bytes) = std::fs::read(path)` 후 전체를 String::from_utf8_lossy 로 다시 복사한다(치환 경로도 동일: service.rs:356). 바이너리 판정은 앞 8000바이트만 보지만(service.rs:214-217) 읽기는 이미 끝난 뒤다. 에디터의 크기 등급은 src-tauri/src/domain/file/types.rs:6 FileSizeTier.", "suggestedDirection": "metadata 로 상한(예: 에디터의 large 티어 기준과 동일)을 먼저 확인해 초과 파일은 건너뛰고, 필요하면 BufReader 라인 스트리밍으로 바꾼다."}, {"severity": "minor", "lens": "L-BRIDGE", "title": "PluginStore 읽기-후-쓰기 캐시가 매 호출 전체 Vec 을 복제하고 동시 호출 시 중복 로드한다", "file": "src-tauri/src/domain/plugin/commands.rs", "line": 30, "claim": "ensure_loaded 는 캐시 히트에도 LoadedPlugin 전체(매니페스트 포함) Vec 을 clone 해서 돌려주고, read 락을 놓은 뒤 write 락을 잡는 구조라 최초 동시 호출이 겹치면 디스크 스캔이 중복 수행된다. 이 함수는 파일 열기·git·ide 핫패스에서 호출된다.", "evidence": "plugin/commands.rs:30-37 `if let Some(cached) = store.0.read().clone() { return cached; }` → 이후 write. 호출부: domain/file/commands.rs:20(file_open 마다), domain/git/commands.rs:95, domain/ide/server.rs:286·370. load_plugins 는 디렉토리 스캔 + 매니페스트 파싱 + grammar 재검증을 수행한다(plugin/service.rs:14-43).", "suggestedDirection": "캐시를 `Arc<Vec<LoadedPlugin>>` 으로 바꿔 clone 비용을 포인터 복제로 낮추고, 로드는 write 락을 잡은 채 double-checked 로 1회만 수행한다."}, {"severity": "minor", "lens": "L-CODE", "title": "워크스페이스에 쓰이지 않는 낡은 src-tauri/Cargo.lock 이 커밋되어 있다", "file": "src-tauri/Cargo.lock", "line": 1, "claim": "이 저장소는 루트 워크스페이스라 실제 잠금 파일은 ./Cargo.lock 하나뿐인데, 멤버 디렉토리에 별개의 Cargo.lock 이 함께 추적되고 있다. 이 파일에는 search 도메인이 쓰는 ignore/globset 이 아예 없어 의존성 감사 시 잘못된 결론을 유도한다.", "evidence": "루트 Cargo.toml 은 `[workspace] members = [\"src-tauri\", \"crates/taide-cli\"]` 이고 ./Cargo.lock:2039 에 ignore 0.4.33, :1654 에 globset 이 있다. 반면 src-tauri/Cargo.lock 에는 `grep -c ignore` = 0, globset = 0 이며 `git ls-files` 결과 두 파일 모두 추적 중이다. src-tauri/Cargo.toml:52 는 ignore 0.4.30 을 선언한다.", "suggestedDirection": "src-tauri/Cargo.lock 을 삭제하고 .gitignore 에 멤버 디렉토리의 Cargo.lock 을 넣어 재생성을 막는다."}]

## R8
요약: 범위 규모: Rust 5도메인 2,253 LOC(terminal 673 / system 729 / task 428 / snippet 364 / font 59, 이 중 약 1,050 이 인라인 테스트) + 수명주기 추적을 위해 infra/pty.rs(384)·infra/shell_integration.rs 일부·remote/ws.rs·dispatch.rs 의 pty arm·프론트 대응부(entities/terminal·task·snippet·system·font, widgets/terminal-pane 2파일)를 참조 감사. 기계 검증 0축(FSD 역방향 import·barrel·any/enum·useCallback/useMemo·function 키워드)은 참조한 프론트 파일 전체 재확인 결과 0.

렌즈별 총평 — L-RUST-ERR: 실질 clean. 프로덕션 경로 unwrap/expect/panic 은 task/service.rs:100 의 정적 리터럴 정규식 `expect` 1건뿐이고 나머지는 전부 테스트, 에러는 전부 AppError 중앙 타입으로 흐른다(다만 메시지 언어 혼재는 발견 15). L-RUST-LAYER: task·snippet·font 는 commands 얇게/service 순수 규율을 지키지만, terminal(커맨드 415줄에 구독자 다중화·IDE 대기 로직 상주)과 system(순수 URL 검증 + sysinfo 어댑터 상주)이 규율에서 이탈했고 spawn_blocking 적용도 도메인마다 갈린다. L-RUST-BOUND: 스코프 내 도메인 간 직접 엣지 4개 확인(system→terminal/lsp/agent, terminal→ide) — 전부 architecture.md:77 금지 대상, 순환은 없음. PTY/프로세스 수명: 가장 취약한 축. Wave I 다중 구독자 전환으로 attach/detach 는 정합해졌으나 **세션 자체의 회수 경로가 통째로 비어 있고**(pty_kill 호출자 0), Drop 안전망 부재·pause 소유권 분열·원격 sink 무조건 Ok 로 인해 프로세스/스레드/메모리/임시파일이 모두 단조 증가한다. 링버퍼는 이름과 달리 O(n) drain 이고, 전역 스토어 락을 잡은 채 블로킹 write/채널 send 를 수행하는 구조가 앱 전체 프리즈 경로를 만든다.

압축으로 제외한 저심각 관찰 6건(계수만): ①`task.query.ts:8-9` 의 `projectId ?? ''` 센티널(enabled 로 무해하나 타입 좁히기로 대체 가능) ②`shared/lib/terminal-write-bridge.ts` 의 `pendingWritesByTabId` 무제한 누적(탭이 끝내 등록하지 않으면 영구 잔류) ③`widgets/terminal-pane/terminal-link-opener.ts:28` catch 블록 내 설명 주석(comments.md 위반, F배치 소관) ④`system_open_app_data_path` 가 조회성 이름으로 디렉터리를 생성하는 부수효과 ⑤`SystemUsageStore::new` 와 `Default` 중복 정의 ⑥`terminal_sessions`/`foreground_pids` 가 전 세션 맵을 락 안에서 선형 순회(현 규모에선 무해). 검토 deferred 4건 및 acknowledge 기결 사항(raw 채널 3종 specta 제외, 원격 pty=RCE 전제, settings 스트립 정책)은 재론하지 않았다.
발견: [{"severity": "critical", "lens": "PTY/프로세스 수명", "title": "pty 세션 회수 경로가 실제로 존재하지 않는다 — 탭 닫기·프로젝트 닫기 어느 쪽도 pty_kill 을 부르지 않음", "file": "src-tauri/src/domain/terminal/commands.rs", "line": 216, "claim": "`pty_kill` 은 구현·등록·원격 dispatch 까지 되어 있으나 호출자가 0 이다. 프론트 래퍼 `killPty`(src/entities/terminal/terminal.ipc.ts:28)는 어디에서도 import 되지 않고, 터미널 탭 닫기는 `useCloseTab`→`layout_close_tab` 만 호출하며(src/widgets/editor-area/pane-tab-bar.tsx:134,143), Rust `close_tab`(domain/layout/service.rs:490-506)·`project_close`(domain/project/commands.rs:143-168, layouts·watchers·git_watchers 만 제거)에도 터미널 정리가 없다. 결과적으로 세션은 앱 종료 시 `kill_all`(lib.rs:759) 전까지 절대 회수되지 않는다.", "evidence": "grep 전수: `pty_kill|ptyKill` 참조는 bindings·ipc 래퍼·lib.rs 등록·dispatch arm·문서뿐, 실제 호출부 0. 세션 1개당 잔존 자원 = 자식 셸 프로세스 + pty master/slave fd + 리더/플러셔/waiter 3 스레드(infra/pty.rs:224,231,247) + 링버퍼 최대 2MB(domain/terminal/types.rs:6). 정본 문서는 반대로 규정한다 — docs/features/terminal.md:155 \"탭 닫기: … → Rust `pty_kill` … → ring buffer 해제\", architecture.md §6.3 \"pty·LSP·watcher 는 세션 drop 시 자식 프로세스 종료까지 보장\".", "suggestedDirection": "터미널 탭 닫기·프로젝트 닫기·보조 창 회수 각각에 세션 소유자를 정하고 그 지점에서 `pty_kill`(또는 project 단위 reap)을 호출하는 경로를 만든다. 소유 주체를 Rust 로 둘지(예: `project_close` 가 해당 projectId 세션을 일괄 kill) 프론트로 둘지(탭 닫기 확인 다이얼로그 후 kill)는 terminal.md §10 의 기존 규정을 기준으로 확정하고, 어느 쪽이든 \"탭은 닫혔는데 세션은 살아 있는\" 상태가 정상 상태로 남지 않게 한다."}, {"severity": "critical", "lens": "L-RUST-STATE", "title": "pty_write 가 TerminalStore 전역 락을 잡은 채 블로킹 write 를 수행한다 — 앱 전체 프리즈 경로", "file": "src-tauri/src/domain/terminal/commands.rs", "line": 200, "claim": "`pty_write` 는 `store.0.lock()`(parking_lot, 전 세션 공유 HashMap)을 함수 끝까지 잡은 상태로 `entry.pty.write()` → `writer.write_all()+flush()`(infra/pty.rs:59-64)를 호출한다. pty master 로의 write 는 자식이 stdin 을 읽지 않으면 블로킹되므로, 한 세션의 write 가 막히면 모든 터미널 커맨드가 같은 뮤텍스에서 정지한다.", "evidence": "commands.rs:201-203 에서 가드 `sessions` 가 살아 있는 동안 IO 수행. 같은 락을 `pty_resize`(208)·`pty_kill`(218)·`pty_set_paused`(228)·`pty_attach`(252)·`pty_detach`(279)·`terminal_sessions`(290)·`foreground_pids`(68, 에이전트 폴러 lib.rs:456 이 주기 호출)가 모두 잡는다. 게다가 `pty_attach`/`pty_detach` 는 전역 `begin_mutation()` 가드(state.rs:58)를 **먼저** 잡고 그 다음 이 락을 기다리므로, 블로킹된 write 하나가 프로젝트 열기/닫기·레이아웃·파일 저장 등 전 도메인 mutation 까지 함께 정지시킨다. 재현 시나리오: 포그라운드에서 `sleep 30` 실행 중(셸도 자식도 stdin 미독) 사용자가 tty 입력 버퍼보다 큰 텍스트를 붙여넣기 → write 블로킹 → 전 터미널·전 mutation 정지, 동시에 tokio 워커 스레드도 점유. architecture.md §2.1 \"AppState 접근은 짧은 잠금 원칙: 잠금 안에서 IO 금지\" 위반.", "suggestedDirection": "락 범위를 \"엔트리(또는 pty 핸들 Arc) 조회\"까지로 좁혀 가드를 drop 한 뒤 write/send 를 수행하도록 바꾸고, 블로킹 write 자체는 `spawn_blocking` 또는 세션별 전용 write 큐로 옮긴다. 같은 원칙을 `pty_attach` 의 링버퍼 스냅샷 복사+채널 send(255-260)에도 적용한다."}, {"severity": "major", "lens": "L-BRIDGE", "title": "원격 채널 sink 가 항상 Ok 를 반환해 broadcast_output 의 프루닝이 원격 구독자에는 작동하지 않는다", "file": "src-tauri/src/domain/terminal/commands.rs", "line": 85, "claim": "`broadcast_output` 은 \"send 실패 = 죽은 구독자\" 를 유일한 자동 회수 신호로 삼는데(commands.rs:85-89, 문서 주석 31-47 이 이를 명시적 전제로 선언), 원격 세션이 만드는 채널 sink 는 WebSocket 이 끊겼는지와 무관하게 무조건 `Ok(())` 를 돌려준다. 따라서 원격 클라이언트가 `pty_detach` 없이 끊기면(탭 종료·네트워크 단절·세션 만료 lib 경로) 그 구독자는 영구히 목록에 남는다.", "evidence": "src-tauri/src/domain/remote/ws.rs:83-98 — `let _ = sink_out.send(...)` 로 UnboundedSender 의 에러를 버리고 클로저는 항상 `Ok(())` 반환. 세션 종료 경로(ws.rs:222-228 `event_task.abort(); drop(factory); drop(tx);`)는 이미 `subscribers` 에 들어간 sink 를 회수하지 않는다. 재접속마다 구독자가 1개씩 누적되고, 누적분마다 매 출력 청크에서 `bytes.to_vec()` + 바이너리 프레임 조립(ws.rs:52-58)이 반복된다.", "suggestedDirection": "원격 sink 가 하위 채널 종료(수신자 drop / 세션 무효화)를 감지해 `Err` 를 반환하도록 만들어 기존 프루닝 계약을 되살리거나, 세션 종료 시 그 세션이 만든 subscription id 들을 일괄 detach 하는 소유권 목록을 remote 쪽에 둔다."}, {"severity": "major", "lens": "L-RUST-STATE", "title": "PtySession 에 Drop 이 없고 pty_kill 은 remove 후 kill 이라 kill 실패 시 자식이 영구 고아가 된다", "file": "src-tauri/src/infra/pty.rs", "line": 49, "claim": "`PtySession`(pty.rs:49-99)에는 `Drop` 구현이 없어 구조체가 떨어져도 자식 프로세스가 종료되지 않는다. `pty_kill` 은 맵에서 엔트리를 먼저 제거한 뒤 `entry.pty.kill()` 을 호출하므로(commands.rs:218-222), kill 이 실패하면 에러만 반환되고 핸들(killer/master)은 그대로 drop 되어 자식에 접근할 방법이 사라진다.", "evidence": "commands.rs:218-222 `let removed = store.0.lock().remove(&session_id); match removed { Some(entry) => entry.pty.kill(), … }` — 실패 시 재삽입·재시도 경로 없음. architecture.md §6.3 은 \"Drop 구현 + 명시적 shutdown 경로 이중화\" 를 전 자원 공통 규칙으로 규정하며, docs/features/terminal.md:162 도 동일하게 요구한다. 현재는 명시 경로 한 겹뿐이고 그 한 겹도 호출자가 없다(발견 1).", "suggestedDirection": "`PtySession` 에 Drop(killer.kill + 리더 스레드 종료 신호)을 넣어 안전망을 만들고, `pty_kill` 은 kill 성공을 확인한 뒤 맵에서 제거하거나 실패 시 엔트리를 되돌리는 순서로 바꾼다."}, {"severity": "major", "lens": "PTY/프로세스 수명", "title": "pause 상태의 소유자가 뷰의 휘발성 ref 라 언마운트·kill 후에도 세션이 영구 정지한다", "file": "src-tauri/src/infra/pty.rs", "line": 235, "claim": "pause 는 세션 전역 게이트(`PauseGate`)인데 그 상태를 관리하는 주체는 위젯 로컬 ref 다. 플로우컨트롤이 paused 인 상태에서 pane 이 언마운트되면 어디에서도 `set_paused(false)` 가 호출되지 않고, Rust 리더 스레드는 condvar 에 영구 park 된다 — 자식은 출력 버퍼가 차는 순간 블로킹되고, 재마운트해도 프론트 상태는 unpaused 로 초기화되어 해제 토글을 보내지 않는다.", "evidence": "infra/pty.rs:234-241 리더 루프는 매 반복 `wait_while_paused()` 를 통과해야 하고, `draining=false`(243)는 그 뒤에만 실행되므로 플러셔 스레드도 5ms tick 으로 영구 잔류한다(224-229). src/widgets/terminal-pane/terminal-pane.tsx:85-90 의 cleanup 은 detach 만 하고 pause 를 풀지 않으며, 87 행에서 `flowStateRef` 를 unpaused 로 리셋하므로 재마운트 후에도 `shouldTogglePause` 가 false → 해제 명령 미발행. `pty_kill`(commands.rs:216) 역시 pause 를 풀지 않아, paused 상태로 kill 되면 스레드 2개와 링버퍼가 그대로 남는다.", "suggestedDirection": "pause 를 세션 상태의 일부로 Rust 가 소유하게 하고(attach 시 현재 pause 상태 동기화, 마지막 구독자 detach 시 자동 해제), kill 경로에서 반드시 게이트를 해제해 리더 스레드가 루프를 빠져나오게 한다."}, {"severity": "major", "lens": "L-RUST-LAYER", "title": "terminal:exited 는 소비자 0, terminal:cwd-changed 는 발행자 0 — 계약 문서와 코드가 드리프트했고 죽은 세션이 alive 로 취급된다", "file": "src-tauri/src/domain/terminal/commands.rs", "line": 172, "claim": "`TerminalExited` 는 발행만 되고 프론트에 구독자가 없으며, `TerminalCwdChanged` 는 타입·이벤트 등록만 있고 발행하는 코드가 없다. 그 결과 셸이 스스로 종료돼도 세션 엔트리(2MB 링버퍼·pty 핸들 포함)는 그대로 남고, 프론트는 그 세션을 여전히 살아 있는 것으로 판단한다.", "evidence": "발행부 commands.rs:172-179. 구독부 grep 결과 `terminalExited`/`terminalCwdChanged` 는 src/shared/api/bindings.ts:398-399 생성물에만 존재하고 ipc-sync-provider 등 어떤 `useTauriEvent` 호출에도 없다. `TerminalCwdChanged`(events.rs:97-102)는 lib.rs:278·658 등록 외 emit 사이트 0. 세션 목록 쿼리는 `staleTime: Infinity`(src/entities/terminal/terminal.query.ts:10)에 무효화 호출도 0 이며, src/widgets/terminal-pane/terminal-session.tsx:47 의 `isPersistedAlive` 는 `session.running` 을 보지 않아 running=false 세션에 attach 하고 재spawn 하지 않는다. 문서는 이 흐름이 살아 있다고 적혀 있다 — docs/ipc-contract.md:210-213, docs/features/terminal.md:157-158.", "suggestedDirection": "exit 이벤트 소비자(세션 목록 invalidate + 종료 표시/재시작)를 두거나, Rust 가 exit 시 엔트리를 스스로 회수하도록 소유권을 정리한다. cwd-changed 는 구현하든 계약에서 제거하든 한쪽으로 정리해 문서-코드 드리프트를 없앤다."}, {"severity": "major", "lens": "L-RUST-STATE", "title": "ring_buffer_append 가 이름과 달리 O(n) front-drain 이라 정상 출력 구간에서 메모리 대역폭을 32배 증폭한다", "file": "src-tauri/src/domain/terminal/service.rs", "line": 10, "claim": "링버퍼가 `Vec` + `drain(0..overflow)` 로 구현돼 있어, 버퍼가 상한(2MB)에 도달한 뒤에는 청크가 들어올 때마다 남은 전체(최대 2MB)를 앞으로 memmove 한다. 배치 청크가 64KB(READ_BUFFER_BYTES) 또는 4ms 단위로 들어오므로 상시 출력 구간에서 실제 복사량이 입력량의 수십 배가 된다.", "evidence": "service.rs:10-16 `buffer.extend_from_slice(incoming); if buffer.len() > capacity { buffer.drain(0..overflow) }`. 용량 domain/terminal/types.rs:6 = 2MiB, 청크 크기 types.rs:7 = 64KiB, 플러시 주기 types.rs:8-9 = 4/5ms. 이 append 는 pty 리더 스레드의 `on_data` 안에서 `batch.lock()` 을 잡은 채 실행된다(infra/pty.rs:127-134 → commands.rs:168-171).", "suggestedDirection": "`VecDeque` 기반 실제 링(또는 청크 리스트 + 앞쪽 청크 통째 폐기)으로 바꿔 상한 도달 후에도 append 가 상수 시간이 되게 한다. `pty_attach` 의 스냅샷은 두 슬라이스를 이어붙여 만들면 된다."}, {"severity": "major", "lens": "L-RUST-LAYER", "title": "system/commands.rs 가 도메인 로직(URL 검증)과 infra(sysinfo 어댑터)를 함께 흡수해 service/infra 경계가 무너져 있다", "file": "src-tauri/src/domain/system/commands.rs", "line": 261, "claim": "`validate_external_url` 은 순수 판정 로직인데 전용 단위테스트까지 커맨드 파일에 있고(261-290, 테스트 316-387), sysinfo 를 직접 다루는 상태 보관·리프레시 코드(`SystemUsageStore` 38-56, `collect_system_usage` 64-91, `refresh_all_process_records` 109-134)도 커맨드 파일에 있다. 같은 도메인의 service.rs 는 순수 함수만 담고 있어 배치 기준이 파일 간에 일관되지 않는다.", "evidence": "architecture.md §2 \"각 domain 모듈은 commands.rs(IPC 노출)/service.rs(로직)/types.rs 로 나눈다. command 는 얇게: 파라미터 검증 → service 호출 → 이벤트 발행. 로직은 service 에만 둔다\", 그리고 \"infra/ = 외부 자원 어댑터\". pty(portable-pty)·watcher(notify)는 infra 로 빠져 있는데 sysinfo 만 commands 에 남아 있다. 커맨드 파일 388줄 중 실제 `#[tauri::command]` 본문은 약 60줄이다.", "suggestedDirection": "`validate_external_url`(+테스트)은 service.rs 로, sysinfo `System` 보관·refresh 는 infra 어댑터로 옮기고 commands 에는 검증→호출→응답만 남긴다."}, {"severity": "major", "lens": "L-RUST-BOUND", "title": "system 도메인이 terminal·lsp·agent 세 도메인의 Store 를 직접 참조한다", "file": "src-tauri/src/domain/system/commands.rs", "line": 136, "claim": "`system_usage_breakdown` 이 라벨을 만들기 위해 `TerminalStore`·`LspStore`·`AgentStore` 를 직접 import 해 각 도메인의 내부 조회 API(`foreground_pids`·`server_pids`·`agents_for`)를 호출한다. 도메인 간 직접 호출 금지 규칙에 정면으로 걸리는 3개 엣지다.", "evidence": "import 11-13, 사용 145(terminals.foreground_pids)·148(agents.agents_for)·174(lsp.server_pids), 커맨드 시그니처 161-167 이 세 Store 를 전부 State 로 받는다. architecture.md:77 \"도메인 간 직접 호출은 금지하고, 필요하면 이벤트 버스(내부 broadcast channel) 또는 상위 조립부를 거친다\". 결과적으로 terminal/lsp/agent 어느 쪽의 Store 시그니처가 바뀌어도 system 이 깨진다.", "suggestedDirection": "\"pid → (kind,label)\" 을 제공하는 얇은 기여자(provider) 계약을 상위 조립부(lib.rs)에서 등록하는 형태로 뒤집어, system 은 등록된 라벨 소스 목록만 순회하게 한다."}, {"severity": "major", "lens": "L-RUST-BOUND", "title": "terminal 이 ide 도메인을 직접 참조하고, 대기 로직이 커맨드 층에 있어 모든 spawn 이 최대 2초 지연될 수 있다", "file": "src-tauri/src/domain/terminal/commands.rs", "line": 104, "claim": "`pty_spawn` 이 ide 도메인의 Store·상수를 직접 import 해 IDE 기동을 폴링 대기한다. IDE 연동 기본값이 켜져 있고 서버 기동이 실패하면 `status.running` 이 계속 false 이므로, 모든 새 터미널이 매번 2초 폴링을 소진한 뒤에야 열린다.", "evidence": "import 12-13(`domain::ide::commands::IdeStore`, `IDE_READY_WAIT_MS`), 대기 루프 104-115(50ms 간격 폴링), 사용 134-139. `IDE_READY_WAIT_MS = 2_000`(domain/ide/types.rs:19), `ide_integration_enabled` 기본 true(domain/settings/types.rs:232), 부팅 시 기동 실패는 경고 로그만 남기고 재시도하지 않는다(lib.rs:681-694). architecture.md:77 도메인 간 직접 호출 금지 + \"command 는 얇게\" 규칙 동시 위반.", "suggestedDirection": "IDE 포트 주입을 \"spawn 시 환경변수 기여자\" 형태의 조립부 계약으로 빼고, 대기가 필요하면 terminal 이 아니라 ide 쪽이 준비 완료를 알리는 신호(watch/broadcast)를 소비하도록 바꾼다. 무한정 폴링 대신 즉시 진행 + 이후 세션부터 반영도 선택지다."}, {"severity": "major", "lens": "L-RUST-LAYER", "title": "블로킹 IO 를 async 커맨드에서 직접 수행한다 — font_list 는 시스템 폰트 전수 스캔을 매 호출 반복", "file": "src-tauri/src/domain/font/commands.rs", "line": 7, "claim": "`font_list` 는 `async fn` 안에서 `Database::load_system_fonts()`(전 시스템 폰트 디렉터리 스캔·파싱)를 그대로 호출해 tokio 워커 스레드를 점유하고, 캐시가 없어 호출마다 전체 스캔을 반복한다. 같은 패턴이 snippet 도메인의 디렉터리 스캔·파일 쓰기에도 있다.", "evidence": "font/commands.rs:5-9 → font/service.rs:7-11 `database.load_system_fonts()`. snippet/commands.rs:7-21 은 `read_dir`+파일별 파싱(service.rs:62-90)·`write_atomic`(101)을 async 커맨드에서 직접 호출. 반면 같은 배치의 task 는 규칙대로 처리한다 — task/commands.rs:14-17 `spawn_blocking(move || service::detect_tasks(&root))`, system 도 마찬가지(system/commands.rs:97, 187). architecture.md §2.1 \"블로킹 작업(git2 호출, 대형 파일 IO)은 spawn_blocking 으로 격리한다\".", "suggestedDirection": "font·snippet 커맨드도 `spawn_blocking` 으로 감싸고, 폰트 목록은 프로세스 수명 캐시(OnceLock 등) + 명시적 갱신 커맨드로 재스캔 비용을 없앤다."}, {"severity": "minor", "lens": "L-CONTRACT", "title": "pty_spawn 의 on_data 채널 인자가 즉시 drop 되는 유령 파라미터다", "file": "src-tauri/src/domain/terminal/commands.rs", "line": 143, "claim": "`pty_spawn` 은 필수 인자로 채널을 받지만 본문 첫머리에서 `drop(on_data)` 하고 구독자로 등록하지 않는다. 계약 표면상으로는 출력 스트림 채널처럼 보이지만 실제로는 아무 역할이 없어, 호출자가 attach 를 빠뜨리면 아무 출력도 받지 못한다.", "evidence": "commands.rs:132(파라미터)·143(`drop(on_data)`), 주석 117-123 이 의도적 유지임을 설명. 호출자는 무의미한 no-op sink 를 넘긴다(src/widgets/terminal-pane/terminal-session.tsx:54 `spawnPty({…}, () => undefined)`). 문서는 이 인자를 실제 출력 채널로 서술한다 — docs/ipc-contract.md:202, docs/features/terminal.md:146. 원격 경로에서는 이 drop 이 즉시 `chanEnd` 프레임을 유발한다(remote/ws.rs:44-50).", "suggestedDirection": "인자를 제거하고 attach 를 유일한 구독 경로로 계약을 정리하거나(원격 dispatch arm·문서 동시 수정), 유지한다면 spawn 직후 첫 구독자로 등록해 이름이 뜻하는 대로 동작시킨다."}, {"severity": "minor", "lens": "L-RUST-STATE", "title": "pty_attach/pty_detach 가 영속 상태를 건드리지 않는데 전역 mutation 가드를 잡는다", "file": "src-tauri/src/domain/terminal/commands.rs", "line": 251, "claim": "두 커맨드는 TerminalStore 의 구독자 목록만 변경하는데 앱 전역 직렬화 락 `begin_mutation()` 을 획득한다. 탭 전환마다 발생하는 attach/detach 가 프로젝트 열기·파일 저장·레이아웃 변경 등 모든 mutation 과 직렬화되고, 발견 2 의 블로킹 시나리오에서는 프리즈 범위를 앱 전체로 넓히는 증폭기가 된다.", "evidence": "attach 251, detach 278 이 `state.begin_mutation().await`(state.rs:58, tokio Mutex) 획득. 반면 같은 성격의 `pty_write`·`pty_resize`·`pty_set_paused`(200/208/227)는 잡지 않아 기준이 일관되지 않는다. 가드가 지켜야 할 대상인 `project_close` 는 터미널 세션을 건드리지 않는다(domain/project/commands.rs:143-168).", "suggestedDirection": "attach/detach 에서 전역 가드를 제거하고, 정말 직렬화가 필요하면 TerminalStore 내부 락만으로 충분한지 판단해 범위를 도메인 안으로 좁힌다."}, {"severity": "minor", "lens": "L-CODE", "title": "resolve_terminal_path 는 호출자 0 인 죽은 IPC 표면이다 (문서상 cmd+click 파일 링크는 미구현)", "file": "src-tauri/src/domain/terminal/commands.rs", "line": 312, "claim": "`resolve_terminal_path` 는 커맨드·바인딩·엔티티 래퍼·원격 dispatch arm 까지 갖췄지만 실제 호출자가 없다. 터미널 링크 클릭은 URL 열기만 처리하므로(파일 경로 분기 없음), 문서가 1차 범위로 명시한 \"파일 링크(cmd+click)\" 는 사실상 없는 기능이다.", "evidence": "래퍼 src/entities/terminal/terminal.ipc.ts:36 은 어디에서도 import 되지 않는다(grep 0). 링크 처리 경로는 src/widgets/terminal-pane/terminal-session.tsx:92-97 → terminal-link-opener.ts:6-9 로 `windowOpen`/`openExternalUrl` 만 호출한다. 문서 범위 표기는 docs/features/terminal.md:171 \"파일 링크(cmd+click)\". 부수적으로 이 커맨드는 루트 가드 없이 임의 경로를 canonicalize 해 반환하며(service.rs:125-141) 원격에서도 호출 가능하다(remote/dispatch.rs 의 `resolve_terminal_path` arm).", "suggestedDirection": "파일 링크 기능을 실제로 배선하거나(그 경우 반환 경로에 루트 가드 정책을 명시), 기능을 접는다면 커맨드·바인딩·엔티티 래퍼·dispatch arm·문서를 함께 제거해 표면을 줄인다."}, {"severity": "minor", "lens": "L-I18N", "title": "사용자에게 그대로 노출되는 Rust 에러 메시지가 i18n 밖이고 한국어/영어가 혼재한다", "file": "src-tauri/src/domain/terminal/commands.rs", "line": 95, "claim": "AppError 의 `message` 는 프론트에서 그대로 토스트·화면 문구로 쓰이는데, 도메인마다 언어가 다르다. 터미널은 영어(\"project not open: …\"), system 은 한국어(\"http:// 또는 https:// 로 시작하는 URL만 열 수 있습니다\")로 작성돼 같은 UI 안에서 언어가 섞인다.", "evidence": "영어: terminal/commands.rs:95,101,221,327. 한국어: system/commands.rs:273,280,286. 전달 경로: src/shared/api/unwrap-result.ts:9 `super(error.message)` → src/widgets/terminal-pane/terminal-session.tsx:71-72 가 `error.message` 를 그대로 `setFailure`/`toast.error` 로 표시한다. AppError 는 5개 coarse variant 뿐이라(src-tauri/src/error.rs:6-21) 프론트가 코드만으로 문구를 재구성할 수도 없다.", "suggestedDirection": "사용자 노출 문구는 프론트 i18n 키로 만들고 Rust 메시지는 진단용(로그·details)으로 격하하거나, 최소한 도메인 무관하게 언어 기준 한 가지를 정해 통일한다."}, {"severity": "minor", "lens": "PTY/프로세스 수명", "title": "셸 통합 임시 디렉터리가 pty spawn 마다 생성되고 정리 경로가 없다", "file": "src-tauri/src/infra/shell_integration.rs", "line": 68, "claim": "zsh/bash 세션마다 `fresh_temp_dir()` 로 uuid 임시 디렉터리를 만들고 rc 스크립트를 쓰지만, 세션 종료·앱 종료 어느 시점에도 삭제하지 않는다. 앱 사용 기간 동안 디스크에 계속 쌓인다.", "evidence": "생성 68-70, 사용 165-170(zsh: .zshenv/.zprofile/.zshrc)·244-248(bash: init.bash). `remove_dir_all` 은 테스트 안에만 있다(394행, `mod tests` 310행 이후). 소유자여야 할 `PtySession` 은 이 경로를 알지도 않는다(infra/pty.rs:49-56 필드 없음).", "suggestedDirection": "임시 디렉터리 경로를 `PtySession` 이 소유하고 Drop/kill 시 삭제하거나, 세션당 새로 만들지 말고 셸 종류별 1회 생성 후 재사용한다."}, {"severity": "minor", "lens": "PTY/프로세스 수명", "title": "링버퍼 재생이 UTF-8·이스케이프 시퀀스 경계를 고려하지 않는다", "file": "src-tauri/src/domain/terminal/commands.rs", "line": 255, "claim": "버퍼가 가득 찬 뒤에는 앞부분이 바이트 단위로 잘리므로, 그 상태에서 새 구독자에게 통째로 재생하면 스냅샷 첫머리가 멀티바이트 문자 중간이나 CSI/OSC 시퀀스 중간에서 시작할 수 있다. 재부착 직후 첫 화면이 깨지거나 파서 상태가 어긋난다.", "evidence": "자르기 service.rs:12-15(바이트 오프셋 drain), 재생 commands.rs:255-258(`entry.ring_buffer.lock().clone()` 전체를 그대로 send). 상한 2MiB(types.rs:6) 를 넘는 출력은 빌드 로그 등에서 흔하다.", "suggestedDirection": "버퍼를 청크 단위로 폐기해 잘림 지점을 청크 경계로 고정하거나, 재생 시 첫 유효 UTF-8 경계까지 스킵하는 최소 보정을 둔다."}, {"severity": "minor", "lens": "L-CODE", "title": "font service 테스트가 호스트의 실제 시스템 폰트에 의존한다", "file": "src-tauri/src/domain/font/service.rs", "line": 41, "claim": "`가족명은_중복_없이_정렬되어_반환된다` 테스트가 `list_families()` 를 호출해 실행 머신의 시스템 폰트를 전부 로드한다. 환경에 따라 결과와 소요 시간이 달라지고, 폰트가 없는 CI 컨테이너에서는 사실상 아무것도 검증하지 못한다.", "evidence": "font/service.rs:39-47 — `let families = list_families();` 로 실제 DB 로드. 같은 파일의 다른 테스트는 순수 함수 `collect_families(&Database::new())` 를 쓰고 있어(35-38) 결정적 방식이 이미 존재한다.", "suggestedDirection": "정렬·중복 제거 검증은 `Database` 를 주입해 결정적으로 하고(이미 `collect_families` 가 그 형태), 실제 시스템 로드는 테스트에서 제외한다."}]

## X1
요약: X1(계약 정합 전용) 결과. 4중 대조는 기계 검증했고 커맨드 축은 완전 정합, 이벤트 축에서 결함이 몰려 나왔다.

4중 대조 실측(HEAD=5f15a84). lib.rs collect_commands! **179** ↔ bindings.ts `__TAURI_INVOKE` **179** ↔ dispatch.rs IMPLEMENTED_JSON_COMMANDS **179** — 세 집합의 양방향 차집합이 전부 공집합이고 중복도 0. RAW_CHANNEL_COMMANDS 3종(pty_spawn·pty_attach·file_read_raw)은 세 목록 밖에서 dispatch/dispatch_raw 전용 arm 으로 처리되며, 프론트 raw invoke 3곳(file.raw.ts:18, terminal.ipc.ts:16·19)과 정확히 1:1. 원격 명시 거부는 코드 실측 16종(dispatch.rs:553,580,624(2종),958,959,960,961,962,1000,1001,1006,1008(2종),1010,1011)으로 문서 표 16종과 완전 일치. 부분 스트립 2종(settings_update 3필드 / app_file_write Settings 3필드)도 일치. 도메인별 카운트도 문서와 맞다(git 41 / agent 9 / ai 8 / ide 9 / remote 7). 유일한 수치 불일치는 문서 머리말 178·181(발견 14) — 같은 문서 §원격 dispatch 정책의 179 가 맞다.

이벤트 25종 전수 매핑(이번 배치의 핵심 산출). events.rs 25 = collect_events! 25 = bindings makeEvent 25, fanout_remote_events! 24(HotExitFlushRequested 제외 — QA6 의도). 발행 0: app:ready·terminal:cwd-changed 2종. 구독 0: 위 2종 + terminal:exited·lsp:session-status-changed·remote:state-changed·project:focus-kind-changed = 6종(프론트 실구독 19종). app:ready 는 기지 이슈로 재확인만 했고, terminal:cwd-changed 는 문서가 살아있는 흐름으로 단언한다는 점에서 새 발견으로 올렸다(발견 1). 그리고 이 축에는 파리티 테스트가 0건이다(발견 8) — 정찰이 지목한 "bindings↔dispatch 만 강제" 라는 공백은 확인됐을 뿐 아니라, 그 하나뿐인 테스트조차 생성 산출물 bindings.ts 를 기준선으로 삼아 collect_commands! 엣지는 무강제다(발견 9).

data-model.md ↔ types.rs 대조. ProjectLayout·PaneNode·Tab(TabKind 9종)·ClosedTab·AuxWindowLayout·ShellViewState·AppFileTarget·SearchQuery·SearchMatch·MirrorFile·UntitledMirrorFile·Task·git 신규 6종·AI 요청응답 4종·AuxiliaryWindowInfo 는 전부 실코드와 일치. 어긋난 곳은 4군데 — sync 페이로드 스키마 통째 누락 + 잘못된 상호참조(발견 5), 디스크 트리의 locales/·lsp/ 누락(발견 15), MirrorFile baseline 출처 주석 낡음(발견 16), ProjectRef.root 타입과 Project 구조체(발견 17). 설정 필드 파리티는 별도 검증했고 현재 드리프트 0 — Settings 56 / SettingsPatch 53 / emptySettingsPatch 53(양방향 차집합 공집합) / settings_to_sync_patch 는 `..Default` 없는 exhaustive 리터럴이라 컴파일러가 강제한다.

문서가 정본으로 기재했으나 실물이 없는 것 3건이 이번 배치의 실질 소득이다: viewState 영속(발견 4), fs:changed 의 echo 플래그(발견 10), revision 게이트(발견 11). 여기에 원격 경계 3건 — agent:external-open 팬아웃+drain 허용 조합(발견 3, QA6 가 file_flush_complete 에 대해 이미 고친 것과 동일 유형), asset 스코프 미회수(발견 7), sync 버전 게이트 무력화(발견 6).

압축으로 누락한 저심각 7건(계수만): projectGetActive 만 반환 타입이 `ProjectId | null` 아닌 `string | null`(specta transparent newtype 인라인 편차 — ProjectId 가 순수 alias 라 무해) / AppReady 만 serde rename_all 누락(R1 기보고) / layout_resize 인자는 `(number|null)[]` 인데 PaneNode.sizes 는 `f32[]` 인 비대칭 / tauri-internals-shim.ts:79 이 모든 `plugin:*` invoke 를 조용히 null 로 삼켜 원격에서 dialog 가 에러 없이 무응답 / ipc-contract §1 의 revision 규정이 25종 중 1종에만 적용된 상태로 일반 규칙처럼 서술 / SyncThemeEntry·SyncLocaleEntry 는 specta::Type 미파생(IPC 미노출이라 정상이나 SyncPayload 만 버전 필드를 가짐) / capabilities/main.json 은 opener 계열 0·dialog 2종만으로 §4 선언과 정합(위반 아님, 확인 결과만 기록).

기계 검증 0 축(FSD 역방향 import·barrel·any/enum·useCallback/useMemo·function 키워드)은 이번 배치 범위가 Rust 계약 표면과 생성 바인딩이라 해당 없음. acknowledge 기결 4건(settings→remote 와일드카드 엣지·ContextKeyExpr/emmet 딥임포트·monaco-editor-core dev 의존·검토 deferred)은 재론하지 않았다. 앱 실행·수정 없이 정적 조회와 bun 단발 스크립트만 사용했다.
발견: [{"severity": "major", "lens": "L-CONTRACT", "title": "terminal:cwd-changed 는 발행처도 구독처도 없는 완전 사망 이벤트인데 정본은 살아있는 흐름으로 단언", "file": "src-tauri/src/events.rs", "line": 99, "claim": "25종 이벤트 중 terminal:cwd-changed 만 Rust emit 0건 + 프론트 구독 0건인 양방향 사망 배선이다. 결과로 터미널 cwd 는 spawn 시점 값에 영구 고정되고, resolve_terminal_path(path, cwd) 로 터미널 링크를 여는 경로는 사용자가 cd 한 뒤 항상 낡은 기준 디렉토리로 해석한다.", "evidence": "타입 정의는 events.rs:96-102 뿐. TerminalCwdChanged 를 src-tauri 전수 grep 하면 lib.rs:32(use)·278(collect_events!)·658(fanout_remote_events!) 3곳만 나오고 .emit( 지점이 없다. 프론트도 events.terminalCwdChanged(bindings.ts:398 에 생성됨)를 어디서도 쓰지 않는다(useTauriEvent 호출 21곳 전수에 없음). Rust cwd 는 terminal/commands.rs:160,185 에서 opts.cwd 로 한 번 채워지고 :297 에서 되읽히기만 한다. 그럼에도 docs/ipc-contract.md:210-213 은 'cwd 보고는 프론트→Rust mutation 이 아니라 Rust→view 이벤트 terminal:cwd-changed 로 흐른다' 고 명시한다.", "suggestedDirection": "(a) OSC 7 파싱을 pty 리더에 붙여 실제 emit 하고 프론트가 TabKind::Terminal{cwd}·링크 해석 기준을 갱신하도록 배선하거나, (b) 이벤트·타입·collect/fanout 등록을 제거하고 ipc-contract.md §terminal 을 'cwd 는 spawn 시점 고정' 으로 정정한다. 어느 쪽이든 resolve_terminal_path 의 cwd 출처를 문서에 명시해야 한다."}, {"severity": "major", "lens": "L-CONTRACT", "title": "이벤트 25종 중 6종이 구독자 0 — LSP 세션 오류·터미널 종료가 UI 에 도달할 경로가 없다", "file": "src-tauri/src/domain/lsp/commands.rs", "line": 198, "claim": "프론트가 실제 구독하는 이벤트는 19종뿐이다. app:ready·terminal:cwd-changed·terminal:exited·lsp:session-status-changed·remote:state-changed·project:focus-kind-changed 6종은 구독처가 없고 그중 4종은 Rust 가 정상 발행하며 원격까지 팬아웃된다. lsp:session-status-changed 는 last_error 를 실어 보내지만 받는 쪽이 없어 언어 서버 기동 실패·크래시가 사용자에게 전혀 보이지 않고, terminal:exited 도 소비되지 않아 죽은 pty 의 탭이 살아 있는 것처럼 남는다.", "evidence": "발행 확인: lsp/commands.rs:198-203, terminal/commands.rs:174-178, remote/commands.rs:391·432, layout/commands.rs:62-66. 구독 확인: useTauriEvent( 호출 21건 전수(ipc-sync-provider.tsx 12건, status-bar-content.tsx 3건, ide.query.ts:11, lsp.query.ts:26, agent.query.ts:63, agent-external-open-provider.tsx:73, app-shell.tsx:139 의 dragDrop)에 위 6종이 없다. 이벤트 이름 문자열('lsp:session-status-changed' 등)도 src/ 전체에서 bindings.ts 외 히트 0. LspSessionStatus 타입 역시 프론트에서 참조 0건. 문서는 ipc-contract.md:234(lsp)·210(terminal)·58(project)·383(remote) 에서 이 6종을 모두 정본 이벤트로 등재한다.", "suggestedDirection": "각 이벤트마다 (구독자 배선) / (발행+등록 제거) 를 결정해 문서와 코드를 한쪽으로 맞춘다. 최소한 lsp:session-status-changed 는 상태바·LSP 패널에 실패 표시로, terminal:exited 는 터미널 탭 종료 표시로 잇는 것이 사용자 영향이 크다."}, {"severity": "major", "lens": "L-CONTRACT", "title": "agent:external-open 이 원격으로 팬아웃되고 drain 커맨드도 원격 허용 — 원격 브라우저가 데스크톱의 CLI 열기 요청을 가로챈다", "file": "src-tauri/src/lib.rs", "line": 664, "claim": "AgentExternalOpen 은 fanout_remote_events! 목록에 있고 agent_pending_external_opens 는 원격 명시 거부 16종에 없어 원격 세션이 그대로 호출할 수 있다. 원격 세션은 window-context 상 main 창으로 판정되어 AgentExternalOpenProvider 를 그대로 마운트하므로, taide <file> 로 발생한 요청 하나를 데스크톱과 원격이 동시에 drain 하려 경쟁하고 원격이 이기면 데스크톱 사용자는 파일이 열리는 것을 보지 못한다. waitMarker 가 붙은 요청이면 마커가 원격 realm 레지스트리에 등록돼 CLI 가 계속 블로킹된다.", "evidence": "lib.rs:647-670 의 fanout 목록에 AgentExternalOpen 이 포함(HotExitFlushRequested 만 QA6 에서 제외됨). dispatch.rs 의 deny arm 16개(553,580,624,958-962,1000,1001,1006,1008,1010,1011)에 agent_pending_external_opens 는 없다. agent-external-open-provider.tsx:73 이 이벤트 수신 시 drainPendingExternalOpens 를, :76 이 마운트 시 한 번 더 호출한다(:59-62 pendingExternalOpens 는 Rust 쪽 drain). window-context.ts:31 은 쿼리 파라미터가 없으면 {kind:'main'} 을 반환하고 원격 셸 URL 에는 projectId/windowSlot 이 없으므로 app.tsx:58-77 의 main 분기가 선택된다. 이는 QA6 가 file_flush_complete/HotExitFlushRequested 쌍에 대해 이미 고친 것과 동일한 실패 유형이다(ipc-contract.md:596-602).", "suggestedDirection": "QA6 선례를 그대로 적용한다 — AgentExternalOpen 을 fanout 목록에서 빼고 agent_pending_external_opens 에 deny_remote_* arm 을 추가한다. 원격 세션이 데스크톱 로컬 CLI 의 열기 요청을 소비할 정당한 근거가 없다."}, {"severity": "major", "lens": "L-CONTRACT", "title": "layout_set_view_state 는 호출부가 0 — 문서가 정본으로 기재한 에디터 viewState(커서·스크롤) 영속이 실재하지 않는다", "file": "src/shared/api/bindings.ts", "line": 27, "claim": "bindings 의 179 커맨드 중 layout_set_view_state 는 src/ 전체에서 호출부가 없다. Rust 쪽 Tab.view_state 는 생성 지점(layout/commands.rs:107·350, layout/service.rs:105·117)에서 항상 None 으로만 채워지고 유일한 writer 인 이 커맨드가 호출되지 않으므로 layout.json 의 view_state 는 영원히 null 이다. 즉 재시작 후 커서·스크롤 복원 기능은 존재하지 않는다.", "evidence": "commands.layoutSetViewState 및 layoutSetViewState 문자열을 src/ 전체에서 grep 하면 bindings.ts:27 외 히트 0. viewState/view_state 도 프론트에 히트 0. 반면 data-model.md:53 은 layout.json 내용으로 '에디터별 viewState(커서·스크롤)' 을, :101 은 Tab.view_state 를 'monaco 가 직렬화한 불투명 JSON 문자열' 로, ipc-contract.md:70 은 layout_set_view_state 를 정본 mutation 으로 등재한다.", "suggestedDirection": "editor-pane 이 모델 전환·탭 비활성화 시 monaco saveViewState() 를 이 커맨드로 넘기고 복원 시 restoreViewState() 하도록 배선하거나, 기능을 접는다면 커맨드·Tab.view_state 필드·문서 3곳을 함께 정리한다."}, {"severity": "major", "lens": "L-CONTRACT", "title": "sync gist 페이로드 스키마가 두 정본 문서 어디에도 없고, ipc-contract 의 상호참조는 엉뚱한 절을 가리킨다", "file": "src-tauri/src/domain/sync/types.rs", "line": 25, "claim": "SyncPayload{schemaVersion, updatedAt, settings, themes[], locales[]} 는 version 필드를 가진 영속(원격) 스키마인데 data-model.md 에 절이 없다. ipc-contract.md:385 은 'data-model.md §6 참조' 라고 안내하지만 data-model.md §6(:126)은 hot-exit 미러 절이라 sync 와 무관하다. 게다가 ipc-contract.md:390-392 의 sync_upload/sync_download 서술은 settings 4필드 제외만 말하고 사용자 테마·로케일 팩이 함께 업로드되고 다운로드 시 앱 데이터 디렉토리에 파일로 기록된다는 사실을 전혀 언급하지 않는다.", "evidence": "sync/types.rs:25-33 이 SyncPayload 정의(themes: Vec<SyncThemeEntry>, locales: Vec<SyncLocaleEntry>). sync/service.rs:137-150 assemble_payload 가 세 축을 묶고, :174-185 가 themes_dir/locales_dir 를 훑어 담고, :195-220 apply_theme_entries/apply_locale_entries 가 다운로드 페이로드를 theme_service::save_theme / locale_service::save_locale 로 디스크에 기록한다. data-model.md 목차는 §1~§12 이며 sync 절이 없다(§5 는 '모든 영속 파일은 version 필드를 가진다' 고만 규정).", "suggestedDirection": "data-model.md 에 sync 페이로드 절을 신설해 SyncPayload 스키마·버전 정책·themes/locales 왕복과 그 쓰기 경로(save_theme/save_locale 의 id 검증 포함)를 기재하고, ipc-contract.md:385 의 §6 상호참조를 새 절 번호로 교정한다."}, {"severity": "major", "lens": "L-CONTRACT", "title": "sync 페이로드 버전 게이트가 구조적으로 발동 불가 — SETTINGS_SCHEMA_VERSION 이 정책상 1 로 동결돼 있다", "file": "src-tauri/src/domain/sync/service.rs", "line": 229, "claim": "ensure_supported_schema_version 은 payload.schemaVersion > SETTINGS_SCHEMA_VERSION 일 때만 거부하는데, 업로드 시 스탬프하는 값도 같은 상수이고 그 상수는 Wave A~I 동안 설정 필드가 20여 개 늘어나는 내내 1 로 고정 유지가 명시적 정책이다. 따라서 신버전 TAIDE 가 쓴 페이로드도 schemaVersion=1 로 도착해 게이트를 통과하고, 구버전이 모르는 필드를 조용히 버린 채 부분 적용된다 — 이 함수의 doc comment 가 막겠다고 선언한 바로 그 실패다.", "evidence": "settings/types.rs:4 `pub const SETTINGS_SCHEMA_VERSION: u32 = 1;`. sync/service.rs:144 이 그 상수로 스탬프하고 :229 가 같은 상수와 비교한다. sync/service.rs:224-228 doc comment: 'a payload written by a future TAIDE version would otherwise deserialize without error and get partially applied'. ipc-contract.md:430 이 'SETTINGS_SCHEMA_VERSION 은 1 유지(전 필드 serde default)' 를 정책으로 확정. 테스트(sync/service.rs:340,350)도 상수 자기 자신과 +1 만 확인해 이 무력화를 잡지 못한다.", "suggestedDirection": "sync 페이로드에 settings 스키마와 분리된 자체 버전 상수를 두고 페이로드 구조가 바뀔 때만 올리거나, 필드 누락 감지를 버전이 아니라 미지 필드 보존(passthrough)으로 바꾼다. 현재처럼 동결된 상수를 게이트로 쓰는 구조는 유지해도 아무 효과가 없다."}, {"severity": "major", "lens": "L-CONTRACT", "title": "asset 프로토콜 스코프가 프로젝트 열 때만 등록되고 닫을 때 회수되지 않아 닫힌 프로젝트 트리를 계속 읽을 수 있다", "file": "src-tauri/src/domain/project/commands.rs", "line": 18, "claim": "allow_asset_access 가 프로젝트 루트를 재귀 허용으로 asset_protocol_scope 에 등록하지만 project_close 에는 대응하는 forbid_directory 가 없다. 그래서 webview 는 프로젝트를 닫은 뒤에도 convertFileSrc 로 그 트리의 임의 파일을 계속 읽을 수 있다. 같은 논리 전송의 원격 백엔드(/__taide/file)는 매 요청마다 살아있는 열린 프로젝트 집합을 재검사하므로 두 백엔드의 인가 수명이 어긋난다.", "evidence": "project/commands.rs:17-21 이 allow_directory(root, true) 를 호출하는 유일한 지점이고, forbid_directory 는 src-tauri 전체에 존재하지 않는다. project_close(:143-170)는 layouts·watchers·git_watchers 만 정리한다. 반면 remote/serving.rs:181-188 의 file_range 는 매 요청 root_guard::resolve_owning_project(&projects, ...) 로 재검증하고 실패 시 403 이다. 커맨드 경로도 동일하게 열린 프로젝트 기준 root_guard 를 쓴다. tauri.conf.json 의 assetProtocol.scope 는 [] 라 런타임 등록이 유일한 권한 출처다.", "suggestedDirection": "project_close 에서 asset_protocol_scope().forbid_directory(root, true) 로 대칭 회수하거나, preview 경로를 asset:// 대신 원격과 동일한 루트 재검증 경로로 통일한다. 어느 쪽이든 asset 프로토콜을 ipc-contract.md §1 전송 매체 표·§4 보안 절에 전송 수단으로 명시해야 한다."}, {"severity": "major", "lens": "L-CONTRACT", "title": "이벤트 축은 손으로 유지되는 목록 4개인데 파리티 테스트가 0건 — 커맨드 축에 있는 강제 장치가 없다", "file": "src-tauri/src/lib.rs", "line": 267, "claim": "events.rs 의 이벤트 struct 25종, lib.rs:267-293 collect_events! 25종, lib.rs:647-670 fanout_remote_events! 24종, bindings.ts:376-400 events 맵 25종 — 네 목록이 전부 수기이고 일치를 검증하는 테스트가 하나도 없다. 커맨드 축에는 bindings↔dispatch 파리티 테스트가 있지만 이벤트 축은 등록 누락·팬아웃 누락이 전부 무증상으로 통과한다. fanout 이 24인 것(HotExitFlushRequested 제외)은 QA6 의 의도된 결정이지만, 그 의도 자체도 테스트가 아니라 주석으로만 남아 있다.", "evidence": "src-tauri/src 전체에서 collect_events·fanout·event_name 를 참조하는 #[test] 가 0건. dispatch.rs:1045-1063 의 파리티 테스트는 __TAURI_INVOKE 정규식만 훑어 커맨드 이름 집합만 비교한다. 실측 카운트: events.rs 의 #[tauri_specta(event_name=...)] 25, collect_events! 25, fanout_remote_events! 24, bindings makeEvent 25.", "suggestedDirection": "dispatch 파리티 테스트와 같은 방식으로 events.rs 의 event_name 집합 ↔ bindings.ts 의 makeEvent 이름 집합을 비교하는 테스트를 추가하고, fanout 제외 목록을 상수 배열로 뽑아 '전체 − 제외 = fanout' 을 어서션한다."}, {"severity": "major", "lens": "L-CONTRACT", "title": "커맨드 파리티 테스트가 생성 산출물 bindings.ts 를 기준선으로 삼아 collect_commands! ↔ bindings 엣지가 무강제", "file": "src-tauri/src/domain/remote/dispatch.rs", "line": 1046, "claim": "파리티 테스트는 include_str! 로 커밋된 bindings.ts 를 읽어 IMPLEMENTED_JSON_COMMANDS 와 비교한다. bindings.ts 는 debug 빌드에서만 재생성되는 파생물이므로, collect_commands! 에 커맨드를 추가하고 bindings 를 재생성·커밋하지 않으면 테스트는 '낡은 bindings = 낡은 dispatch' 로 통과한다. 3개 출처 중 실제로 묶인 것은 2개뿐이고 진짜 등록부인 collect_commands! 는 아무것도 강제하지 않는다.", "evidence": "dispatch.rs:1045-1051 invoke_command_names() 가 include_str!(\"../../../../src/shared/api/bindings.ts\") 를 파싱한다. lib.rs:38-39 BINDINGS_PATH 재생성은 #[cfg(debug_assertions)] 블록(lib.rs 의 run() 초입)에서만 일어난다. 현재는 세 출처가 모두 179 로 일치함을 실측 확인했다(collect_commands 179 / bindings __TAURI_INVOKE 179 / IMPLEMENTED_JSON_COMMANDS 179, 양방향 차집합 전부 공집합) — 결함은 값이 아니라 강제 장치의 공백이다.", "suggestedDirection": "collect_commands! 목록을 상수 배열이나 매크로로 뽑아 Rust 내부에서 직접 이름 집합을 얻고, 그것을 세 번째 축으로 파리티 테스트에 추가한다. bindings.ts 는 검증 대상이지 기준선이 되어서는 안 된다."}, {"severity": "major", "lens": "L-CONTRACT", "title": "FsChange.from_app 은 상수 false 이고 읽는 곳이 없다 — 문서가 말하는 echo 플래그가 실재하지 않는다", "file": "src-tauri/src/infra/watcher.rs", "line": 87, "claim": "fs:changed 페이로드의 from_app 은 유일한 생성 지점에서 하드코딩 false 이고 Rust·프론트 어디서도 읽히지 않는다. 따라서 앱 자신의 쓰기(file_save·git 조작·search_replace)가 그대로 watcher 이벤트로 되돌아오고, 프론트는 그 경로의 FILE.CONTENT 쿼리를 무효화한 뒤 부모 디렉토리마다 tree_refresh 를 돌리고 다시 TREE.ROWS 를 무효화한다. 자기 쓰기와 외부 변경을 구분할 수단이 페이로드에 있는 척만 하고 없다.", "evidence": "infra/watcher.rs:84-88 이 유일한 FsChange 생성부이며 from_app: false 리터럴. file/types.rs:40 이 필드 정의. from_app/fromApp 을 src·src-tauri 전체 grep 하면 이 두 줄과 bindings.ts 생성 타입 외 히트 0. ipc-contract.md:98 은 이 이벤트를 'fs:changed(paths[], kind, origin) (watcher — debounce·배치·echo 플래그)' 로 기재한다. 소비부는 ipc-sync-provider.tsx:72-83 이며 from_app 분기가 없다.", "suggestedDirection": "앱 내부 쓰기 경로에서 경로를 단기 억제 집합에 넣어 watcher 콜백이 from_app=true 로 표시하도록 만들거나(그리고 프론트가 그 경우 무효화를 건너뛰도록), 아니면 필드와 문서의 'echo 플래그' 문구를 함께 제거한다. 문서의 인자 이름도 origin 이 아니라 실제 페이로드({projectId, change:{kind,paths,fromApp}})로 정정이 필요하다."}, {"severity": "minor", "lens": "L-CONTRACT", "title": "revision 게이트가 규약에만 있고 소비자가 없다 — LayoutChanged.revision 은 유지·발행되지만 프론트가 읽지 않는다", "file": "src/app/providers/ipc-sync-provider.tsx", "line": 38, "claim": "ipc-contract.md §1 은 늦게 도착한 이벤트로 인한 stale 갱신을 막기 위해 페이로드에 revision 을 싣는다고 규정하고 data-model.md 는 이를 '낙관적 동시성 카운터' 로 부른다. Rust 는 실제로 revision 을 증가시키고 이벤트에 싣지만, 유일한 구독자인 layoutChanged 핸들러는 payload.revision 을 무시하고 무조건 무효화한다. 결과적으로 revision 은 25종 중 1종에만 붙은, 아무도 읽지 않는 필드다.", "evidence": "ipc-contract.md:27-28, data-model.md:78. Rust 증가: layout/service.rs:408,424,486,504,539,554,571,578,610 등. 발행: layout/commands.rs:55-59. 소비: ipc-sync-provider.tsx:38-40 이 payload.projectId 만 쓴다. src/ 전체에서 'revision' 은 editor-area.tsx:330 의 주석 한 줄 외 히트 0.", "suggestedDirection": "실제 게이트가 필요하면 레이아웃 캐시에 마지막 revision 을 기록하고 역행 이벤트를 버리는 로직을 넣고, 필요 없다고 판단되면 ipc-contract.md §1 의 revision 게이트 규정을 '현재 미적용' 으로 정정한다."}, {"severity": "minor", "lens": "L-CONTRACT", "title": "project:focus-kind-changed 가 모든 레이아웃 변이마다 무조건 발행 — 변화 감지도 소비자도 없이 이벤트 트래픽만 2배", "file": "src-tauri/src/domain/layout/commands.rs", "line": 62, "claim": "finish_mutation 은 레이아웃 변이 18종 전부의 공통 종결부인데, LayoutChanged 를 보낸 직후 focus_kind 를 계산해 ProjectFocusKindChanged 를 조건 없이 한 번 더 emit 한다. 직전 값과 비교하는 diff 가 없어 탭 클릭·이동·리사이즈마다 동일한 kind 가 반복 발행되고, 이 이벤트는 원격까지 팬아웃되지만 프론트에는 구독자도 FocusKind 타입 참조도 0이다.", "evidence": "layout/commands.rs:51-69 finish_mutation. 계산은 layout/service.rs:848 focus_kind. 발행 상태를 기억하는 필드가 state.rs 에 없다(focus_kind grep 결과 0). lib.rs:653 fanout 목록 포함. 프론트에서 FocusKind/focusKind grep 히트 0(bindings.ts 제외).", "suggestedDirection": "소비자를 배선할 계획이 없으면 발행·타입·collect/fanout 등록을 제거한다. 유지한다면 최소한 직전 kind 를 AppState 에 들고 변할 때만 emit 하도록 diff 를 넣는다(AgentStore::diff 가 이미 같은 패턴을 쓴다)."}, {"severity": "minor", "lens": "L-CONTRACT", "title": "179 커맨드 중 10종이 프론트 호출부 0 — ide/remote 시작·중단은 settings_update 부수효과로만 구동된다", "file": "src/shared/api/bindings.ts", "line": 271, "claim": "bindings 에 생성된 179종 중 layout_set_view_state·git_ahead_behind·git_fetch·git_undo_last_commit·ide_start·ide_stop·ide_notify_at_mention·remote_start·remote_stop·window_open_auxiliary 10종은 src/ 전체에서 호출부가 없다. ide_*/remote_* 시작·중단은 Rust 가 settings_update 핸들러에서 직접 구동하므로 IPC 표면이 중복이고, remote_start/remote_stop 은 원격 허용이라 소비자 없는 원격 표면까지 남는다. window_open_auxiliary 도 프론트는 layout_move_tab_to_window 로만 창을 만든다.", "evidence": "bindings.ts:27,190,200,201,271,272,278,310,311,349 의 각 항목에 대해 camelCase 이름을 src/ 전수 grep 한 결과 히트 0. Rust 내부 구동: settings/commands.rs:51-54(ide), :71-74(remote), lib.rs 부팅 블록의 ide_start/remote_start 자동 시작. 반면 ipc-contract.md:319·379 는 이 넷을 프론트가 부르는 정본 mutation 으로 기재한다.", "suggestedDirection": "각 커맨드를 (프론트 배선) / (내부 함수로 강등해 커맨드 등록 해제) 로 분류한다. 커맨드 등록을 풀면 dispatch 표·문서에서도 함께 빠져 원격 표면이 좁아진다."}, {"severity": "minor", "lens": "L-CONTRACT", "title": "ipc-contract.md 머리말의 커맨드 실측치(178/181)가 같은 문서 §원격 dispatch 정책(179)과도, 코드(179+3)와도 어긋난다", "file": "docs/ipc-contract.md", "line": 7, "claim": "머리말은 command 178종 + raw 3종 = 181종이라고 실측(2026-08-16)을 못 박지만, 같은 문서 :755 는 179종이라고 적고 실제 코드도 179종(+raw 3 = 182)이다. 2026-08-18 손 QA 수정으로 추가된 system_open_external_url 이 머리말에 반영되지 않은 결과이며, 한 문서 안에 서로 다른 정본 수치가 공존한다.", "evidence": "실측: collect_commands! 179, bindings.ts __TAURI_INVOKE 179, IMPLEMENTED_JSON_COMMANDS 179 (세 집합 양방향 차집합 공집합). RAW_CHANNEL_COMMANDS(lib.rs:299) 3종. 도메인별 실측도 문서와 일치(git 41 / agent 9 / ai 8 / remote 7 / ide 9). 문서 :7-10 vs :755.", "suggestedDirection": "머리말 수치를 179/182 로 갱신하고, 이런 수치를 두 곳에 적지 않도록 §원격 dispatch 정책의 문장을 단일 출처로 삼아 머리말은 그쪽을 참조만 하게 한다."}, {"severity": "minor", "lens": "L-CONTRACT", "title": "data-model.md §2 디스크 레이아웃이 locales/ 와 lsp/ 를 누락 — 다운로드된 언어 서버 바이너리가 영속 정본에 없다", "file": "docs/data-model.md", "line": 22, "claim": "§2 트리는 settings.json·session.json·projects/·themes/·snippets/·plugins/·prompts/·logs/ 만 그린다. 실제 AppPaths 는 locales/(사용자 언어팩)와 lsp/<serverId>/<version>/(lsp_install 이 내려받아 푸는 실행 파일)도 소유한다. 특히 lsp/ 는 앱 데이터 중 가장 위험도가 높은 내용(실행 파일)인데 영속 정본 문서에 항목 자체가 없다.", "evidence": "paths.rs:30-32 locales_dir, :58-68 lsp_dir/lsp_server_dir/lsp_server_version_dir. 사용처: locale/service.rs:3835,3860,3892 등, infra/lsp_install 의 추출 경로. ipc-contract.md:290 은 '{app_data}/locales/*.json' 을 언급하지만 data-model.md §2 트리에는 없다.", "suggestedDirection": "§2 트리에 locales/ 와 lsp/ 를 추가하고, lsp/ 에는 추출 하드닝(extract_zip)·버전 디렉토리 규칙·GC 여부를 한 줄로 기재한다."}, {"severity": "minor", "lens": "L-CONTRACT", "title": "data-model.md §6.1 의 disk_modified_ms 출처 주석이 낡음 — Rust 가 직접 stat 하는데 'view 가 전달' 이라고 적혀 있다", "file": "docs/data-model.md", "line": 142, "claim": "§6.1 의 MirrorFile 주석은 disk_modified_ms 를 'view 가 file.modifiedMs 전달' 로 설명하지만, Wave B 하드닝 이후 baseline 은 Rust 가 쓰기 시점에 직접 stat 해 반환값으로 돌려준다. 같은 저장소의 ipc-contract.md 는 이 변경을 명시했는데 data-model.md 만 갱신되지 않아 두 정본이 상반된다.", "evidence": "file/service.rs:192-208 mirror_dirty 가 current_modified_ms(storage_path) 로 스스로 채우고 Ok(disk_modified_ms) 를 반환한다. bindings.ts 의 fileMirrorDirty 시그니처도 (projectId, path, content) → number | null 로 baseline 인자가 없다. ipc-contract.md:509-514 가 이 정정을 서술. data-model.md:142 만 구 서술 유지.", "suggestedDirection": "data-model.md:142 주석을 'Rust 가 쓰기 시점에 stat 한 baseline(반환값으로 호출자에게 전달)' 로 교정한다."}, {"severity": "minor", "lens": "L-CONTRACT", "title": "data-model.md §3 핵심 타입이 실제 Rust 와 어긋남 — ProjectRef.root 타입, Project 구조체 누락", "file": "docs/data-model.md", "line": 72, "claim": "§3 은 ProjectRef { id, root: PathBuf, name } 으로 적었지만 실제는 root: String 이다(IPC 직렬화 대상이라 PathBuf 를 쓸 수 없다). 또한 실제 영속·IPC 를 오가는 Project { id, root, name, capabilities, root_missing } 구조체가 §3 에 전혀 없어, project.json 이 실제로 무엇을 담는지는 산문 한 줄(:52)로만 남는다.", "evidence": "project/types.rs:19-24 ProjectRef(root: String), :26-37 Project(capabilities: Vec<CapabilityKind>, root_missing: bool). data-model.md:72 의 선언과 불일치. root_missing 은 :108 에 서술만 있고 타입 정의에는 없다.", "suggestedDirection": "§3 의 ProjectRef 를 String 으로 교정하고 Project·CapabilityKind 를 §3 블록에 추가한다. 이 절이 'Rust 정본' 을 옮겨 적는 구조인 이상 실코드와 기계적으로 대조 가능한 형태를 유지해야 한다."}, {"severity": "minor", "lens": "L-TYPE", "title": "specta 가 7개 타입을 _Serialize/_Deserialize 로 분할하고 절반들의 필드가 다른데, 문서는 이 현상을 Snippet 1종에만 기록하고 '두 절반은 동일' 이라 단정", "file": "src/shared/api/bindings.ts", "line": 1014, "claim": "생성된 bindings 는 ResolvedLocale·ResolvedTheme·SnippetEntry·SnippetFile·Theme·TokenColorRule·TokenColorSettings 7종을 두 절반의 유니온으로 쪼갠다. data-model.md §9.1 은 이 현상을 Snippet 2종에 대해서만 기록하며 '두 절반의 필드 구성은 현재 동일해 기능상 문제는 없다' 고 단정하지만 ResolvedTheme 은 그렇지 않다 — syntaxOverrides·warnings·author·license·source 5필드가 _Deserialize 에서는 선택, _Serialize 에서는 필수다. theme_get 은 ResolvedTheme_Serialize 를 반환하고 theme_save 는 Theme_Deserialize 를 받아, 테마 편집기의 읽기→편집→저장 왕복이 서로 다른 생성 타입을 가로지른다.", "evidence": "bindings.ts:998,1014,1286,1330,1513,1560,1572 의 유니온 선언. :1016-1029(_Deserialize, 5필드 옵셔널) vs :1031-1046(_Serialize, 동일 5필드 필수). 커맨드 시그니처 :247-249(themeGet/themeGetCurrent → ResolvedTheme_Serialize, themeSave ← Theme_Deserialize), :251-252(snippet). data-model.md:320-325 의 단정.", "suggestedDirection": "data-model.md §9.1 의 서술을 7종 전수로 확장하고 '절반들이 항상 동일하지는 않다(ResolvedTheme 반례)' 를 명시한다. 아울러 ipc-contract.md §theme 의 Theme/ResolvedTheme 페이로드 설명이 실제 생성 타입 이름과 맞도록 보강한다."}]

