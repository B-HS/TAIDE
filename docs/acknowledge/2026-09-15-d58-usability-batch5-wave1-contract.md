# d-58 — 사용성 배치 5 웨이브 1: 알림 문구 · Open Recent 메뉴 · 퀵오픈 결함 · Welcome 터미널 · 실시간 검색+아이콘 · + 메뉴 · git 패널 그래프 분리 · 메뉴 아이콘 정렬 (2026-09-15)

> 사용자 항목 4·6·7·8·9·10·11·14 (`acknowledge/2026-09-15-usability-batch5-user-decisions.md` §1·§2 결정 전제).
> 조사 정본 `research/2026-09-15-batch5-research.md`(T3·T5·T6·T7·T8). 실행 방식 `docs/agent-operations.md` §2. 이 문서의 §3 은 구현 에이전트가 실시간 기록한다.
> 규칙: 코드 주석 금지(JSDoc 만, 영어)·arrow only·매직넘버 상수화·이모지 금지·`#[allow]`/`@ts-ignore`/`eslint-disable` 금지·신규 의존성 0.
> 신규 로케일 키는 `src-tauri/resources/locales/{en,ko,ja}.json` 3종 + 새 네임스페이스면 `src-tauri/src/domain/locale/service.rs` `MESSAGE_NAMESPACES` 동시 등재(키 집합 일치 테스트가 강제).
> Rust 커맨드 추가 시 `lib.rs` 등록 + `domain/remote/dispatch.rs` `IMPLEMENTED_JSON_COMMANDS` + `REMOTE_ALLOWED_COMMANDS`/`REMOTE_DENIED_COMMANDS` 등재 + `docs/ipc-contract.md`. bindings 재생성은 `cargo test -p taide typescript_바인딩을_생성한다`(`src/shared/api/bindings.ts` 갱신·커밋 대상).

## 0. 근본 원인 · 현행 (메인이 소스로 재확인)

| 항목 | 사실 | 근거 |
|------|------|------|
| 14 메뉴 아이콘 정렬 | `ContextMenuSubTrigger` 클래스에 `gap-2` 가 없다. 일반 `ContextMenuItem`(`gap-2`)·`DropdownMenuSubTrigger`(`gap-2`)와 달라 하위메뉴 트리거(탭 Split·탐색기 Open With·터미널)의 아이콘-라벨 간격이 어긋난다 | `src/shared/ui/context-menu.tsx:44` vs `:97`, `src/shared/ui/dropdown-menu.tsx:149` |
| 9 검색 아이콘 | `FileGroupHeader` 가 아이콘 레지스트리를 안 거치고 lucide `File` 을 하드코딩(`opacity-80`, 색 없음). 탐색기·탭 바는 `FileTypeIcon → resolveFileIcon`. 문제 패널도 같은 헤더를 써 동일 증상 | `src/shared/ui/file-group-header.tsx:29`, `src/features/explorer/file-tree-row.tsx:73`, `src/features/problems/problems-panel.tsx:100` |
| 9 검색 실시간 | 검색 패널은 Enter 전용(`key !== 'Enter'` 즉시 return). `run()` 진입 즉시 이력 push(settings 전체 write) + 정규식 오류마다 `toast.error`. 명시 `searchCancel` 은 과거 레이스 회귀라 금지(generation ref + Rust `begin_search` 토큰 flag 로 supersede) | `src/features/search/search-panel.tsx:118-121`, `src/entities/search/use-search-run.ts:16,45-51,125,133,152-156` |
| 8 Welcome 터미널 | `terminal.new` 실행부가 `command-palette.tsx` 로컬 클로저에 갇혀 재사용 불가. 터미널은 프로젝트 소속(Rust `ensure_project_open`) → 비활성 조건은 기존 `canOpenFile`(`projectId !== null`)과 동일 | `src/widgets/command-palette/command-palette.tsx:166-172`, `src/widgets/welcome/welcome-container.tsx:91` |
| 10 + 버튼 | `IconButton` onClick 이 곧바로 폴더 피커. 드롭다운 정본 패턴은 `tab-bar-add-menu.tsx`(Tooltip↔DropdownMenuTrigger 중첩 순서 포함). `~` 확장은 `terminal/service.rs` `expand_home` 만 존재, `open_project` 는 `canonicalize` 직행 | `src/widgets/app-sidebar/app-sidebar.tsx:78-84`, `src/features/tab/tab-bar-add-menu.tsx:24-33`, `src-tauri/src/domain/project/service.rs:57-68`, `src-tauri/src/domain/terminal/service.rs:212` |
| 6 Open Recent | 앱 메뉴는 TAIDE/Edit/Window 3개(영문 하드코딩), File 메뉴·Open Recent 없음. `handle_menu_event` 는 `MENU_ID_QUIT` 외 early return. `list_recent_projects` 는 `last_opened_at` 내림차순·상한 없음·`root_missing` 재계산. `close_project` 는 디스크 레코드(`projects/<id>/`)를 남겨 Clear Recent 백엔드 없음. Dock 메뉴 API 부재는 결정 문서 §2 | `src-tauri/src/domain/window/commands.rs:155-200,269-276`, `src-tauri/src/domain/project/service.rs:120-132,417-427` |
| 4 알림 | `agentCompleted` body 가 `agent.name`("claude") 한 단어. `AGENT_COMPLETION_ACTIVITIES = ['idle','awaitingInput']` 라 권한 대기도 "작업 완료" 제목으로 나감. `evaluateAgentCompletions` 의 `workedForMs` 는 소비처 없음. payload 에 `projectId` 있음. 프로젝트명·탭 제목은 쿼리 캐시(`QUERY_KEY.PROJECT.LIST`, `layoutQueryOptions`)로 IPC 없이 조달 가능(선례 `git.query.ts:214`). 플러그인 데스크톱은 title/body 만 전달, 클릭 콜백 없음 | `src/app/providers/native-notification-provider.tsx:63`, `src/shared/lib/native-notification-gate.ts:30,76` |
| 7 퀵오픈 | ① 랭킹이 점수 단독 정렬 후 200 상한 → 동점 다수 시 walk 순서가 커트라인(비결정·간헐) ② 코드베이스에 유니코드 정규화 0건, `fuzzyMatch` 는 코드포인트 정확 비교 → NFD 한글 파일명은 NFC 질의로 0건 ③ `target:null` 에 `focused_pane` 이 트리에 없으면 로케일 키 없는 `NotFound("pane not found")` + FE 가 파일 소실로 오인해 인덱스 무효화(백로그 기존 결함) ④ 심링크 파일은 `file_type().is_file()` 필터로 누락 ⑤ `openFile` 이 프로젝트 없으면 조용히 return ⑥ 보조 창엔 팔레트 미마운트라 ⌘P 무반응 ⑦ 선검증 실패 토스트가 "파일을 찾을 수 없습니다: {path}" (사용자 보고 문구) | `src/widgets/command-palette/command-palette.tsx:60,291,324-328`, `src/shared/lib/fuzzy-match.ts:43-56,105`, `src-tauri/src/domain/layout/service.rs:429,1331`, `src-tauri/src/domain/search/service.rs:592-598`, `src/app/app.tsx:57-97`, `docs/backlog.md:154` |
| 11 git 패널 | 그래프 뷰포트가 `VIEWPORT_HEIGHT_PX = 320` 고정 + `overflow-y-auto scrollbar-hidden`(레포 유일의 비-`ScrollContainer` 스크롤). 짧은 패널에선 뷰포트를 삼키고 긴 패널에선 커지지 않으며, 접힌 섹션이 양보한 공간을 그래프가 못 받아 하단이 빈다. 접힘 상태는 모듈 메모리(재시작 초기화, Settings 승격을 의도적으로 거절한 주석) | `src/widgets/git-panel/commit-graph.tsx:45,125`, `src/widgets/git-panel/git-panel.tsx:380-493`, `src/entities/git/git-section-collapse-memory.ts:21-35` |

## 1. 수정 방향

### 1.A 항목 14 — 컨텍스트 메뉴 하위메뉴 트리거 간격 (S, TS)

- `src/shared/ui/context-menu.tsx` `ContextMenuSubTrigger` 클래스에 `gap-2` 추가(일반 Item·Dropdown SubTrigger 와 동일). `ChevronRightIcon` 은 `ml-auto` 유지(크기는 `[&_svg:not([class*='size-'])]:size-4` 가 담당). 다른 변경 없음.
- 검증: 탭 컨텍스트 메뉴 Split·탐색기 Open With·터미널 하위메뉴가 Pin/Move 와 같은 아이콘 열에 정렬. 시각 확인은 사용자 실기.

### 1.B 항목 9 — 검색 패널 실시간 검색 + 결과 아이콘 (M, Rust settings + TS)

**B1 아이콘** — `src/shared/ui/file-group-header.tsx:29` 의 `<File …/>` 를 `<FileTypeIcon fileName={fileNameOf(path)} className='size-3.5 shrink-0' />`(`@shared/icons/file-type-icon`, 동일 레이어 참조) 로 교체. `opacity-80` 제거(레지스트리 `colorClass` 가 색을 준다). 검색·문제 패널 동시 해결. git `StatusRowItem` 은 범위 외.

**B2 설정 필드(Rust)** — `Settings` 에 `search_on_type: bool`(기본 `true`) · `search_on_type_debounce_ms: u32`(기본 `300`, `service.rs` 에서 `[50, 2000]` 클램프, 상수 `SEARCH_ON_TYPE_DEBOUNCE_MIN_MS/MAX_MS/DEFAULT_MS`). `SettingsPatch` 옵션 필드·`Default`·`apply_patch`·클램프 테스트. bindings 재생성. `docs/ipc-contract.md` settings 절 + `docs/features/search.md`(있으면) 갱신.

**B3 설정 UI** — `src/widgets/settings-view/` 에 검색 절이 없으므로 `settings-interface-section.tsx` 에 "검색" 소제목 아래 토글(`settings.searchOnType` / `…Description`) + 숫자 입력(`settings.searchOnTypeDebounceMs` / `…Description`, ms 단위, 기존 숫자 설정 UI 패턴 재사용). 로케일 4키 × 3.

**B4 실시간 실행(TS)** — `src/widgets/search-panel/search-panel-container.tsx`:
- 설정 `searchOnType` 이 켜져 있고 `query.trim()` 이 비어 있지 않으면 질의·옵션(대소문자/정규식/단어/제외 glob)이 바뀔 때 **trailing 전용 디바운스**(`searchOnTypeDebounceMs`)로 `run(buildQuery(), { recordHistory: false, live: true })`. 타이머는 `useEffect` cleanup 으로 취소(외부 시스템=타이머 동기화라 허용). `createLeadingTrailingDebouncer` 는 leading 즉시 실행이라 부적합 — trailing 옵션을 추가하거나 effect 타이머로 구현(2회 이상 쓰이지 않으면 새 공용 유틸 만들지 않는다).
- Enter 는 현행대로 즉시 실행 + `recordHistory: true`(대기 중 디바운스 취소). 이력은 Enter 에서만 쌓인다.
- `RunSearchOptions` 에 `live?: boolean` 추가: live 실행은 (a) 정규식 컴파일 실패 시 `toast.error` 를 띄우지 않고 `status='failed'` + 패널 인라인 문구(기존 실패 표시 재사용, 없으면 `search.invalidRegexInline` 1키)만, (b) `perfMark(SEARCH_RUN_REQUESTED)` 를 찍지 않는다(metric 6 표본 보존). Enter 실행은 현행 유지.
- 이전 실행 취소는 **추가 작업 없음**(generation ref + Rust supersede). 명시 `searchCancel` 을 넣지 않는다(`use-search-run.ts:45-51` 회귀).
- IME: WKWebView 가 composition 이벤트를 주지 않으므로(`src/shared/lib/ime-composition.ts`) keydown 229 가드는 한글 입력 전체를 라이브에서 제외해 버린다 → **디바운스만으로 감수**(문서에 명시). 최소 질의 길이는 두지 않는다(VS Code 동형).
- 안내 문구: 설정 on 이면 `search.liveSearchHint`("입력하면 바로 검색됩니다. Enter 로 이력에 저장") 신규 1키, off 면 기존 `search.pressEnterHint`.
- Search Editor 탭(`search-editor-pane.tsx`)은 범위 외(Enter 유지).
- 테스트: 디바운스 스케줄/취소·Enter 즉시·live 옵션 분기(토스트 억제·이력 미기록)·설정 off 시 현행 — `search-panel-container` 컴포넌트 테스트(RTL) 또는 순수 헬퍼 추출 후 단위 테스트. Rust 클램프·기본값 테스트.
- 문서: `docs/features/search.md`(또는 검색 절이 있는 문서) 실행 트리거 절, `docs/quality-assurance/2026-09-04-perf-baseline.md` metric 6 정의에 "live 실행은 표본 제외" 1줄.

### 1.C 항목 8 — Welcome "터미널 열기" 버튼 (S, TS)

- `src/entities/layout/layout.query.ts` 에 `useOpenTerminalTab(projectId: ProjectId | null)` 추출(선례 `useOpenAppFileTab` :80-92): projectId 없으면 `toast.info(t('app.openProjectFirst'))`, 있으면 `openTab({ kind: { kind: 'terminal', sessionId: '' }, title: t('terminal.title'), target: currentWindowFocusedPane(layout), preview: false })` + 에러 토스트. `command-palette.tsx:166-172` 로컬 클로저를 이 훅으로 교체(동작 동일 — 단 `target` 은 기존 `null` 유지가 팔레트 동작이면 그대로 두고 훅 인자로 받는다; 두 호출부가 같은 target 규칙이면 통일).
- `src/features/welcome/welcome-screen.tsx`: props `canOpenTerminal: boolean` · `onOpenTerminal: () => void` 추가, 기존 "파일 열기" 옆에 `variant='outline'` + lucide `Terminal` 아이콘 + 라벨 `t('keymap.newTerminal')`(기존 키 재사용) 버튼, `disabled={!canOpenTerminal}`. 힌트 줄은 기존 `app.openFileHint` 를 "파일이나 터미널을 열려면 먼저 폴더를 여세요" 로 문안 갱신(en/ja 동일 의미). `welcome-container.tsx` 는 `canOpenTerminal={projectId !== null}`.
- 테스트: `pane-node-view-welcome.test.tsx`·welcome 관련 테스트에 버튼 존재/비활성/클릭 → 훅 호출 단언. `docs/features/*` Welcome 절 1줄.

### 1.D 항목 10 — 사이드바 + 버튼 드롭다운 (S~M, TS + Rust `~` 확장)

- 신규 `src/features/project/sidebar-add-project-menu.tsx`(순수 UI): `tab-bar-add-menu.tsx` 의 `DropdownMenu > Tooltip > TooltipTrigger asChild > DropdownMenuTrigger` 조립을 그대로 따른다(현 `IconButton` 은 자체 Tooltip 을 품어 중첩 충돌 → 사용하지 않음). 항목: **경로로 열기…**(`FolderInput` 아이콘) · **Finder 로 열기…**(`FolderOpen`, 현 폴더 피커) · 구분선 · **최근 프로젝트**(라벨 `app.recentItems` 재사용) 아래 최근 N개(`RECENT_PROJECT_MENU_LIMIT = 10`, `src/shared/constants/project.ts` 신설 — Welcome 의 `RECENT_PROJECT_DISPLAY_LIMIT = 8` 은 그대로 두되 같은 파일로 승격) — 각 항목은 `resolveProjectDisplay` 라벨(없으면 `name`) + 보조 텍스트 `root`, `rootMissing` 이면 disabled + `app.recentProjectRootMissing`. 현재 열려 있는 프로젝트는 목록에서 제외. props 로만 동작(`onOpenByPath`·`onOpenViaFinder`·`recentProjects`·`onSelectRecent`).
- 신규 `src/features/project/open-project-by-path-dialog.tsx`(순수 UI, `create-tag-dialog.tsx` 패턴): 제목 `sidebar.openByPathTitle`, 입력 placeholder `sidebar.openByPathPlaceholder`("/path/to/project 또는 ~/project"), 확인 버튼(기존 공통 키 있으면 재사용), Enter 제출, 빈 값 비활성.
- `src/widgets/app-sidebar/app-sidebar.tsx`: `IconButton` 을 위 메뉴로 교체, `useOpenFolderDialog`(Finder)·`useOpenProject().mutate(path, { onError: toast.error(describeIpcError) })`(경로)·`useQuery(recentProjectsQueryOptions())`·`useActivateProject`/`useOpenProject`(최근 선택 — Welcome `onSelectRecent` 과 같은 규칙) 배선. 열기 성공 시 다이얼로그 닫힘.
- **Rust `~` 확장**: `open_project` 진입에서 `~`/`~/…` 를 홈으로 확장한다. `terminal/service.rs` 의 `expand_home` 을 `infra`(예: `infra/paths` 또는 기존 공용 위치)로 옮겨 두 곳이 공유(2회 사용 → 공통화). 홈은 `HOME`(Windows `USERPROFILE`) — `agent/commands.rs:592 home_dir_env` 와 중복이면 하나로. 원격 dispatch 경로에도 동일 적용됨을 `docs/ipc-contract.md` project_open 절에 명시. 테스트: `~` 단독·`~/x`·`~user`(확장 안 함) 케이스.
- 경로 검증은 `open_project` 기존 에러(`error.project.pathNotDirectory`·io NotFound)를 그대로 토스트. 존재하지 않는 경로의 io 에러가 로케일 키 없이 원문으로 뜨면 `error.project.pathNotFound` 1키를 추가해 `NotFound` 로 감싼다.
- 로케일 신규(3종): `sidebar.addProjectMenu`(aria/툴팁), `sidebar.openByPath`, `sidebar.openViaFinder`, `sidebar.openByPathTitle`, `sidebar.openByPathPlaceholder`(+ 필요 시 `error.project.pathNotFound`). 기존 `sidebar.openFolderAriaLabel` 은 사용처가 없어지면 제거(카탈로그 미참조 테스트 확인).
- 테스트: 메뉴 렌더/항목 클릭 콜백/최근 비활성(RTL), 다이얼로그 제출, Rust `expand_home` 이동 후 기존 테스트 유지.

### 1.E 항목 6 — 메뉴바 `File > Open Recent` + Clear Recent (M, Rust)

- `build_app_menu`(`src-tauri/src/domain/window/commands.rs`): `File` 서브메뉴(id `taide-file`)를 TAIDE 와 Edit 사이에 신설. 내용: `Open Recent ▸`(서브메뉴 id `taide-open-recent`) → 최근 프로젝트 최대 `RECENT_PROJECT_MENU_LIMIT`(= 10, `src-tauri/src/constants.rs`) 항목(id `taide-recent:<projectId>`, 라벨 = `display.label` 있으면 그것, 없으면 `name`; `root_missing` 이면 `enabled(false)`), 없으면 비활성 항목 `No Recent Projects`, 구분선, `Clear Recent`(id `taide-clear-recent`, 목록이 비면 비활성).
- `handle_menu_event`: `taide-recent:` 접두 → 해당 `Project.root` 로 `project::commands::project_open(app.clone(), app.state(), root)` 를 `tauri::async_runtime::spawn` 으로 직접 호출(선례 `remote/dispatch.rs:863`; 프론트 우회 없음 — 창 0개여도 동작). 실패는 `log::warn!`. `taide-clear-recent` → 신규 커맨드 `project_forget_recent` 로직 호출.
- 신규 커맨드 `project_forget_recent`(인자 없음, 원격 정책 `LocalProjectHistoryExposure` 로 `REMOTE_DENIED_COMMANDS`): 현재 세션에 **열려 있지 않은** 프로젝트의 `projects/<id>/` 레코드를 삭제(열린 것은 유지). 완료 후 FE 최근 목록(`QUERY_KEY.PROJECT.RECENT`)이 갱신되도록 기존 `ProjectListChanged` 이벤트를 방출하고, `ipc-sync-provider.tsx:237` 핸들러가 `PROJECT.RECENT` 도 무효화하게 한다(현재 무효화 범위 확인 후 추가). `lib.rs` 등록·dispatch 테이블·bindings·`docs/ipc-contract.md` project 절.
- **동적 갱신**: `project_open`·`project_close`·`project_activate`·`project_forget_recent` 뒤에 `window::menu::refresh_recent_menu(app)`(신규 헬퍼)가 `AppHandle::menu()` → `taide-open-recent` 서브메뉴의 항목을 전부 제거 후 재구성. macOS NSMenu 변경은 메인 스레드여야 하므로 tauri `Submenu::remove_at/append` 가 내부에서 메인 스레드 마샬링을 하는지 `~/.cargo/registry/src/*/tauri-2.11.5/src/menu/submenu.rs` 로 확인하고, 아니면 `app.run_on_main_thread` 로 감싼다(확인 결과를 §3 에 기록). 갱신 실패는 `log::warn!`.
- 라벨 로케일: `domain/locale/service.rs` 가 내장 카탈로그를 Rust 에서 조회할 수 있는 함수를 노출하면 현재 `settings.locale` 로 `menu.file`·`menu.openRecent`·`menu.noRecentProjects`·`menu.clearRecent` 를 조회해 쓴다(키 4개 × 3, 새 네임스페이스 `menu` 등재). 조회 함수가 없으면 카탈로그 소유자인 locale 서비스에 `lookup_builtin_message(locale, key) -> Option<String>` 을 추가한다(다른 도메인이 JSON 을 직접 읽지 않는다). 기존 TAIDE/Edit/Window/Quit 영문 하드코딩은 범위 외(§4 후속). 로케일 변경 시 메뉴 재구성은 `SettingsChanged` 처리 지점에서 `refresh_recent_menu` 호출로 해결(전체 메뉴 재빌드가 필요하면 `set_menu` 재호출 — 1회 비용이라 허용).
- Dock 메뉴는 구현하지 않는다(결정 §2). `docs/features/window-chrome.md`(또는 메뉴 절이 있는 문서)에 File 메뉴·Open Recent·Clear Recent·Dock 제약을 기술.
- 테스트: `list_recent_projects` 상한 적용 헬퍼(`recent_menu_entries`)·`forget_recent`(열린 프로젝트 보존·레코드 삭제)·메뉴 id 파싱. 메뉴 실물은 사용자 실기.


### 1.E-보강 도메인 경계 (메인 2차 검증에서 발견, 2026-09-15)

`cargo test --workspace` 의 `tests/domain_boundaries.rs` 가 R 의 메뉴 구현이 만든 교차 참조 5건을 거부했다: `project/commands.rs → window::menu`(열기·닫기·활성·forget 뒤 refresh), `settings/commands.rs → window::commands`(언어 변경 시 refresh_app_menu), `window/commands.rs → project::commands`(메뉴 이벤트 → project_open/forget_recent), `window/menu.rs → locale::service`·`project::service`(라벨·최근 목록 조회). 수정 방향(architecture.md §2 — 조립은 `lib.rs`):
- **실행 경로 3건은 제거**: (a) 메뉴 갱신은 `lib.rs` 가 `app.listen_any` 로 `ProjectListChanged`·`ProjectActivated`·`SettingsChanged`(locale 변경 시) 를 구독해 `window::menu::refresh_recent_menu` / `window::commands::refresh_app_menu` 를 부른다(선례: `fanout_remote_events!`). 리스너는 뮤테이션 가드 밖에서 실행되도록 `tauri::async_runtime::spawn_blocking` 으로 넘긴다. project/settings 커맨드에서 `window::*` 호출을 삭제한다. (b) 메뉴 이벤트 → 프로젝트 커맨드는 `window::commands::handle_menu_event` 가 **행동 열거값**(`MenuAction::OpenRecent(root)` / `ClearRecent` / `Quit`)을 돌려주고, `lib.rs` 의 `on_menu_event` 클로저가 그것을 `domain::project::commands::*` 로 디스패치한다(`lib.rs` 는 모든 도메인 commands 참조가 허용된 부트스트랩).
- **데이터 제공자 2건은 화이트리스트 등재**: `window/menu.rs → project::service`(최근 목록 읽기)·`→ locale::service`(라벨 조회) — 선례 `remote/login_page.rs → locale::service`("locale is a data provider") 와 동일 성격. `ALLOWED_CROSS_DOMAIN_EDGES` 에 사유 주석과 함께 추가. 대안(lib.rs 가 데이터를 주입)은 메뉴 갱신 시점마다 lib.rs 가 조회 로직을 갖게 돼 오히려 조립 지점이 비대해진다.
- 검증: `cargo test -p taide --test domain_boundaries` 통과 + 화이트리스트 stale 검사 통과 + 기존 메뉴 테스트 5건 유지.

### 1.F 항목 4 — 알림 문구 강화 (S~M, TS 전용)

- `src/shared/lib/native-notification-gate.ts`: `evaluateAgentCompletions` 의 완료 항목이 `activity`('idle' | 'awaitingInput')·`workedForMs`·`sessionId`·`name` 을 싣도록(이미 있는 값 노출). 테스트 갱신.
- `src/app/providers/native-notification-provider.tsx`: `useQueryClient()` 로 (a) `QUERY_KEY.PROJECT.LIST` 캐시에서 `projectId` 의 `ProjectRef` → 표시명 = `resolveProjectDisplay({display}).label ?? name`, (b) `layoutQueryOptions(projectId)` 캐시에서 `sessionId` 를 가진 터미널 탭의 제목(없으면 생략). 새 IPC 없음(선례 `git.query.ts:214`).
- 포맷(결정 §1 #5): **title = 프로젝트 표시명**(프로젝트를 못 찾으면 기존 이벤트 제목으로 폴백), **body = `{에이전트} · {이벤트}: {세부}`**.
  - idle: `notification.agentFinishedBody` = "{{agent}} · 작업 완료 · {{duration}}{{tab}}" (tab 은 " · {탭 제목}" 조각, 없으면 빈 문자열 — i18n 보간으로 처리)
  - awaitingInput: `notification.agentAwaitingInputBody` = "{{agent}} · 입력 대기 (권한 요청 또는 질문){{tab}}"
  - category 는 둘 다 기존 `agentCompleted` 유지(설정 카테고리 분리는 웨이브 3 에서 Rust 타입과 함께).
- 같은 규칙을 다른 카테고리에도 적용: task(`taskCompleted*`) → title 프로젝트명(payload 에 projectId 가 없으면 sessionId 로 레이아웃 캐시 역인덱스해 프로젝트를 찾고, 못 찾으면 현행), body = "{탭 제목 또는 cwd} · 종료 코드 {n} · {duration}"; git → title 프로젝트명, body = "{이벤트} · {브랜치}"; searchReplace → title 프로젝트명, body 현행 요약. lspInstall 은 프로젝트 무관이라 현행 유지.
- 경과 시간 포맷: `src/shared/lib/format-duration.ts` 신설(`formatDurationShort(ms)` → "3분 12초"/"45초"/"1시간 2분", 로케일 키 `duration.hours/minutes/seconds` 또는 `Intl` 기반 — 기존 헬퍼가 없음을 확인함) — task 와 agent 두 곳이 쓰므로 공통화 정당. 테스트 포함.
- 로케일: 신규 body 키들 × 3, 기존 `notification.agentCompleted` 는 폴백 제목으로 유지.
- 테스트: `native-notification-provider.test.ts`(idle/awaitingInput 문구·프로젝트명·탭 제목·폴백)·gate 테스트·format-duration 테스트.
- 문서: `docs/features/notifications.md`(또는 알림 절이 있는 문서)에 포맷 표 + "클릭 라우팅은 플러그인 제약으로 불가" 명시.

### 1.G 항목 7 — 퀵오픈 결함 일괄 (M, TS + Rust)

- **G1 랭킹 결정화(TS)** — `src/shared/lib/fuzzy-match.ts` `fuzzyFilter` 정렬: 점수 내림차순 → 파일명(마지막 세그먼트) 매치 우선 → 대상 문자열 길이 오름차순 → `localeCompare`. 안정·결정적. 테스트(동점 집합의 순서 고정). `docs/features/command-palette.md:84-86` 의 "MRU" 서술을 실제 규칙으로 정정(MRU 는 미구현 — 후속 §4).
- **G2 NFC 정규화(TS)** — 질의와 매칭 대상 라벨을 `normalize('NFC')` 로 통일해 매칭·하이라이트한다(표시 문자열도 정규화본 — 시각 동일). **열기에는 원본 경로**를 그대로 쓴다(디스크 바이트 보존). `fuzzyFilter` 에 정규화를 넣으면 keybindings-editor 등 다른 소비처도 영향 → 팔레트 파일 그룹에서만 적용할지 `fuzzyFilter` 옵션으로 둘지는 "2회 이상" 기준으로 판단(팔레트 커맨드·심볼 그룹도 한글 라벨을 가질 수 있으므로 `fuzzyFilter` 내부 정규화가 자연스럽다). `splitFileMatchForDisplay` 의 인덱스 정합 테스트(NFD 파일명 + NFC 질의).
- **G3 pane 소실 폴백(Rust)** — `layout/service.rs` `open_tab_and_finish`: `target` 이 `None` 이고 `focused_pane` 이 트리에 없으면 `NotFound` 대신 **첫 leaf pane 으로 폴백**하고 `focused_pane` 을 그 pane 으로 갱신(`log::info!`). 명시 `target` 이 없는 pane 이면 현행 `NotFound` 유지하되 로케일 키 `error.layout.paneNotFound` 를 붙인다(원문 노출 제거). 테스트 2건. `docs/backlog.md:154` 항목을 "해결(d-58)" 로, `docs/features/layout-shell.md`(또는 tabs.md) 열기 규칙 1줄.
- **G4 심링크 파일(Rust)** — `search/service.rs::list_project_files`: 엔트리가 심링크면 `std::fs::metadata(path)`(대상 추적)로 파일 여부를 판정해 포함. 디렉토리 심링크는 현행(미추적) 유지 → 순환 위험 없음. 테스트(임시 디렉토리에 파일 심링크).
- **G5 하드 제외 디렉토리 완화 — 보류.** `IGNORED_DIR_NAMES` 가 트리·워처와 공유됨이 확인돼 결정 문서 §2.1 로 재질문 중. 이 계약에서는 **손대지 않는다**(회신 후 별도 항목).
- **G6 무반응 제거(TS)** — `command-palette.tsx` `openFile`: 프로젝트 없으면 `toast.info(t('app.openProjectFirst'))`(같은 파일 `openTerminalTab` 과 동일).
- **G7 보조 창 안내(TS)** — 보조 창 셸(`src/app/app.tsx` aux 분기 또는 `widgets/auxiliary-window-shell`)에 `useGlobalKeymap({ 'quick-open': …, 'command-palette': … })` 로 `toast.info(t('palette.mainWindowOnly'))`(신규 1키 × 3, "명령 팔레트는 메인 창에서 사용할 수 있습니다"). 창 스코프 팔레트는 웨이브 5.
- **G8 진단 로그(Rust)** — `log` 매크로로 ① `layout/commands.rs` `ensure_file_tab_target_exists` 실패 시 `warn!(path, 사유: 경계/부재)` ② G3 폴백 발생 시 `info!(이전 focused_pane)` ③ `search/commands.rs` `search_list_files` 반환 건수·소요 `debug!`. 시크릿 없음(경로만). `docs/debugging.md` §4 에 "퀵오픈 실패 사후 판별" 항목(로그 파일 위치는 §1 참조).
- 테스트·문서는 각 항목에 명시. `command-palette.md` 에 정렬·NFC·심링크 규칙, 보조 창 제약.

### 1.H 항목 11 — git 패널 그래프 하단 Panel 분리 + Settings 영속 (M, TS + Rust settings)

- **레이아웃(TS)** — `src/widgets/git-panel/git-panel.tsx` 본문을 `react-resizable-panels` 세로 `Group` 으로: **Panel A** = 기존 `ScrollContainer`(가상화 변경 3그룹 + 스태시 섹션, sticky 헤더·로빙·`scrollPaddingStart` 전부 현행 유지) / `PaneSeparator`(`src/features/split/pane-separator.tsx` 재사용, 방향 세로) / **Panel B** = 그래프: 헤더(`GitSectionHeader`, `shrink-0` 24px, sticky 아님) + 본문 `flex-1 min-h-0` 을 `ScrollContainer` 로(OverlayScrollbar — `scrollbar-hidden` 원시 뷰포트 제거, `VIEWPORT_HEIGHT_PX` 삭제). Panel B 는 `collapsible` + `collapsedSize` = 헤더 높이(24px), `minSize` = `MIN_PANEL_SIZE_PX`(`@shared/constants/layout`)… 헤더 접힘 토글은 `panelRef.collapse()/expand()` 와 접힘 상태를 동기화(`onLayoutChanged`/`isCollapsed`). 접힘 시 Panel A 가 남은 공간을 전부 받는다.
  - rrp 4.12.2 실 API(명령형 collapse/expand·`collapsedSize` px 문자열·Group 자식 제약)는 `node_modules/react-resizable-panels`(설치 완료) 의 타입/README 로 확인하고 §3 에 기록. 기존 사용처 `app-shell.tsx:221-250`(collapsible 사이드바) 패턴을 우선 따른다.
  - 로빙 포커스: 그래프 헤더는 더 이상 가상화기 인덱스 공간에 없다. `rovingItemCount`/`scrollToIndex` 에서 그래프 항목을 분리하고, 목록 끝(스태시 헤더)에서 ↓ 는 그래프 헤더 버튼으로 DOM 포커스 이동, 그래프 헤더에서 ↑ 는 목록 마지막 항목으로 복귀. `change-row-navigation.ts`·테스트 갱신.
  - e2e `e2e/specs/19-git-sections-collapse.e2e.ts:40` 의 sticky 단언은 변경/스태시 헤더에만 적용되도록 수정(그래프 헤더는 패널 내 고정). e2e 실행은 하지 않는다(사용자 몫) — `typecheck:e2e` 만 통과.
- **영속화(Rust settings 승격, 결정 §2 #3)** — `Settings` 에 `git_sections_collapsed: Vec<GitSectionId 문자열>`(기본 `["stashes"]`) · `git_graph_panel_size_px: u32`(기본 `240`, `[24, 4000]` 클램프) 추가(`SettingsPatch`·`Default`·`apply_patch`·테스트·bindings). 모듈 메모리 `git-section-collapse-memory.ts` 는 제거하고 settings 쿼리를 읽는다(초기 렌더는 settings 캐시가 있으므로 깜빡임 없음 — `settings` 가 아직 없으면 기본값). 쓰기: 접힘 토글은 즉시 `updateSettings({...emptySettingsPatch(), gitSectionsCollapsed})`, 리사이즈는 `onLayoutChanged(isUserInteraction)` 에서 `pane-resize-commit.ts` 와 같은 120ms 트레일링 디바운스로 `gitGraphPanelSizePx` 저장(상수 재사용). UI 노출 없음(`recent_searches` 선례).
  - `git-section-collapse-memory.ts` 의 "승격 거절" JSDoc 근거는 이번 결정으로 폐기 — 파일 삭제 시 참조 문서(`docs/features/git.md`·`architecture.md` §6.4 언급) 갱신.
- 문서: `docs/features/git.md` §2(구조)·§5(로빙)·"스크롤바 하나" 서술을 "변경/스태시 영역 + 그래프 영역 2개" 로 정정, 그래프 Panel·영속화 기술. `docs/ipc-contract.md` settings 절.
- 테스트: `git-sections.test.ts`·`change-row-navigation.test.ts` 갱신, git-panel 컴포넌트 테스트(접힘 시 헤더만·펼침·설정 저장 호출) RTL, Rust settings 테스트.

### 1.I 범위 외

- 항목 1·2(키맵, 웨이브 2)·5(에이전트, 웨이브 3)·13(테마, 웨이브 4)·3·12(프로젝트 split, 웨이브 5). Dock 메뉴(불가). 알림 클릭 라우팅(불가). 권한 요청/질문 구분(웨이브 3). G5(보류). Search Editor 실시간. git `StatusRowItem` 파일 아이콘. 메뉴바 기존 영문 문자열 로케일화. 팔레트 MRU 정렬.

## 2. 실행 계획

- **구현 wf(opus·xhigh)**: Rust 는 한 시점 한 에이전트 — **R** 에이전트가 B2(settings 검색)·H(settings git)·D(`expand_home` 이동+`open_project` 확장)·E(File 메뉴·`project_forget_recent`)·G3/G4/G8 을 순차 구현 후 bindings 재생성·`cargo fmt`·`clippy -D warnings`·`cargo test --workspace` 통과 → 이어서 TS 에이전트 병렬: **T1** A+C, **T2** B1·B3·B4, **T3** D(TS), **T4** F, **T5** G1·G2·G6·G7, **T6** H(TS). 각 TS 에이전트 종료 조건: `bun run typecheck`·`bun run lint`·`bun run format:check`·관련 `bun test` exit 0, §3 자기 항목 기록(아래 플레이스홀더만 교체 — 다른 항목 텍스트 수정 금지).
- **렌즈 검토 wf(sonnet·xhigh)**: 근본성/회귀/경계 3렌즈. major 는 적대적 검증 2표.
- **메인 2차 검증**: `bun run verify` + `bunx vite build` + `bun run typecheck:e2e` → 분할 커밋(항목별 feat/fix) → dev 푸시 → main ff.
- 사용자 실기: 메뉴 정렬·검색 실시간·Welcome 버튼·+ 메뉴·File 메뉴·알림 문구·git 그래프 리사이즈/접힘·퀵오픈.

## 3. 구현 기록 (에이전트 실시간 기록)

- **§1.B B2 · §1.D(Rust) · §1.E · §1.G G3·G4·G8 · §1.H(영속화 Rust) 완료 (2026-09-15, R)**
  - **변경 파일**
    - `src-tauri/src/domain/settings/types.rs` — `Settings`/`SettingsPatch`/`Default` 에 `search_on_type`(기본 `true`) · `search_on_type_debounce_ms`(기본 300) · `git_sections_collapsed`(기본 `["stashes"]`) · `git_graph_panel_size_px`(기본 240) 4필드. 상수 `DEFAULT_SEARCH_ON_TYPE_DEBOUNCE_MS`·`DEFAULT_GIT_GRAPH_PANEL_SIZE_PX`·`DEFAULT_GIT_SECTIONS_COLLAPSED`(레포의 기존 `DEFAULT_*` 네이밍 관행).
    - `src-tauri/src/domain/settings/service.rs` — `apply_patch` 4필드 병합 + `sanitize` 에 클램프/상한(`SEARCH_ON_TYPE_DEBOUNCE_MIN_MS/MAX_MS` `[50,2000]`, `GIT_GRAPH_PANEL_SIZE_MIN_PX/MAX_PX` `[24,4000]`, `GIT_SECTIONS_COLLAPSED_MAX` 16). patch·손편집 `settings.json`·동기화 gist 세 입구 전부를 지난다.
    - `src-tauri/src/domain/sync/service.rs` — `settings_to_sync_patch` 에 4필드 추가(`recent_searches` 선례대로 gist 동기화 대상).
    - `src-tauri/src/infra/home.rs`(신규) · `infra/mod.rs` — `expand_home`(터미널 `service.rs` 에서 이동) + `home_dir_env`(`agent/commands.rs` 에서 이동) + `expand_home_from_env`. `terminal/service.rs`·`agent/commands.rs`·`agent/hooks.rs` 가 여기를 참조하도록 정리(중복 2벌 → 1벌).
    - `src-tauri/src/domain/project/service.rs` — `resolve_open_root`(`open_project` 진입 `~` 확장) · 부재 경로를 `error.project.pathNotFound` 로케일 `NotFound` 로 포장 · `forget_recent_projects(paths, open_ids)`(열려 있지 않은 `projects/<id>/` 삭제, 개수 반환, 실패 항목은 warn 후 스킵).
    - `src-tauri/src/domain/project/commands.rs` — 신규 커맨드 `project_forget_recent() → u32`(뮤테이션 가드 블록 안에서 삭제 → `ProjectListChanged` 재발행 → 메뉴 갱신). `project_open`/`project_close`/`project_activate` 끝에 `refresh_recent_menu`(close·activate 는 디스크 재스캔이 가드 아래 들어가지 않도록 `drop(_guard)` 후 호출).
    - `src-tauri/src/domain/window/menu.rs`(신규) · `window/mod.rs` — 메뉴 id 상수, `recent_menu_entries`(상한·표시라벨·`root_missing` 비활성), `recent_menu_item_id`/`parse_recent_menu_id`, `menu_label`(로케일 조회 + en → 하드코딩 2단 폴백), `build_file_submenu`, `refresh_recent_menu`, `recent_project_root`.
    - `src-tauri/src/domain/window/commands.rs` — `build_app_menu` 에 `File` 서브메뉴(TAIDE 와 Edit 사이), `handle_menu_event` 가 `taide-recent:<id>`·`taide-clear-recent` 를 처리(둘 다 `async_runtime::spawn` — 이 핸들러는 메인 스레드에서 돈다), 신규 `refresh_app_menu`(언어 변경 시 전체 재빌드 + `set_menu`).
    - `src-tauri/src/domain/settings/commands.rs` — `apply_and_broadcast` 가 `language` 가 실제로 바뀐 경우에만 `refresh_app_menu` 호출.
    - `src-tauri/src/constants.rs` — `RECENT_PROJECT_MENU_LIMIT = 10`.
    - `src-tauri/src/domain/locale/service.rs` — `lookup_builtin_message(localeId, key)` · `builtin_locale_for_language(language)` 신설(카탈로그 JSON 을 읽는 통로를 locale 도메인 1곳으로 유지), `MESSAGE_NAMESPACES` 에 `menu` 네임스페이스 4키 + `error` 의 `layout.paneNotFound`·`project.pathNotFound`.
    - `src-tauri/src/domain/layout/service.rs` — **G3**: `resolve_default_open_pane`(`target: null` 인데 `focused_pane` 이 트리에 없으면 첫 leaf 로 폴백 + `focused_pane` 교정 + `log::info!`), `pane_not_found`(명시 target 부재 시 `error.layout.paneNotFound` 로케일 `NotFound`), `open_tab_and_finish` 배선.
    - `src-tauri/src/domain/search/service.rs` — **G4**: `is_indexable_file`(엔트리가 심링크일 때만 `std::fs::metadata` 로 대상 추적 → 파일이면 포함. 디렉토리 심링크·끊긴 링크는 제외).
    - `src-tauri/src/domain/layout/commands.rs` · `domain/search/commands.rs` — **G8**: 파일 탭 선검증 실패를 사유(경계 밖/파일 부재)와 함께 `warn!`, `search_list_files` 의 건수·소요를 `debug!`.
    - `src-tauri/src/domain/remote/dispatch.rs` · `src-tauri/src/lib.rs` — `project_forget_recent` 를 `IMPLEMENTED_JSON_COMMANDS` + `REMOTE_DENIED_COMMANDS`(`LocalProjectHistoryExposure`) + `collect_commands!` 에 등재, `LocalProjectHistoryExposure` 문서 보강. `lib.rs` 의 `set_menu` 를 `app.manage(state)` 뒤로 이동.
    - `src-tauri/resources/locales/{en,ko,ja}.json` — 신규 6키 × 3(`error.layout.paneNotFound`·`error.project.pathNotFound`·`menu.file`·`menu.openRecent`·`menu.noRecentProjects`·`menu.clearRecent`). 전부 Edit 로 자기 키만 삽입.
    - `src/shared/api/bindings.ts` — 재생성(`cargo test -p taide typescript_바인딩을_생성한다`). 순증 55줄: `projectForgetRecent` 1커맨드 + `Settings`/`SettingsPatch` 4필드.
    - `src/entities/settings/settings.ipc.ts` — `emptySettingsPatch()` 에 새 4필드 `null` 4줄(아래 이탈 ①).
    - 문서: `docs/ipc-contract.md`(d-58 Rust 절 신설 — settings 4필드 표·`project_forget_recent`·`~` 확장·pane 폴백·심링크·로케일 6키) / `docs/features/window-chrome.md` §7 신설(메뉴 구성·클릭 처리·동적 갱신·알려진 제약) / `docs/features/tabs.md` §1(열 pane 해소 규칙) / `docs/features/command-palette.md` §3(인덱스에 들어가는 것 — 심링크 규칙 + 신선도 표의 pane 오인 해소) / `docs/debugging.md` §4.4 신설(퀵오픈 실패 사후 판별 로그 4종) / `docs/backlog.md` 퀵오픈 pane 항목 **해결(d-58)** 표시.
  - **테스트** — 신규 21건 + 이관 3건(터미널 → `infra/home`). 모듈별 최종: `infra::home` 4 · `domain::window::menu` 5 · `domain::settings::service` 69(신규 7) · `domain::project::service` 34(신규 4) · `domain::layout::service` 82(신규 3) · `domain::search::service` 49(신규 1, unix 한정 심링크). 실행: `cargo fmt --all` 적용 · `cargo clippy --workspace --all-targets -- -D warnings` **경고 0** · `cargo test --workspace` **1547 pass / 1 fail**(실패 1건은 §1.F/T4 의 미등재 키, 아래 미확인 ①) · `bun run typecheck` exit 0 · `bun run lint` **0 error / 11 warning**(전부 기존 `useVirtualizer` 경고) · `bunx prettier --check`(만진 TS·JSON·md 11파일) 전부 통과 · `bun test src/entities/settings` 15 pass.
  - **이탈**
    - ① **TS 1파일을 불가피하게 수정**: `SettingsPatch` 에 필드가 늘면 `emptySettingsPatch(): SettingsPatch` 가 `TS2739`(누락 프로퍼티)로 깨진다 → `src/entities/settings/settings.ipc.ts` 에 `searchOnType`·`searchOnTypeDebounceMs`·`gitSectionsCollapsed`·`gitGraphPanelSizePx` 를 `null` 로 4줄 추가했다. bindings/로케일 외 TS 는 이 4줄이 전부다(T2·T6 가 값을 쓰기만 하면 된다).
    - ② **`lib.rs` 의 `set_menu` 호출 위치 이동**(계약 미기재, 필수): `File > Open Recent` 는 `AppState::paths` 가 가리키는 디스크 기록에서 만들어지는데 기존 `set_menu` 는 `app.manage(state)` **앞**에 있었다 → `app.state::<AppState>()` 패닉. `drop(state_restore_span)` 직후로 옮겼다(창 생성·이벤트 마운트 뒤라 관측되는 동작 변화 없음).
    - ③ **Clear Recent 를 `project_forget_recent` 커맨드 함수 직접 호출로 통일**: 처음엔 `menu::clear_recent_projects` 를 따로 뒀으나 뮤테이션 가드·이벤트 발행·메뉴 갱신이 커맨드와 중복돼 제거하고, 메뉴 핸들러가 커맨드 함수를 그대로 부르게 했다(경로 1벌).
    - ④ **언어 변경 시 `refresh_recent_menu` 가 아니라 전체 `set_menu` 재빌드**: 서브메뉴 제목(`File`·`Open Recent`)은 생성 시 고정이라 항목 교체만으로는 안 바뀐다. 계약이 허용한 대안(`set_menu` 재호출)을 택했다.
    - ⑤ **`git_sections_collapsed` 는 `Vec<String>` passthrough + 길이 상한 16**: 계약은 정규화를 명시하지 않았다. Rust 에 `GitSectionId` 유니온을 한 벌 더 두면 섹션이 늘 때 두 곳이 조용히 어긋나므로(프론트가 추가한 새 id 를 Rust 가 버린다) 값 검증은 하지 않고, 손편집/동기화 gist 발 무한 배열만 상한으로 막았다(`recent_searches` 선례).
    - ⑥ **`expand_home` 이동처는 `infra/paths` 가 아니라 신규 `infra/home.rs`**: 기존 `src-tauri/src/paths.rs` 는 `AppPaths`(앱 데이터 디렉토리) 전용이라 성격이 다르다. 겸사겸사 `agent/commands.rs` 의 중복 `home_dir_env` 도 이 모듈로 흡수했다(계약 §1.D 의 "중복이면 하나로").
    - ⑦ **`File` 메뉴 라벨은 `display.label`(1~4자) 우선**: 계약 §1.E 문구 그대로다. 표시 라벨을 지정한 프로젝트는 메뉴에도 그 짧은 문자열만 뜬다(§1.F 알림 제목 규칙과 동일).
    - ⑧ 함수 본문 안의 설계 근거는 `///` 가 붙지 않는 위치라 `//` 인라인 주석 3곳을 썼다(`lib.rs` 의 기존 관행과 동일).
  - **확인(계약이 §3 기록을 요구한 항목)** — tauri **2.11.5** 의 `Submenu::append`/`remove_at`/`items` 는 전부 `run_item_main_thread!` 를 지나고, 그 매크로는 `AppHandle::run_on_main_thread` → `tauri-runtime-wry` 의 `send_user_message` 로 간다. `send_user_message` 는 `current_thread().id() == main_thread_id` 이면 태스크를 **그 자리에서 인라인 실행**하고 아니면 이벤트 루프 proxy 로 보낸다(`tauri-runtime-wry-2.11.4/src/lib.rs:235-255`). 즉 메인 스레드/워커 어디서 불러도 안전하고 데드락도 없어 **별도 `app.run_on_main_thread` 래핑이 필요 없다**.
  - **미확인·후속**
    - ① `cargo test --workspace` 의 유일한 실패 `domain::locale::service::tests::en_메시지의_모든_키는_required_message_keys에_포함된다` — `MESSAGE_NAMESPACES에 등록되지 않은 키: common.durationHoursMinutes`. §1.F(T4)가 로케일 JSON 에만 넣고 `MESSAGE_NAMESPACES` 등재를 못 한 키다(T4 는 Rust 금지 범위 — T4 기록의 "미확인 ①"과 같은 건). **3회 재시도 후에도 잔존** → 메인이 `common` 네임스페이스에 `durationHoursMinutes`·`durationMinutesSeconds`·`durationSeconds` 와 `notification` 에 `agentAwaitingInputBody`·`agentFinishedBody`·`exitCode` 를 등재한 뒤 `cargo test --workspace` 재실행 필요. 내 키 6종은 등재·JSON 3종 모두 반영돼 있고 이 실패의 원인이 아니다.
    - ② **[TS 범위라 미수행]** `src/app/providers/ipc-sync-provider.tsx` 의 `projectListChanged` 핸들러가 `QUERY_KEY.PROJECT.LIST` 만 무효화한다(`['project','list']`). `project_forget_recent`·`project_open` 후 사이드바/Welcome 의 최근 목록이 갱신되려면 `QUERY_KEY.PROJECT.RECENT`(`['project','recent']`)도 함께 무효화해야 한다 — 한 줄 추가면 되지만 해당 파일은 다른 에이전트 몫이라 건드리지 않았다. 계약 §1.E 의 해당 문장이 아직 미이행 상태다.
    - ③ **`language: "system"`(기본값)이면 메뉴 라벨이 영어다.** OS 로케일은 프론트의 `navigator.language`(=`locale_get_current(systemLanguage)` 인자)로만 들어오고 Rust 프로세스는 그 값을 관측하지 않는다. `ko`/`ja` 를 명시 선택하면 메뉴도 그 언어다. 후속안(프론트가 해석한 로케일 id 를 AppState 에 보관 → 메뉴가 그것을 읽음)은 `docs/features/window-chrome.md` §7.3 에 적어 뒀다.
    - ④ **메뉴 실물·`~` 경로 열기·Clear Recent 는 사용자 실기 확인 대상**이다(앱 실행 금지 범위). 특히 macOS 에서 창 0개 상태의 `File > Open Recent` 동작.
    - ⑤ `docs/features/git.md`(§1.H 의 구조·로빙·스크롤바 서술 정정)는 TS 레이아웃 변경과 한 묶음이라 T6 몫으로 남겼다. `docs/features/search.md` 는 **존재하지 않아**(검색 정본은 `explorer-sidebar.md`) 계약의 "(있으면)" 조건대로 건너뛰고 settings 4필드는 `docs/ipc-contract.md` 에 기술했다.
    - ⑥ `project_open` 의 `~` 확장은 **원격 dispatch 경로에도 동일 적용**된다(같은 서비스 함수). 원격 세션이 보낸 `~` 는 **데스크톱의 홈**으로 풀린다 — 기존 `project_open` 이 이미 데스크톱 로컬 경로를 그대로 받는 커맨드라 노출 면이 넓어지지는 않지만, 기록해 둔다.
- **§1.A·§1.C 완료 (2026-09-15)** —
  **변경 파일**: `src/shared/ui/context-menu.tsx`(`ContextMenuSubTrigger` 클래스에 `gap-2` 1토큰 추가 — `ContextMenuItem`·`DropdownMenuSubTrigger` 와 동일 열 정렬, `ChevronRightIcon` 의 `ml-auto` 는 그대로) / `src/entities/layout/layout.query.ts`(신규 `useOpenTerminalTab(projectId)` — 프로젝트 가드 토스트 + 터미널 탭 payload + 에러 토스트를 한 곳에) / `src/widgets/command-palette/command-palette.tsx`(로컬 `openTerminalTab` 클로저 제거 → 훅 사용, `CommandContext`·`new-terminal` 키맵 배선은 그대로) / `src/features/welcome/welcome-screen.tsx`(`canOpenTerminal`·`onOpenTerminal` props + lucide `Terminal` 아이콘 `variant='outline'` 버튼, 힌트 조건을 `!canOpenFile || !canOpenTerminal` 로) / `src/widgets/welcome/welcome-container.tsx`(`useOpenTerminalTab(projectId)` 배선, `canOpenTerminal={projectId !== null}`) / `src-tauri/resources/locales/{en,ko,ja}.json`(`app.openFileHint` 문안만 갱신 — 신규 키 0, 버튼 라벨은 기존 `keymap.newTerminal` 재사용) / `docs/features/layout-shell.md` §1.1(액션 3개·비활성 조건·focused pane 규칙) · `docs/features/command-palette.md` §2.1(`terminal.new` 가 훅을 공유하며 target 이 `null`→focused pane 으로 바뀐 사실).
  **테스트**: 신규 2파일 — `src/features/welcome/welcome-screen.test.tsx`(5건: 버튼 존재·클릭 콜백·비활성 시 콜백 미호출·힌트 노출/미노출), `src/entities/layout/use-open-terminal-tab.test.tsx`(2건: 캐시된 focused pane 을 `target` 으로 넘김 / 레이아웃 캐시 없으면 `target: null`). 갱신 1파일 — `pane-node-view-welcome.test.tsx` 에 터미널 버튼 존재 단언 1줄. 실행: `bun test`(내 4파일) **18 pass / 0 fail / 45 expect**, context-menu 소비처 회귀로 `tab-bar-context-menu`·`terminal-context-menu` **22 pass / 0 fail**. `bunx prettier --check`(만진 11파일) 전부 통과. `bun run lint` 에서 내 파일 지적 0.
  **이탈**: 없음. 계약이 열어 둔 target 선택지는 **통일** 쪽으로 결정했다 — 팔레트의 기존 `target: null` 은 Rust 가 `ProjectLayout.focused_pane`(메인 트리)으로 폴백하는 값이고, 팔레트는 메인 창 전용이라 `currentWindowFocusedPane(layout)` 과 같은 pane 이다(`layout/service.rs:1331` · `pane-tree.ts:91-92` 대조). 반면 Welcome 탭은 보조 창에도 존재할 수 있어 `null` 이면 메인 창에 터미널이 열리므로, 계약 §1.C 본문대로 훅이 `currentWindowFocusedPane` 을 쓰고 팔레트가 이를 따른다(훅 인자로 target 을 받지 않는다). 힌트 조건은 `canOpenFile`·`canOpenTerminal` 두 prop 이 생겼으므로 `||` 로 넓혔다(현재 컨테이너는 둘 다 같은 값이라 동작 동일). 로케일은 기존 `app.openFileHint` 문안 갱신뿐이라 새 네임스페이스·새 키 없음.
  **미확인·후속**: (1) `bun run lint` 는 최종 **0 error / 11 warning**(전부 기존 TanStack Virtual `incompatible-library` 경고, 내 파일 아님)으로 통과. `bun run typecheck` 는 재시도 4회 끝에 `src/shared/lib/native-notification-gate.test.ts` **TS2769 2건만 남았고, 이는 §1.F(T4) 진행 중 파일이다**(중간에 함께 떴던 `src/entities/git/git.query.ts` TS2304 4건과 `native-notification-provider.tsx` unused 5건은 재시도 중 해소됨). 내 파일은 전 실행에서 오류 0 이었다 — T4 종료 후 메인이 재확인 필요. (2) 하위메뉴 아이콘 정렬·Welcome 버튼의 실제 렌더는 happy-dom 에 레이아웃이 없어 **사용자 실기 확인 대상**이다. (3) 팔레트가 `useOpenTerminalTab` 을 통해 `layoutQueryOptions(activeProjectId)` 를 상시 구독하게 됐다 — 같은 쿼리 키를 `editor-area` 등 다수 위젯이 이미 구독하고 `staleTime` 60s 라 추가 IPC 는 사실상 없지만, 팔레트는 기존에도 `enabled:false` 옵저버로 같은 키 갱신에 리렌더되고 있었으므로 리렌더 빈도 변화도 없다.
- **§1.B B1·B3·B4 완료 (2026-09-15, T2)**
  - **변경 파일**
    - `src/shared/ui/file-group-header.tsx` — **B1**: 하드코딩 lucide `File`(+`opacity-80`) → `FileTypeIcon fileName={fileNameOf(path)}`. 검색·문제 패널이 탐색기·탭 바와 같은 `resolveFileIcon` 레지스트리를 쓴다(색은 `colorClass` 담당).
    - `src/shared/constants/search.ts` — `DEFAULT_SEARCH_ON_TYPE`·`DEFAULT_SEARCH_ON_TYPE_DEBOUNCE_MS`·`MIN/MAX_SEARCH_ON_TYPE_DEBOUNCE_MS`(Rust `settings::service::sanitize` 의 `[50,2000]` 클램프 미러, `SEARCH_MATCH_LIMIT` 선례대로 JSDoc 에 미러 사실 명시). 설정 UI·패널 두 곳이 쓰므로 shared.
    - `src/widgets/settings-view/settings-interface-section.tsx` — **B3**: 섹션 하단에 `border-t` 로 구분된 "검색" 소제목(`search.title` 재사용) + `SwitchField`(`settings.searchOnType`/`…Description`) + `NumericField`+힌트(`settings.searchOnTypeDebounceMs`/`…Description`, `min`/`max` = 위 상수). `autoSaveDelayMs` 의 기존 "NumericField + 힌트 span" 패턴 그대로.
    - `src/entities/search/use-search-run.ts` — **B4**: `RunSearchOptions.live?: boolean` 추가, live 실행의 `.catch` 는 `toast.error` 를 건너뛰고 `status='failed'` 만 남긴다(JSDoc 에 "덜 쓴 정규식" 근거).
    - `src/widgets/search-panel/search-panel-container.tsx` — **B4**: `settingsQueryOptions` 구독 → `searchOnType`/`searchOnTypeDebounceMs`, 질의·옵션(대소문자/단어/정규식/gitignore/제외 glob/폴더 범위) 변경 시 **트레일링 전용** `setTimeout` 디바운스로 `run(buildQuery(), { recordHistory: false, live: true })`. Enter(`handleSubmit`)는 대기 타이머를 취소하고 현행대로 즉시 실행·이력 기록·`perfMark`. `perfMeasure` 는 live 실행이면 건너뛴다.
    - `src/features/search/search-panel.tsx` — `isLiveSearch: boolean` prop(순수 UI 유지) → 유휴 안내 문구를 `search.liveSearchHint` / `search.pressEnterHint` 로 분기.
    - `src-tauri/resources/locales/{en,ko,ja}.json` — 신규 5키 × 3(`search.liveSearchHint` + `settings.searchOnType`·`…Description`·`…DebounceMs`·`…DebounceMsDescription`). 전부 Edit 로 자기 키만 삽입, 기존 키 무수정.
    - `src-tauri/src/domain/locale/service.rs` — 위 5키를 `MESSAGE_NAMESPACES` 의 기존 `search`·`settings` 네임스페이스에 등재(신규 네임스페이스 없음). 아래 이탈 ①.
    - 문서: `docs/features/explorer-sidebar.md` §3.1 에 결과 그룹 헤더 아이콘 1항목 + **§3.3 "실행 트리거 — 실시간 검색" 신설**(트레일링 디바운스·Enter 이력 단독·live 토스트 억제·성능 표본 제외·취소 정책 위임·IME 감수·Search Editor 범위 외) / `docs/quality-assurance/2026-09-04-perf-baseline.md` §3.1 에 **지표 7** 주의 1항목(live 는 마크를 찍지도 닫지도 않음) / `docs/features/settings-ui.md` §1.1 에 Interface 섹션의 "검색" 소제목 2필드 기술.
  - **테스트** — 신규 1파일 `src/widgets/search-panel/search-panel-container.test.tsx` 9건(디바운스 지연 실행 / 연속 입력 1회 / 옵션 변경 재실행 / live 는 이력 미기록 / Enter 즉시+대기 타이머 취소+이력 기록 / live 실패는 토스트 없이 `search.failed` 인라인 / Enter 실패는 토스트 / 설정 off 는 Enter 에서만 실행 / 힌트 문구 분기). 실행: `bun test`(이 파일) **9 pass / 0 fail / 21 expect**, `bun test`(전체) **2424 pass / 0 fail**(244 파일), `bun run typecheck` exit 0, `bun run lint` **0 error / 11 warning**(전부 기존 `useVirtualizer` 경고, 내 파일 아님), `bunx prettier --check`(만진 13파일) 전부 통과, `cargo fmt --all -- --check` 무출력, `cargo test -p taide --lib locale` 19건 중 18 pass / 1 fail(아래 미확인 ① — 내 키 원인 아님).
  - **이탈**
    - ① **Rust 1파일을 불가피하게 수정**(T2 는 Rust 금지 범위): 신규 로케일 키를 `MESSAGE_NAMESPACES` 에 등재하지 않으면 `en_메시지의_모든_키는_required_message_keys에_포함된다` 가 깨진다(계약 서문은 "새 네임스페이스면"이라 적었지만 실테스트는 **전 키**를 강제 — T5 기록의 이탈 ②와 같은 건). 기존 네임스페이스에 키 5줄 추가가 전부다.
    - ② **`search.invalidRegexInline` 신규 키를 만들지 않았다**: 계약이 "기존 실패 표시 재사용, 없으면 신규 1키"라 열어 뒀는데 `SearchPanel` 의 `view === 'failed'` 분기가 이미 `search.failed` 를 렌더한다 → 재사용.
    - ③ **"검색" 소제목 라벨로 기존 `search.title` 을 재사용**해 신규 키를 계약대로 4개로 유지했다(소제목용 `settings.search` 를 새로 만들지 않음).
    - ④ **`perfMeasure` 쪽에도 live 가드를 넣었다**(계약은 `perfMark` 만 명시). 마크를 안 찍기만 하면, 결과 0건으로 끝난 직전 Enter 의 **미소비 마크**를 뒤이은 실시간 실행 결과가 소비해 그 경과를 검색 시간으로 보고한다 — "표본 보존"이라는 계약 취지대로 양쪽을 막았다(`isLiveRunRef`).
    - ⑤ **디바운스 effect 가 `run` 을 의존성에 넣지 않고 `liveRunRef`(매 렌더 갱신 ref)로 읽는다.** `run` 은 매 렌더 새로 만들어지므로 의존성에 넣으면 **그 실행이 유발한 리렌더마다 타이머가 재무장돼 무한 재검색**이 된다(`search-editor-pane.tsx` 의 `latestRef` 선례와 같은 방식). 근거는 JSDoc 에 기재.
    - ⑥ 계약이 적은 "metric 6" 은 실제 baseline 문서에서 **지표 7(전역 검색, `search.results`)** 이다(지표 6 은 git status). 문서에는 실제 번호로 적었다.
    - ⑦ 계약의 "`docs/features/search.md`(있으면)" 는 존재하지 않아 검색 정본인 `explorer-sidebar.md` §3 에 기술했다(R 기록 ⑤와 동일 판단).
  - **미확인·후속**
    - ① `cargo test -p taide --lib locale` 의 유일한 실패는 여전히 `MESSAGE_NAMESPACES에 등록되지 않은 키: common.durationHoursMinutes` — **§1.F(T4)의 미등재 6키**(`common.durationHoursMinutes`·`durationMinutesSeconds`·`durationSeconds`, `notification.agentAwaitingInputBody`·`agentFinishedBody`·`exitCode`)가 원인이다. 스크립트로 en.json ↔ `MESSAGE_NAMESPACES` 전수 대조한 결과 미등재 키는 정확히 이 6개뿐이고 **내 5키는 모두 등재·3종 JSON 반영**돼 있다. 메인이 6키를 등재한 뒤 `cargo test --workspace` 재실행 필요(R 기록 미확인 ①과 같은 건).
    - ② 중간 전체 `bun test` 1회는 `src/widgets/git-panel/git-sections.test.ts` 의 `@entities/git/git-section-collapse-memory` 모듈 부재로 실패했으나, **§1.H(T6) 진행 중 편집**이었고 재시도에서 해소됐다(최종 2424 pass / 0 fail).
    - ③ **실기 확인 대상**: happy-dom 에는 레이아웃이 없어 아이콘 색·정렬, 설정 화면의 "검색" 소제목 렌더, 실제 타이핑 체감(디바운스 300ms)은 사용자 확인이 필요하다. 특히 **한글 입력**은 WKWebView 가 composition 이벤트를 주지 않아 조합 중 문자열로 검색될 수 있다(계약대로 감수, 문서에 명시).
    - ④ 검색 패널을 선택 텍스트로 여는 경로(`seedText`)는 설정이 켜져 있으면 **열자마자 디바운스 후 자동 실행**된다(VS Code 동형). 계약이 금지하지 않아 그대로 뒀다.
- **§1.D(TS) · §1.E(FE 몫) 완료 (2026-09-15, T3)**
  - **변경 파일**
    - `src/shared/constants/project.ts`(신규) — `RECENT_PROJECT_MENU_LIMIT = 10`(Rust `constants::RECENT_PROJECT_MENU_LIMIT` 와 같은 값) + Welcome 에서 승격한 `RECENT_PROJECT_DISPLAY_LIMIT = 8`. R 이 Rust 쪽 JSDoc 에서 이미 이 경로를 가리키고 있었다.
    - `src/features/project/sidebar-add-project-menu.tsx`(신규, 순수 UI) — `tab-bar-add-menu.tsx` 의 `DropdownMenu > Tooltip > TooltipTrigger asChild > DropdownMenuTrigger` 중첩 그대로. 경로로 열기 / Finder 로 열기 / 구분선 + `app.recentItems` 라벨 + 최근 목록(표시 라벨 우선, 보조줄 `root`, `rootMissing` 이면 `disabled` + 경고 문구). props 만 받는다.
    - `src/features/project/open-project-by-path-dialog.tsx`(신규, 순수 UI) — `create-tag-dialog.tsx` 패턴(닫힘→열림 전이에서 렌더 중 입력 리셋). Enter 제출·trim·빈 값/진행 중 제출 차단, 확인 라벨은 기존 `app.openProject` 재사용.
    - `src/widgets/app-sidebar/app-sidebar.tsx` — `IconButton`(폴더 피커 직행)을 위 메뉴 + 다이얼로그로 교체. `recentProjectsQueryOptions()` 구독 → **열려 있는 프로젝트 제외** 후 `RECENT_PROJECT_MENU_LIMIT` 로 자름. Finder 는 기존 `useOpenFolderDialog`, 경로·최근은 `useOpenProject`(에러 `describeIpcError` 토스트, 성공 시 다이얼로그 닫힘, `isPending` 으로 재제출 차단).
    - `src/widgets/welcome/welcome-container.tsx` — 로컬 `RECENT_PROJECT_DISPLAY_LIMIT` 상수를 `@shared/constants/project` import 로 교체(값·동작 동일).
    - `src/app/providers/ipc-sync-provider.tsx` — **§1.E FE 몫**: `projectListChanged` 핸들러가 `PROJECT_LIST_CHANGED_INVALIDATIONS`(신규 export, `PROJECT.LIST` + `PROJECT.RECENT`)를 돌며 무효화. `PROJECT.ALL` 접두사를 쓰지 않아 `ACTIVE`/`DETAIL`(각자 이벤트 있음)은 끌어들이지 않는다. R 의 미확인 ② 해소.
    - `src-tauri/resources/locales/{en,ko,ja}.json` — 신규 5키 × 3(`sidebar.addProjectMenu`·`openByPath`·`openByPathTitle`·`openByPathPlaceholder`·`openViaFinder`), **`sidebar.openFolderAriaLabel` 제거**(마지막 사용처가 사라짐 — 전 레포 grep 0). Edit 로 자기 키만 삽입/삭제.
    - `src-tauri/src/domain/locale/service.rs` — `MESSAGE_NAMESPACES` 의 기존 `sidebar` 네임스페이스 키 목록만 갱신(위 5키 추가 + `openFolderAriaLabel` 제거). 새 네임스페이스 없음(아래 이탈 ③).
    - `docs/features/layout-shell.md` §2.1 — 항목 2 를 "프로젝트 추가 메뉴"로 고치고 §2.1.1 신설(구성 표·열린 프로젝트 제외 근거·상한 2곳 일치·라벨 사다리·`project_open` 단일 경로·`IconButton` 을 안 쓴 이유·다이얼로그 규칙·`~` 확장은 Rust 몫·`PROJECT.RECENT` 무효화·표시 라벨 크로스창 동기화).
  - **테스트** — 신규 2파일 13건: `sidebar-add-project-menu.test.tsx` 6건(경로/Finder 콜백, 최근 0건이면 섹션 자체 없음, 표시 라벨 우선·이름 폴백, 선택 시 프로젝트 전체 전달, `rootMissing` 비활성·콜백 미발생), `open-project-by-path-dialog.test.tsx` 7건(빈 값 비활성·공백만 제출 차단·trim·Enter 제출·진행 중 재제출 차단·취소·재오픈 리셋). 갱신 1파일: `ipc-sync-provider.test.ts` +2건(`PROJECT_LIST_CHANGED_INVALIDATIONS` 구성·`PROJECT.ALL` 미사용). 실행: `bun run typecheck` exit 0 · `bun run typecheck:e2e` exit 0 · `bun run lint` **0 error / 11 warning**(전부 기존 `useVirtualizer` 경고) · `bunx prettier --check`(만진 13파일) 전부 통과 · `bun test` 전체 **2408 pass / 0 fail**(243 파일) · `cargo fmt --all` 적용 · `cargo clippy --workspace --all-targets -- -D warnings` **경고 0** · `cargo test --workspace` **1547 pass / 1 fail**(실패 1건은 내 범위 밖 — 아래 미확인 ①).
  - **이탈**
    - ① **최근 선택에 `useActivateProject` 분기를 두지 않았다.** 계약은 "Welcome `onSelectRecent` 과 같은 규칙"이라 적었지만, 이 메뉴는 열려 있는 프로젝트를 애초에 목록에서 빼므로 활성 분기가 도달 불가 코드가 된다. 게다가 Rust `open_project` 는 이미 열린 root 를 만나면 `already_open` 으로 **재활성화**하므로(`service.rs` 의 `projects.values().find(...)` 분기) 목록이 잠깐 낡아 열린 프로젝트를 골라도 결과가 같다 → `useOpenProject` 단일 경로.
    - ② **확인 버튼 라벨은 신규 키 없이 기존 `app.openProject`**("프로젝트 열기") 재사용(계약의 "기존 공통 키 있으면 재사용"). 신규 로케일 키는 계약이 적은 5개뿐이고 `error.project.pathNotFound` 는 R 이 이미 넣었다.
    - ③ **Rust 1파일을 불가피하게 수정**: 내 배정은 TS 전용이지만 `MESSAGE_NAMESPACES` 에 새 `sidebar` 키를 등재하지 않으면 `en_메시지의_모든_키는_required_message_keys에_포함된다` 가 **내 키 때문에** 깨진다(T5 와 같은 사유). 기존 네임스페이스의 키 목록 한 곳만 바꿨고 새 네임스페이스는 만들지 않았다.
    - ④ 최근 항목 라벨이 `resolveProjectDisplay`(1~4자 짧은 라벨) 우선이라 표시 라벨을 지정한 프로젝트는 메뉴에서도 그 짧은 문자열만 보인다 — 계약 §1.D 문구 그대로이자 R 의 네이티브 `File > Open Recent`(이탈 ⑦)와 같은 규칙이다. `docs/features/layout-shell.md` §2.2 의 "적용 범위는 1차로 앱 사이드바만" 문장은 손대지 않고, §2.1.1 에 "이 메뉴는 `project:list-changed` 로 다른 창의 라벨 변경까지 따라가고 Welcome 최근 목록은 여전히 `project.name` 을 그린다"를 덧붙여 어긋남을 남기지 않았다.
  - **미확인·후속**
    - ① `cargo test --workspace` 의 유일한 실패는 R 의 미확인 ①과 **같은 건**이다 — `MESSAGE_NAMESPACES에 등록되지 않은 키: common.durationHoursMinutes`. 3회(로케일 단독 → workspace 전체 → 로케일 단독) 재실행해도 잔존. en.json 을 `MESSAGE_NAMESPACES` 와 기계 대조한 결과 미등재 키는 정확히 §1.F/T4 의 6개(`common.durationHoursMinutes`·`durationMinutesSeconds`·`durationSeconds`, `notification.agentAwaitingInputBody`·`agentFinishedBody`·`exitCode`)뿐이고 **`sidebar.*` 는 0건**이다. 메인이 그 6키를 등재하면 해소된다.
    - ② 드롭다운·다이얼로그의 실제 렌더(메뉴 폭·긴 경로 truncate·라이트/다크)는 happy-dom 에 레이아웃이 없어 **사용자 실기 확인 대상**이다. 특히 `~/…` 경로 열기와 존재하지 않는 경로의 `error.project.pathNotFound` 토스트 문구.
    - ③ Radix `Tooltip` 은 `TooltipProvider` 없이는 throw 하므로 두 신규 테스트 중 메뉴 테스트는 `TooltipProvider` 로 감싸 렌더한다(실앱은 `app-providers.tsx` 가 제공). `renderWithProviders` 자체에 Tooltip 을 넣지는 않았다 — 그 헬퍼의 "필요한 것만 싣는다" 규약을 바꾸는 일이라 범위 밖.
    - ④ `entities/project/project.query.ts` 의 `recentProjectsQueryOptions` JSDoc("추가 무효화 배선 불필요")은 **같은 창의 mutation `onSuccess`** 에 대한 서술이라 여전히 맞다. 다른 창·메뉴 이벤트 경로는 이번에 `PROJECT_LIST_CHANGED_INVALIDATIONS` 로 메워졌고, 그 근거는 그 상수의 JSDoc 에 적었다.
- **§1.F 완료 (2026-09-15)** —
  **변경 파일**:
  `src/shared/lib/format-duration.ts`(신규 — `formatDurationShort(ms)`. 최대 2단위, 큰 단위가 시간이면 초를 버린다. 로케일 키 `common.durationHoursMinutes`·`…MinutesSeconds`·`…Seconds`) /
  `src/shared/lib/notification-text.ts`(신규 — `joinNotificationBody`·`notificationBodyFragment`·`resolveProjectNotificationTitle`·`findTerminalTabTitle`·`findTerminalSessionOwner`. provider·git.query·search-panel 4곳이 쓰므로 shared 로 공통화) /
  `src/shared/lib/native-notification-gate.ts`(`completed` 항목에 `activity` 추가. `AGENT_COMPLETION_ACTIVITIES` 를 `as const satisfies` 튜플 + 타입가드로 좁혀 `AgentCompletionActivity` 를 export — 완료 활동이 늘면 문구 쪽도 같이 갱신되게) /
  `src/app/providers/native-notification-provider.tsx`(`useQueryClient` 로 `PROJECT.LIST`·`LAYOUT.DETAIL` 캐시 조달(IPC 0). 제목=프로젝트 표시명·폴백=기존 이벤트 제목. `buildAgentCompletionBody`·`buildTaskCompletionBody` export. `terminal:command-finished` 는 projectId 가 없어 열린 프로젝트 레이아웃을 훑어 세션 주인을 찾는다) /
  `src/entities/git/git.query.ts`(push·pull 성공/실패 모두 제목=프로젝트 표시명, 본문=`{이벤트} · {브랜치 또는 실패 사유}`) /
  `src/widgets/search-panel/search-panel-container.tsx`(치환 완료 알림 제목만 프로젝트 표시명. 본문은 계약대로 현행 `search.replaceDone` 요약) /
  `src-tauri/resources/locales/{en,ko,ja}.json`(신규 6키 × 3 — `common.duration*` 3, `notification.agentAwaitingInputBody`·`agentFinishedBody`·`exitCode`. 기존 키 무수정, Edit 로 자기 키만 삽입) /
  `docs/features/settings-ui.md`(§2.1.3 발화 지점 표의 제목/본문 열 갱신 + §2.1.4 "문구 포맷" 신설 — 포맷 표·`{{tab}}` 조달 경로·종료 코드 0 포함 근거·클릭 라우팅 불가 명시)
  **테스트**: 추가 3파일 30건(`format-duration.test.ts` 6 · `notification-text.test.ts` 17 · `native-notification-provider.test.ts` 7), 갱신 1파일(`native-notification-gate.test.ts` — 기존 단언 2건에 `activity` 반영 + 완료 활동 구분 1건 추가, 29 pass). 실행: `bun run typecheck` exit 0 · `bun run lint` 0 errors(11 warnings 전부 기존 `useVirtualizer` 경고) · `bunx prettier --check <변경 파일 13개>` all pass · `bun test`(전체) 2392 pass / 0 fail(240 파일).
  **이탈**:
  ① 계약이 명시한 신규 파일은 `format-duration.ts` 뿐이지만 제목·본문 조립 헬퍼를 `shared/lib/notification-text.ts` 로 함께 신설했다 — 소비처가 4곳(provider 2경로·git.query·search-panel)이라 "2회 이상" 공통화 기준을 넘고, provider(app 레이어)에 두면 entities·widgets 가 참조할 수 없다.
  ② task 본문의 종료 코드는 계약 문구대로 싣되 **셸이 코드를 보고하지 않은 `exitCode: null` 만 그 조각을 만들지 않는다**(없는 값을 지어내지 않기 위해). 성공(`0`)도 싣는다 — 제목이 프로젝트명이 된 뒤 성공/실패를 말하는 것이 본문뿐이라서.
  ③ git 은 계약이 성공 경로만 적었으나 **실패 경로에도 같은 제목 규칙을 적용**하고 본문을 `{실패 제목} · {에러}` 로 바꿨다. 제목만 프로젝트명으로 바꾸고 본문을 그대로 두면 "무엇이 실패했는지"가 알림 어디에도 남지 않는다.
  ④ 경과 시간 로케일 키를 계약이 예시한 `duration.*` 대신 **`common.duration*` 3키**로 넣었다 — 신규 네임스페이스 금지(동시성 규칙 2, `menu` 는 R 전용).
  ⑤ `docs/features/notifications.md` 가 없어 알림 절 정본인 `docs/features/settings-ui.md` §2.1 에 기술했다(계약이 허용한 "알림 절이 있는 문서").
  **미확인·후속**:
  ① **[차단 — Rust 등재 필요]** 신규 로케일 키 6개를 `src-tauri/src/domain/locale/service.rs` 의 `MESSAGE_NAMESPACES` 에 등재해야 한다. T4 는 Rust 파일 금지라 미수행 — 미등재 상태에서는 `cargo test -p taide` 의 `en_메시지의_모든_키는_required_message_keys에_포함된다` 가 실패한다. 추가 항목: `"common"` ← `"durationHoursMinutes"`·`"durationMinutesSeconds"`·`"durationSeconds"`, `"notification"` ← `"agentAwaitingInputBody"`·`"agentFinishedBody"`·`"exitCode"`.
  ② (해소) 중간에 전체 `bun test` 가 `src/entities/app/perf.ipc.test.ts` 1건에서 실패했으나 T4 범위 밖의 동시 작업(`auxiliary-window-shell.test.tsx` 의 process-global `mock.module('@tauri-apps/api/window', …)`)이 원인이었고, 재실행 시점에는 전체 통과다.
  ③ 문구 실물(라이트/다크·실제 알림 센터)은 사용자 실기. 권한 요청/질문 구분과 "입력 대기" 카테고리 분리는 계약 §4 대로 웨이브 3.
- **§1.G G1·G2·G6·G7 완료 (2026-09-15, T5)**
  - **변경 파일**
    - `src/shared/lib/fuzzy-match.ts` — G1 전순서 비교자 `compareRankedItems`(점수 내림차순 → 마지막 경로 세그먼트 매치 우선 → 라벨 길이 오름차순 → `localeCompare`) + G2 NFC 정규화 `toNormalizedForMatching`(질의·라벨 양쪽, ASCII 는 모든 정규화 형태에서 동일하므로 패스 생략) + `FuzzyRankedItem.label`(매칭에 쓴 정규화본) 추가. 토큰이 없는 질의(빈·공백)는 전원 0점이라 정렬 자체를 건너뛴다 — 기존 "원 순서 반환" 계약 유지.
    - `src/features/command-palette/command-palette-files-group.tsx` — 표시를 `label` 기준으로 바꾸고 `toProjectRelativePath` prop 제거. 여는 경로는 `item` 원본 유지(디스크 바이트 보존).
    - `src/features/command-palette/command-palette-commands-group.tsx` · `command-palette-symbol-group.tsx` — 강조 문자열을 `label` 로(라벨 재계산 제거, 인덱스 정합).
    - `src/widgets/command-palette/command-palette.tsx` — 파일 그룹 prop 정리 + **G6**: `openFile` 이 활성 프로젝트 없을 때 조용히 반환하지 않고 `toast.info(t('app.openProjectFirst'))`(형제 핸들러와 동일 규칙).
    - `src/widgets/auxiliary-window-shell/auxiliary-window-shell.tsx` — **G7**: `useGlobalKeymap({ 'quick-open', 'command-palette' })` 로 `toast.info(t('palette.mainWindowOnly'))`.
    - `src-tauri/resources/locales/{en,ko,ja}.json` — `palette.mainWindowOnly` 1키 × 3(Edit 삽입). `src-tauri/src/domain/locale/service.rs` — `MESSAGE_NAMESPACES` 의 `palette` 에 `mainWindowOnly` 등재.
    - `docs/features/command-palette.md` — §3 의 "MRU" 정렬 서술을 실제 규칙으로 정정(+ MRU 미구현·후속 명시), NFC 규칙(표시=정규화본 / 열기=원본 / ASCII 생략) 추가, §5 에 보조 창 제약과 G6 규칙 추가.
  - **테스트** — 신규 2파일: `src/features/command-palette/command-palette-files-group.test.tsx`(2건: NFD 경로의 NFC 표기·강조, 열기 인자가 원본 경로), `src/widgets/auxiliary-window-shell/auxiliary-window-shell.test.tsx`(3건: ⌘P·⌘⇧P 안내 토스트, 무관 키 무반응). 갱신 2파일: `fuzzy-match.test.ts` +10건(동점 정렬 5 · NFC 5, 파일 전체 48 pass), `command-palette-file-match.test.ts` +1건(NFD 경로 + NFC 질의의 파일명/디렉토리 인덱스 정합, 파일 전체 8 pass). 실행: `bun test` 전체 **2392 pass / 0 fail**(240 파일), `bun run typecheck` 에러 0, `bun run lint` **에러 0**(warning 11건은 전부 기존 `useVirtualizer` 경고, 내 파일 아님), 만진 파일 `bunx prettier --check` 통과.
  - **이탈** — ① 계약은 "`fuzzyFilter` 내부 정규화"까지만 정했으나, 정규화하면 `match.indices` 가 정규화본 기준이 되므로 `FuzzyRankedItem` 에 `label` 을 실어 **표시 문자열을 한 곳에 고정**했다(소비처 3곳이 라벨을 각자 다시 만들면 강조가 밀린다 — 근본 수정). ② T5 는 Rust 금지 범위지만 `locale/service.rs` 의 `MESSAGE_NAMESPACES` 1줄은 넣지 않으면 `en_메시지의_모든_키는_required_message_keys에_포함된다` 테스트가 깨진다(계약 서문은 "새 네임스페이스면"이라 적었지만 실테스트는 전 키를 강제) → 등재만 추가. ③ G6 은 현재 UI 로는 도달 불가한 방어 경로라(프로젝트가 없으면 파일 행 자체가 만들어지지 않음) 전용 컴포넌트 테스트 대신 문서에 성격을 명시했다.
  - **미확인·후속** — ① `cargo test -p taide --lib locale` 을 3회 재시도(90s·240s·300s 간격)한 결과 **다른 에이전트의 진행 중 편집 때문에 1건이 계속 실패**: `MESSAGE_NAMESPACES에 등록되지 않은 키: common.durationHoursMinutes`(§1.F/T4). 1차 시도에 함께 났던 `missing message key: menu.file`(§1.E/R)과 `window/commands.rs` 컴파일 에러는 재시도에서 해소됐다. 내 키(`palette.mainWindowOnly`)가 원인인 실패는 한 번도 없었고 JSON 3종 + `MESSAGE_NAMESPACES` 등재가 모두 반영돼 있다 — 웨이브 종료 시 `cargo test --workspace` 재실행 필요. ② `command-palette.md` 의 "심링크 규칙" 1줄은 G4(Rust) 실구현을 확인해야 정확히 쓸 수 있어 남겼고, R 이 §3 "인덱스에 들어가는 것" 항목으로 채운 것을 확인했다. ③ `command-palette-workspace-symbol-group.tsx` 는 `fuzzyFilter` 가 아니라 자체 `fuzzyMatch` 로 강조만 하므로(필터링은 LSP) 이번 NFC 정규화 범위 밖이다 — NFD 심볼명의 강조가 빠질 수 있으나 표시 자체는 정상.
- **§1.H(TS) 완료 (2026-09-15, T6)**
  - **변경 파일**
    - `src/widgets/git-panel/git-panel.tsx` — 본문을 세로 `Group` 으로 재구성(**Panel A** `git-changes` = 기존 `ScrollContainer`(가상화 변경 3그룹 + 스태시, sticky·`scrollPaddingStart`·컨텍스트메뉴 전부 현행) / `PaneSeparator`(세로, `resizerThickness`) / **Panel B** `git-graph` = 헤더(`shrink-0`) + 그래프 + 커밋 상세). Panel B 는 `collapsible` + `collapsedSize=GIT_SECTION_HEADER_HEIGHT_PX(24)` + `minSize=MIN_PANEL_SIZE_PX(120)` + `defaultSize=gitGraphPanelSizePx`. 접힘·높이를 settings 에서 읽고(`toGitSectionCollapsedMap`) 토글 시 즉시 `updateSettings`, 드래그는 `onLayoutChanged(isUserInteraction)` → 접힘이면 `gitSectionsCollapsed`, 아니면 `schedulePaneResizeCommit`(120ms) → `gitGraphPanelSizePx`. 설정 → 패널 방향 동기화는 `app-shell.tsx` 선례대로 `useEffect` + 명령형 핸들. 로빙은 `rovingItemCount` 에서 그래프를 빼고(`= listRows + 스태시`) 그래프 헤더를 ref 포커스 정거장으로 분리.
    - `src/widgets/git-panel/commit-graph.tsx` — `VIEWPORT_HEIGHT_PX = 320` + 원시 `overflow-y-auto scrollbar-hidden` 뷰포트 제거 → `ScrollContainer className='min-h-0 min-w-0 flex-1'`(OverlayScrollbar 포함). `parentRef` → `viewportRef` 로 이름 정리.
    - `src/widgets/git-panel/change-row-navigation.ts` — `GitPanelFocusTarget` + `resolveGitPanelFocusTarget(key, origin, itemCount, hasGraphPane)` 신설(pane 경계를 넘는 ↑↓ 해소). `resolveNextChangeRowIndex` 는 그대로 두고 그 위를 감싼다(클램프 규칙 1벌 유지).
    - `src/entities/git/git-section.ts`(신규) · `git-section-collapse-memory.ts`(+테스트) 삭제 — `GIT_SECTION_IDS`/`GitSectionId`/`GIT_SECTION_DEFAULT_COLLAPSED` + settings 문자열 목록 ↔ 맵 변환(`toGitSectionCollapsedMap`/`toGitSectionCollapsedIds`). 모르는 id 는 읽는 쪽에서 무시(R 이탈 ⑤ 의 passthrough 와 짝).
    - `src/entities/git/git.constant.ts` — `DEFAULT_GIT_GRAPH_PANEL_SIZE_PX = 240`(Rust 기본값 미러, settings 미로딩 구간 폴백).
    - `src/features/git/git-section-header.tsx` — `GIT_SECTION_HEADER_HEIGHT_PX = 24` export(그래프 pane 의 `collapsedSize` 가 헤더 자신의 `h-6` 을 참조하도록).
    - `src/widgets/git-panel/git-sections.ts`·`git-sections.test.ts` — `GitSectionId` import 경로만 새 모듈로.
    - `e2e/specs/19-git-sections-collapse.e2e.ts` — sticky 단언의 적용 범위를 "스크롤 pane 안 헤더(변경/스태시)"로 문서화하고 그래프 헤더는 pane 내 고정이라 범위 밖임을 명시, 접힘 상태 전제를 모듈 메모리 → `Settings.gitSectionsCollapsed` 로 갱신(단언 자체는 기존대로 `Changes` 헤더에만 적용).
    - 문서: `docs/features/git.md`(§2 구성=2-pane, §2.1 접힘/높이 영속·드래그 접힘 동기화, §2.2 "스크롤바 하나"→"pane 당 하나"·로빙 서술, §5 그래프 pane 내부 구성) / `docs/architecture.md` §6.4 레지스트리 표에서 `git-section-collapse-memory` 행 제거 / `docs/quality-assurance/2026-08-18-e2e-harness.md` 스펙 19 전제 갱신.
  - **테스트** — 신규 2파일: `entities/git/git-section.test.ts` **9건**(기본값 폴백·빈 목록·미지 id 무시·왕복), `widgets/git-panel/git-panel.test.tsx` **12건**(RTL — 그래프가 변경 pane 밖 별도 `Panel`, 접힘 시 본문 미렌더/펼침 시 자체 스크롤 영역, 설정 없을 때 기본값, 커밋 0건이면 pane 자체 없음, 접기·펼치기·스태시 토글의 설정 저장, ↓/↑ 의 pane 경계 이동·그래프 끝 클램프·본문 키 무시). 갱신 2파일: `change-row-navigation.test.ts` **+8건**(총 24 — 그래프 정거장 분리 7 + 스태시 전용 인덱스 1 수정), `git-sections.test.ts`(import 경로만, 16건 유지). 삭제 1파일: `git-section-collapse-memory.test.ts`(9건, 모듈과 함께).
    실행: `bun test` **전체 2435 pass / 0 fail**(244 파일) · `bun run typecheck` exit 0 · `bun run typecheck:e2e` exit 0(e2e 실행 안 함) · `bun run lint` **0 error / 11 warning**(전부 기존 `useVirtualizer` incompatible-library) · `bunx prettier --check`(만진 15파일) 전부 통과.
  - **이탈**
    - ① **커밋 상세 패널 위치**: 계약은 "본문 `flex-1` 을 `ScrollContainer` 로"만 적고 상세 패널을 언급하지 않았다. 그래프 가상목록과 상세를 한 `ScrollContainer` 에 넣으면 상세가 **전체 커밋 목록 높이(수천 px) 아래**로 밀려 사실상 도달 불가 → 그래프 본문(`flex-1`, 자체 ScrollContainer) 아래에 상세를 **pane 높이의 1/2 상한 + 자체 ScrollContainer** 로 뒀다. 접힘 시 둘 다 숨는 기존 동작은 그대로.
    - ② **`git-section.ts` 신설**: 계약은 메모리 파일 "제거"만 적었지만 `GitSectionId`·기본 접힘·settings 변환은 세 소비처(`git-sections`·`change-row-navigation`·패널)가 계속 쓰고, 변환은 순수 함수로 테스트돼야 해서 entities 에 남겼다(파일명만 바뀐 셈).
    - ③ **그래프 헤더는 `data-git-roving-index` 없이 ref 로** 포커스한다(인덱스 공간이 Panel A 의 가상화기 것이라 pane 밖 항목에 인덱스를 주면 셀렉터 검색이 pane 을 넘나든다). `resolveGitPanelFocusTarget` 이 그 경계 이동만 순수 함수로 해소.
    - ④ **`Panel A` 에는 `minSize` 를 주지 않았다**(계약 미기재 → 기본 0%). 그래프 pane 만 `minSize`/`collapsible` 을 갖는다.
    - ⑤ **컴포넌트 테스트의 설정 저장 확인은 `MutationCache.state.variables`** 로 한다. 처음에는 `spyOn(commands, 'settingsUpdate')` 를 썼는데 **단일 파일은 통과 / 전체 스위트에서 3건 실패**했다 — `@entities/settings/settings.ipc` 를 이미 두 테스트 파일이 `mock.module`(프로세스 전역·마지막 등록 승)로 잡고 있어 스파이 아래까지 호출이 내려오지 않는다. 같은 이유로 "패치의 나머지 필드가 `null`" 단언은 뺐다(`emptySettingsPatch` 자체가 그 목에 따라 달라진다).
    - ⑥ **문서 1건 추가 갱신**: 계약이 지목한 `git.md`·`architecture.md` 외에 `docs/quality-assurance/2026-08-18-e2e-harness.md` 의 스펙 19 전제도 고쳤다(삭제된 파일을 가리키는 **살아있는 지침**이라). `git.md` §2.1 의 "이 저장소에는 DOM 렌더 하네스가 없으므로" 문장도 test-conventions §6 대상이라 함께 정정.
  - **확인(계약이 §3 기록을 요구한 rrp 4.12.2 실 API — `node_modules/react-resizable-panels/dist/*` 타입·번들 직접 확인)**
    - `Panel` props: `collapsible` · `collapsedSize` · `defaultSize` · `minSize`/`maxSize` 전부 `number | string`, **숫자 = px**, 단위 없는 문자열 = %(`"50"`), `"240px"`·`"40%"`·`rem`/`vh` 도 가능. `panelRef` 명령형 핸들 = `collapse()` · `expand()` · `getSize(): { asPercentage, inPixels }` · `isCollapsed()` · `resize(number | string)`.
    - `isCollapsed()` 는 **상태 플래그가 아니라 크기 비교**다(`collapsible && fuzzyEquals(collapsedSize%, 현재%)`). `collapse()` 는 현재 크기를 내부 `expandToSize` 에 적고 `collapsedSize` 로 가며, **`expand()` 는 `expandToSize ?? minSize` 로 복귀** — 접힌 채 마운트된 pane 은 그 기억이 없어 `minSize` 로 열린다. 그래서 펼침은 `resize(저장된 px)` 로 했다.
    - `Group.onLayoutChanged(layout, meta)` 의 `meta.isUserInteraction` 은 **포인터 드래그 릴리즈·리사이즈 키만 true**, 명령형 API·초기 마운트·제약 재계산·`defaultSize` 변경은 false. `Layout` 은 `{ [panelId]: 퍼센트 }`뿐이라 **px 는 `panelRef.getSize().inPixels`**(요소 offset)로만 얻는다.
    - Panel DOM 은 2겹이다 — 바깥 `div[data-panel][data-testid=id]`(`display:flex`, flex-grow/basis 계산분) + 안쪽 div(여기에 `className`/`style` 적용, `maxHeight/maxWidth:100%` · `flexGrow:1` · **`overflow:auto`**). 그래서 높이 채움은 className 의 `flex-1`(행축에 걸린다)이 아니라 **자식의 `h-full`** 로 했고(`pane-node-view.tsx` 선례), 테스트는 `data-testid`(=panel id)로 pane 을 집는다. Panel/Separator 는 Group 직계 자식이어야 하고 **조건부 렌더는 정상 지원**(커밋 0건이면 separator+Panel B 미렌더).
  - **미확인·후속**
    - ① **실기 확인 필요(라이트/다크 포함)**: happy-dom 에는 레이아웃이 없어 `@tanstack/virtual-core` 가 `outerSize === 0` 이면 빈 range 를 돌려준다 → **변경 행·커밋 행이 컴포넌트 테스트에서 한 줄도 마운트되지 않는다**(그래서 테스트는 pane 구조·설정 쓰기·포커스만 본다). 실제 리사이즈/접힘 지오메트리, sticky 유지, 상세 패널 1/2 상한, separator 히트 영역은 사용자 실기 대상이다.
    - ② e2e 는 계약대로 **실행하지 않았다**(`typecheck:e2e` 만 통과). 스펙 19 가 중간에 실패하면 접힌 `Changes` 가 이제 settings 에 남아 **재시작 후에도 접힌 채**다(`finally` 가 있지만 실행 시 유의).
    - ③ `groupResizeBehavior` 는 기본값(`preserve-relative-size`)을 썼다 — 사이드바 높이가 바뀌면 그래프 pane 이 비율을 유지하므로 저장된 px 와 일시적으로 어긋날 수 있다(다음 마운트에서 저장값 복원). 픽셀 고정이 필요하면 Panel B 에 `groupResizeBehavior='preserve-pixel-size'` 한 줄.
    - ④ **[내 변경과 무관·기존]** `src/widgets/git-panel/commit-detail-panel.test.ts` 는 **단독 실행 시 실패**한다(monaco worker `SyntaxError` — 이 파일이 `commit-detail-panel` → `layout.query` → `tab-path-change` 체인을 목 없이 정적 import). 전체 `bun test` 에서는 다른 파일이 먼저 `mock.module('@shared/lib/monaco/setup')` 을 걸어 통과한다(2435/0). 손대지 않았다.


- **메인 2차 검증 1차 (2026-09-15)** — 프론트: `bun run typecheck` 0 · `bun run lint` 0 error/11 기존 warning · `bun run format:check` 통과 · `bun test` 2435 pass/0 fail(244 파일) · `bunx vite build` exit 0 · `bun run typecheck:e2e` exit 0. Rust: `cargo fmt --check` 통과 · `clippy -D warnings` 0 · lib 테스트 1548 pass — 단 **T4 로케일 6키 미등재**(T4 가 Rust 금지 규칙으로 못 넣음)를 메인이 `locale/service.rs` `MESSAGE_NAMESPACES` 에 직접 등재(common 3·notification 3, 실행이 막힌 소수정 예외) → locale 19 pass. **`tests/domain_boundaries.rs` 1건 실패**(교차 참조 5건) → §1.E-보강으로 방향 확정, 검토 후 fixer wf 에서 수정. 워킹트리 78파일 +2353/-475.


- **렌즈 검토 (2026-09-15, wf `wf_ce04e34c`, sonnet·xhigh 3렌즈 + major 반박 2표)** — 발견 8: major 3(전부 반박 실패)·minor 5·info 0. 메인 판정: **전건 수용**.

| id | 심각도(표결) | 내용 | 판정·수정 방향 |
|----|------|------|------|
| R-1 | major(2/2 확증) | File 메뉴가 만든 도메인 간 실행 경로 5건 → `domain_boundaries` 테스트 실패 | §1.E-보강대로: 실행 경로 3건은 `lib.rs` 조립(이벤트 구독 + 메뉴 액션 디스패치)으로 제거, 데이터 제공자 2건은 화이트리스트 등재 |
| G-1 | major(major·minor 분열, 반박 실패) | `settings_update` 등이 language 변경 시 `apply_and_broadcast` 안(뮤테이션 가드 보유)에서 `refresh_app_menu`(디스크 전수 스캔) 호출 — §2.1 위반 | R-1 수정으로 자연 해소(`SettingsChanged` 구독 → `spawn_blocking`, 가드 밖). settings 커맨드의 직접 호출 삭제 |
| G-2 | major(major·minor 분열, 반박 실패) | git 섹션 접힘 토글이 settings 캐시 스냅샷 기반 IPC 왕복이라 연속 토글 시 lost-update | git-panel 이 **로컬 state 를 정본**(초기값 = settings, functional update)으로 두고 settings 는 write-through 미러로 — 최신 로컬 맵 전체를 저장. 테스트: 연속 토글 2회 후 마지막 patch 가 둘 다 반영 |
| G-3 | minor | `refresh_recent_menu` 의 remove/append 가 여러 main-thread 왕복이라 동시 갱신 시 인터리빙(코스메틱) | 재구성 시퀀스를 `run_on_main_thread` 클로저 1개 안에서 수행 |
| G-4 | minor | `resolve_terminal_path` 홈 조회가 HOME 단독 → USERPROFILE 폴백으로 넓어짐(계약 밖 동작 변화, 테스트 없음) | 의도된 통일로 수용 — `home::home_dir_env` 사용을 §3 에 명시 + USERPROFILE 폴백 단위 테스트 추가 |
| B-1 | minor | 인라인 `//` 주석 4곳(기록은 3곳) | `///` 문서 주석으로 이전 또는 삭제, §3 이탈 ⑧ 개수 정정 |
| B-2 | minor | `docs/debugging.md` §4.4 "debug 는 dev 빌드에서만" 이 실제 로그 설정과 불일치 | 문서를 실제 동작(빌드 무관 기록)으로 정정(게이팅 추가는 범위 밖) |
| B-3 | minor | 루트 밖을 가리키는 심링크 파일이 인덱스엔 뜨지만 열기는 root_guard 가 거부 | `command-palette.md` §3 에 비대칭 명시 + 회귀 테스트(루트 밖 심링크 열기 → Forbidden) |

- **검토 수정 R 완료 (2026-09-15)** — **R-1+G-1**(교차 참조 5건 제거·등재): `project/commands.rs` 4곳·`settings/commands.rs` 1곳의 `window::*` 직접 호출 삭제(close/activate 의 `drop(_guard)` 도 함께 제거 — 메뉴 재빌드 전용이었음), `lib.rs` 에 `listen_for_app_menu_refresh` 신설(`project:list-changed`·`project:activated` → `menu::refresh_recent_menu`, `settings:changed` → 직전 language 를 `parking_lot::Mutex` 로 기억해 실제 변경 시에만 `commands::refresh_app_menu`; 셋 다 `spawn_blocking` 으로 가드·이벤트 루프 밖에서 실행). `window/commands.rs::handle_menu_event` 는 순수 `menu::menu_action(id) → MenuAction`(`Quit`/`OpenRecent(ProjectId)`/`ClearRecent`/`Ignored`, `MENU_ID_QUIT` 도 `menu.rs` 로 이동)과 `commands::request_quit` 로 분리, `lib.rs` 의 `on_menu_event` → 신설 `dispatch_menu_action` 이 project 커맨드로 spawn 디스패치. `tests/domain_boundaries.rs` 에 데이터 제공자 2건(`window/menu.rs → project::service`·`locale::service`)만 사유 주석과 함께 등재(선례 `remote/login_page.rs → locale::service`). `fanout_remote_events!` 매크로·호출은 미변경(파리티 테스트 유지). **G-3**: `refresh_recent_menu` 가 디스크 읽기를 호출 스레드에서 끝내고 서브메뉴 조회·remove·append 를 `run_on_main_thread` 클로저 **하나** 안에서 수행(`replace_open_recent_items`/`rebuild_open_recent_items` 분리). **G-4**: `resolve_terminal_path` 는 `home::home_dir_env` 유지, 폴백 순서를 순수 `select_home_dir(home, user_profile)` 로 분리해 단위 테스트(HOME 우선·HOME 부재 시 USERPROFILE·둘 다 없으면 `None`) — `set_var`/`remove_var` 는 같은 테스트 바이너리의 `project::service` `~` 테스트와 경합하므로 쓰지 않았다. **B-1**: 인라인 `//` 주석은 **4곳**이었다(§3 기록의 "3곳" 정정 — `project/commands.rs` 2 · `settings/commands.rs` 1 · `lib.rs` 1). 앞 3곳은 R-1 로 코드와 함께 사라졌고, `lib.rs` 의 `set_menu` 순서 주석은 `commands::build_app_menu` 의 `///` 문서 주석으로 이전했다. 신규 코드도 `//` 를 쓰지 않았다(설계 근거는 전부 함수 `///`). **B-2**: `docs/debugging.md` §4.4 의 "debug 레벨은 dev 빌드에서만" 을 실제 동작으로 정정 — `lib.rs` 의 `tauri_plugin_log::Builder` 는 전역 레벨을 설정하지 않아 기본 `Trace` 이고(`tungstenite`/`tokio_tungstenite` 만 `Warn`), `cargo tree -e features -i log` 로 `release_max_level_*` 피처 0건 확인 → 설치본에도 남는다. **B-3**: `docs/features/command-palette.md` §3 "인덱스에 들어가는 것" 에 비대칭(루트 밖을 가리키는 심링크는 인덱스엔 뜨지만 `root_guard` 가 `Forbidden` 으로 거부, 루트 안 링크는 정상) 항목 추가 + `infra/root_guard.rs` 회귀 테스트 1건(unix — 루트 안 심링크 → 루트 밖 파일 → `ensure_within_root`·`resolve_owning_project` 둘 다 `Forbidden`). 문서: `features/window-chrome.md` §7.1~§7.4 를 lib.rs 조립 구조로 재작성(§7.2 클릭 디스패치·§7.3 이벤트 구독 갱신 신설), `ipc-contract.md` `project_forget_recent` 절의 "메뉴 재구성" 주체를 리스너로 정정, `architecture.md` §2 의 "이벤트 버스 미실현" 문장에 조립부 `listen_any` 구독(원격 팬아웃·메뉴 갱신) 실사를 반영.
  - **테스트** — Rust 신규 4건(`domain::window::menu` 2 — `menu_action` 매핑·무시 대상, `infra::home` 1 — 폴백 순서, `infra::root_guard` 1 — 루트 밖 심링크 거부). `cargo fmt --all --check` 통과 · `cargo clippy --workspace --all-targets -- -D warnings` **경고 0** · `cargo test --workspace` **1582 pass / 0 fail**(lib 1552 · capability_symmetry 4 · **domain_boundaries 3 — 이전 실패 해소** · session_restore 6 · cli 17). 프론트: `bun run typecheck` exit 0 · `bun run lint` 0 error / 11 warning(전부 기존 `useVirtualizer`) · `bunx prettier --check`(만진 md 5파일) 통과. `src/shared/api/bindings.ts` 는 커맨드 표면 무변경이라 재생성 후에도 diff 동일(+56).
  - **이탈** — ① `MenuAction::OpenRecent` 는 계약 §1.E-보강의 `OpenRecent(root)` 대신 **`OpenRecent(ProjectId)`** 다. root 를 담으려면 순수 함수가 디스크(`recent_project_root`)를 읽어야 하는데 메뉴 이벤트 핸들러는 메인 스레드에서 돌아 그 자리에서 IO 를 할 수 없다 — root 해소는 `lib.rs` 가 spawn 한 태스크 안에서 한다(클릭 시점 재조회라는 기존 의도도 그대로다). ② `MENU_ID_QUIT` 을 `window/commands.rs` → `window/menu.rs` 로 이동(순수 id→행동 표가 모든 id 를 알아야 한다. 메뉴 id 상수가 한 모듈에 모인다). ③ `project_close`/`project_activate` 의 `drop(_guard)` 2줄 삭제 — 메뉴 재빌드를 가드 밖으로 빼기 위한 장치였고, 호출이 사라지면서 함수 끝 자동 drop 과 동일해졌다. ④ `settings/commands.rs` 는 `refresh_app_menu` 호출 삭제로 **HEAD 와 동일해졌다**(d-58 이전 상태 — git status 에서 빠진다). ⑤ `architecture.md` 수정은 브리핑이 지정한 2개 문서(window-chrome·ipc-contract) 밖이지만, R-1 이 바꾼 것이 바로 그 문서가 정의한 "도메인 간 연동은 조립부가 배선한다" 규칙의 실사여서 1문장만 갱신했다.
  - **미확인** — 네이티브 메뉴 실동작(File 메뉴 클릭·Clear Recent·언어 변경 후 라벨·최근 목록 실시간 갱신)은 앱 실행이 금지 범위라 **사용자 실기 필요**. 특히 이벤트 구독 경로로 바뀐 갱신 타이밍과, `spawn_blocking` 전환 후에도 갱신이 눈에 띄는 지연 없이 도는지를 확인한다.
- **검토 수정 T 완료 (2026-09-15)** — **G-2**: `src/widgets/git-panel/git-panel.tsx` 의 접힘 맵을 로컬 `useState` 정본으로 전환(초기값 `toGitSectionCollapsedMap(settings?.gitSectionsCollapsed)`, 늦게 도착한 settings 는 `storedCollapseAdoptedRef` 가드로 사용자 토글 전 1회만 채택, 헤더 토글·드래그 접힘 모두 `collapsedSectionsRef`(최신 맵)에서 다음 맵을 계산해 로컬 적용 + 그 맵 전체를 `gitSectionsCollapsed` 로 write-through, 그래프 pane 의 `panelRef` collapse/expand 동기화는 로컬 맵을 따름) / `src/widgets/git-panel/git-panel.test.tsx` +3건(연속 토글 2회 → 마지막 patch 에 `['stashes','graph']` 둘 다 · 토글 전 늦은 settings 1회 반영 · 토글 뒤 도착한 settings 는 로컬 미덮어씀) → 파일 15 pass, 전체 `bun test` **2438 pass / 0 fail**(244파일), `bun run typecheck` exit 0, `bun run lint` **0 error / 11 warning**(전부 기존 `useVirtualizer`), `bunx prettier --check`(만진 3파일) 통과 / `docs/features/git.md` §2.1 영속화 서술 1줄 정정(로컬 정본 + settings 미러) / **이탈** ① 계약의 "functional setState" 대신 `collapsedSectionsRef` 기반 — updater 안에서 `updateSettings` 를 부르면 `main.tsx` 의 StrictMode 가 updater 를 2회 실행해 IPC 가 중복되고 updater 순수성 규칙을 깬다(동일 보장: 같은 태스크 연속 토글도 최신 맵에서 계산) ② 테스트는 계약 2건 + cold start 채택 1건(once-sync 가드의 반대 방향이 없으면 회귀를 못 잡음) ③ `renderPanel(null)` 은 settings 쿼리를 `enabled: false` 로 고정 — `settings.ipc` 의 `mock.module` 승자가 파일 순서에 따라 달라져 격리 실행만 통과하고 전체 스위트에서 실패했다.


- **메인 2차 검증 최종 (2026-09-15, 검토 수정 반영 후)** — `bun run verify` exit 0(typecheck 0 · lint 0 error/11 기존 warning · prettier 통과 · bun test 2438 pass · cargo fmt/clippy 0 · cargo test lib 1552 + domain_boundaries 3 + session_restore 6 + cli 17 + capability_symmetry 4, 전부 0 fail) · `bunx vite build` exit 0 · `bun run typecheck:e2e` exit 0. 커밋 10분할 → dev 푸시 → main ff. 사용자 실기 대상: 메뉴 정렬·실시간 검색(한글 조합 포함)·Welcome 터미널 버튼·+ 메뉴(경로/Finder/최근)·File > Open Recent·Clear Recent·언어 변경 후 메뉴 라벨·알림 문구·git 그래프 pane 리사이즈/접힘·퀵오픈(한글 파일명·심링크·보조 창 안내).

## 4. 후속

- G5 하드 제외 디렉토리 정책(결정 문서 §2.1 회신 후).
- 메뉴바 기존 문자열(TAIDE/Edit/Window/Quit) 로케일화.
- 팔레트 MRU 정렬(문서가 약속했던 동작) — 실수요 확인 후.
- 알림 카테고리 "입력 대기" 분리 + 권한/질문 구분(웨이브 3 d-60 예정).
- 보조 창 팔레트(웨이브 5).
