# d-62 — 사용성 배치 5 웨이브 5: 프로젝트 split view(셸 슬롯 분할) · 프로젝트 그룹 — 설계 계약 (2026-09-15, 구현은 3·4 머지 후)

> 사용자 항목 3·12("프로젝트 묶음 및 한 화면에 여러 프로젝트 표시 — 프로젝트를 드래그해 상하좌우로 split"). 결정 전제: `acknowledge/2026-09-15-usability-batch5-user-decisions.md` §1 #4(완전한 프로젝트 셸 단위 분할, 이름 있는 그룹) · §3 #3(2단계·ProjectGroup 엔티티·`shell_slots` 가산·그룹 열기 첫 멤버 즉시 + 백그라운드 큐) · §4(구현은 웨이브 3·4 머지 후).
> 조사 정본 `research/2026-09-15-batch5-research.md` T2. 상태 소유 원칙 ADR-0004(Rust 소유·view 표시 전용), `docs/features/layout-shell.md` §7(보조 창), `docs/data-model.md` §2·§5(무마이그레이션 필드 추가 규칙)·§20(ProjectDisplay 선례).

## 0. 현행 (T2 — 메인이 파일 위치 재확인)

| 사실 | 근거 |
|------|------|
| "활성 프로젝트" 정본은 `SessionState.active_project: Option<ProjectId>` 1필드. Rust 는 이미 `projects`·`layouts` 를 `HashMap<ProjectId, _>` 로 N개 동시 보유 | `src-tauri/src/domain/project/types.rs:94`, `src-tauri/src/state.rs:35-36` |
| 프론트에서 전역 활성 세션(`activeProjectQueryOptions`)을 읽는 비-테스트 파일 6개: app-shell·title-bar-content·status-bar-content·command-palette·task-runner-dialog·ide-sync-provider. **패널 위젯(Explorer·EditorArea·GitPanel·SearchPanel·PaneNodeView·EditorPane)은 전부 `projectId` prop 구동** → 멀티 인스턴스화 가능 | `src/entities/project/project.query.ts:30`, `src/widgets/app-shell/app-shell.tsx:219-250` |
| 보조 창 = 프로젝트 고정(`?projectId=&windowSlot=`)·`ProjectActivated` 무시·에디터 전용 셸(explorer·git·search·팔레트·TaskRunner 없음 — Wave I F1 미결). 레이아웃은 `ProjectLayout.auxiliary_windows: Vec<AuxWindowLayout>` | `src/shared/lib/window-context.ts:13-34`, `src/widgets/auxiliary-window-shell/*`, `src-tauri/src/domain/layout/types.rs:134-140,207`, `src/app/app.tsx:22-81` |
| 창 단위 싱글턴 3종이 병목: fire-and-forget 브리지(`shared/lib/bridge/*` — 사이드바 토글·뷰 전환·reveal·rename·search 열기), 전역 키맵(`use-global-keymap` + document capture), `getWindowContext()`(`location.search` 파생, 창 안 분할 개념 없음) | T2 Q1 |
| root_guard 는 열린 프로젝트 전체를 훑어 가장 긴 canonical root 를 고름 → 다른 프로젝트 소유 경로도 열기·저장 통과. 프로젝트 스코프 자원(미러·트리·검색)은 인자 `projectId` 로 검증 | `src-tauri/src/infra/root_guard.rs:24-57` |
| LSP 세션은 창(owner) 단위 격리, pty 는 멀티 attach 브로드캐스트, hot-exit 미러는 projectId 별 → 슬롯 분할에서 무변경 | `docs/features/layout-shell.md` §7.4, `docs/data-model.md` §2·§6·§16 |
| 그룹 개념 0. 사이드바 = `session.projects` 1차원 + dnd-kit `verticalListSortingStrategy`. 순서 변경 `project_reorder`. `ProjectDisplay{icon,label,color}` 가 `#[serde(default)]` 무마이그레이션 필드 선례 | `src-tauri/src/domain/project/service.rs:155-177`, `src/widgets/app-sidebar/app-sidebar.tsx:40-76`, `types.rs:26,52-60` |
| `project_open` 은 capability attach 를 await(응답 시간 불변) → 그룹 열기 직렬은 멤버 수 × 열기 시간. 부팅 복원은 `restore_project_watchers` 순차·활성 우선·중단 가능 | `src-tauri/src/domain/project/commands.rs:85-95,343-382` |
| 탭 분할 드롭존 선례 `src/features/split/split-drop-zones.tsx`(4방향), 레이아웃 `PaneNode` split 트리 | `src/features/split/*`, `layout/types.rs:116` |
| 보조 창은 `tauri-plugin-window-state` 추적 제외(위치·크기 복원 없음) | `layout-shell.md` §7.6 |


## 0.1 설계 사전 검토 반영 (2026-09-15, wf `wf_fefded11`, sonnet·xhigh 2렌즈 — major 10·minor 4·info 2, 전건 수용)

| id | 지적 | 계약 보정 |
|----|------|----------|
| S-1 | 슬롯 트리 축약이 프론트 후속 IPC 라 close 와 원자성 없음·부팅 복구 부재 | **`close_project` 서비스 안에서** 프로젝트 제거 + 세션 목록 정리 + `shell_slots` 축약 + `focused_slot` 재계산을 한 뮤테이션·한 저장으로. `ProjectClosed` 와 `SessionShellSlotsChanged` 를 같은 커맨드에서 emit. 부팅 복원(`restore_state`)에 dangling leaf 정규화(세션에 없는 projectId 리프 제거, 빈 트리면 active 로 리프 1개) |
| S-2 | 그룹 백그라운드 열기가 `project_open` 재사용 시 매번 활성화·`ProjectActivated` 발화 | `open_project` 에 `activate: bool` 인자(기존 커맨드는 `true`). 그룹 큐는 첫 멤버만 `true`, 나머지 `false`(세션 목록 추가·워처·capability 만, 활성·이벤트 없음 — `ProjectListChanged` 만) |
| S-3 | 같은 프로젝트 다중 슬롯 금지가 드롭존에서만 강제 | `project_open_in_slot` 서비스가 다른 리프에 같은 projectId 가 있으면 `AppError::InvalidArgument`(로케일 키 `error.shellSlot.projectAlreadyInSlot`) 로 거부. 드롭존 거부는 UX 선검사 |
| S-4 / U-2 | EditorArea 약 20개·TerminalPane 의 `useGlobalKeymap` 핸들러가 슬롯 수만큼 복제 → ⌘S/⌘W 동시 발화. when 컨텍스트도 전역 activeElement | **슬롯 포커스 게이트**: `ShellSlotContext` 의 `isFocused` 로 EditorArea·TerminalPane 의 모든 `useGlobalKeymap` 핸들러를 `isFocused ? fn : undefined` 게이팅(terminal-pane 선례). 리스너 등록 위치는 현행(컴포넌트별) 유지 — 게이트가 동시 발화를 막는다. 규모에 반영 |
| U-1 | `editor-pane-command-bridge`·`active-editor-actions-bridge` 등 EditorArea/TerminalPane 이 구독하는 브로드캐스트 브리지가 slotId 필터 대상에서 빠짐 | 구독 측 전수 게이팅: EditorArea·TerminalPane 이 구독하는 모든 브리지(editor-pane-command·active-editor-actions·explorer-panel/reveal/rename·search-panel·terminal 관련)는 `isFocused` 일 때만 처리. 발행 측은 무변경(포커스 슬롯만 반응하므로) — 단 "특정 슬롯 지정" 이 필요한 발행(외부 파일 열기·CLI 진입)은 `slotId` 를 실어 보내고 구독자가 자기 것만 처리 |
| U-5 | 발행부 12곳+·순수 함수 run() 은 포커스 슬롯을 모름 | 위 구독 측 게이팅으로 발행부 개조를 회피. `CommandContext` 에 `focusedShellSlotId`(읽기)만 추가 — 팔레트·TaskRunner 가 프로젝트를 고를 때 사용 |
| S-5 | 사이드바 클릭(`project_activate`)의 슬롯 의미 부재 | `activate_project`: 대상이 어느 슬롯에 있으면 그 슬롯을 `focused_slot` 으로; 없으면 **포커스 슬롯의 프로젝트를 교체**(기존 단일 슬롯 동작과 동일). 원격 dispatch 도 같은 서비스 경유 |
| S-6 / U-3 | `ShellViewState`(zen·sidebarCollapsed) 가 프로젝트별인데 사이드바 레일은 공용 → 포커스 전환 시 깜빡임. Zen·Problems 패널 상태 미정의 | **재분배**: 창 크롬 축(zen·사이드바 아이콘 레일 접힘·상태바)은 `SessionState.window_chrome`(무마이그레이션 가산)으로 이동, 슬롯 로컬 축(explorer 패널 접힘·Problems 열림)은 `ProjectLayout.shell_view` 유지. Zen = 포커스 슬롯만 남기고 나머지 슬롯 숨김(트리 보존). Problems 토글(상태바)은 포커스 슬롯의 `ProjectShell` 상태를 제어(컨텍스트 콜백) |
| U-4 | 사이드바 DndContext 와 EditorArea 탭 DndContext 가 분리돼 사이드바→슬롯 드롭 불가 | **AppShell 최상위 프로젝트 전용 DndContext** 가 사이드바 재정렬(기존)과 슬롯 드롭존을 함께 담고 `active.data.current.type`('project') 으로 분기. EditorArea 의 탭 DndContext 는 중첩 유지 — 중첩 동작은 구현 초기 스파이크 테스트로 확인([미확인]); 불가 시 사이드바→슬롯 드래그는 pointer 이벤트 오버레이로 대체 |
| U-6 | 그룹 편입 조작 부재 | 컨텍스트 메뉴 "그룹에 추가 ▸"(그룹 목록 서브메뉴)·"그룹에서 제거" 추가. 크로스 컨테이너 드래그는 범위 외 |
| U-7 | 포커스 슬롯 전달 경로 미정 | React Context 단일 경로(앱 루트 Provider, DOM 캡처 갱신). `session_focus_slot` 은 영속화 용도 |
| S-7 | `forget_recent` 가 그룹 멤버를 못 지움·큐의 결손 멤버 정책 | `forget_recent_projects(&mut session)` 로 확장해 `groups[].members` 정리 + 저장. 그룹 열기 큐는 결손 멤버 스킵 + `log::warn` + 진행 |
| S-8 / U-8 | `windowSlot`(보조 창) 과 명칭 충돌 | 신규 개념은 **`ShellSlotId`/`shellSlot`** 로 명명, 문서에 구별 표기 |

**규모 재추정**: 2단계는 L → **XL**. 단계 재편: **2a**(Rust: 슬롯 트리·window_chrome·activate/close/open 의미 + 프론트: 슬롯 트리 렌더·포커스 컨텍스트·키맵/브리지 게이팅·Zen/Problems 재배치, 분할은 메뉴/커맨드 "오른쪽/아래에 열기" 로) → **2b**(사이드바 드래그 분할 DndContext 통합) → **2c**(그룹). 각 단계 끝에 렌즈 검토.

## 1. 설계

### 1.A 데이터 모델 (Rust, 무마이그레이션 가산)

- `SessionState` 에 `#[serde(default)] shell_slots: ShellSlotTree`(비면 `active_project` 단독 해석 = 슬롯 1개). `ShellSlotTree = { node: 'leaf', project_id } | { node: 'split', dir: Horizontal|Vertical, children: [tree, tree], sizes }` — 레이아웃 `PaneNode` 와 같은 모양(재사용 가능하면 제네릭화하지 않고 별도 타입, 이유: 슬롯 리프의 페이로드는 탭 목록이 아니라 프로젝트). `active_project` 는 유지하되 의미를 **포커스 슬롯의 프로젝트**로 재정의(하위 호환: 슬롯 트리가 없으면 그대로). `focused_slot: SlotId`.
- `ProjectGroup { id, name, color: Option<ProjectColorToken>(ProjectDisplay 팔레트 재사용), members: Vec<ProjectId>, collapsed: bool }`, `SessionState.groups: Vec<ProjectGroup>`(`#[serde(default)]`). **`session.projects` 가 열림 여부·전역 순서의 단일 진실**, `members` 는 소속 집합. 프로젝트 close 시 멤버십은 유지(그룹은 "열 수 있는 것" 의 조직화)하되 레코드(`projects/<id>/`)가 forget 되면 멤버에서 제거. 사이드바는 그룹 헤더 아래 멤버 순서 = `session.projects` 순서로 필터.
- IPC: `session_set_shell_slots(tree)` · `session_focus_slot(slot)` · `project_open_in_slot(path|project_id, target_slot, edge: Left|Right|Top|Bottom|Replace)` · `project_group_create/rename/set_color/delete/set_members/reorder` · `project_group_open(id)`. 이벤트: `SessionShellSlotsChanged`·`ProjectGroupsChanged`. dispatch 테이블·bindings·ipc-contract.
- 그룹 열기: 첫 멤버 `project_open` 동기 → 나머지는 `restore_project_watchers` 패턴의 백그라운드 순차 큐(`is_shutting_down` 중단 가능), 진행은 `ProjectListChanged` 로 자연 반영. 그룹 열기 = 사이드바 조직화만(슬롯 자동 배치 없음, 결정 §3 #3).

### 1.B 프론트 — 셸 슬롯 (L)

- `AppShell` → 공용 `AppSidebar` + `ShellSlotTreeView`(react-resizable-panels 중첩 Group, 선례 `pane-node-view.tsx`) → 리프마다 `ProjectShell{projectId, slotId}`(Explorer Panel + EditorArea + 터미널/Problems 하단 — 현재 app-shell 의 프로젝트 부분을 그대로 컴포넌트화). 슬롯 트리 크기는 세션에 영속(`sizes`, 120ms 디바운스 — `pane-resize-commit.ts` 선례).
- **포커스 슬롯 컨텍스트** `ShellSlotContext{slotId, projectId}`: 브리지 3계열(`explorer-panel/reveal/rename-bridge`·`search-panel-bridge`)에 `slotId` 채널 필터(publish 시 포커스 슬롯 id 를 싣고 구독자는 자기 슬롯만 처리), 전역 키맵 핸들러(⌘B·⌘⇧F·⌘P 등)는 포커스 슬롯의 프로젝트에 적용(`useGlobalKeymap` 등록은 슬롯 셸이 아니라 앱 1곳에서, 포커스 슬롯 컨텍스트를 읽어 위임). 포커스 판정 = 마지막 pointerdown/focusin 이 속한 슬롯(DOM 이벤트 캡처 1곳). `getWindowContext()` 는 창 단위 그대로.
- 전역 활성 세션 소비 6파일: title-bar·status-bar·command-palette·task-runner·ide-sync 는 **포커스 슬롯 프로젝트**를 읽도록(컨텍스트 또는 `session_focus_slot` 이벤트). `activeProjectQueryOptions` 는 "포커스 슬롯 프로젝트" 의미로 유지.
- 드래그 분할: 사이드바 프로젝트 아이콘을 드래그해 슬롯 영역에 드롭 → `split-drop-zones.tsx` 와 같은 4방향 드롭존(+ 가운데 = 교체) → `project_open_in_slot`. 같은 프로젝트를 두 슬롯에 띄우는 것은 **1차 금지**(hot-exit 미러 prune sweep 상호작용 [미확인] — T2 리스크) → 드롭 거부 토스트.
- 슬롯 닫기: 슬롯 헤더 X 또는 프로젝트 닫기 → 트리 축약(형제가 부모 자리로). 마지막 슬롯은 항상 존재(프로젝트 0개면 Welcome).
- Welcome·빈 pane 규칙은 슬롯별 pane 트리에 그대로.

### 1.C 프론트 — 그룹 (M)

- 사이드바: 그룹 헤더(이름·색·접기) + 멤버 아이콘, dnd-kit 2단 `SortableContext`(그룹 순서 / 그룹 내), 그룹 밖 프로젝트는 "미분류" 로 하단. 컨텍스트 메뉴: 새 그룹·이름/색 변경·그룹 열기·그룹에서 제거·그룹 삭제. 그룹 생성 다이얼로그(`project-display-dialog` 패턴). 로케일 키 3종.
- Welcome 최근 목록·File > Open Recent 는 그룹과 직교(변경 없음).

### 1.D 1단계 — 보조 창 완성 (S, 선행)

- `app.tsx` aux 분기에 `ExplorerContainer`/`GitPanelContainer`/`SearchPanelContainer` 를 `projectId={windowContext.projectId}` 로 마운트, `CommandPalette`·`TaskRunnerDialog` 를 `projectId` prop 으로 좁힘(Wave I F1 종결). 보조 창의 위치·크기 복원(§7.6 제외)은 유지.

### 1.E 범위 외

- 같은 프로젝트를 여러 슬롯에(공유 탭 동기화), 슬롯 간 탭 드래그 이동(프로젝트가 다른 pane 트리 — d-55 ‘Copy into New Window’ 와 같은 급), 그룹 열기 = 슬롯 자동 배치, 보조 창 위치 복원.

## 2. 실행 계획 (3·4 머지 후)

1. **설계 검토 wf(sonnet·xhigh 2렌즈: 상태 소유·동시성 / UI 상호작용)** 로 이 계약을 먼저 검토(구현 전 major 는 계약 수정).
2. 구현 wf(opus·xhigh, §0.1 재편): **1단계 F1**(1.D 보조 창 완성) → **2a R**(Rust 단일: 슬롯 트리·window_chrome·activate/close/open(activate 옵션)·open_in_slot 검증·forget 그룹 정리·복원 정규화·이벤트·dispatch·bindings) → **2a F2**(슬롯 트리 렌더·ShellSlotContext·EditorArea/TerminalPane 키맵·브리지 게이팅·Zen/Problems 재배치·"오른쪽/아래에 열기" 메뉴) → 렌즈 검토 → **2b F3**(DndContext 통합·드롭존, 스파이크 선행) → **2c R2+F4**(그룹 엔티티·IPC·UI) → 렌즈 검토. 테스트: Rust 슬롯 트리 연산(split/remove/축약)·그룹 CRUD·멤버십 정리, TS 브리지 필터·포커스 판정·드롭존·2단 정렬.
3. 렌즈 검토 wf(3렌즈 + major 반박 2표) → 메인 2차 verify·vite build → 분할 커밋 → dev 푸시 → main ff → 사용자 실기(2 프로젝트 좌우 분할·포커스 전환 시 ⌘B/⌘⇧F 대상·그룹 열기).

## 3. 구현 기록

<!-- d62-record-design-review -->
- **§1.A + §0.1 2a Rust 단일 완료 (2026-09-15)** — 변경 파일: `src-tauri/src/ids.rs`(`ShellSlotId`=`shellslot-<uuid>`, 보조 창 `windowSlot` 과 구별 — §0.1 S-8) · `domain/project/types.rs`(`ShellSlotTree`{split{dir,children[2],sizes}|leaf{slotId,projectId}} — `dir` 은 `layout::types::SplitDir` 재사용, split 노드에는 id 없음 / `ShellSlotEdge`{left,right,top,bottom,replace — `DropEdge` 의 `center` 없음} / `WindowChrome`{zen,sidebarRailCollapsed}·`WindowChromePatch` / `SessionShellState` / `OpenProjectInSlotRequest` / `SessionState` 3필드 `#[serde(default)]` 가산 + `active_project` 의미를 "포커스 슬롯의 프로젝트"로 재정의) · **신규** `domain/project/shell_slots.rs`(순수 트리 연산: `leaf`/`leaves`/`leaf_count`/`first_slot`/`slot_of_project`/`project_of_slot`/`set_slot_project`/`split_slot`(4방향)/`remove_slot`/`remove_project`/`retain_projects`(공통 `prune` — 자식 1개 남은 split 은 그 자식으로 축약)/`set_sizes`(자식 인덱스 경로 주소)) · `domain/project/mod.rs` · `domain/project/service.rs`(`open_project` 에 `activate: bool` 추가(S-2, 기존 호출부 전부 `true`) / `close_project` 가 같은 뮤테이션에서 프로젝트 제거+세션 목록 정리+슬롯 축약+포커스 재계산(S-1) / `activate_project` 슬롯 의미(S-5: 있으면 그 슬롯 포커스, 없으면 포커스 슬롯 교체) / `normalize_shell_slots`·`reconcile_focus`(복원 정규화 — 결손 리프 제거·빈 트리 재구성·포커스/활성 재계산) / `ensure_slot_placement_allowed`(S-3 서버측 중복 거부, `error.shellSlot.projectAlreadyInSlot`; replace 는 대상 슬롯 자신이면 허용, 방향 분할은 대상 포함 전 리프 검사) / `place_project_in_slot`·`close_shell_slot`(마지막 슬롯 거부)·`focus_shell_slot`·`set_shell_slot_sizes`·`set_window_chrome`·`shell_state`·`find_open_project_by_root`(열기 전 중복 판정용) / `promote_legacy_window_chrome`(S-6 부팅 1회 승격 + 원본 축 비우기) / `restore_session` 이 정규화 호출) · `domain/project/commands.rs`(신규 커맨드 6종 `project_open_in_slot`·`shell_slot_close`·`session_get_shell_state`·`session_focus_shell_slot`·`session_set_shell_slot_sizes`·`session_set_window_chrome`, `emit_shell_slots_changed` 헬퍼(세션 read 락을 emit 전에 해제), `project_open`/`project_activate`/`project_close` 에 `SessionShellSlotsChanged` 동반 emit, `restore_state` 에 창 크롬 승격+`dirty_layouts` 등록+세션 저장) · `events.rs`(`SessionShellSlotsChanged{tree,focused}`·`WindowChromeChanged{chrome}` — 25→27종) · `lib.rs`(`collect_commands!` 6종·`collect_events!`/`fanout_remote_events!` 2종·이벤트 수 단언 25→27) · `domain/remote/dispatch.rs`(`IMPLEMENTED_JSON_COMMANDS`·`REMOTE_ALLOWED_COMMANDS` 6종 등재(=`project_open`/`project_activate` 와 같은 등급, 거부 테이블 무변경)·match arm 6종·허용 테이블 doc 카운트 정정 159/158→167/166 — 기존 주석이 2 어긋나 있었다) · `domain/locale/service.rs` `MESSAGE_NAMESPACES` + 로케일 3종(`error.shellSlot.projectAlreadyInSlot` 자기 키만 Edit) · `src/shared/api/bindings.ts`(재생성) · 문서 `docs/data-model.md`(§2 `session.json` 필드·§3 `SessionState`/`ShellSlotTree`/`WindowChrome` 타입·신설 §22)·`docs/ipc-contract.md`(신설 "session — 셸 슬롯·창 크롬" 절 + project 절 상호참조 + 말미 "d-62 2a" 절). / 테스트: 신규 Rust 31건 — `shell_slots` 유닛 11건(4방향 분할의 축·순서, replace 는 모양 불변, 없는 슬롯 분할은 트리 무변경, 슬롯 닫기 축약, 중첩 분할 축약, 마지막 리프 제거, 프로젝트별 전 리프 제거, 복원 retain, 전부 닫힘, 크기 경로 지정, 잘못된 경로·개수 거부) + `project::service` 유닛 19건(구버전 세션의 단일 리프 해석, 프로젝트 0개, 복원 정규화, activate 2의미 + 미개방 거부, close 축약·비포커스 close, 마지막 슬롯 거부, 슬롯만 닫기, 중복 거부(로케일 키·kind 검증)·replace 예외·없는 대상 슬롯, 배치 후 포커스/활성, 크기 영속 왕복, 창 크롬 승격 3건·패치 1건) + `tests/session_restore.rs` 통합 1건(슬롯 분할이 `session.json` 왕복으로 복원되고, 세션에서 사라진 프로젝트의 리프는 정규화로 제거). 측정: `cargo fmt --all -- --check` exit 0 · `cargo clippy --workspace --all-targets -- -D warnings` exit 0 · `cargo test --workspace` 1665 passed / 0 failed(lib 1634 + capability_symmetry 4 + **domain_boundaries 3** + session_restore 7 + taide-cli 17) · bindings 재생성 후 `bun run typecheck` exit 0(수정 불필요) · `bun run lint` 0 error(기존 warning 11건, 내가 만진 파일엔 없음) · `prettier --check` 만진 TS/JSON 전부 통과(docs·bindings.ts 는 `.prettierignore` 대상) · `bun test` 2608 pass / 0 fail. / 이탈: ① 계약 §1.A 의 `session_set_shell_slots(tree)`(트리 통째 쓰기)는 만들지 않고 `session_set_shell_slot_sizes(path, sizes)` 로 좁혔다 — 배정 문언이 "디바운스된 크기"만 요구하고, 클라이언트가 트리 전체를 써 넣게 하면 projectId·슬롯 id 까지 임의로 바꿀 수 있어 서버 불변식(중복 금지·세션 목록 일치)을 매 호출 재검증해야 한다. split 노드에 id 가 없으므로(계약이 지정한 모양) 주소는 루트부터의 자식 인덱스 경로로 잡았다. ② 계약에 없던 query `session_get_shell_state()` 를 추가했다 — 두 이벤트는 전환 시점에만 오므로 이것이 없으면 갓 뜬 창이 현재 배치를 알 방법이 없다(`project_get_active` 가 `project:activated` 때문에 추가된 것과 같은 이유). ③ 계약에 없던 mutation `session_set_window_chrome(patch)` 를 추가했다 — `WindowChromeChanged` 이벤트에 발행자가 없으면 필드가 죽은 상태로 남는다. ④ `shell_slots` 는 `Option<ShellSlotTree>` 다(계약 문언은 비-Option) — `ShellSlotTree` 는 리프에 projectId 가 필수라 "프로젝트 0개" 를 표현할 기본값이 없고, `None` 이 계약의 "비어 있으면 active_project 단독 리프로 해석" 을 그대로 담는다. ⑤ 창 크롬 승격은 **원본(`ProjectLayout.shell_view.zen`/`sidebar_collapsed`)을 비운다** — 안 비우면 사용자가 Zen 을 껐을 때 `window_chrome` 이 다시 기본값이 되어 다음 부팅에 낡은 값이 되살아난다("부팅 1회" 가 깨진다). 필드 자체는 계약대로 남겼고 비워진 레이아웃은 `dirty_layouts` 로 기존 2초 flush 에 맡겼다. ⑥ 데이터 모델 문서 절 번호는 계약이 적은 §21 이 아니라 **§22** 다(§21 은 d-60 이 이미 쓰고 있다). ⑦ `docs/features/layout-shell.md` 는 건드리지 않았다 — 배정 문언의 문서 목록(ipc-contract·data-model)에 없고 §1.B 프론트(F2) 범위라 동시 편집 충돌을 피했다. ⑧ 도메인 경계: 새 참조는 `project::{types,service,shell_slots} → layout::types`(`SplitDir`·`ShellViewState`) 뿐이고 `::types` 는 구조적 허용이라 화이트리스트 추가가 없다(`domain_boundaries` 3건 통과). ⑨ **창 크롬 승격은 값을 잃는다(렌즈 검토 S-102 로 사후 기록)** — `promote_legacy_window_chrome` 은 활성 프로젝트(없으면 목록 순서 첫 보유자)의 값 **1벌만** 세션으로 올리고, 승격 시점에 열려 있던 나머지 프로젝트의 레거시 `zen`/`sidebar_collapsed` 는 병합 없이 함께 비운다. 두 축이 창 단위 단일 값이 된 이상 N 벌을 무손실로 합칠 방법이 없고, OR 병합은 "한 프로젝트만 Zen 이었는데 부팅하니 창 전체가 Zen" 이라는 더 놀라운 결과가 된다 — 되돌릴 값이 UI 토글 2개뿐인 1회성 이행이라 마이그레이션 없이 수용했다. 승격 시점에 세션에 없던(닫혀 있던) 프로젝트의 `layout.json` 은 비워지지 않아, 사용자가 두 축을 모두 기본값으로 되돌린 뒤 그 프로젝트를 열면 다음 부팅에 낡은 값이 승격될 수 있다(이행 창 한정). `docs/data-model.md` §22 에 명시하고 `docs/quality-assurance/2026-08-11-qa6-checklist.md` 에 실기 1항목을 추가했다. / 미확인·후속: ① 앱 실행 금지라 **런타임 실기 미확인** — 실제 분할 화면·포커스 전환·Zen 승격은 F2 배선 후 확인이 필요하다. ② `project_open_in_slot` 이 `path` 로 새 프로젝트를 열 때 capability attach 는 `project_open` 과 같이 뮤테이션 밖에서 await 하고 실패 시 `project_close` 로 되감는데(슬롯도 함께 사라진다), 이 실패 경로는 유닛 테스트 하네스(`tauri::test` mock app 부재)로 덮지 못했다. ③ `SessionShellSlotsChanged` 는 창 구분 없이 브로드캐스트한다 — 보조 창이 이 이벤트를 무시해야 하는지는 F2 판단이다. ④ 그룹(§1.C·2c)·사이드바 드래그 분할(2b)은 범위 밖이라 `forget_recent_projects` 는 무변경이고 `ProjectGroup`/`ProjectGroupsChanged` 는 아직 없다. ⑤ 슬롯 트리는 `session.json` 1개 파일에만 살고 부팅 정규화는 저장하지 않는다(다음 뮤테이션이 저장) — 구버전 세션은 첫 슬롯 변경 전까지 부팅마다 새 `ShellSlotId` 를 받는다(ID 는 영속 핸들이 아니라 세션 내 주소).
<!-- d62-record-F -->
- **§1.D 1단계 보조 창 완성 (2026-09-15)** — 변경 파일: `src/widgets/auxiliary-window-shell/auxiliary-window-shell.tsx`(사이드바 없는 셸에 접힘 가능한 `ExplorerContainer` 패널 + `PaneSeparator` + `EditorArea` 의 `Group` 구성, `toggle-sidebar`/`explorer`/`git` 전역 키맵 + search/view/reveal/rename 브리지 펼침 구독, `palette.mainWindowOnly` 토스트 제거) · `src/widgets/command-palette/command-palette.tsx`·`src/widgets/task-runner/task-runner-dialog.tsx`(`activeProjectQueryOptions` 자기-읽기 → `projectId: ProjectId | null` prop) · `src/app/main-window-dialogs.tsx`(신규 — 메인 창에서 활성 프로젝트를 읽어 두 다이얼로그에 주입. `App` 은 `AppProviders` 를 *렌더*하므로 그 본문에서 `useQuery` 를 못 쓰고, `app-shell.tsx` 는 다른 에이전트 범위라 프로바이더 안쪽 호스트가 따로 필요했다. §1.B 가 포커스 슬롯 프로젝트로 바꿀 단일 지점) · `src/app/app.tsx`(aux 분기에 두 다이얼로그를 창 프로젝트로 마운트, main 분기는 `MainWindowDialogs`, JSDoc 4→3항목 갱신) · `src/entities/layout/layout.query.ts`(`withCurrentWindowTarget` — `target: null` 을 `layout_open_tab` 의 main 트리 폴백 대신 **이 창의 포커스 pane** 으로 해소. `useOpenTab`/`useOpenTabInProject` 두 곳에만 넣어 파일 탭·터미널·diff·settings 전 경로를 한 번에 덮음) · `src/widgets/explorer/explorer-container.tsx`(`openToTheSide` 가 응답 레이아웃의 main `focusedPane` 대신 `resolveWindowPaneTree` 로 자기 창 트리에서 split) · `src/shared/lib/command-catalog.ts`(`tab.moveToMainWindow` 의 "팔레트는 메인 창 전용" JSDoc 현행화) · 로케일 3종 + `domain/locale/service.rs` `MESSAGE_NAMESPACES`(사용처가 사라진 `palette.mainWindowOnly` 제거) · 문서 `layout-shell.md` §7.1·§7.2·§7.6 / `window-chrome.md` §5 / `command-palette.md` §5.
  / 테스트: `auxiliary-window-shell.test.tsx` 전면 교체(5건 — 탐색기 패널이 창 프로젝트로 마운트·검색 뷰 전환 시 `SearchPanelContainer`·SCM 뷰 전환 시 `GitPanelContainer`·⌃⇧G/⌘⇧E 가 창 안 브리지로 뷰 전환), 신규 `command-palette.test.tsx`(2건) · `task-runner-dialog.test.tsx`(3건), `use-open-file-tab.test.tsx` +1건(null target → 이 창 포커스 pane). TS: `bun run typecheck` exit 0 · `bun run lint` 0 error(기존 warning 11건, 내가 만진 파일엔 없음) · `prettier --check` 만진 파일 전부 통과 · `bun test` 2608 pass / 0 fail(255 파일). Rust: `cargo clippy --workspace --all-targets -D warnings` 통과 · `cargo test --workspace` 1665 passed / 0 failed(locale 19건 포함) · `cargo fmt --all -- --check` 는 내가 만진 `domain/locale/service.rs` 에 diff 0(최종 확인 시점에 R 작업 중인 `domain/project/service.rs` 에만 diff — 남의 범위라 `cargo fmt` 를 돌리지 않았다).
  / 이탈: ① 계약 문언은 세 컨테이너를 `app.tsx` aux 분기에 마운트하라고 적었으나, `GitPanelContainer`/`SearchPanelContainer` 는 이미 `ExplorerPanel` 의 뷰 스위처가 소유한다 — 따로 마운트하면 이중 마운트가 되므로 `ExplorerContainer` 한 번으로 셋을 다 들였고, 위치도 창 크롬을 소유한 `AuxiliaryWindowShell` 로 했다(팔레트/TaskRunner 만 `app.tsx`). ② 보조 창 탐색 패널의 접힘은 영속하지 않는다 — `shell_view.sidebarCollapsed` 가 프로젝트 단위라 같은 프로젝트를 연 main 창과 서로 덮어쓴다(재분배는 §0.1 S-6). ③ 계약에 없던 `withCurrentWindowTarget` 을 추가했다: 이게 없으면 보조 창 탐색기에서 클릭한 파일이 **메인 창에** 열려 §1.D 가 기능적으로 깨진 채 나간다(`layout_open_tab` 의 `target: None` 폴백이 main 트리의 `focused_pane`). 호출부 12곳을 고치는 대신 진입점 2개에서 해소했고, 메인 창에서는 두 값이 같아 동작 무변경이다. ④ 쓰이지 않게 된 `palette.mainWindowOnly` 키를 3종 JSON + `MESSAGE_NAMESPACES` 에서 제거했다(자기 키 범위).
  / 미확인·후속: ① bindings 재생성은 하지 않았다 — 이번 변경이 건드린 Rust 는 `MESSAGE_NAMESPACES` 상수뿐이라 export 타입이 없고, R 의 `ShellSlotTree` 작업이 진행 중이라 지금 재생성하면 미완성 타입이 섞인다(R 이 자기 단계에서 생성). ② Rust 검증은 R 의 편집 타이밍 때문에 중간에 2회 컴파일 실패했고(`open_project` 인자 추가·`ShellSlotTree` 미완성 — 둘 다 내 변경과 무관), 지시대로 재시도해 최종적으로 전부 통과한 실행을 근거로 보고했다. ③ 보조 창 검증은 하네스 한계로 유닛 테스트가 **main 창 컨텍스트**에서만 돈다(`location` 에 쿼리 없음 — `test-conventions.md` §4). 보조 창 실제 동작(탐색기 클릭이 그 창에 열리는지, ⌘P 가 그 창 프로젝트로 뜨는지)은 실기 확인이 필요하다. ④ 보조 창에서 아웃라인 뷰의 심볼 이동·`FileHistoryPanel` 등 파일 탭이 아닌 경로도 이제 `withCurrentWindowTarget` 을 타지만, 실기 미확인.

<!-- d62-record-F2 -->
- **§1.B 2a 프론트 완료 (2026-09-15, F2 재개)** — 직전 F2 에이전트가 남긴 골격(`shell-slot.ts`·`shell-slot-context.tsx`·`shell-slot-provider.tsx`·`entities/session/*`·`app-shell` 분해·`project-shell`·`shell-slot-tree-view`·`use-window-chrome`·`features/shell-slot/shell-slot-header`)을 되돌리지 않고 이어받아 완성했다. 변경 파일(역할): **소비 시그니처 확장(골격이 남긴 tsc 오류 4건)** — `widgets/window-chrome/title-bar-content.tsx`·`status-bar-content.tsx`(자기-읽기 `activeProjectQueryOptions` 제거 → `projectId` prop; 상태바는 Problems 쌍도 함께 받는다) · `widgets/explorer/explorer-container.tsx` + `use-explorer-auto-reveal.ts`(`zen` 을 prop/입력으로 — auto-reveal 의 `sidebarVisible` 이 `shell_view.zen` 을 읽고 있었는데 그 필드가 승격으로 비워지므로 방치하면 Zen 중에도 트리가 움직인다) · `widgets/editor-area/editor-area.tsx`(`zen` prop, 내부 `layout.shellView.zen` 파생 제거) · `widgets/auxiliary-window-shell/auxiliary-window-shell.tsx`(둘 다 `zen={false}` — `window_chrome` 은 세션 단일 값이라 메인 창 Zen 이 보조 창까지 숨기면 안 된다). **게이팅(§0.1 S-4/U-1/U-2)** — `editor-area.tsx`(`useGlobalKeymap` 27개 핸들러를 `whenFocused()` 로 `isFocused ? fn : undefined`, `editor-pane-command`·`editor-opener` 구독 핸들러 선두 가드, `active-editor-actions` **발행** 이펙트도 포커스 슬롯만) · `terminal-pane.tsx`(기존 xterm 포커스 게이트와 AND) · `widgets/explorer/explorer-panel.tsx`(뷰 전환·reveal·rename·search 4구독 — 여기가 실제로 뷰를 바꾸는 쪽이라 `project-shell` 의 "패널 펼치기" 게이트만으로는 부족했다) · `widgets/file-history/file-history-panel.tsx`(슬롯마다 다이얼로그가 열리던 경로) · `shared/lib/command-registry.ts`(`CommandContext.focusedShellSlotId` 읽기 전용 추가) + `widgets/command-palette/command-palette.tsx`(`useShellSlotFocus` 로 채움). **전역 소비 6파일** — 위 타이틀/상태바 2개 + `app/main-window-dialogs.tsx`·`app/providers/ide-sync-provider.tsx`(둘 다 `useFocusedProjectId()`) + `widgets/app-shell/app-shell.tsx`(골격이 이미 `useShellSlotFocus`) + 팔레트/TaskRunner(F1 의 prop 을 포커스 슬롯 프로젝트로 전환). **배선** — `app/app.tsx`(main 분기에만 `ShellSlotProvider` 마운트, aux 는 슬롯 개념 없음) · `shared/constants/query-key.ts` 는 골격 그대로. **메뉴·닫기** — `widgets/app-sidebar/sortable-project-icon.tsx`(context menu 에 오른쪽/아래/왼쪽/위에 열기 4항목, `canOpenInShellSlot` 비활성) · `widgets/app-sidebar/app-sidebar.tsx`(`useOpenProjectInSlot` + 포커스 슬롯 타깃 + `describeIpcError` 토스트) · 슬롯 헤더 ✕ 는 골격의 `shell_slot_close` 배선 그대로. **Problems** — `shared/lib/shell-slot.ts` 에 순수 `withShellSlotToggled` 추가하고 `app-shell` 이 슬롯별 집합으로 소유. **로케일** — `shellSlot.close`/`openToTheRight`/`openBelow`/`openToTheLeft`/`openAbove` 5키 × 3종 + `MESSAGE_NAMESPACES` 에 새 네임스페이스 `shellSlot` 등재. **문서** — `docs/features/layout-shell.md`(§1 구조도 갱신 + 신설 §8 셸 슬롯: 트리·분할/닫기 조작표·포커스 판정·게이팅표·제한) · `docs/features/window-chrome.md`(§6 을 `window_chrome` 소유로 재작성, §6.0 축 재분배표 + Zen 이 비포커스 슬롯을 숨긴다 + 레일 필드 현황).
  / 테스트: 신규 6파일 30건 — `shared/lib/shell-slot.test.ts` 13(리프 평탄화 순서·id↔프로젝트 조회·포커스 폴백·이벤트 타깃 해소·Problems 토글 불변성) · `shared/lib/shell-slot-context.test.tsx` 3(두 슬롯이 같은 ⌘S 를 등록해도 포커스 슬롯만 실행 / 스코프 없으면 항상 실행 / 포커스 이동 시 핸들러도 이동 — 실제 `useGlobalKeymap` + 실제 keydown 으로) · `app/providers/shell-slot-provider.test.tsx` 4(세션 포커스로 시작·pointerdown·focusin·슬롯 밖 클릭은 무변화) · `features/shell-slot/shell-slot-header.test.tsx` 3(라벨·닫기·마지막 슬롯 비활성) · `widgets/app-shell/shell-slot-tree-view.test.tsx` 4(단일 리프 = 헤더 없는 기존 화면 / 2분할 = 셸·헤더·슬롯 id 2개씩 / 헤더 ✕ 는 그 슬롯만 / Zen 은 포커스 슬롯만 남기고 Zen 해제 시 배치 복귀) · `widgets/app-sidebar/sortable-project-icon.test.tsx` 3(4항목 순서·선택 시 edge 전달·슬롯 없으면 전부 비활성). 기존 테스트 수정 6파일 — `query-key.test.ts`(SESSION 2리프 분류표 등재), `use-explorer-auto-reveal.test.tsx`(`zen` 입력화), `command-catalog`/`command-registry`/`agent.commands`/`git.commands`/`task.commands` 의 `dummyContext` 에 `focusedShellSlotId`. 측정: `bun run typecheck` exit 0 · `bun run lint` 0 error(기존 warning 11건, 내가 만진 파일엔 없음) · `bunx prettier --check` 만진 TS/JSON 전부 통과 · `bun test` **2638 pass / 0 fail (261 파일)** — 재개 시점 2608/255 대비 +30건 · `cargo fmt --all -- --check` exit 0 · `cargo test -p taide --lib locale` 19 passed / 0 failed(새 `shellSlot` 네임스페이스가 3종 카탈로그와 키 집합 일치).
  / 이탈: ① 배정 문언은 tsc 오류 4건 중 하나를 "AppSidebar 에 projectId prop 없음"이라 적었으나 실제 `app-shell.tsx:163` 은 **`TitleBarContent`** 다(AppSidebar 는 이미 `activeProjectId` 를 받고 있었다) — 파일·줄 그대로 고쳤다. ② `ExplorerContainer` 의 `zen` 은 죽은 prop 이 아니었다: `use-explorer-auto-reveal.ts` 가 유일한 소비처이고, 그 지점을 함께 고치지 않으면 승격으로 비워진 `shell_view.zen` 때문에 Zen 중 auto-reveal 이 되살아난다. 그래서 골격의 prop 을 지우는 대신 훅까지 관통시켰다. ③ **발행부 무변경 원칙의 예외 1건**: `editor-area` 의 `setActiveEditorActionIds`(= `active-editor-actions` **발행**)도 포커스 게이트를 걸었다. 배정 문언은 이 브리지를 "구독"으로 분류했지만 EditorArea 는 발행 측이고, 게이트가 없으면 비포커스 슬롯이 자기 액션 집합으로 전역 값을 덮어써 팔레트의 `isEnabled` 가 엉뚱한 에디터를 보고 판정한다. ④ **게이팅 대상 2곳 추가**: 배정 목록에 없던 `explorer-panel.tsx`(4구독)와 `file-history-panel.tsx`(1구독)가 슬롯마다 마운트되는 실제 구독처였다 — `project-shell` 의 게이트는 "접힌 패널 펼치기"만 막고 뷰 전환·reveal·rename 자체는 그대로 통과한다. ⑤ 로케일 키는 "3종"이 아니라 **5종**이다(`close` + 방향 4개) — 배정 문언의 메뉴 항목이 4방향이라 3개로는 모자란다. ⑥ `window_chrome.sidebar_rail_collapsed` 는 **읽기만** 배선했다. 레일을 접는 UI 는 d-62 이전에도 없었고(Zen 만 레일을 숨겼다) 새 커맨드·단축키를 만드는 것은 배정 범위 밖이라, 필드를 존중하되 생산자는 만들지 않았다. ⑦ `docs/architecture.md:398` 의 `use-zen-mode` 언급은 2026-08-18 이동 **이력** 기록이라 건드리지 않았다(삭제된 파일을 가리키지만 과거 사실 서술).
  / 미확인·후속: ① **런타임 실기 미확인**(앱 실행 금지) — 2 프로젝트 좌우 분할, 포커스 전환 시 ⌘B/⌘⇧F/⌘S 대상, Zen 진입/이탈 시 슬롯 복귀, 슬롯 크기 드래그 영속은 전부 사용자 실기 확인이 필요하다. 특히 `react-resizable-panels` 중첩 그룹의 실제 크기 계산은 happy-dom(레이아웃 0)에서 검증되지 않는다. ② `SessionShellSlotsChanged`/`WindowChromeChanged` 는 창 구분 없이 브로드캐스트되고, 보조 창도 `IpcSyncProvider` 를 마운트하므로 두 이벤트로 `SESSION.SHELL_STATE` 를 무효화한다 — 보조 창엔 그 쿼리의 관찰자가 없어 무해하지만(2a R 기록의 미확인 ③), 관찰자가 생기면 재검토가 필요하다. ③ 사이드바→슬롯 **드래그**(2b)와 그룹(2c)은 범위 밖이다. `AppShell` 최상위 프로젝트 DndContext(§0.1 U-4)는 아직 없다. ④ 포커스 판정은 `pointerdown`/`focusin` 캡처뿐이라, 슬롯 안을 **키보드로만** 오가되 `focusin` 이 나지 않는 경로(있다면)는 포커스를 옮기지 않는다 — 실기에서 관찰되면 신호를 늘린다. ⑤ `project_open_in_slot` 이 `path` 로 새 프로젝트를 여는 경로는 프론트에서 아직 호출하지 않는다(사이드바 메뉴는 이미 열린 `projectId` 만 보낸다) — 드래그 분할(2b)이 그 경로의 첫 소비자가 된다.


- **메인 1차 검증 (2026-09-15, 1단계+2a)** — `bun run verify` exit 0(bun test 2638/0 · cargo lib 1634 + 통합 31 · clippy 0 · prettier) · `bunx vite build` 0 · `bun run typecheck:e2e` 0. 워킹트리 51파일 +2259/-497 (+신규).
- **렌즈 검토 (2026-09-15, wf `wf_b97fbc5d`, sonnet·xhigh 3렌즈 + major 반박 2표)** — major 1 · minor 4 · info 1. 메인 판정: **전건 수용**.

| id | 심각도 | 내용 | 수정 방향 |
|----|------|------|------|
| B-1 | major | Zen 토글이 `ShellSlotTreeView` 루트의 컴포넌트 타입을 바꿔(Node↔Leaf) 단일 슬롯에서도 ProjectShell·EditorArea·Monaco 가 재마운트 — 이전 app-shell 은 재마운트 없었음(회귀) | zen 여부와 무관하게 항상 같은 트리 렌더, 비포커스 리프는 `hidden`(display:none) 으로만 숨김. 테스트: zen 토글 전후 EditorArea 인스턴스 동일(마운트 카운트) |
| S-101 | minor | `project_open_in_slot` 으로 이미 열린 프로젝트를 배치할 때 `last_opened_at` 미갱신 | `place_project_in_slot` 이 recency stamp + 저장 |
| S-102 | minor | window_chrome 1회 승격 시 비활성 프로젝트의 레거시 zen/sidebar 값 소실 미기록 | §3 이탈 목록·data-model §22 에 트레이드오프 명시 + QA 실기 항목 |
| G-1 | minor | 슬롯 전환 클릭 1회에 `session_focus_shell_slot` IPC 2회(pointerdown+focusin) | 마지막 호출 slotId ref 로 중복 억제 + 테스트 |
| G-2 | info | 슬롯 닫을 때 `problemsOpenSlotIds` 미정리 | onCloseSlot 에서 filter |
| B-2 | minor | 트리 뷰 파일에 FC 3개(선례 pane-node-view 는 단일 재귀) | 분리 근거 JSDoc 1줄(합치지 않음 — B-1 수정 후 leaf/node 분리가 hidden 처리에 유리) |

<!-- d62-fix-2a -->
- **렌즈 검토 수정 — TS (2026-09-15, B-1·B-2·G-1·G-2)** — **B-1(Zen 재마운트 회귀)**: `widgets/app-shell/shell-slot-tree-view.tsx` 가 이제 zen 여부와 무관하게 **항상 `ShellSlotNodeView` 한 경로**를 렌더한다(zen 전용 `ShellSlotLeafView` 루트 분기 제거). Zen 화면은 비포커스 서브트리를 감싼 `Panel` 과 그 옆 구분선에 **`hidden` 속성**만 얹어 만든다 — 숨김 판정은 `isSubtreeHidden(child) = zen && 그 서브트리에 포커스 리프가 없음` 이라 중첩 분할에서도 각 층에서 한쪽만 남는다. `hidden` 을 `Panel` **자신**에 얹는 것이 핵심이다: Panel 이 그룹의 flex item 이라 `[hidden]{display:none!important}`(Tailwind v4 preflight, 인라인 `display:flex` 를 !important 로 이김)로 flow 에서 빠져야 남은 슬롯이 `flexBasis:0 + flexGrow` 로 창 전체를 먹고, 레이아웃 숫자(그룹의 layout·sizes)는 건드리지 않아 Zen 을 나가면 비율이 그대로 돌아온다. `focusedShellSlotId` 를 `SlotRenderContext` 에 추가하고 트리 뷰 진입점에서 `resolveShellSlotFocus` 로 한 번 해소한다(낡은 id 는 첫 슬롯으로 폴백 — 기존 zen 분기의 `?? leaves[0]` 를 공용 헬퍼로 대체). 슬롯 헤더만 zen 에서 조건 렌더로 빠진다(상태가 없어 숨길 필요가 없고, 형제 위치가 고정이라 `ProjectShell` 을 재마운트하지 않는다). 지원 변경 1건: `features/split/pane-separator.tsx` 에 선택적 `hidden` prop passthrough(`Separator` 는 rest 를 그대로 div 에 뿌리므로 스타일 무영향, 기존 호출부 `pane-node-view` 무변경). **B-2**: 같은 파일에 leaf/node 분리 근거 JSDoc — `ShellSlotLeafView` 에 "리프가 Zen 이 숨기는 단위라 위치별 컴포넌트 타입이 토글에 따라 바뀌면 안 된다" 1블록, 트리 뷰 export JSDoc 의 Zen 문단을 "다른 트리를 그리면 살아남은 셸까지 재마운트된다 → hidden 으로만 숨긴다"로 교체. **G-1(포커스 IPC 2회)**: `app/providers/shell-slot-provider.tsx` 에 `overrideRef`(기존 `override` state 의 동기 미러) 추가 + 중복 가드를 `slotId === focusedSlotOf(overrideRef.current)` 한 줄로 통합. 한 번의 클릭이 내는 `pointerdown` → `focusin` 두 신호는 React 커밋 전에 연달아 도착해 렌더 상태만 읽는 가드로는 둘 다 통과한다. ref 는 나가는 길에 쓰이므로 쌍의 두 번째가 첫 번째가 이미 잡은 슬롯으로 해소돼 멈춘다. ref 는 state 와 **같은 `serverFocused` 비교로 스스로 폐기**되므로(사이드바 클릭·슬롯 닫기 등 다른 경로가 서버 포커스를 옮기면 무효), 별도 동기화 effect 가 없다. **G-2(슬롯 닫을 때 Problems 잔류)**: `widgets/app-shell/app-shell.tsx` 에 `forgetSlotProblems`(닫기·패널 자체 닫기 2곳 공용) + `handleCloseSlot` 추가 — 정리는 **`shell_slot_close` 성공 시에만**(마지막 슬롯 거부처럼 실패한 닫기가 살아 있는 슬롯의 Problems 를 꺼 버리면 안 된다). 문서: `docs/features/layout-shell.md` §8.1 에 Zen 구현 불릿 신설(hidden·전원 마운트 유지·단일 슬롯도 같은 경로), `docs/features/window-chrome.md` §6.1 의 "(렌더만 접는다)" 를 같은 사실로 정정.
  / 테스트: 신규 2파일 + 기존 2파일 = **+6건**(2638 → 2644). `widgets/app-shell/shell-slot-tree-view.test.tsx`: Zen 케이스를 "포커스 슬롯만 남는다" → "**포커스 슬롯만 보이고**(비포커스 리프가 `hidden` 조상을 갖는다) 트리·마운트는 그대로" 로 교체 + 신규 2건(2분할 Zen 토글 왕복에 좌우 셸 **DOM 노드 동일** / 단일 슬롯 창의 Zen 토글도 동일 — 회귀의 진원지). `app/providers/shell-slot-provider.test.tsx` +1(한 `act` 안에서 pointerdown+focusin 을 연달아 쏘고 `spyOn(commands,'sessionFocusShellSlot')` 호출 1회). **신규** `widgets/app-shell/app-shell.test.tsx` 3건(상태바 토글이 포커스 슬롯만 연다 / 닫기 성공 시 그 슬롯의 Problems 표시가 사라진다 / 닫기 거부 시 남는다). 회귀 검출력 실측: 수정 전 코드로 되돌려 돌리면 트리 뷰 3건·프로바이더 1건(호출 2회)·app-shell 1건이 실제로 실패한다. 측정: `bun run typecheck` exit 0 · `bun run lint` 0 error(기존 warning 11건, 전부 내가 안 만진 파일) · `bunx prettier --check` 만진 TS 7파일 통과(docs 는 `.prettierignore`) · `bun test` **2644 pass / 0 fail (262 파일)**. Rust 는 건드리지 않았다.
  / 이탈: ① 배정에 없던 `features/split/pane-separator.tsx` 를 고쳤다 — 구분선을 조건 렌더로 빼면 그룹의 자식 구성이 바뀌어 라이브러리의 separator↔panel 등록이 흔들리고, 배정 문언도 "Group/Separator 를 hidden 으로" 라 요구하므로 prop 하나를 통과시키는 것이 최소 변경이었다. ② 배정이 예비안으로 준 "Panel collapse/minSize 0" 은 쓰지 않았다 — `hidden` 만으로 레이아웃이 성립한다(위 B-1 근거). ③ Zen 에서 슬롯 헤더는 `hidden` 이 아니라 조건 렌더다(상태가 없어 보존할 것이 없고, 형제 배열 위치가 고정이라 아래 셸이 재마운트되지 않는다). ④ G-2 정리 시점을 "즉시" 가 아니라 **성공 시**로 했다(위 근거). ⑤ 배정 문언은 `layout-shell.md` §8.1 의 Zen 서술을 "일치시키라" 했는데 §8.1 에는 Zen 서술이 **없었다** — 새로 썼고, 대신 같은 사실을 반대로 적고 있던 `window-chrome.md` §6.1 한 문장("렌더만 접는다")을 함께 정정했다(범위 밖이지만 그대로 두면 문서끼리 모순된다). ⑥ `app-shell.test.tsx` 는 프로젝트 목록·셸 상태를 `setQueryDefaults(staleTime: Infinity)` 로 얼려 시드한다 — 다른 파일이 프로세스 전역으로 건 `@entities/project/project.ipc` 목(`listProjects: () => []`)이 마운트 refetch 를 이겨 Welcome 화면으로 떨어뜨리기 때문이다(전체 스위트에서만 재현, 단일 파일 실행에선 안 보였다). ⑦ 같은 파일의 `ProjectShell` 스텁은 `shell-slot-tree-view.test.tsx` 의 것과 **출력이 동일**하게 맞췄다(`mock.module` 은 프로세스 전역·마지막 등록 승리라, 어느 쪽이 이겨도 두 파일이 다 읽을 수 있어야 한다). ⑧ 재마운트 단언은 마운트 카운터가 아니라 **DOM 노드 동일성**으로 했다(⑦ 때문에 스텁이 카운터를 들고 있으면 안 된다).
  / 미확인·후속: ① **런타임 실기 미확인**(앱 실행 금지) — Zen 진입 시 포커스 슬롯이 실제로 창을 꽉 채우는지, 나올 때 좌우 비율이 그대로인지는 `react-resizable-panels` 의 실제 flex 계산이라 happy-dom(레이아웃 0)에서 검증되지 않는다. ② Zen 중에도 비포커스 슬롯이 **마운트된 채로** 남는 것은 의도지만(그게 B-1 의 요구), 그동안 그 슬롯의 monaco(`automaticLayout`)·xterm(ResizeObserver + `proposeDimensions` 유한성 가드)이 0 크기를 거쳐 되돌아오는 경로는 실기 미확인이다 — 두 구현 모두 0 크기를 건너뛰게 돼 있어 재-fit 은 기대되지만 눈으로 봐야 한다. ③ Zen 이 이전에는 비포커스 슬롯을 통째로 언마운트했으므로, 그 슬롯의 터미널 PTY·LSP 구독이 Zen 동안 살아 있는 것이 이제 정상이다(자원 사용량 변화는 실기 관찰 대상). ④ `app-shell.test.tsx` 는 `ShellSlotProvider` 를 마운트하지 않고 포커스를 직접 주입한다 — 포커스 판정 자체는 프로바이더 테스트가 본다. ⑤ G-1 은 `pointerdown`/`focusin` 쌍만 다룬다. 같은 슬롯을 겨냥한 다른 신호(드래그 중 반복 focusin 등)가 실기에서 관찰되면 같은 ref 로 흡수된다.

<!-- d62-fix-2a-rust -->
- **렌즈 검토 수정 — Rust (2026-09-15, S-101·S-102)** — **S-101(`last_opened_at`)**: `domain/project/service.rs` `place_project_in_slot` 이 `projects: &mut HashMap<ProjectId, Project>` 를 받아 배치 대상 프로젝트의 `last_opened_at` 을 `now_epoch_ms()` 로 찍고 `save_project` 로 영속화한다 — `activate_project`/`focus_shell_slot` 과 같은 순서(포커스·활성 설정 → 프로젝트 stamp+저장 → 세션 저장)이고, 그 이유(슬롯 배치 = 사용자가 지금 보고 있는 화면 = 활성화)를 함수 doc 에 적었다. 호출부 3곳 시그니처 갱신: `domain/project/commands.rs` `project_open_in_slot` · 유닛 테스트 `슬롯에_두면_새_슬롯이_포커스와_활성_프로젝트가_된다` · `tests/session_restore.rs`. **S-102(기록만, 코드 무변경)**: `docs/data-model.md` §22 에 창 크롬 승격 트레이드오프 2불릿 추가(① 세션으로 올라가는 건 **한 프로젝트의 값 1벌**뿐이고 승격 시점에 열려 있던 나머지 프로젝트의 레거시 `zen`/`sidebar_collapsed` 는 병합 없이 초기화된다 ② 승격 시점에 **닫혀 있던** 프로젝트의 `layout.json` 은 비워지지 않아, 사용자가 두 축을 모두 기본값으로 되돌린 뒤 그 프로젝트를 열면 다음 부팅에 낡은 값이 승격될 수 있다), 같은 내용을 위 2a R 기록의 **이탈 ⑨** 로 추가, `docs/quality-assurance/2026-08-11-qa6-checklist.md` 에 "d-62 2a 창 크롬 승격 재검" 절 + 실기 1항목 append.
  / 테스트: 신규 Rust 1건 — `슬롯_배치는_이미_열린_프로젝트의_last_opened_at을_갱신하고_영속화한다`(`last_opened_at: 0.0` 레코드를 배치한 뒤 in-memory 맵과 `project.json` 양쪽이 같은 값으로 갱신되는지). 측정: `cargo fmt --all -- --check` exit 0 · `cargo clippy --workspace --all-targets -- -D warnings` exit 0 · `cargo test --workspace` **1666 passed / 0 failed**(lib 1635 + capability_symmetry 4 + domain_boundaries 3 + session_restore 7 + taide-cli 17 — 직전 1665 대비 +1) · `bun run typecheck` exit 0(커맨드 시그니처가 안 바뀌어 bindings 재생성 불필요). TS 파일은 하나도 만지지 않았다.
  / 이탈: ① `place_project_in_slot` 은 stamp 를 **무조건** 찍는다 — 서비스 함수는 호출자가 방금 연 프로젝트인지 알 수 없고, 그걸 알리는 플래그를 더하는 것보다 "슬롯 배치 = 활성화" 를 한 규칙으로 두는 편이 `activate_project`/`focus_shell_slot` 과 일관된다. 대가로 `project_open_in_slot` 의 **신규 열기 경로는 `project.json` 을 2회 쓴다**(`open_project` 가 한 번, 배치가 한 번). ② 그 경로의 `ProjectOpened` 이벤트는 `open_project` 시점의 `last_opened_at`(수 ms 이른 값)을 실어 보낸다 — 프론트에 `lastOpenedAt` 소비처가 없고(`src/shared/api/bindings.ts` 의 타입 선언 1곳뿐) 최근 순서는 `list_recent_projects` 가 디스크에서 다시 읽으므로, 이벤트 payload 를 되읽는 코드를 더하지 않았다.
  / 미확인·후속: ① 앱 실행 금지라 **런타임 실기 미확인** — "오른쪽에 열기" 직후 Welcome 최근 목록 순서가 실제로 바뀌는지는 실기 확인이 필요하다. ② S-102 의 QA 항목은 "값이 안 사라지는가" 가 아니라 **이행이 1회로 끝나는가**(Zen 껐다 켠 뒤 재시작에 낡은 값이 안 살아나는가)를 묻는다 — 값 손실 자체는 수용한 트레이드오프이기 때문이다. ③ 승격 시점에 닫혀 있던 프로젝트의 stale `layout.json` 을 일괄 비우는 정리 경로는 만들지 않았다(열려 있지 않은 프로젝트의 레이아웃까지 부팅마다 전수 로드해야 해서, 이행 1회의 이득보다 비용이 크다). ④ `focus_shell_slot` 은 2a 시점부터 유닛 테스트가 없다 — S-101 범위 밖이라 이번에 추가하지 않았다.


- **메인 2차 검증 최종 (2026-09-15, 1단계+2a 검토 수정 후)** — `bun run verify` exit 0(bun test 2644/0 · cargo lib 1635 + 통합 31 · clippy 0 · prettier) · `bunx vite build` 0 · `bun run typecheck:e2e` 0. 커밋 4분할(feat(window) 보조 창·feat(session) 슬롯 트리 Rust·feat(shell) 셸 슬롯 프론트·docs) → dev 푸시 → main ff. 2b·2c 는 이어서. 사용자 실기 대상(2a): 2 프로젝트 좌우/상하 분할(사이드바 아이콘 우클릭 → 오른쪽/아래에 열기)·포커스 전환 시 ⌘S/⌘W/⌘B/⌘⇧F 대상·Zen 진입/이탈 시 배치 복귀·슬롯 헤더 X·슬롯 크기 드래그 영속·보조 창 탐색기 클릭이 그 창에 열림·재시작 후 슬롯 복원·레거시 Zen 값 1회 승격.

- **§1.A 그룹 + §0.1 S-7 — 2c Rust 완료 (2026-09-16)** — 변경 파일(역할): `src-tauri/src/ids.rs`(`ProjectGroupId`=`group-<uuid>`, 다섯 번째 string id) · `domain/project/types.rs`(`ProjectGroup{id,name,color:Option<String>,members,collapsed}` + `ProjectGroupOpenResult{opened,skipped}` + `SessionState.groups: Vec<ProjectGroup>` `#[serde(default)]` 가산) · **신규** `domain/project/groups.rs`(순수 목록 연산: `find`/`find_mut`/`dedupe_members`/`claim_members`(한 프로젝트 = 한 그룹 강제)/`forget_members`/`reorder`/`plan_open`(멤버 순서·이미 열림 스킵·결손 스킵·활성 1회를 한 곳에서 결정하는 `GroupOpenPlan` + `GroupSkipReason{AlreadyOpen,Unavailable}` — 결손만 `log::warn`, 이미 열림은 정상 경로라 조용히 넘긴다)) · `domain/project/mod.rs` · `domain/project/service.rs`(`GROUP_NAME_MAX_CODEPOINTS=40`·`sanitize_group_name`(제어문자 제거+트림+1~40)·`sanitize_group_color`(`sanitize_display_color` 재사용 = `lane1..lane12`)·`known_project_ids`/`normalize_group_members`(디스크 레코드 대조 + 중복 제거, 하나라도 미상이면 호출 전체 거부) / `list_groups`·`group_members`·`group_member_root`(read-only 레코드+루트 해소) / `create_group`·`rename_group`·`set_group_color`·`set_group_collapsed`·`delete_group`(그룹만 삭제)·`set_group_members`·`reorder_groups` / **`forget_recent_projects` 를 `(paths, &mut session, open_ids)` 로 확장**해 실제로 지운 id 만 `groups[].members` 에서 빼고 변경이 있을 때만 세션 저장 — §0.1 S-7) · `domain/project/commands.rs`(신규 커맨드 9종 `project_group_list`·`create`·`rename`·`set_color`·`set_collapsed`·`set_members`·`delete`·`reorder`·`open`, `emit_groups_changed` 헬퍼(세션 read 락을 emit 전에 해제), `project_forget_recent` 가 세션을 되쓰고 `ProjectGroupsChanged` 동반 emit, `project_group_open` + 사설 `open_group_member`(루트를 이미 아는 `project_open` 과 같은 모양 — 가드 안 뮤테이션 → 가드 밖 attach → 실패 시 `project_close` 되감기, `activate=false` 면 `ProjectActivated`·슬롯 이벤트 없음)) · `events.rs`(`ProjectGroupsChanged{groups}` — 27→28종) · `lib.rs`(`collect_commands!` 9종·`collect_events!`/`fanout_remote_events!` 1종·이벤트 수 단언 27→28) · `domain/remote/dispatch.rs`(`IMPLEMENTED_JSON_COMMANDS`·`REMOTE_ALLOWED_COMMANDS` 9종 등재(=`project_reorder`/`project_open` 과 같은 등급, 거부 테이블 무변경)·match arm 9종·허용 테이블 doc 카운트 167/166→176/175) · `domain/locale/service.rs` `MESSAGE_NAMESPACES` + 로케일 3종(`error.projectGroup.invalid`·`error.projectGroup.memberUnknown` 자기 키만 Edit) · `src/shared/api/bindings.ts`(재생성 — TS 중 유일하게 만진 파일) · 문서 `docs/ipc-contract.md`(신설 "project group — 사이드바 프로젝트 그룹" 절 + project 절 상호참조 + 말미 "d-62 2c" 절 + 2a 절의 "그룹은 아직 없다" 문장 정정)·`docs/data-model.md`(§2 `session.json` 필드·§3 `SessionState`/`ProjectGroup` 타입·신설 §23 + §22 의 전방 참조 정정).
  / 테스트: 신규 Rust **21건** — `groups` 유닛 9건(멤버 중복 제거가 앞 순서를 남김, 멤버 차지 시 다른 그룹에서 빠짐·소유 그룹 자신은 무변경, forget 정리 + 변경 없음 보고, 재정렬 누락분 뒤에 붙임, 열기 계획의 멤버 순서·활성 1회, 이미 열림/결손 스킵 후 다음이 활성 승계, 열 수 있는 멤버 0개, 조회 none) + `project::service` 유닛 11건(생성 시 트림·색·중복 제거 + `session.json` 왕복, 구버전 세션의 groups 기본값, 이름 공백·상한 초과·팔레트 밖 색 거부(로케일 키·kind 검증) + 거부는 아무것도 남기지 않음, 레코드 없는 멤버 거부(로케일 키) + 기존 멤버십 보존, 한 그룹 배타성, 이름·색·접힘 영속, 없는 그룹의 rename/delete/members 는 NotFound, 삭제가 멤버 레코드를 안 지움, 재정렬 영속, forget 이 멤버십 정리 + 저장, `group_member_root` 3분기) + `tests/session_restore.rs` 통합 1건(그룹이 `session.json` 왕복으로 복원되고 **닫힌 멤버는 유지**되지만 forget 된 멤버는 빠진다). 측정: `cargo fmt --all -- --check` exit 0 · `cargo clippy --workspace --all-targets -- -D warnings` exit 0 · `cargo test --workspace` **1687 passed / 0 failed**(lib 1655 + capability_symmetry 4 + **domain_boundaries 3** + session_restore 8 + taide-cli 17 — 직전 1666 대비 +21) · bindings 재생성 후 `bun run typecheck` exit 0(내가 만진 파일 0건 오류) · `bun run lint` 0 error · `bunx prettier --check` 만진 로케일 JSON 3종 통과(`bindings.ts`·docs 는 `.prettierignore` 대상) · `bun test` **2665 pass / 0 fail (265 파일)**.
  / 이탈: ① 배정 커맨드 목록에 없던 **query `project_group_list()`** 를 추가했다 — `ProjectGroupsChanged` 는 전환 시점에만 오므로 이것이 없으면 갓 뜬 창이 현재 그룹을 알 방법이 없다(2a 이탈 ②의 `session_get_shell_state` 와 같은 이유). ② 역시 없던 **mutation `project_group_set_collapsed(groupId, collapsed)`** 를 추가했다 — `collapsed` 는 배정이 지정한 데이터 모델 필드인데 쓰기 경로가 없으면 죽은 필드가 된다(2a 이탈 ③의 `session_set_window_chrome` 과 같은 이유). ③ **한 프로젝트는 한 그룹에만 속한다**는 규칙을 서버에서 강제했다(계약 문언에 명시 없음) — 계약 §0.1 U-6 의 컨텍스트 메뉴가 "그룹에서 제거"(그룹을 지목하지 않음)라 소속이 1개임을 전제하고, 허용하면 사이드바가 같은 프로젝트를 두 헤더 아래 그려야 한다. ④ `color` 는 계약이 적은 `ProjectColorToken` 타입이 아니라 **`Option<String>` + `sanitize_display_color`(`lane1..lane12`)** 다 — 코드베이스에 그런 타입이 없고 `ProjectDisplay.color` 자체가 같은 모양이라 "팔레트 재사용" 을 문자 그대로 구현했다. ⑤ 멤버 검증의 어휘는 "**디스크에 `projects/<id>/` 레코드가 있는 프로젝트**"다(열려 있을 필요 없음) — 그룹이 닫힌 프로젝트를 담는다는 계약 문언과, forget 이 멤버를 빼는 유일한 경로라는 §0.1 S-7 이 함께 이 정의를 지시한다. ⑥ `project_group_open` 은 계약 §1.A 의 "나머지는 **백그라운드** 순차 큐" 가 아니라 **호출 안에서 순차로 전부** 연다 — 배정 문언이 "응답은 열린/스킵 목록" 이라 백그라운드로 보내면 응답에 담을 것이 첫 멤버뿐이 된다. 큐 자체의 성질(순차·`is_shutting_down` 중단·결손 스킵)은 계약대로다. ⑦ 활성은 **계획 시점에 고정**된다(`plan_open` 이 첫 열기 대상에 `activate: true`) — 그 멤버의 열기가 실패하면 그 호출에서는 아무것도 활성화되지 않는다. 열기 실패 후 다음 멤버로 활성을 넘기려면 계획을 소비하며 재계산해야 하는데, 그러면 "순서/스킵/활성" 규칙이 커맨드 본문으로 흩어져 유닛 테스트가 불가능해진다. ⑧ `is_shutting_down` 은 break 가 아니라 **매 멤버 재검사 + 스킵 기록**이다 — 플래그는 한 번 서면 내려가지 않으므로 실제 작업은 즉시 멈추고, 대신 남은 멤버가 응답의 `skipped` 에 빠짐없이 들어간다. ⑨ 로케일 키는 3종이 아니라 **2종**이다 — 계약 §1.C 의 "로케일 키 3종" 은 프론트 그룹 UI(2c F4) 몫이고, Rust 가 내는 사용자 대면 거부는 이름·색(`invalid`, `error.project.displayInvalid` 가 세 축을 한 키로 묶는 선례)과 멤버(`memberUnknown`) 둘뿐이다. 그룹 미존재는 `shell slot not found` 와 같이 비로케일 `AppError::NotFound` 다. ⑩ 데이터 모델 문서 절은 배정이 적은 §22 가 아니라 **신설 §23** 이다 — §22 는 제목부터 "d-62 2a" 라 2c 내용을 그 아래 넣으면 문서가 스스로 틀려진다(§22 의 전방 참조 문장만 정정). ⑪ `docs/features/layout-shell.md` 는 건드리지 않았다 — 배정의 문서 목록(ipc-contract·data-model)에 없고 사이드바 UI(§1.C·2c F4) 범위라 동시 편집 충돌을 피했다. ⑫ 도메인 경계: 새 참조는 project 도메인 내부뿐이라 화이트리스트 추가가 없다(`domain_boundaries` 3건 통과).
  / 미확인·후속: ① 앱 실행 금지라 **런타임 실기 미확인** — 그룹 생성·열기·접힘이 실제 사이드바에서 어떻게 보이는지는 2c F4(프론트) 배선 후 확인이 필요하다. ② `project_group_open` 의 capability attach 실패·롤백 경로는 `project_open` 과 같은 이유로 유닛 테스트 하네스(`tauri::test` mock app 부재)로 덮지 못했다 — 커맨드 계층 전체가 같은 제약이라 `plan_open` 까지만 순수 함수로 끌어내 테스트했다. ③ **부팅 시 그룹 멤버 정규화는 없다**(§23 에 명시) — 사용자가 `projects/<id>/` 를 손으로 지우면 그 멤버는 다음 `project_forget_recent` 나 멤버 재지정 전까지 남는다. 전수 정규화는 닫힌 프로젝트까지 매 부팅 디스크 확인을 요구해 비용이 이득보다 크다고 봤다. ④ 그룹 열기의 **자동 슬롯 배치는 범위 밖**이다(계약 §1.E) — 여러 멤버를 열어도 슬롯 트리는 첫 멤버의 활성만 반영한다. ⑤ `project_group_open` 이 여러 멤버를 열면 멤버마다 `ProjectListChanged` 가 나간다 — 의도지만(진행 반영) 멤버 수가 많은 그룹의 프론트 무효화 부하는 실기 관찰 대상이다. ⑥ 그룹 자체를 원격 세션이 만들고 열 수 있다(허용 등급 `project_reorder`/`project_open`) — 원격이 이미 `project_open` 으로 열 수 있는 프로젝트만 여는 것이라 새 노출은 없지만, `project_list_recent` 가 원격 거부인 것과 달리 **그룹 멤버 목록은 닫힌 프로젝트의 id 를 노출**한다(root·name 은 아니다).

<!-- d62-record-F3-2b -->
- **§1.B 드래그 분할(2b) + §0.1 U-4 완료 (2026-09-16)** — **스파이크 결론: 중첩 `DndContext` 채택**(예비안인 pointer 이벤트 오버레이는 쓰지 않았다). 근거: `@dnd-kit/core` 6.3.1 은 등록부를 React 컨텍스트 하나(`InternalContext`)에 담고 `useDroppable` 은 **가장 가까운** 컨텍스트의 reducer 로 dispatch 하므로, 안쪽 `DndContext` 가 자기 하위 트리에서 바깥을 가린다(`node_modules/@dnd-kit/core/dist/core.esm.js` 2558·3476 직접 확인). RTL 실측 3건으로 고정했다(`src/widgets/app-shell/project-drag-nesting.test.tsx`: 바깥 컨텍스트의 `droppableContainers` 에 안쪽 드롭존이 없다 / 안쪽 아이템 드래그가 바깥 `onDragStart` 를 깨우지 않는다 / 그 역도 성립).
  / 변경 파일(역할): **신규** `src/shared/lib/project-drag.ts`(드래그 페이로드 타입 `ProjectDragData`·`ShellSlotDropData`, narrowing `projectDragDataOf`/`shellSlotDropDataOf`, `DropEdge→ShellSlotEdge` 표(`center`→`replace`), 순수 `resolveShellSlotDrop`·`resolveProjectReorder`) · **신규** `src/widgets/app-shell/use-project-drag.ts`(창 단일 프로젝트 DndContext 의 상태·센서·충돌 판정·드롭 처리) · **신규** `src/features/project/project-drag-preview.tsx`(DragOverlay 내용 — 글리프만, 버튼 아님) · `src/widgets/app-shell/app-shell.tsx`(사이드바+슬롯 트리를 `DndContext` 로 감싸고 `DragOverlay` 추가, `draggingProjectId`/`slotDropTarget` 전달) · `src/widgets/app-shell/shell-slot-tree-view.tsx`(리프마다 `useDroppable` 5종 등록 + 드래그 중에만 `SplitDropZones` 렌더, `SlotRenderContext` 에 2필드) · `src/widgets/app-sidebar/app-sidebar.tsx`(자기 `DndContext`·센서·`handleDragEnd`·`draggingId` state·`useReorderProjects` 제거 → `SortableContext` 만 남기고 `draggingProjectId` prop 수신) · `src/widgets/app-sidebar/sortable-project-icon.tsx`(`useSortable` 에 `data: { type: 'project', projectId }` + 메뉴 JSDoc 현행화) · 문서 `docs/features/layout-shell.md`(§8.2 조작표에 드래그 행 추가·"2b 범위다" 불릿 교체, **신설 §8.3 드래그 분할** — 기존 8.3~8.5 는 8.4~8.6 으로 밀림, §8.6 제한 갱신, §8 제목 `2a`→`2a·2b`).
  / 테스트: **+21건 (2644 → 2665 pass / 0 fail, 265 파일)**. 신규 3파일 — `shared/lib/project-drag.test.ts` 8(4방향 edge 그대로 매핑 / `center`→`replace` / 자기 슬롯 가운데는 no-op / 같은 슬롯이라도 방향 분할은 서버로 / 한쪽이 프로젝트 드래그가 아니면 null / 재정렬 순서 · 제자리 · rail 밖 · 모르는 id · 원본 배열 불변) · `widgets/app-shell/project-drag-nesting.test.tsx` 3(위 스파이크) · `widgets/app-shell/use-project-drag.test.tsx` 7(방향 드롭존 → `project_open_in_slot` edge, 가운데 → `replace`, 자기 슬롯 가운데는 무요청, 서버 거부 → `describeIpcError` 토스트, 레일 재정렬 회귀 2건, 드래그 상태 수명). 기존 1파일 수정 — `shell-slot-tree-view.test.tsx` +3(슬롯마다 5종 드롭존을 자기 slotId 로 등록 / 드래그 중이 아니면 0개 / 드래그 중 10개) + 기존 6건에 `isProjectDragging`·`slotDropTarget` prop 추가. 측정: `bun run typecheck` exit 0 · `bun run typecheck:e2e` exit 0 · `bun run lint` 0 error(기존 warning 11건, 전부 내가 안 만진 파일) · `bunx prettier --check` 만진 TS 11파일 통과(docs 는 `.prettierignore`) · `bun test` **2665 pass / 0 fail** (연속 2회 동일).
  / 이탈: ① 계약 §1.B 는 "4방향 드롭존(+ 가운데 = 교체)"이라 적었고 그대로 **5종**을 넣었다 — UI 의 `center` 를 커맨드의 `replace` 로 번역하는 표를 `project-drag.ts` 에 두었다(탭 분할의 `center` 는 "탭 추가"라 의미가 다르다). ② 새 IPC 를 만들지 않았다 — 2a 의 `project_open_in_slot` 가 그대로 드래그의 종착지다. ③ **로케일 키를 추가하지 않았다**: 드롭존·오버레이에는 텍스트가 없고 거부 메시지는 서버 문구를 `describeIpcError` 가 그대로 쓴다. 덕분에 동시 작업 중인 2c 와 로케일 3종·`MESSAGE_NAMESPACES` 가 겹치지 않았다. ④ 드래그 state 를 `AppShell` 로 올리면서 `AppSidebar` 에 `draggingProjectId` prop 이 늘었다 — 사이드바가 `useDndContext()` 로 직접 읽게 하면 드래그 중 공개 컨텍스트 변화마다 리렌더된다. ⑤ 충돌 판정은 `pointerWithin` 우선 + **슬롯 드롭존을 후보에서 뺀** `closestCenter` 폴백이다. 폴백을 통째로 빼면 아이콘 사이 틈에서 떼는 기존 재정렬이 회귀하고, 슬롯을 빼지 않으면 드롭 대상이 없는 자리(타이틀바 등)에서 떼도 "가장 가까운 슬롯"이 분할된다. ⑥ 재정렬 테스트는 `commands.projectReorder` 가 아니라 `@entities/project/project.ipc` 의 `reorderProjects` 를 spy 한다 — 그 모듈이 다른 파일에서 프로세스 전역으로 목 처리돼 커맨드까지 내려가지 않는다(`docs/memory/test-conventions.md` §3). ⑦ 스파이크 테스트는 드래그를 **끝내고 60ms 를 실제로 기다린다**: dnd-kit 은 드래그 시작 시 document 에 capture-phase `click` 삼킴 리스너를 걸고 종료 **50ms 뒤에야** 치운다(`AbstractPointerSensor.detach`). 이 대기를 빼면 다음 파일의 클릭이 통째로 죽는다 — 실제로 `app-shell.test.tsx` 3건·`shell-slot-tree-view.test.tsx` 1건이 그렇게 깨졌다가 복구됐다(단일 파일 실행에선 보이지 않는다). ⑧ **Rust 는 한 줄도 건드리지 않았고 bindings 도 재생성하지 않았다.** 2b 는 TS 전용이고, `cargo fmt --all` 은 2c 가 편집 중인 파일을 덮어쓴다 — 내 변경에 Rust 가 없어 Rust 검증은 근거가 되지 못하므로 돌리지 않았다.
  / 미확인·후속: ① **런타임 실기 미확인**(앱 실행 금지) — 드롭존의 실제 히트 영역·미리보기 위치·DragOverlay 추종은 전부 요소 박스 계산이고 happy-dom 은 레이아웃이 0 이다(`test-conventions.md` §5). 사용자 실기 대상: 레일 아이콘을 슬롯 좌/우/상/하로 끌어 분할·가운데로 끌어 교체, 이미 다른 슬롯에 있는 프로젝트를 떨궜을 때 거부 토스트, 레일 재정렬이 예전 그대로인지, **슬롯 안에서 탭을 끌어도 슬롯이 분할되지 않는지**(중첩 격리의 육안 확인). ② `onDragStart`/`onDragOver`/`onDragEnd` 가 `AppShell` state 를 바꾸므로 그때마다 슬롯 트리 전체가 리렌더된다(전환 시점에만 — dnd-kit 은 `over` 가 바뀔 때만 `onDragOver` 를 부른다). 기존 `isDragActive`(OS 파일 드롭)와 같은 성격이라 React Compiler 메모이제이션에 기댔지만, 실기에서 끊김이 보이면 드래그 상태를 전용 컨텍스트로 내리는 것이 다음 수다. ③ `project_open_in_slot` 의 `path` 경로(아직 열지 않은 폴더를 바로 슬롯에)는 여전히 소비자가 없다 — 드래그 소스가 레일의 이미 열린 프로젝트뿐이다. OS 파일 드롭을 특정 슬롯에 떨구는 경로는 범위 밖. ④ Zen 중에는 레일이 숨겨져 드래그를 시작할 수 없고, 숨겨진 슬롯의 드롭존은 `hidden` 조상 아래라 잡히지 않는다. ⑤ 키보드만으로 분할하려면 여전히 context menu 4항목을 쓴다 — dnd-kit `KeyboardSensor` 는 붙이지 않았다(포커스 슬롯을 겨냥하는 메뉴가 이미 그 역할이다).

<!-- d62-record-F4-2c -->
- **§1.C 그룹 프론트 + §0.1 U-6 (2c TS) 완료 (2026-09-16)** — 2c Rust 가 낸 9 커맨드·`ProjectGroup`·`ProjectGroupsChanged` 를 UI 로 잇는 단계. **Rust 는 `MESSAGE_NAMESPACES` 한 곳만 건드렸고 bindings 는 재생성하지 않았다**(내 Rust 변경에 export 타입이 없고, 2c Rust 가 이미 현행화했다).
  / 변경 파일(역할): **신규 엔티티** `src/entities/project-group/project-group.ipc.ts`(9 커맨드 래퍼) · `project-group.query.ts`(`projectGroupListQueryOptions` + 사설 `useProjectGroupMutation` 팩토리(7 뮤테이션이 같은 `PROJECT_GROUP.ALL` 무효화를 공유 — `useSetProjectDisplay` 와 같은 "이벤트 왕복을 기다리지 않는 즉시성" 목적) + `useOpenProjectGroup` 만 `PROJECT.ALL` 무효화(그룹이 아니라 열린 프로젝트가 바뀌므로)) · **신규 순수 로직** `src/shared/lib/project-group.ts`(`resolveProjectGroupSections`(섹션/미분류/`groupIdByProjectId`) · `withProjectGroupMember`/`withoutProjectGroupMember` · `clampProjectGroupName`/`normalizeProjectGroupName` · `resolveProjectGroupColorVar`) · `src/shared/constants/project-group.ts`(`PROJECT_GROUP_NAME_MAX_CODEPOINTS=40`, Rust `GROUP_NAME_MAX_CODEPOINTS` 미러) · **신규 UI** `src/features/project/project-group-dialog.tsx`(생성·편집 공용, `project-display-dialog` 패턴 그대로 — 닫힘→열림 전이 렌더 중 리셋 + `PROJECT_COLOR_TOKENS` 12 스와치 재사용) · `src/widgets/app-sidebar/sortable-project-group-header.tsx`(접기 토글 겸 드래그 핸들 + 그룹 열기/이름·색 변경/삭제 메뉴 + 삭제 확인 `AlertDialog`) · **배선** `src/widgets/app-sidebar/app-sidebar.tsx`(2단 `SortableContext` — 바깥 그룹 헤더 / 안쪽 그룹별 멤버·미분류, 멤버십 쓰기(`handleAddToGroup`/`handleRemoveFromGroup`)와 새 그룹 다이얼로그 소유) · `src/widgets/app-sidebar/sortable-project-icon.tsx`("그룹에 추가 ▸" 서브메뉴(그룹 목록 + "새 그룹…")·"그룹에서 제거" 5 prop 추가) · `src/shared/lib/project-drag.ts`(`PROJECT_GROUP_DRAG_TYPE`·`ProjectGroupDragData`·`projectGroupDragDataOf`·`resolveProjectGroupReorder`; 2b 의 `resolveProjectReorder` 와 공통 `reorderById` 로 묶음 — 두 번째 사용처가 생긴 시점) · `src/widgets/app-shell/use-project-drag.ts`(`handleDragEnd` 선두에 그룹 분기 — 헤더 드래그는 슬롯 드롭존이 아예 렌더되지 않으므로 슬롯 판정보다 먼저 끝낸다) · `src/shared/constants/query-key.ts` + `query-key.test.ts` 매니페스트(`PROJECT_GROUP.ALL/LIST`, 프로젝트 스코프 아님 — 그룹은 멤버가 닫혀도 남는다) · `src/app/providers/ipc-sync-provider.tsx`(`projectGroupsChanged` → `PROJECT_GROUP.ALL` 무효화) · 로케일 3종 `projectGroup.*` 15키 + `domain/locale/service.rs` `MESSAGE_NAMESPACES` 새 네임스페이스(자기 키만 Edit) · 문서 `docs/features/layout-shell.md`(§2.1 구성에 그룹 섹션·미분류 추가, §2.3 context menu 에 그룹 2항목 + 슬롯 분할 4항목 명시, **신설 §2.4 프로젝트 그룹** — 두 축 분리표·헤더·2단 정렬표·다이얼로그·동기화).
  / 테스트: **+40건 (2665 → 2705 pass / 0 fail, 269 파일)**. 신규 4파일 — `shared/lib/project-group.test.ts` 11(멤버는 `session.projects` 순서·미분류·빈 그룹·닫힌 멤버 제외·그룹 0개는 전부 미분류·이중 소속은 마지막 그룹만 / 멤버 추가·제거의 불변성 / 이름 clamp·normalize·색 토큰) · `features/project/project-group-dialog.test.tsx` 8(정규화 제출·스와치 토글·Enter·공백 이름 비활성·진행 중 무제출·편집 모드 씨앗·취소 후 재열기·모르는 색) · `widgets/app-sidebar/sortable-project-group-header.test.tsx` 8(aria-expanded·collapsed 기본값·접기 커맨드·메뉴 3항목·열기 결과 토스트·열기 거부 토스트·삭제는 확인 후에만·편집은 바뀐 축만) · `widgets/app-sidebar/app-sidebar.test.tsx` 4(그룹 섹션의 멤버 순서·미분류 하단·접힘은 헤더만·그룹 0개면 전부 미분류). 기존 3파일 — `sortable-project-icon.test.tsx` +4(서브메뉴 새 그룹·그룹 목록과 소속 그룹 비활성·제거 비활성/동작), `use-project-drag.test.tsx` +3(헤더끼리는 그룹 순서·헤더↔아이콘 교차는 무동작·멤버 이동은 전역 `project_reorder`), `shared/lib/project-drag.test.ts` +2(`resolveProjectGroupReorder`). 측정: `bun run typecheck` exit 0 · `bun run typecheck:e2e` exit 0 · `bun run lint` 0 error(기존 warning 11건, 전부 내가 안 만진 파일) · `bun run format:check` 전체 통과 · `bun test` **2705 pass / 0 fail**(연속 2회 동일) · `cargo fmt --all -- --check` exit 0 · `cargo clippy --workspace --all-targets -- -D warnings` exit 0 · `cargo test --workspace` exit 0, **1687 passed / 0 failed**(lib 1655 + capability_symmetry 4 + domain_boundaries 3 + session_restore 8 + taide-cli 17).
  / 이탈: ① **그룹 헤더용 `DragOverlay` 내용을 만들지 않았다.** 2b 의 오버레이는 `draggingProjectId` 로만 그리는데, 헤더 드래그는 그 값을 세우지 않는다(`projectDragDataOf` 가 null) — 헤더는 `useSortable` 의 transform 으로 따라가고 오버레이는 비어 있다. 프로젝트 전용 상태를 그룹까지 확장하면 `AppShell`→`AppSidebar` prop 이 또 한 줄 늘고, 얻는 것이 56px 헤더의 반투명 사본뿐이라 넣지 않았다. ② **`resolveProjectReorder` 의 본문을 공통 `reorderById` 로 옮겼다**(2b 파일). 동작·시그니처·기존 테스트 전부 그대로이고, 그룹 순서가 같은 연산의 두 번째 사용처라 복붙 대신 묶는 쪽을 골랐다(common.md "2회 이상"). ③ **"미분류" 는 헤더 없이 하단 영역**이다 — 56px 레일에 "미분류" 라벨을 한 줄 더 그리면 그룹이 없을 때도 늘 보이게 된다. 대신 `role='group' aria-label='projectGroup.ungrouped'` 로 접근성 이름만 주고, 그룹이 있을 때만 위쪽 경계선을 붙인다(그래서 테스트가 이 영역을 이름으로 집는다). ④ **그룹 편집은 바뀐 축만 쓴다** — 다이얼로그가 이름·색을 함께 받지만 `project_group_rename`/`set_color` 는 별개 커맨드다. 둘 다 무조건 보내면 색만 고쳤는데도 `ProjectGroupsChanged` 가 두 번 나가고 세션이 두 번 저장된다. ⑤ **계약 §1.C 의 "그룹 열기" 는 헤더 메뉴에만 뒀다**(프로젝트 아이콘 메뉴에는 없다) — 아이콘 메뉴의 그룹 항목은 소속 편집 2개뿐이다. ⑥ `session.projects` 가 유일한 순서 정본이라 **그룹 내 순서 변경도 `project_reorder`(전역)** 이다. 멤버 아이콘을 다른 그룹의 아이콘 위에 떨구면 전역 순서만 바뀌고 소속은 그대로다 — 크로스 컨테이너 이동이 범위 밖(§0.1 U-6)이라는 계약과 일치한다. ⑦ **Rust 는 `MESSAGE_NAMESPACES` 만 건드렸다**: 새 네임스페이스 `projectGroup` 15키 등재 + 로케일 3종. `cargo fmt --all` 은 돌리지 않고 `--check`(exit 0)로 확인했다 — 남의 범위 파일을 덮어쓰지 않기 위해서다. ⑧ Welcome 최근 목록·`File > Open Recent` 는 계약대로 무변경.
  / 미확인·후속: ① **런타임 실기 미확인**(앱 실행 금지). 사용자 실기 대상: 56px 레일에서 그룹 이름이 실제로 읽히는지(9px 트렁케이트)·색 스와치가 라이트/다크 양쪽에서 구분되는지, 접기/펼치기가 다른 창까지 즉시 따라오는지, 그룹 열기 토스트의 개수가 맞는지, 헤더 드래그로 그룹 순서가 바뀌는지, 그룹이 있는 레일에서 기존 아이콘 재정렬이 예전 그대로인지. happy-dom 은 레이아웃이 0 이라 폭·트렁케이트·대비는 어떤 테스트로도 못 잡는다(`test-conventions.md` §5). ② **그룹 열기의 백그라운드 큐는 프론트에서 진행 상황을 보여주지 않는다** — 커맨드가 끝난 뒤 최종 `{opened, skipped}` 토스트 하나뿐이고, 진행 중 멤버는 `project:list-changed` 로 레일에 하나씩 나타난다. 멤버가 많을 때의 체감은 실기 확인이 필요하다. ③ **그룹은 슬롯과 아직 무관하다** — "그룹 열기 = 슬롯 자동 배치" 는 계약 §1.E 범위 외 그대로다. ④ 닫힌 멤버는 레일에 그려지지 않으므로, **그룹에 속한 채 닫혀 있는 프로젝트를 여는 UI 경로는 "그룹 열기" 뿐**이다(개별 멤버를 이름으로 골라 여는 목록은 없다). ⑤ `project_group_create` 는 항상 프로젝트 아이콘 메뉴의 "새 그룹…" 에서만 호출되므로 **빈 그룹을 만드는 경로가 없다**. 빈 그룹 자체는 서버가 허용하고 렌더도 되지만(테스트로 고정), 만들려면 만든 뒤 멤버를 빼야 한다.


- **메인 1차 검증 (2026-09-16, 2b+2c)** — `bun run verify` exit 0(bun test 2705/0 · cargo lib 1655 + 통합 32 · clippy 0 · prettier) · `bunx vite build` 0 · `bun run typecheck:e2e` 0.
- **렌즈 검토 (2026-09-16, wf `wf_36b523e7`, sonnet·xhigh 3렌즈)** — major 0 · minor 6 · info 1. 메인 판정:

| id | 심각도 | 내용 | 판정 |
|----|------|------|------|
| S-9 | minor | 그룹 열기의 활성 대상이 계획 시점 고정 → 첫 스텝 실패 시 활성화 없음(문서는 "실제로 연 첫 멤버") | **수용** — 루프에서 활성 미완료 플래그로 다음 성공 스텝에 승계 + 테스트 |
| B-1 | minor | `project_forget_recent` 가 그룹 변경 없이도 `ProjectGroupsChanged` 항상 발행(문서와 불일치) | **수용** — 변경 시에만 발행 |
| G-1 | minor | closestCenter 폴백이 슬롯 헤더·리사이저 위 드롭을 레일 재정렬로 오판 | **수용** — 포인터가 사이드바 rect 안일 때만 폴백 허용 + 테스트 |
| G-3 | minor | 중첩 DndContext 격리가 실제 AppShell 트리에서 pin 되지 않음 | **수용** — app-shell 테스트에 EditorArea DndContext stub 중첩 1건 |
| B-2 | minor | layout-shell.md §8.6 "그룹 없음(2c)" 잔존 | **수용** — 문서 정정 |
| G-2 | minor | 드래그 상태가 AppShell state 라 over 변경마다 슬롯 트리 리렌더(실측 없음) | **후속(§4)** — 실기에서 끊김 관측 시 전용 컨텍스트로 |
| S-10 | info | set_members 낙관적 동시성 없음(세션 API 전반 설계) | **후속(§4)** |

- **렌즈 검토 수정 — TS (2026-09-16, G-1·G-3·B-2)** — **G-1(레일 밖 closestCenter 폴백)**: `widgets/app-shell/use-project-drag.ts` 의 폴백이 이제 **포인터가 레일 rect 안일 때만** 돈다. 레일 밖에서 드롭존을 빗나간 릴리스는 `[]`(대상 없음)으로 끝나므로 슬롯 헤더·리사이저·타이틀바 위에서 손을 떼면 아무 일도 일어나지 않는다(전에는 "가장 가까운 아이콘" 재정렬로 읽혔다). 레일 rect 는 `AppSidebar` 가 `nav` 에 거는 **측정 전용 droppable**(`PROJECT_RAIL_DROPPABLE_ID`/`PROJECT_RAIL_DROP_TYPE` + `projectRailDropDataOf`, `shared/lib/project-drag.ts` 에 추가)을 dnd-kit 이 측정한 값에서 읽고, 그 droppable 은 `pointerWithin`·`closestCenter` 후보에서 **항상 제외**한다 — 후보로 두면 레일 위 릴리스가 전부 레일 자신에 붙어 재정렬이 죽는다. ref 대신 droppable 로 간 이유: 레일은 `AppShell` 이 아니라 `AppSidebar` 안에 있고, dnd-kit 이 드래그 시작 시 이미 전 droppable 을 측정하므로 별도 측정 경로나 ref 전달이 필요 없다. 슬롯 드롭존을 폴백 후보에서 빼던 기존 규칙은 그대로다. **G-3(중첩 격리 pin)**: `widgets/app-shell/app-shell.test.tsx` 의 `ProjectShell` 목이 `EditorArea` 자리에 **자기 `DndContext` 를 가진 얇은 stub**(탭 대역 draggable 1개 + 컨텍스트 프로브 2개)을 렌더한다. 프로브는 자기 위치의 컨텍스트가 등록한 슬롯 드롭존과 `active` 를 읽는다 — 바깥 프로브가 두 슬롯의 드롭존을 보므로 stub 이 **실제로 창의 프로젝트 `DndContext` 하위**이고, stub 안 pointerdown 이 안쪽 `active` 만 세우고 바깥은 `none` 이므로 **격리**다(합성 트리인 `project-drag-nesting.test.tsx` 와 달리 진짜 `AppShell` 트리에서). stub 은 `shell:<projectId>` 줄을 그대로 렌더해 같은 모듈을 목하는 `shell-slot-tree-view.test.tsx` 와 호환이고, 드래그 뒤 dnd-kit 의 50ms 클릭 스왈로어 해제까지 기다려 다음 파일을 오염시키지 않는다. **B-2(문서)**: `docs/features/layout-shell.md` §8.6 제목을 `(2b 시점)`→`(2c 시점)` 으로, "프로젝트 그룹 없음(2c)" 을 실제 남은 제한으로 교체(그룹×슬롯 자동 배치 없음 · 같은 프로젝트 다중 슬롯 금지 · 키보드 분할 없음 — `APP_KEYMAP` 에 슬롯 액션이 없음을 확인). 같은 내용이 두 불릿으로 중복돼 있던 "슬롯 간 탭 드래그 이동 없음" 은 하나로 합쳤고, §8.3 충돌 판정 불릿을 G-1 사실로 갱신했다. / 테스트: 신규 TS 4건 — `use-project-drag.test.tsx` 3건(포인터가 슬롯 드롭존 안이면 그 존, 레일 밖에서 빗나가면 대상 없음, 레일 안 아이콘 틈이면 가장 가까운 아이콘이고 슬롯은 폴백 후보가 아님 — happy-dom 은 rect 가 0 이라 레일·아이콘·슬롯 rect 를 손으로 지정) + `app-shell.test.tsx` 1건(위 중첩·격리). / 측정: `bun run typecheck` exit 0 · `bun run lint` 0 error(기존 warning 11건, 만진 파일엔 없음) · `prettier --check` 만진 파일 전부 통과 · `bun test` 2709 pass / 0 fail(2b+2c 1차 검증 2705 대비 +4). / 이탈·미확인: ① 레일이 화면에 없으면(Zen·레일 접힘) 폴백이 통째로 꺼진다 — 그 상태에선 레일 아이콘이 없어 프로젝트 드래그가 시작될 수 없으므로 동작 차이는 없다. ② **런타임 실기 미확인**(앱 실행 금지): 레일 경계 픽셀에서의 재정렬 체감과 슬롯 헤더·리사이저 위 드롭 무동작은 사용자 실기 대상이다. ③ Rust 검증(cargo)은 동시 작업자 담당이라 돌리지 않았다 — 이번 변경은 TS·문서뿐이고 Rust 파일은 건드리지 않았다.

- **렌즈 검토 수정 — Rust (2026-09-16, S-9·B-1)** — **S-9(그룹 열기 활성화 승계)**: `domain/project/commands.rs` 의 `project_group_open` 큐를 사설 `run_group_open_plan(steps, result, stop, open_member)` 로 뽑고, 계획의 활성화 표식을 "아직 활성화 못 함" 플래그(`pending_activation`)로 들고 다니게 했다 — `groups::plan_open` 은 무변경(첫 스텝만 `activate: true`)이고, 루프가 그 표식을 **실제로 열린 멤버에서만 소모**한다. 실패한 열기는 `ProjectActivated` 를 내지 않으므로 표식은 다음 성공 멤버가 이어받는다. 러너를 열기 함수에 대해 제네릭(`FnMut(GroupOpenStep, bool) -> Future<Output = Option<ProjectId>>`)으로 둔 이유는 하나다: 멤버 열기는 `AppHandle` 이 필요하고 이 레포엔 `tauri::test` mock app 하네스가 없어, 승계 규칙을 단위 테스트하려면 열기를 주입 가능하게 해야 했다(`projects_pending_watcher_restore` 가 순수 선택 함수로 빠진 것과 같은 제약). 종료(`is_shutting_down`) 재검사와 skipped 보고 의미는 그대로 러너 안으로 옮겼다. 결과 타입과 `docs/ipc-contract.md` 의 "실제로 연 첫 멤버" 문구는 이제 참이라 유지했고, `plan_open` 문서에 "최종 결정은 여기가 아니다" 1문장만 덧붙였다. **B-1(그룹 미변경 시 미발행)**: `service::forget_recent_projects` 의 반환을 `usize` → `ForgetRecentOutcome { removed, groups_changed }` 로 바꿔 `groups::forget_members` 의 판정을 호출부까지 그대로 전달한다(세션 재저장 조건과 **같은 값 1벌** — 스냅샷 재비교 없음). `project_forget_recent` 는 `ProjectListChanged` 는 항상, `ProjectGroupsChanged` 는 `outcome.groups_changed` 일 때만 발행한다. `docs/ipc-contract.md` 의 그룹 이벤트 발행 주체 줄도 "멤버십이 실제로 바뀌었을 때만" 으로 정정했다(말미 d-62 2c 절은 이미 조건부로 적고 있어 무변경). / 변경 파일: `src-tauri/src/domain/project/commands.rs` · `domain/project/service.rs` · `domain/project/groups.rs`(문서 1문장) · `src-tauri/tests/session_restore.rs`(호출부) · `docs/ipc-contract.md`. TS·바인딩 무변경(커맨드 시그니처와 노출 타입이 그대로다 — `ForgetRecentOutcome` 은 IPC 타입이 아니다). / 테스트: 신규 3건 + 기존 호출부 4곳 갱신. 신규 — ① `commands` `#[tokio::test]`(첫 멤버 실패·둘째 성공: `activate` 요청 순서가 `[true, true, false]`, 실제로 열린 멤버 중 활성화는 정확히 1건 = `ProjectActivated` 1회, 실패 멤버는 `skipped`), ② `commands` 소스 스캔(`emit_groups_changed` 는 한 곳이고 `if outcome.groups_changed {` 뒤에 있다 — 커맨드를 `AppHandle` 없이 못 돌리므로 기존 attach 순서 테스트와 같은 수단), ③ `service`(그룹은 있지만 지운 프로젝트가 어느 그룹의 멤버도 아니면 `groups_changed == false`). 갱신 — `service` 3건 + `tests/session_restore.rs` 1건이 새 반환 타입을 읽고 `groups_changed` 를 함께 단언. ①은 뮤테이션 검증까지 했다(플래그를 `step.activate` 로 되돌리면 실패). / 측정: `cargo fmt --all -- --check` exit 0 · `cargo clippy --workspace --all-targets -- -D warnings` exit 0 · `cargo test --workspace` **1690 passed / 0 failed**(lib 1658 + capability_symmetry 4 + domain_boundaries 3 + session_restore 8 + taide-cli 17) · `bun run typecheck` exit 0 · `bun run lint` 0 error(기존 warning 11건) · `bun run format:check` exit 0 · `bun test` 2709 pass / 0 fail(동시 진행 중인 TS 수정 포함). / 이탈: ① 계약 문언대로 플래그지만 그 플래그가 사는 루프를 사설 함수로 뽑았다 — 배정된 테스트("둘째가 활성·ProjectActivated 1회")를 커맨드 본문에 둔 채로는 쓸 수 없었다. ② 플래그 초기값은 무조건 `true` 가 아니라 `plan.steps` 에 활성화 표식이 있는지로 잡는다 — 계획이 "아무도 활성화하지 않는다"고 결정한 경우(현재 `plan_open` 은 그런 계획을 만들지 않는다)를 루프가 뒤집지 않기 위해서다. ③ B-1 은 "bool 반환 또는 스냅샷 비교" 중 bool(구조체 필드)을 택했다 — 스냅샷 비교는 그룹 목록을 한 벌 더 복제해 `forget_members` 가 이미 내놓은 답을 다시 계산하는 일이다. / 미확인·후속: ① 앱 실행 금지라 **런타임 실기 미확인** — 첫 멤버가 실패하는 그룹 열기에서 포커스가 둘째 멤버로 가는지, clear-recent 뒤 그룹 레일이 불필요하게 갱신되지 않는지는 실기 대상이다. ② 열기 실패 자체(capability attach 실패·롤백)는 여전히 하네스 부재로 유닛 테스트가 없다 — 러너는 실패를 `None` 주입으로만 검증한다.


- **메인 2차 검증 최종 (2026-09-16, 2b+2c 검토 수정 후)** — `bun run verify` exit 0(bun test 2709/0 · cargo lib 1658 + 통합 32 · clippy 0 · prettier) · `bunx vite build` 0 · `bun run typecheck:e2e` 0. 커밋 4분할(feat(session) 그룹 Rust·chore(locale)·feat(shell) 드래그 분할+그룹 UI(사이드바·app-shell 파일이 2b·2c 에 걸쳐 있어 프론트는 한 단위)·docs) → dev 푸시 → main ff → **v0.2.0 draft 릴리스**. 사용자 실기 대상(2b·2c): 레일 아이콘을 슬롯 좌/우/상/하로 드래그 분할·가운데 드롭 교체·이미 다른 슬롯에 있는 프로젝트 드롭 거부 토스트·레일 재정렬 회귀 없음·슬롯 안 탭 드래그가 슬롯을 분할하지 않음·슬롯 헤더/리사이저 위 드롭 무동작 / 그룹 생성·이름/색·접기·"그룹에 추가/제거"·그룹 열기(첫 멤버 활성, 결손 스킵 토스트)·그룹 삭제(멤버 유지)·Clear Recent 뒤 그룹 멤버 정리.

## 4. 미결·후속

- (2b·2c 검토 후속) 드래그 상태 전용 컨텍스트화(G-2, 실기 끊김 관측 시) · 그룹 멤버 set_members 낙관적 동시성(S-10, 세션 API 전반 합의 필요).

- 같은 프로젝트 다중 슬롯(미러 prune 상호작용 실측 후), 그룹 열기 자동 배치, 보조 창 위치·크기 복원, 슬롯별 explorer 폭 영속.
