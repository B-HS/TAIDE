# 사용성 배치 4 사용자 결정 (2026-09-04)

> 배경: 배치 3 구현 wf 진행 중 사용자가 추가 요청 7건을 접수. 조사 wf(opus·xhigh, 읽기 전용)
> `wf_c018b9c9`(7주제)·`wf_6aa56329`(2주제) 기동과 동시에 결정 질문 4건을 발신, 전부 추천안으로
> 회신. 계약은 조사 결과 합산 후 `2026-09-04-usability-batch4-contract.md` 로 작성한다.

## 요청 원문 요지

1. TAIDE 알림을 OS 자체 알림으로. 권한 필요 시 해당 권한 설정 창을 바로 띄우는 것도 필요.
2. ⌘⇧P 에서 Welcome 창을 띄울 수 있게, 혹은 프로젝트에 어떤 창도 없으면 Welcome 이 뜨게.
3. 속도 최적화를 극한까지 — 하드코딩 금지, 기존 기능 완전 보존, 추상화가 없으면 확장성을 위한
   추상화, 안정성을 위한 더 세세하고 많은 테스트 생성.
4. 좌측 프로젝트 목록: 폴더 아이콘만이 아니라 우클릭으로 아이콘 변경 또는 텍스트(overflow 없이) 표시.
5. git 탭: stash·changes 영역 구분이 모호 — UI/UX 향상.
6. 코드뷰 터미널 우클릭 컨텍스트 메뉴에 분할(상황에 따라 상하좌우 모두).
7. 코드뷰 위 파일 탭 우클릭 컨텍스트 메뉴(새 파일·새 터미널 + 더 있으면 추가).

## 결정 (질문 4건 — 전부 추천안)

1. **알림 정책 — 비활성 창 + 완료성 이벤트만.** 창이 포커스를 잃었을 때만, 에이전트·태스크·git 원격
   작업·검색 치환·LSP 설치 완료와 error 를 OS 알림으로 보낸다. 포커스 중에는 기존 sonner 토스트
   유지. 설정에서 카테고리별 on/off. **공식 `tauri-plugin-notification` 신규 의존성 도입 승인.**
   권한 거부 시 macOS 시스템 설정의 알림 창을 바로 연다.
2. **성능 — 8지표 계측 내장 + FE·Rust 전부.** 부팅→첫 페인트 · 프로젝트 전환 · 파일 열기(소/대) ·
   팔레트 열기/입력 응답 · 트리 펼침 · git status · 전역 검색 · 터미널 출력 처리량 + 메모리를 앱에
   계측 코드로 내장해 기준선을 재고, 병목 상위부터 반복 개선. 신규 의존성 0. 기존 기능 완전 보존,
   추상화 지점 명시, 테스트 대폭 보강 동반.
3. **탭 바 우클릭 — 빈 여백 메뉴 신설 + 기존 탭 메뉴 보강.** 여백: 새 파일·새 터미널·최근 닫은 탭
   다시 열기·분할·모두 닫기·Welcome 열기. 기존 탭 메뉴(닫기류·고정·분할·경로 복사·탐색기에서
   보기)에는 가치 있는 항목만 보강(중복 금지).
4. **Welcome — 커맨드 + 탭 0 자동 표시.** ⌘⇧P 에 "Welcome 열기" 커맨드 추가, 프로젝트에 열린 탭이
   하나도 없으면 Welcome 자동 표시(설정으로 끌 수 있게).

## 질문 없이 확정한 전제 (사용자에게 고지 — 이의 없음)

- **프로젝트 목록(4)**: 우클릭 메뉴에서 lucide 아이콘 선택 + 1~4자 텍스트 라벨(넘치면 잘라서
  툴팁) + 색 토큰. 프로젝트별 저장. 이모지는 컨벤션상 제외.
- **git 탭(5)**: VS Code SCM 파리티 — 접이식 섹션 헤더 + 개수 뱃지 + sticky 헤더 + Stash 독립 섹션.
- **터미널 우클릭(6)**: 분할은 VS Code 처럼 "새 터미널을 그 방향에 생성", 불가능한 방향(최소 크기
  등)은 비활성 표시. 복사·붙여넣기·모두 선택·지우기·새 터미널·종료 동반.

## 진행 규칙 (재확인)

- 조사·구현 opus·xhigh / 리뷰 sonnet·xhigh / 테스트 fable·medium, 전부 Workflow(서브에이전트 금지) —
  `docs/agent-operations.md` §1.

## 2차 결정 (조사 결과 회신 후 질문 3건 — 전부 추천안, 2026-09-04)

5. **테스트 하네스 — happy-dom + @testing-library/react + @testing-library/dom 도입(devDependency 3종).**
   컨벤션 `frontend.md` §11·`docs/tech-stack.md` 의 Testing Library 명시와 정합. 앱 번들 무영향.
6. **CI 게이트 신설 — push(dev·main) + pull_request.** 프론트 job(typecheck·lint·format:check·bun test·
   typecheck:e2e)은 ubuntu, Rust job(fmt·clippy·test)은 macOS 러너.
7. **성능 배치 범위 — 계측 + 안전 수정 + 2단계 개선까지.** 1단계: 8지표 계측(env 게이트, 기본 off)·벤치
   스크립트·예산 테스트 + 회귀 위험 0 수정. 2단계: 정적 분석·마이크로벤치로 순위가 확정된 대형 3건
   (project_open 락 후절화·IdCache 필터·git status 캐시 TTL) + FE 가상화 확대(키보드 접근성 동반).
   monaco 지연 로딩·트리 응답 재설계·`ps` fork 제거·`update_index` 는 별도 배치/보류. 실기 8지표 기준선
   측정은 사용자 몫(앱 실행 필요).

## 조사 추천안으로 확정한 세부 (질문 없이 — 계약에 반영)

- 알림: 권한 거부 감지는 플러그인 데스크톱 API 가 항상 granted 를 반환해 원리적으로 불가 → 설정
  Notifications 섹션에 "알림 설정 열기"·"테스트 알림" 버튼 상시 노출 + 첫 발송 1회 안내 토스트. 설정은
  플랫 bool(enabled·onlyWhenUnfocused·카테고리별). 에이전트 완료는 10초 이상 작업 후 Idle 전이만,
  태스크 완료는 OSC 133 `D`(exitCode) 재사용. dev 실행에서 'Terminal' 이름으로 뜨는 상류 제약은 문서화.
- Welcome: 탭 제목 리터럴 'Welcome', 빈 pane 렌더 교체(메인 창 한정), 끄기 설정 `welcomeOnEmptyEditor`
  포함(사용자 1차 결정 문언), 기본 단축키 없음, 문서는 layout-shell.md.
- 프로젝트 목록: 아이콘/라벨/기본 3택 배타, FR-A4 보류 각주, `graph.lane1..12` 색 재사용, 글리프 색만,
  1차 사이드바만, 원격 허용, 큐레이션 약 56종 정적 레지스트리.
- git 탭: Stash 를 Changes 아래로·0건이면 미렌더·Stash 버튼 헤더 바 이동, 접힘은 모듈 스코프 메모리
  (기본 Stashes 만 접힘), 헤더 대비는 구분선+sticky+들여쓰기, 로빙에 헤더 포함(←/→ 접기), Graph 헤더 통일.
- 성능 계측 노출: 환경변수 `TAIDE_PERF=1` 게이트로 양 빌드 모두, 기본 off(dev 빌드는 기본 on). git status
  캐시는 무효화 + 2초 TTL. `[profile.profiling]` 추가(릴리스 프로필 불변).
- 테스트: `mock.module` 누수 대응 `beforeEach(mock.restore)`, 커버리지 게이트 미도입, e2e 확장 선결로
  원격 페이지 HMR 클라이언트 비활성(dev 전용).
