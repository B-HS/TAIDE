# 배치 큐 소진 후 사용자 결정 일괄 (2026-08-25)

> 커밋 5분할(`d695aba`~`ccb1c05`)·main ff 병합·origin 푸시 완료 직후, 잔여 항목 전체에 대한
> 사용자 결정. 이 문서가 후속 배치(d-36~d-39)의 승인 정본이다.

## 1. 테마 — "알아서 잘 고치고 모든 테마 전수검사" (d-36 승인)

- builtin_light(`taide-light`) `panel.matchHighlight` 저대비(2.15 — d-31 §5.2) **정정 승인**,
  값 선정은 위임(업스트림 Catppuccin Latte 팔레트 내 재선정 원칙 — d-31 L2-1 A안 선례).
- 검사 범위를 **카탈로그 38종 전체**(번들 36 + builtin 2)로 확장 — builtin 2종이 어떤
  기계 검사에도 빠지지 않게 한다(d-31 §5.2 결정 옵션 2 채택). 예외는 기존 등재 2종
  (everforest-light·rose-pine-dawn — 업스트림 대체 후보 부재) 사유 승계.
- 번들 재변환 정책: 재변환·재생성 하지 않음 — `theme-system.md` §8.2.3 손수정 예외가 정본
  (재변환 일치 강제는 d-31 적대적 기각 확정, 재론 금지).

## 2. AI 묶음 (d-37 승인 — bindings 표면·행동 변경 승인 포함)

- **AI 응답 동형 타입 3종 통합**(d-28 §3.1 #17) — bindings 표면 변경 승인("통합추상화").
- **codex 절단 보정**(d-28 #18) — 항상-에러 → 관용 분기 행동 변경 승인("잘림은 당연히 수정").
- ollama↔omlx 파이프라인 트레이트 재설계는 위 둘과 묶어 구현 시 판단(큐 정본 §3-3 문언 승계).

## 3. 원격 dispatch 정책 (d-38 — 2026-08-25 객관식 확정)

| 항목 | 결정 | 처리 |
|------|------|------|
| `ide_publish_diagnostics`/`ide_notify_at_mention` 원격 게이트 | **현재대로 허용** | 정책 문서에 "검토 완료·허용 확정" 명문화만 |
| `agent_hooks_install`/`uninstall` 비대칭(User 스코프 제거는 허용) | **현재대로 유지** | 의도된 비대칭으로 명문화만(제거는 백도어를 없애는 방향) |
| 키링 변경(AI 토큰 3종 set/clear·GitHub PAT) | **변경 전부 원격 금지** | 코드 전환 — 거부 목록 이동+거부 정책 variant, 상태 조회(`ai_token_status` 등)는 허용 유지 |

## 4. 기각·순서 확정

- **"원격 dispatch QoS" 캠페인(d-35 §5.1 이월) = 사용자 기각**("필요없어") — 착수하지 않는다.
  선행 조건·채택 불가 목록은 기록으로만 존속.
- 실기 확인(테마 색·오류 토스트 3언어·qa6 d-35 신설 2항목)은 **사용자 본인이 수행**.
- **e2e = A안**(원격 미러+Playwright webkit, 앱 무변경 — `2026-08-14-e2e-path-research.md` 정본)
  + **신규 의존성 Playwright 승인**(devDependency 한정). e2e 파일럿+전문 QA(d-39) 완료가
  Phase 8 배포 진입 조건.

## 5. 추가 결정 (2026-08-25 2차 객관식)

- 선택 행 표면 대비(d-36 §5) = **정공법**(전면 게이트+데이터 정정 — d-40) / omlx base_url(d-38 §6) = **스트립 편입**(d-41).
- d-36~38 산출물 커밋 = 4분할(fix d-36 / feat d-37 / fix d-38 / docs) + main ff 병합 + 푸시.
