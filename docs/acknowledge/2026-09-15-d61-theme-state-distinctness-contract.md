# d-61 — 사용성 배치 5 웨이브 4: 테마 상태색 구별성·대비 전수 린트 · UI 토큰 배선 갭 · 형제 variant 9종 · 스크린샷 매트릭스 (2026-09-15)

> 사용자 항목 13("더 많은 스킨 테마, 모든 UI 를 고려해서 제대로"). 결정 전제: `acknowledge/2026-09-15-usability-batch5-user-decisions.md` §1 #13 · §3 #2·§3.1(상태색 겹침 정정 + 메인 집계) · §4(웨이브 3 과 worktree 병렬).
> 조사 정본 `research/2026-09-15-batch5-research.md` T9. 테마 정본 `docs/theme-system.md`. **worktree `/Users/gkn/taide-w4`(브랜치 `d61-themes`, dev 기점)에서 구현·검토 후 dev 머지.**
> 규칙: 코드 주석 금지(JSDoc·Rust /// 만)·arrow only·매직넘버 상수화·이모지 금지·`#[allow]` 금지·신규 의존성 0. 로케일 키 추가 시 3종 + MESSAGE_NAMESPACES 등재.

## 0. 현행 (T9 + 메인 직접 집계 — 근거 확인)

| 사실 | 근거 |
|------|------|
| 번들 테마 **38종**(+Rust 리터럴 builtin 2 = 카탈로그 40). 토큰 184/테마(colors 133·syntax 31·terminal 20) **전량 명시**(변환 스크립트가 미충족 시 exit 1) → "누락 폴백" 은 0. 실제 위험은 **서로 다른 토큰이 같은 값으로 접혀 상태·경계가 사라지는 것** | `src-tauri/resources/themes/*.json`, `src-tauri/src/domain/theme/service.rs:14-85,753-758`, `src/shared/lib/theme-convert/ui-token-vocabulary.ts` |
| **상태색 = 바탕색 동일값**(메인 집계, 38종): 에디터 드래그 선택 = 에디터 배경 6종(vscode-dark-plus·light-plus·dark-modern·light-modern, visual-studio-cpp-dark·light) / 설정 토글 켜짐 트랙(`button.primaryBackground`=`--primary`) = 설정 카드 배경(`panel.background`=`--card`) 5종 / 토글 손잡이(`app.background`) = 켜짐 트랙 6종 / 터미널 선택 = 터미널 배경 21종 / 비활성 선택 = 에디터 배경 17종 / 활성 탭 = 탭바 배경 10종 / 현재 줄 강조 = 에디터 배경 7종 / 버튼 hover = 버튼 배경 8종. 스위치: 켜짐 `bg-primary`·꺼짐 `bg-input`(=`input.border`)·손잡이 `bg-background` | 결정 문서 §3.1, `src/shared/ui/switch.tsx:11,17`, `src/shared/styles/global.css:132-149` |
| 대비 가드는 **7쌍**(앱·에디터·패널 헤더·툴팁·매치 강조 5 blocking + 선택 행 2 advisory), `MIN_CONTRAST_RATIO = 3`. ΔE 헬퍼 `deltaE76`(CIE76) + `MATCH_HIGHLIGHT_MIN_DISTINCT_DELTA_E = 2.3` 존재. Rust 카탈로그 린트 7종(토큰 전량·app 대비·list 구분·matchHighlight 불투명/ΔE·대비 3축·list 전경) + 예외 등재분 역검증 테스트 | `src/shared/lib/theme-convert/contrast.ts:33,70-83,207,250,274`, `mapping-tables.ts:82-114`, `src/shared/lib/color.ts`, `service.rs:1349-1919` |
| 미배선 토큰 9종: `menu.background/border/itemHover/separator`(드롭다운·컨텍스트메뉴는 `--popover/--accent/--border` 사용), `scrollbar.thumb/thumbHover/track`(`::-webkit-scrollbar` 규칙 없음·OverlayScrollbar 미소비), `app.shadow`(그림자는 Tailwind 기본), `popover.separator`·`tooltip.itemHover/separator`·`modal.itemHover/separator`, `explorer.folderIcon/gitModified/gitAdded/gitDeleted` | T9 Q2(b), `src/shared/ui/dropdown-menu.tsx`, `context-menu.tsx`, `src/shared/scroll/overlay-scrollbar.tsx` |
| raw 색 5곳: `dialog.tsx:33`·`alert-dialog.tsx:24` `bg-black/50`, `button.tsx:14` `text-white`, `features/preview/html-preview.tsx:9` `bg-white`, `features/theme/color-picker.tsx:217,242` `border-white`. 죽은 `dark:` 분기 6곳(`button.tsx:8,14,16,18`·`dropdown-menu.tsx:56`·`context-menu.tsx:97` — `.dark` 클래스를 붙이는 코드 없음) | T9 Q2(a) |
| 형제 variant 후보(라이선스 MIT 1차 확인, 기존 등재 저장소): Catppuccin Latte(light)/Frappé/Macchiato, Tokyo Night Storm/Light(light), Gruvbox Light Medium(light)/Dark Hard/Dark Soft, Rosé Pine Moon, Ayu Mirage, GitHub Dark Dimmed (+ GitHub HC 2종은 `--type` 스키마 밖 → 제외·backlog). 변환 CLI `bun run themes:convert --input --id --name --type --source-url --author --license [--include-dir]`. 등록 4단계: 변환 → `service.rs` `include_str!` 배열 → `THIRD_PARTY_LICENSES.md` 백틱 id → 검증(licenses·contrast 테스트·Rust 린트). `THIRD_PARTY_LICENSES.md:17` "36 color themes"·`mapping-tables.ts:79`·`contrast.ts:57` 주석 stale(실제 38) | T9 Q3 |
| 신규 토큰 추가는 5곳 동기(vocabulary·theme-tokens·Rust types·builtin 2종·JSON 전량, `service.rs:2146` 대조 테스트) → **이 계약은 신규 토큰을 만들지 않는다** | T9 리스크 5 |
| e2e 하네스: `e2e/specs/09-theme-switch-revert.e2e.ts`(테마 전환 선례), `bun run e2e` 는 에이전트 실행 가능(dev 인스턴스 `TAIDE_E2E_NO_HMR=1 bun run tauri dev` 기동은 사용자) | `docs/quality-assurance/2026-08-18-e2e-harness.md` |

## 1. 수정 방향

### 1.A 상태색 vs 바탕색 구별성 린트 + 데이터 정정 (M~L, TS+Rust+JSON) — 최우선

- **쌍 표 정본** `src/shared/lib/theme-convert/state-distinctness-pairs.ts`: 컴포넌트가 실제로 어떤 바탕 위에 어떤 상태색을 그리는지 코드(`global.css` `@theme inline` 매핑 → `shared/ui/*`·위젯 클래스)로 도출한 전수 표 `STATE_DISTINCTNESS_PAIRS: { label, stateKey, containerKey, minDeltaE, alternative? }[]`. 최소 포함: 에디터 선택/비활성 선택/현재 줄 강조/찾기 매치/괄호 매치 vs `editor.background`; 터미널 선택 vs `terminal.background`; 목록 hover/선택 vs `list.background`, 선택 vs hover; 활성 탭 vs `tabBar.background`(대안: 활성 탭 상단 테두리 토큰이 있으면 그것의 구별성으로 대체); 스위치·체크박스 켜짐(`button.primaryBackground`) vs 설정 카드(`panel.background`)·앱 배경, 켜짐 vs 꺼짐 트랙(`input.border`), 손잡이(`app.background`) vs 켜짐·꺼짐 트랙; 버튼 hover vs 버튼; `input.border` vs `input.background`; `app.focusBorder` vs `app.background`·`panel.background`; `scrollbar.thumb` vs `scrollbar.track`; 배지·상태바·사이드바 활성 항목 등 실사용에서 발견되는 쌍 전부. 각 쌍은 어느 컴포넌트·클래스에서 유래했는지 JSDoc 1줄.
- **판정**: `#rrggbbaa` 는 바탕 위에 알파 합성 후 비교. `deltaE76(state, container) >= minDeltaE`(기본 상수 `STATE_MIN_DISTINCT_DELTA_E`, 값은 fixer 가 38종 분포를 실측해 "위 §0 동일값 사례는 전부 잡고 의도적 subtle 디자인은 통과" 하는 경계로 정하고 근거를 §3 에 기록 — 예: 동일값 = 0, subtle = 2~4, 명확 = 5+). 현재 줄 강조·비활성 선택처럼 관례상 subtle 한 쌍은 개별 `minDeltaE` 를 낮게.
- **TS**: `contrast.ts` 옆 `state-distinctness.ts`(`validateStateDistinctness(colors)` + `repairStateDistinctness(colors, palette)`), 변환기 `convert.ts` 파이프라인에 검증·수리 단계 추가, `bundled-theme-state-distinctness.test.ts`(38종 + 신규 전수 통과). 수리 규칙: **바탕이 아니라 상태 토큰을 고친다.** 테마 자체 `palette` 안에서 조건을 만족하는 가장 가까운 색을 고르고, 없으면 바탕↔전경 혼합(`mix(container, app.foreground, t)`, t 를 최소 ΔE 를 넘는 최소값으로 이진 탐색)으로 파생. 수리 결과는 결정적.
- **Rust 미러**: `service.rs` 카탈로그 린트에 같은 쌍 표·임계·알파 합성으로 `번들_테마는_상태색이_바탕색과_구별된다` 추가(선례 matchHighlight ΔE 린트). 쌍 표는 TS 정본을 Rust 가 복제하되 개수·라벨 일치 테스트로 드리프트 방지(선례 `service.rs:2146` 토큰 대조).
- **데이터 정정**: 38종 JSON 을 수리 규칙으로 갱신하는 1회 스크립트(`scripts/repair-theme-state-distinctness.ts`, 실행 후 결과 JSON 을 커밋·스크립트는 재실행 가능·멱등). 테마별 변경 토큰·전후 값 표를 `docs/theme-system.md` 신규 절에 기록. 예외 등재는 원칙적으로 0(의도적 flat 디자인이라 주장하려면 원본 VS Code 테마의 같은 토큰이 동일값임을 근거로 제시하고 역검증 테스트 짝 추가).

### 1.B 글자/배경 대비 전수 (M, TS+Rust)

- `CONTRAST_PAIRS` 를 컴포넌트 실사용 전경/배경 쌍 전수로 확장(목록 글자/hover·선택 행, 배지, 상태바, 입력 placeholder·글자, 버튼 글자/배경(primary·secondary·destructive), 탭 글자/활성·비활성 탭 배경, 메뉴 항목 글자/메뉴 배경·hover, 터미널 ANSI 16색 vs `terminal.background`(advisory), git 데코 색 vs 트리 배경, diff 추가/삭제 글자 vs 배경, 문제 심각도 색 vs 패널 배경). `MIN_CONTRAST_RATIO = 3` 유지(기존 임계). 실패는 `repairContrastPairs` 확장으로 전경 쪽 수리. Rust 대비 린트도 동일 확장. 38종 결과·정정 표 기록.

### 1.C UI 배선 갭 (M, TS/CSS)

- `menu.*` 4토큰: `global.css` `@theme inline` 노출 + `dropdown-menu.tsx`·`context-menu.tsx` 가 `bg-menu-background`·`border-menu-border`·`focus:bg-menu-item-hover`·구분선 `bg-menu-separator` 사용(팝오버 토큰 의존 제거). `popover.separator`·`tooltip.*`·`modal.*` 미배선 5종은 해당 컴포넌트(popover/tooltip/dialog 구분선·hover)에 배선.
- 스크롤바: `overlay-scrollbar.tsx` 가 `scrollbar.thumb/thumbHover/track` 을 사용, `global.css` 에 네이티브 `::-webkit-scrollbar` 규칙(OverlayScrollbar 를 안 쓰는 잔여 표면용).
- 그림자: `app.shadow` 를 팝오버·다이얼로그·드롭다운의 `shadow-*` 대신 `box-shadow` 색으로 배선. 모달 오버레이 `bg-black/50` 2곳 → `app.shadow` 기반 스크림(`color-mix(in srgb, var(--taide-app-shadow) 50%, transparent)` 또는 동등, 신규 토큰 없이).
- raw 색: `text-white`→`text-button-primary-foreground`(있으면) 또는 대응 토큰, `bg-white`(html 프리뷰 iframe)→`bg-editor-background`, `border-white`(색상환 핸들)→`border-app-border`. 죽은 `dark:` 6곳 제거. `explorer.folderIcon`→파일트리 폴더 아이콘 색, `explorer.gitModified/gitAdded/gitDeleted`→트리 git 데코 색(현 `git.*` 사용처를 explorer 토큰으로 전환하되 변환기가 두 토큰을 같은 원천에서 파생하도록 mapping 확인).
- 테스트: 토큰 소비자 존재를 기계 검사하는 테스트(`ui-token-vocabulary` 의 모든 colors 토큰이 `src/**` 에서 최소 1회 소비되거나 명시 예외 목록) 신설 — 재발 방지.

### 1.D 형제 variant 9종 추가 (M, 스크립트+JSON+Rust 등록+문서)

- 대상 9종(라이트 3 포함): Catppuccin Latte·Frappé·Macchiato, Tokyo Night Storm·Light, Gruvbox Light Medium·Dark Hard·Dark Soft, Rosé Pine Moon, Ayu Mirage, GitHub Dark Dimmed 중 **9종을 라이트 비율 ≥ 20% 가 되도록 선정**(fixer 가 최종 목록을 §3 에 기록). 원본 JSON 은 해당 저장소 태그의 raw 파일(WebFetch, 1차 출처)로 받아 `scripts/convert-vscode-theme.ts` 로 변환(`--include-dir` 필요 여부 확인). 1.A·1.B 린트를 **통과한 상태로** 추가.
- 등록: `service.rs` `include_str!` 배열, `THIRD_PARTY_LICENSES.md` 기존 섹션 "Bundled as:" id 추가 + 상단 개수 정정(36→47), `mapping-tables.ts:79`·`contrast.ts:57` 주석 개수 정정, `docs/theme-system.md` 카탈로그 표. `bundled-theme-licenses.test.ts` 가 개수도 검증하도록 보강(stale 재발 방지).

### 1.E 스크린샷 매트릭스 + 비전 검토 (M, e2e — 실행은 사용자 dev 기동 후)

- `e2e/specs/26-theme-surface-matrix.e2e.ts`: 번들 테마 전수를 순회하며 표면별 스크린샷(사이드바+탐색기 선택/hover 행, 에디터 드래그 선택·현재 줄·찾기 매치, 터미널 선택, git 패널, 탭바 활성/비활성, 명령 팔레트, 컨텍스트 메뉴, 설정 화면 토글 켜짐/꺼짐, 다이얼로그 오버레이, 툴팁)을 `e2e/artifacts/theme-matrix/<theme>/<surface>.png` 로 저장. 선례 `09-theme-switch-revert` 의 테마 전환 방식 재사용. 스펙은 작성·`typecheck:e2e` 통과까지, 실행은 사용자가 `TAIDE_E2E_NO_HMR=1 bun run tauri dev` 를 띄운 뒤 메인이 `bun run e2e --grep theme-surface-matrix` 로.
- 실행 후 비전 검토 wf(sonnet): 스크린샷을 읽어 "상태가 안 보임/경계 소실/글자 묻힘" 을 표면별로 판정 → 결과를 `docs/quality-assurance/2026-09-15-theme-matrix-review.md` 에, 결함은 1.A/1.B 표·임계 보정으로 환류.

### 1.F 범위 외

- 고대비(hc) 테마 타입, 신규 토큰 추가, Tier B 서드파티 테마(라이선스 미검증), 취약 테마 전면 재변환(O4 — 1.A/1.B 수리로 대체), Monaco syntax 토큰 19종 확장(backlog 기존).

## 2. 실행 계획

- worktree: `git worktree add /Users/gkn/taide-w4 -b d61-themes dev` → `bun install --frozen-lockfile` → 첫 `cargo test` 로 빌드 캐시 생성(메인이 준비).
- 구현 wf(opus·xhigh, 작업 경로 `/Users/gkn/taide-w4`): **A1**(1.A TS 쌍 표·검증·수리·스크립트·JSON 정정) → **R**(1.A Rust 미러 + 1.B Rust 확장 + 1.D Rust 등록, Rust 단일) ; **A1 완료 후 B1**(1.B TS) ; **C**(1.C, 병렬) ; **D**(1.D 변환·문서, A1 의 수리 규칙이 convert.ts 에 들어간 뒤 실행) ; **E**(1.E 스펙, 병렬). 각 에이전트 종료 조건: worktree 에서 typecheck·lint·prettier·관련 bun test·(Rust) fmt/clippy/test.
- 렌즈 검토 wf(sonnet·xhigh 3렌즈: 데이터 정정의 근거·회귀(기존 테마 외관 변화 최소성)·경계(라이선스·5곳 동기)). 메인 2차 `bun run verify` + vite build(worktree) → 커밋(분할: feat(theme) 린트·data(theme) 정정·feat(ui) 배선·feat(theme) 9종·test(e2e)) → dev 머지(3 과 순서 조율) → 사용자 dev 기동 → 1.E 실행·비전 검토 → 환류 수정.

## 3. 구현 기록

- **§1.A TS 완료 (2026-09-15)** — 변경 파일: `src/shared/lib/theme-convert/state-distinctness-pairs.ts`(신규, 실사용 도출 쌍 표 37행 + 임계 2종, Rust 미러가 복제할 plain literal export), `state-distinctness.ts`(신규, `validateStateDistinctness`/`repairStateDistinctness` — surface→container→state 알파 합성 + `deltaE76` 판정, palette 최근접 → 바탕↔`app.foreground` 혼합 이진탐색 수리, 다중 패스), `convert.ts`(구별성 수리를 대비 수리 **앞에** 배치 + `stateDistinctnessErrors` 필드), `state-distinctness.test.ts`(신규 단위 13건), `bundled-theme-state-distinctness.test.ts`(신규 게이트 5건: 38종 전수·수리 멱등·`terminal` 미러 일치·라벨 유일·어휘 참조), `convert.test.ts`(+2건), `scripts/repair-theme-state-distinctness.ts`(신규, 멱등·파일별 prettier 형식 보존), `scripts/convert-vscode-theme.ts`(경고 출력), `package.json`(`themes:repair-state-distinctness`), `docs/theme-system.md` §8.5 신설(임계 근거 축별 분포표 37행 + 테마별 정정 전후 표 36행 + 제외 축 표). **번들 JSON 36종 139토큰 정정**(`src-tauri/resources/themes/*.json`, 재실행 0건). 임계: `STATE_MIN_DISTINCT_DELTA_E = 2.3`(CIE76 JND, 기존 `MATCH_HIGHLIGHT_MIN_DISTINCT_DELTA_E` 와 동일 근거), 관례상 옅은 3축만 `SUBTLE_STATE_MIN_DISTINCT_DELTA_E = 1` — 전 축에서 접힌 값 0.00~2.19 / 임계 이상 최솟값 2.32+ 로 빈 구간에 떨어진다. §0 동일값 사례 전수 적발 확인(에디터 선택 6·현재 줄 7·비활성 선택 17·터미널 선택 21(+근접 3=24)·토글 켜짐트랙 5·손잡이 6·버튼 hover 8), 활성 탭 10종은 `tabBar.tabActiveIndicator` 대안으로 전부 통과(정정 0). 예외 등재 0. / 테스트: `bun test` 2462 pass 0 fail(신규 20건 포함), `bun run typecheck` 통과, `bun run lint` 0 error(기존 경고 11), `bunx prettier --check` 전 변경 파일 통과. / 이탈: ① 쌍 타입에 `surfaceKey` 추가하고 `alternative?` 를 `alternativeStateKey` 로 명명 — 반투명 컨테이너를 raw RGB 로 읽으면 dracula 식 `explorer.itemHover #44475A75` vs `itemSelected #44475A` 가 ΔE 0 오탐이 되어 14종을 헛수리하게 된다(surface 합성 후 6종만 실제 위반). ② 이진 탐색을 연속 t 가 아니라 256단계 정수 인덱스에서 수행하고 상한을 "검증된 통과 단계"에만 둬, 단조성 가정 없이도 반환값이 반드시 임계를 넘게 했다. ③ `alternativeStateKey` 가 있는 쌍은 대안 토큰을 수리한다(평평한 탭 스트립은 의도된 디자인, 인디케이터가 구별을 지라고 있는 토큰). ④ 계약 파일 목록 밖 추가: `state-distinctness.test.ts`(기존 `contrast.test.ts`/`bundled-*` 2단 구성 관례), `convert.test.ts` 2건, `package.json` 스크립트 1줄. ⑤ 남은 위반은 `outputColorErrors` 가 아니라 `stateDistinctnessErrors` 로 분리 — 신규 임포트 거부 케이스 금지(d-40 §1-a). ⑥ `docs/theme-system.md` §8.4 는 이미 "UI 노출" 이라 **§8.5** 로 신설. / 미확인·후속: (a) Rust 미변경이라 cargo 계열 미실행 — 대신 번들 JSON 을 읽는 기존 Rust 불변식(`list.activeBackground` != `panel.background`/`list.hoverBackground`, `app.foreground` != `app.background`, `list.foreground` != `list.background`/`hoverBackground`, 색값 전량 hex|transparent)을 스크립트로 직접 재확인해 통과. R 이 미러 추가 후 `cargo test --workspace` 필요. (b) `popover.itemHover`·`tooltip.itemHover`·`modal.itemHover` 는 현재 소비처가 없어 표에서 제외 — §1.C 가 배선하면 3행 추가 필요(§8.5 제외 축 표에 명시). (c) 수리는 임계를 막 넘는 최소 변화라 결과가 JND 경계다 — 밝은 테마의 `github-light editor.bracketMatch #f8f8f8`·`vitesse-light app.focusBorder #f8f8f8` 등은 §1.E 스크린샷 매트릭스로 실제 가독성 확인 후 임계 상향 여부 판단. (d) `app.border` vs `app.background`/`panel.background` 축은 borderless 디자인(everforest·rose-pine·intellij-islands-light 5종) 판단이 필요해 제외, 후속. (e) `scripts/convert-vscode-theme.ts` 는 여전히 들여쓰기 없는 `JSON.stringify` 로 써서 새로 변환하는 테마가 기존 35종(확장형)이 아닌 collapsed 형식으로 저장된다 — 내 정정 스크립트는 파일별 형식을 감지·보존하지만 변환기는 미수정(§1.D 범위와 겹쳐 건드리지 않음).
- **§1.A·§1.B·§1.D Rust 완료 (2026-09-15)** — **변경 파일**: `src-tauri/src/domain/theme/service.rs` 단일(① `BUNDLED_THEME_SOURCES` 에 신규 9종 `include_str!` 등록 → 47종 ② §1.A 미러 — `STATE_DISTINCTNESS_PAIRS` 37행 + 임계 2종 + `compositeOverBackground`·`deltaE76`(CIE76/D65) 이식 + 린트 `카탈로그_테마는_상태색이_바탕색과_구별된다` ③ §1.B 미러 — `COMPONENT_CONTRAST_PAIRS` 35행 + `foregroundContrastRatio`(전경 알파 합성)·`resolveComponentSurface`(표면→`app.background` 합성) 이식 + 린트 `카탈로그_테마는_컴포넌트_전경색이_실제_배경과_최소_대비를_가진다` ④ 드리프트 테스트 2건 — TS 정본 파일을 `include_str!` 로 다시 읽어 행 순서·라벨·키·표면·대안·임계를 통째로 대조(`상태색_구별성_쌍_표는_ts_정본과_일치한다`·`컴포넌트_대비_쌍_표는_ts_정본과_일치한다`, `STATE_MIN_DISTINCT_DELTA_E`/`SUBTLE_STATE_MIN_DISTINCT_DELTA_E`/`MIN_CONTRAST_RATIO` 값 자체도 TS 에서 파싱해 비교) ⑤ 등록 게이트 `번들_테마_등록_배열은_리소스_디렉터리_및_theme_id_와_일치한다` ⑥ `light_colors()` 11토큰 정정 ⑦ stale 카탈로그 개수 주석 5곳 38/40→47/49), `docs/theme-system.md` §8.3 확장(등록 게이트 · Rust 미러 대응표 · 순회 범위 · `taide-light` 정정 전후 표). **TS·번들 JSON 은 만지지 않았다.** / **테스트**: `cargo fmt --all -- --check` 통과, `cargo clippy --workspace --all-targets -- -D warnings` 0건, `cargo test --workspace` **1587 pass 0 fail**(lib 1557 + 통합 30, theme 도메인 47건에 신규 4건 포함), `bun test` 2478 pass 0 fail, `bun run typecheck` 통과, `bun run lint` 0 error(기존 경고 11 = `useVirtualizer`), `bunx prettier --check docs/theme-system.md` 통과. **공회전 아님 반증 4건**: `taide-dark editor.selection` 을 `editor.background` 와 같게 만들면 상태 린트가 ΔE 0.00 으로, `taide-light statusIndicator.warning` 을 정정 전 값으로 되돌리면 대비 린트가 **2.15**(TS 실측과 동일 수치)로 잡는다. Rust 표의 라벨 1개를 바꾸면 드리프트 테스트가 37/35행 diff 로, 등록 행을 지우거나 파일을 서로 바꿔 짝지으면 등록 게이트가 각각 잡는다. / **이탈**: ① **린트 순회 범위를 `theme_catalog()`(번들 47 + Rust 리터럴 builtin 2 = 49)로** 뒀다 — 배정 문구는 "47종" 이지만 이 모듈의 기존 색 린트 전부가 `theme_catalog()` 를 돌고, 그렇게 바뀐 이유가 정확히 `bundled_themes()` 만 도는 린트를 `taide-light` 결함이 통과해 출시된 사건이다(d-36 §1-b). ② 그 결과 **`light_colors()` 11토큰을 정정**했다(컴포넌트 대비 13축 2.15~2.83 미달). §8.6 수리 규칙(`repairComponentContrast`)을 그대로 적용한 결정적 산출값이며, 전후 표는 `docs/theme-system.md` §8.3 에 있다. 계약의 데이터 정정 대상은 번들 JSON 이었으나, 예외 등재 0 원칙을 지키려면 이 두 리터럴도 같은 규칙을 받아야 한다(예외로 빼면 기본 라이트 테마의 git 데코·상태색이 영구 미달로 남는다). `taide-dark` 는 두 린트 모두 정정 0건. ③ **린트 테스트 이름을 `번들_테마는…` 이 아니라 `카탈로그_테마는…`** 으로 뒀다 — ①의 순회 범위와 파일 내 기존 명명 규칙에 맞췄다. ④ 드리프트 테스트가 계약 문구("개수·라벨")보다 넓다 — 키·표면·대안·임계까지 행 단위로 대조한다. 라벨만 보면 `containerKey` 를 잘못 옮긴 미러가 통과한다. ⑤ **등록 게이트를 추가**했다(계약 문구는 "카탈로그 수 테스트 갱신"). Rust 에는 하드코딩된 개수 테스트가 없었고(`BUNDLED_THEME_SOURCES.len()` 을 쓰는 상대 비교뿐), 개수를 손으로 박는 것보다 `CARGO_MANIFEST_DIR` 로 `resources/themes` 를 읽어 **파일 목록 = 등록 id 집합**과 **행↔파일의 `theme.id` 일치**를 보는 쪽이 §1.D 가 실제로 틀릴 수 있는 방식을 잡는다(TS `bundled-theme-licenses.test.ts` 와 같은 짝). ⑥ Rust 미러 표에 **행별 유래 주석을 두지 않았다** — 코드 주석 금지 규칙상 `///` 는 배열 원소에 붙지 않고, 산문을 양쪽에 두면 드리프트 테스트가 못 보는 사본이 하나 더 생긴다. 유래는 TS 정본이 단일 출처임을 표 doc 에 명시했다. ⑦ 공유 파일 `docs/theme-system.md` 는 §8.3 한 절만 Edit 로 추가했다(§8.5·§8.6·§8.1.1 등 타 에이전트 구역 미접촉, prettier 재포맷 없이 통과). / **미확인·후속**: (a) **실제 렌더 미확인** — `taide-light` 정정 11토큰(git 데코·상태색이 살짝 어두워지고 placeholder·비활성 탭 글자가 진해짐)은 §1.E 스크린샷 매트릭스에서 육안 확인 대상이다. 수치상 여유는 3.00~3.03 경계. (b) §1.C 가 `popover.itemHover`·`tooltip.itemHover`·`modal.itemHover` 를 배선해 TS 쌍 표에 3행이 추가되면 Rust 미러도 같은 3행을 넣어야 한다 — 방치하면 드리프트 테스트가 **실패로** 알려준다(조용한 누락은 불가). (c) 신규 9종은 Rust 린트 11종 + 신규 2종 전부를 예외 없이 통과했다(D 의 TS 미러 예측과 일치). (d) `docs/theme-system.md` §8.5 에 남은 "38종" 표기(측정 시점 기록)는 A1/메인 판단 영역이라 건드리지 않았다 — D 기록 (d) 와 같은 항목. (e) `bun run e2e`·`tauri dev` 는 배정상 미실행.
- **§1.B TS 완료 (2026-09-15)** — **변경 파일**: `src/shared/lib/theme-convert/component-contrast-pairs.ts`(신규, 컴포넌트 실사용 전경/배경 쌍 표 35행 + `surfaceKey` **필수**, Rust 미러가 복제할 plain literal export), `contrast.ts`(`CONTRAST_PAIRS`/`ContrastPair` export + `validateComponentContrast`/`repairComponentContrast`/`validateTerminalAnsiContrast` 신설, `repairContrastPairs` 가 blocking → 선택 행 → 컴포넌트 순으로 통합 실행), `contrast.test.ts`(+9건), `bundled-theme-contrast.test.ts`(+7건: 47종 전수 게이트·수리 멱등·ANSI 비게이트·라벨 유일·어휘 참조·전경↔배경 비중첩), `scripts/repair-theme-contrast.ts`(신규, 멱등·파일별 prettier 형식 보존·정정 후 §8.5 구별성 재검증), `package.json`(`themes:repair-contrast`), `docs/theme-system.md` §8.6 신설(정본 파일 표 + 도출·판정 + 수리 규칙 5항 + 테마별 정정 전후 표 29행 + §1.D 신규 9종 표 + 제외 축 실측 표 7행 + ANSI 47종 분포표 16행). **번들 JSON 37종 167토큰 정정**(`src-tauri/resources/themes/*.json`, 재실행 0건 — 기존 29종 132토큰은 §8.5 반영본 대비 전후값 전량 기록, §1.D 신규 8종 35토큰은 커밋된 "전" 값이 없어 결과값만). 임계는 기존 `MIN_CONTRAST_RATIO = 3` 유지. / **테스트**: `bun test` 2478 pass 0 fail(247 파일, 신규 16건), `bun run typecheck` 통과, `bun run lint` 0 error(기존 경고 11 = `useVirtualizer`), `bunx prettier --check` 전 변경 파일 + 테마 JSON 전량 통과. / **이탈**: ① **기존 `CONTRAST_PAIRS` 배열에 행을 더하지 않고 별도 표(`COMPONENT_CONTRAST_PAIRS`)로 분리**했다 — blocking 5쌍은 임포트 거부를 결정하고(d-40 §1-a "기존 5쌍 판정·수리 불변") advisory 2쌍은 예외 등재 테마별 **위반 개수까지** 테스트가 고정하므로, 같은 배열에 넣으면 `validateOutputColors`/`validateSelectionRowContrast` 의 의미가 둘 다 바뀐다. 린트 커버리지는 계약 의도대로 전수다. ② 수리는 후보 토큰 교체가 아니라 **자기 색상 유지 최소 이동**(검정·흰색 두 극으로 256단계 순차 탐색, ΔE 작은 쪽)이다 — 후보 교체는 hue 를 통째로 바꿔 테마 정체성을 잃고, TAIDE 색만으로 동작해 vscodeColors 가 없는 번들 JSON 정정 스크립트에서도 같은 결과를 낸다. 이분 탐색을 쓰지 않은 이유는 표면 명도가 두 극 사이면 광선 위 대비가 단조롭지 않아 최근접 해를 지나칠 수 있어서다. ③ 수리 단위가 쌍이 아니라 **전경 토큰**이다(한 토큰이 여러 표면에 그려지면 전부를 동시에 만족하는 값으로 1회 이동). 전경 토큰이 어떤 쌍의 배경·표면으로도 쓰이지 않음을 테스트로 고정해 1패스로 완결시켰다. ④ **공유 축은 제약으로만 사용**: `list.foreground` 는 d-40 `selectionForeground` 의 전경도 겸하는데, 그 축의 미달이 수리를 촉발하면 예외 등재분(rose-pine-dawn)이 조용히 "고쳐져" 예외 테스트가 깨진다(실제로 1차 구현에서 발생 → 트리거/제약 분리하고 해당 값 원복). ⑤ **계약이 나열한 축 중 4종을 제외**하고 실측 근거를 §8.6 제외 축 표에 남겼다 — destructive 버튼·Monaco 배지(`button.primaryForeground` 한 토큰이 서로 다른 색 배경 2~3개를 섬겨 4종은 해가 아예 없고, 나머지도 파괴 축 때문에 **기본** 버튼 라벨이 최대 ΔE 93.7 까지 끌려간다 → 표면별 토큰이 정답인데 §1.F 가 신규 토큰 금지), diff 추가/삭제 글자·찾기 매치 글자·메뉴 hover 글자(전경이 전역 본문색 `editor.foreground`/`app.foreground` 이라 "전경 쪽 수리" 로는 앱 전체 글자색을 옮기게 되고, 실제 원인은 §8.5 소관 상태 토큰), `explorer.gitIgnored`·`editor.lineNumber`·`editorBlame.foreground`(흐린 것이 토큰의 목적이라 3:1 게이트가 설계와 충돌하고 "의도적으로 흐림" 에 원칙 있는 하한이 없다). ⑥ **ANSI 16색은 자문 전용**(게이트·수리 없음) — 무채색 양 끝이 배경과 겹치는 것은 터미널 관행이고(`black` 34/47·`brightBlack` 21/47·`brightWhite` 12/47), 유채색까지 포함하면 137건인데 ANSI 는 `graph.lane*`·에이전트 아이콘·신택스 폴백이 공유하는 팔레트 정체성이라 수리하면 터미널보다 훨씬 넓게 다시 칠해진다. ⑦ `convert.ts` 는 미변경 — `repairContrastPairs` 안에서 수리가 자동으로 돌고, 남은 위반 보고는 `validateSelectionRowContrast` 와 동일하게 `ThemeConversionResult` 밖(게이트·Rust 린트)에서 감사한다(동시 작업 중인 파일 접촉 최소화도 겸). ⑧ 계약 파일 목록 밖 추가: `component-contrast-pairs.ts`(§1.A 의 `state-distinctness-pairs.ts` 선례), `scripts/repair-theme-contrast.ts`(A1 스크립트에 단계 추가 대신 별도 — 계약이 허용), `package.json` 1줄. / **미확인·후속**: (a) Rust 미변경이라 cargo 계열 미실행 — 대신 번들 JSON 을 읽는 기존 Rust 불변식(app 전경≠배경, `list.activeBackground`≠`panel.background`/`hoverBackground`, `list.foreground`≠`list.background`/`hoverBackground`·불투명, `panel.matchHighlight` 불투명·≠`app.foreground`, blocking 5쌍 대비, 색값 전량 hex|transparent)을 스크립트로 재확인해 통과. **R 이 §1.B 미러(쌍 표 35행 + `surfaceKey` 합성 + 임계 3)를 추가한 뒤 `cargo test --workspace` 필요** — 표는 `COMPONENT_CONTRAST_PAIRS` 를 그대로 복제하고 라벨/개수 비교로 드리프트를 막는다. (b) 수리값은 임계를 막 넘는 최소 변화라 결과가 3.00~3.03 경계다 — `everforest-light button.primaryForeground #fdf6e3→#595750`, `rose-pine-dawn #faf4ed→#4d4c49`, `night-owl-light button.foreground #F0F0F0→#464646` 처럼 **밝은 라벨이 어두운 라벨로 뒤집힌 3건**과 `github-light statusIndicator.warning #f9c513→#b08b0d`(ΔE 29) 는 §1.E 스크린샷 매트릭스로 실제 인상 확인 후 임계·전략 재검토 대상이다. (c) `app.foreground` 가 표면 위에 놓이는 두 축(`menu.background` 최소 3.09 · `panel.inputBackground` 최소 3.07)은 47종 전부 통과하지만 여유가 0.1 미만이다 — 수리 불가라 게이트에 올리지 않았으니 새 테마 추가 시 함께 확인해야 한다(§8.6 에 명시). (d) 정정으로 `appSidebar.badge`(§8.5 의 `sidebarBadge` 상태 축과 공유)가 11종에서 움직였다 — 스크립트가 정정 후 `validateStateDistinctness` 를 재실행해 47종 0건을 확인했고, 게이트 테스트도 두 린트를 함께 돌린다.
<!-- d61-record-B1 -->
- **§1.C 완료 (2026-09-15)** — **변경 파일**: `src/shared/styles/global.css`(`:root` 부트스트랩 기본값 6개 추가 · `@theme inline` 유틸 11개 추가(explorer 4·menu 4·popover.separator·modal.itemHover/separator) · `@utility shadow-overlay`/`shadow-overlay-lg`/`modal-scrim` · `@layer base` 네이티브 `::-webkit-scrollbar` 4규칙) / `shared/ui/dropdown-menu.tsx`·`context-menu.tsx`(menu.* 4토큰 배선, 팝오버 토큰 의존 제거, 그림자 토큰화, 죽은 `dark:` 제거) / `shared/ui/popover.tsx`(popover.border·separator, 그림자) / `shared/ui/dialog.tsx`·`alert-dialog.tsx`(오버레이 `bg-black/50`→`modal-scrim`, 그림자, 닫기 버튼 hover→modal.itemHover) / `shared/ui/command.tsx`(명령 팔레트 입력 구분선→modal.separator) / `widgets/plugin-manager/vsix-import-dialog.tsx`(섹션 구분선→modal.separator) / `shared/scroll/overlay-scrollbar.tsx`(scrollbar.track) / `shared/ui/button.tsx`(`text-white`→`text-primary-foreground`, `dark:` 4곳 제거) / `features/preview/html-preview.tsx`(`bg-white`→`bg-editor-background`) / `features/theme/color-picker.tsx`(`border-white` 2곳→`border-app-border`) / `shared/lib/file-icon.ts`(+`file-icon.test.ts`, 기본 폴더 아이콘→explorer.folderIcon) / `features/explorer/file-tree-row.tsx`(git 데코→explorer.gitAdded/gitModified/gitDeleted) / 신규 `src/shared/styles/theme-token-consumer.test.ts`(토큰 소비자 존재 기계 검사) / `docs/theme-system.md` §3.1(토큰 소비처 정본 + 예외 표). **테스트**: `bun test` 2462 pass / 0 fail(247 파일, 신규 4 tests — 첫 전체 실행에서 난 `state-distinctness.test.ts` 1건 실패는 동시 작업 중이던 §1.A 파일이라 범위 밖, 재시도에서 해소), `typecheck` 0 에러, `lint` 0 에러(11 warning 은 기존 `useVirtualizer` 건), `prettier --check` 통과, `vite build` 성공 + 생성 CSS 에서 `bg-menu-background`·`shadow-overlay(-lg)`·`modal-scrim`(+`@supports` 폴백)·`border-b-popover-separator`·`text-explorer-folder-icon`·`::-webkit-scrollbar` 규칙 생성 확인. **이탈**: (1) §0 의 "죽은 `dark:` 6곳(`.dark` 클래스를 붙이는 코드 없음)" 은 **사실과 다르다** — `global.css:4` 가 `@custom-variant dark (&:where([data-appearance='dark'], …))` 로 변형을 재정의하고 `app/providers/theme-provider.tsx` 가 `documentElement.dataset.appearance = theme.type` 을 세팅하므로 6곳은 다크 테마에서 실제 적용되던 live 분기였다. 그래도 제거했다: TAIDE 토큰이 이미 테마별 명시값을 주므로 shadcn 시절의 다크 전용 알파 오버라이드는 두 번째 테마 레이어가 되어 같은 시맨틱이 라이트/다크에서 달라 보였다. **다크 테마에서 destructive 버튼·outline/ghost hover·aria-invalid 링 외관이 바뀐다(라이트와 같아지는 방향)**. (2) 미배선 5종 중 **3종만 배선**(popover.separator·modal.itemHover·modal.separator). `tooltip.itemHover`·`tooltip.separator` 는 앱의 툴팁이 전부 한 줄 텍스트라 hover 항목도 구분선도 없어 **UI 를 발명하지 않고** 테스트 예외로 등재했다(`terminal.commandBlockBorder` 와 함께 예외 3건). (3) 모달 스크림은 계약이 제시한 `color-mix(in srgb, var(--taide-app-shadow) 50%, transparent)` 를 그대로 썼다. app.shadow 알파 실측 분포(38종, 결과 스크림 알파): 0 = 2종(tokyo-night `#ffffff00` · vitesse-dark `#00000000`), <0.15 = 11종(라이트 다수 + gruvbox-dark), 0.15~0.35 = 20종, 0.5 = 5종(불투명 shadow). 기존 `bg-black/50` 대비 전반적으로 옅어지고 2종은 스크림이 사라진다. 대안 `rgb(from var(--taide-app-shadow) r g b / 50%)` 는 세기를 유지하지만 알파를 무시해 tokyo-night(다크 테마에 흰 스크림)·night-owl-light·rose-pine-dawn 에서 **밝은 스크림으로 뒤집혀 더 나쁨** → 채택하지 않았다. (4) 계약에 없던 `popover.border` 를 함께 배선했다(PopoverContent 가 bare `border`=app.border 를 써 tooltip/modal 과 불일치했음 — 한 단어 교체). (5) `explorer.gitModified/gitAdded/gitDeleted` 는 `mapping-tables.ts` 에서 `git.*` 과 **동일한 `gitDecoration.*ResourceForeground` 체인**으로 파생함을 확인했다(번들 테마에서 값 동일, 토큰 정체성만 분리). (6) §0 의 "OverlayScrollbar 미소비" 도 stale — thumb/thumbHover 는 이미 소비 중이었고 `track` 만 새로 배선했다. **미확인·후속**: 실제 렌더 화면(라이트·다크)은 확인하지 못했다(tauri dev 금지) — 생성 CSS 규칙 검증까지만이며 육안 확인은 §1.E 스크린샷 매트릭스에서. 네이티브 `::-webkit-scrollbar` 규칙은 WebKit 에서 해당 표면의 오버레이 스크롤바를 상시 표시형으로 바꾼다(영향 표면: welcome-screen · spreadsheet-preview 탭 스트립 · markdown-preview `<pre>` · error-boundary `<pre>` · breadcrumbs-bar · vsix-import-dialog 목록 · xterm viewport. `scrollbar.track` 은 38종 전부 `transparent` 라 바탕은 그대로). `app.shadow` 데이터 정정(알파 0 인 2종, 밝은 RGB 3종)은 데이터 계열 작업의 후속이다. 명령 팔레트 선택 행은 d-40/palette-ux 계약이 `list.activeBackground`+ring 으로 고정한 부분이라 `modal.itemHover` 로 덮지 않았다.
- **§1.D 완료 (2026-09-15)** — **선정 9종**: `catppuccin-latte`(light)·`catppuccin-frappe`·`catppuccin-macchiato`·`tokyo-night-storm`·`tokyo-night-light`(light)·`gruvbox-light`(light)·`rose-pine-moon`·`ayu-mirage`·`github-dark-dimmed`. 후보 11종 중 **Gruvbox Dark Hard·Dark Soft 를 제외**했다 — 둘은 이미 번들된 `gruvbox-dark`(Dark Medium)와 배경 명도만 다르고 액센트 팔레트가 동일해 후보 중 기존 카탈로그와 가장 덜 구별되는 쌍이라, 대신 Gruvbox 는 카탈로그에 없던 **라이트**를 채웠다. 라이트 비율: 신규 9종 중 3종 = **33.3%**, 카탈로그 전체 12→15 / 38→47 = **31.9%** (둘 다 ≥ 20% 충족). / **1차 출처**(전부 MIT 재확인): raw 파일 직수신 3저장소 — rose-pine 태그 `v2.15.2` `themes/rose-pine-moon-color-theme.json`, tokyo-night 커밋 `7c0f11eaef322f293621ca7befe462214b7ea468` `themes/tokyo-night-{storm,light}-color-theme.json`, ayu 커밋 `444ef92911cb75c3933c8003e3a7c79b6b6c914f` 루트 `ayu-mirage.json`. VSIX 경유 3저장소 — catppuccin GitHub 릴리스 자산 `catppuccin-vsc-v3.19.0`/`catppuccin-vsc-3.19.0.vsix`, `GitHub.github-vscode-theme` 6.3.5, `jdinhlife.gruvbox` 1.29.1. 전 URL·경로는 `docs/theme-system.md` §8.1.1 표에 기록. / **변경 파일**: 신규 `src-tauri/resources/themes/{9종}.json`(각 133 colors + 31 syntax + 20 terminal = 184 토큰 전량 명시), `THIRD_PARTY_LICENSES.md`(상단 개수 36→47 + 기존 6개 섹션의 `Bundled as:` 에 id 추가 + 그 섹션 제목을 실제 번들 목록과 일치하게 갱신 + Tokyo Night 저장소 이전 주석), `mapping-tables.ts`(개수 36→47 + ΔE 계층 재실측 반영), `contrast.ts`(개수 36→47), `bundled-theme-licenses.test.ts`(개수 게이트 신설), `docs/theme-system.md`(§8 머리말 38→47 · §8.1 카탈로그 표 9행 · **§8.1.1 신설**(선정 근거·원본 URL/고정 ref·VSIX 경유 사유·변환 결과) · §8.2.4 에 `ayu-mirage` 손수정 1행 + 머리말 개수). **기존 38종 JSON 은 만지지 않았다**(git 상 신규 9개만 untracked, 기존 36개 수정분은 전부 §1.A 것). Rust `include_str!` 등록은 R 몫이라 하지 않았다. / **변환 결과**: 9종 전부 include 체인이 없어 **`--include-dir` 불필요**, `terminal.ansi*` 16색을 원본이 모두 보유해 **ANSI 폴백 0건**, safe-default 폴백 0건, `outputColorErrors` 0, 잔존 `stateDistinctnessErrors` **0**. §1.A 구별성·§1.B 대비 수리는 변환 파이프라인 안에서 자동 발동(`catppuccin-*` 3종·`tokyo-night-light`·`gruvbox-light`·`github-dark-dimmed` 6종, 총 21개 토큰). / **테스트**: `bun test src/shared/lib/theme-convert` 70 pass 0 fail(신규 1건), `bun test` 전체 **2478 pass 0 fail**, `bun run typecheck` 통과, `bun run lint` 0 error(기존 `useVirtualizer` 경고 11), `bunx prettier --check` 신규 9 JSON + `THIRD_PARTY_LICENSES.md` + 만진 3 TS 통과. 중간에 `bundled-theme-contrast.test.ts` 의 "예외 등재분은 선언된 축에서만 위반한다" 1건이 뜬 적이 있으나 **범위 밖**이었다 — 동시 작업 중이던 §1.B 가 `validateSelectionRowContrast` 를 반투명 배경 **알파 합성** 판정으로 바꾸면서 `rose-pine-dawn` 의 `selectionForeground` 예외(사유 자체가 "게이트가 raw RGB 로 읽는다")가 무효가 된 것이라, 해당 축 소유자인 §1.B 가 레지스트리를 갱신하며 해소됐다(재시도 3회차에 통과). 내 9종은 그 변경 전후 모두 `validateOutputColors`·`validateSelectionRowContrast` 가 전부 빈 배열이다. 개수 게이트는 값을 46 으로 변조하면 실제로 실패함을 확인(공회전 아님). Rust 미변경이라 cargo 미실행 — 대신 Rust 카탈로그 린트 11종의 불변식(토큰 전량·tokenColors 비어있지 않음·`app.foreground`≠`app.background`·`list.activeBackground`≠`panel.background`/`hoverBackground`·`list.foreground`≠`list.background`/`hoverBackground`·`list.foreground`/`matchHighlight` 불투명·matchHighlight ΔE≥2.3·matchHighlight vs `panel.background`/`list.activeBackground` 대비≥3·`list.foreground` vs `list.activeBackground` 대비≥3·색값 전량 hex|transparent)을 TS 로 미러해 **47종 전수 통과** 확인 → R 등록 시 신규 예외 등재는 불필요할 전망. / **이탈**: ① **VSIX 경유 3저장소** — `catppuccin/vscode`·`primer/github-vscode-theme`·`jdinhify/vscode-theme-gruvbox` 는 테마 JSON 을 릴리스 시 생성하고 저장소에 커밋하지 않아(`themes/` 가 `.gitignore` 대상) "저장소 태그의 raw 파일"이 **존재하지 않는다**. 그 버전의 발행 산출물에서 꺼냈고, 마켓플레이스 2종은 **발행 버전이 GitHub 태그와 정확히 일치**(6.3.5=`v6.3.5`, 1.29.1=`v1.29.1`)함을 확인해 태그와 대응시켰다. 또 수신 수단은 배정이 지정한 WebFetch 가 아니라 `curl`(+ 태그·라이선스 확인은 GitHub API)을 썼다 — WebFetch 는 페이지를 마크다운으로 변환해 모델로 요약하므로 바이트 단위 원본 JSON 을 얻을 수 없다. 1차 출처·URL 기록이라는 요구는 그대로 지켰다. ② **`tokyo-night` 저장소 이전** — `enkia/…` 가 `tokyo-night/…` org 로 이동(리다이렉트 유지, MIT 재확인). 신규 2종의 `source` 는 현재 canonical URL 로 적었고 기존 `tokyo-night.json` 은 건드리지 않아 한 섹션 안에 두 URL 이 공존한다(라이선스 문서에 이전 사실 1줄 주석). ③ **`tokyo-night-light` 의 `type`** — 원본 테마 JSON 안의 `"type": "dark"` 는 업스트림 오기다. 확장 매니페스트가 `"uiTheme": "vs"` 로 등록하고 있어 `--type light` 로 변환했다. ④ **`ayu-mirage` `list.foreground` 손수정 1토큰**(`#707a8c`→`#cccac2`, 업스트림 `list.activeSelectionForeground`) — 변환 그대로는 `selectionForeground` 대비 2.77 로 §1.B 게이트에 걸린다. ayu 계열은 `list.activeSelectionBackground` 가 자기 `list.hoverBackground` 와 같아(`#63759926`) §8.2.2 가드가 VS Code 기본 `#04395E` 로 폴백시키는데, 이 테마는 `sideBar.foreground` 가 없어 `list.foreground` 체인이 dim 한 chrome 전경으로 떨어진다. **이미 번들된 `ayu-dark`/`ayu-light` 가 d-40 에서 받은 것과 완전히 같은 처방**이라 예외 등재 대신 §8.2.4 레지스트리에 1행 추가했다. nord 식 다표면 회귀를 재점검한 결과 `#cccac2` 는 `list.background` 3.58→9.45 · `hoverBackground` 1.07→2.82 · `app.background` 3.36→8.85 로 전 표면 개선이라 역효과 없음. ⑤ **`mapping-tables.ts` 주석은 개수만 바꾸지 않았다** — 계층 서술("13.8+ (the remaining 31)")이 `tokyo-night-storm` ΔE 11.36 때문에 거짓이 되어, 47종 재실측으로 계층을 다시 적었다(5.4 one-monokai / 7.2 kimbie / **11.4 tokyo-night-storm** / 13.8+ 나머지 44). 임계 2.3 의 근거인 빈 구간 (0, 5.39) 는 47종에서도 유지됨을 확인. ⑥ **id `gruvbox-light`** — 업스트림 라벨은 "Gruvbox Light Medium" 이지만 기존 `gruvbox-dark`(= Dark Medium 인데 `gruvbox-dark`)와 대칭을 맞췄다. ⑦ 라이선스 문서 **섹션 제목 갱신**은 계약 문구("`Bundled as:` id 추가 + 개수 정정") 밖이지만, "Catppuccin (Mocha)" 가 4 flavor 를 담게 되는 식으로 제목이 사실과 어긋나 MIT 표시 정확성을 위해 6곳을 함께 고쳤다. / **미확인·후속**: (a) **Rust `include_str!` 미등록**(R 몫) — 등록 전까지 Rust 는 신규 9종을 모른다. 등록 후 `cargo test --workspace` 필요. (b) **실제 렌더 미확인** — §1.E 스크린샷 매트릭스에서 육안 확인. 특히 §1.A/§1.B 자동 수리가 들어간 6종은 결과가 JND 경계일 수 있다. (c) **§1.A 기록 (e) 의 변환기 포맷 지적은 실측과 다르다** — `scripts/convert-vscode-theme.ts` 는 `JSON.stringify` 결과를 prettier `format()` 에 통과시키고 `resolveConfig(outputPath)` 가 **출력 경로 기준**이라, repo 안 `src-tauri/resources/themes/` 로 쓰면 4-space·printWidth 150 이 적용돼 기존 38종과 동일 형식이 나온다(신규 9종 `prettier --check` 통과). 2-space collapsed 는 repo **밖** 경로로 변환했을 때만 나온다 — 변환기 수정 불필요로 판단해 건드리지 않았다. (d) `docs/theme-system.md` 에 "38종" 이 남은 곳(§8.5 의 테스트 표 1행·실측 분포 서술 3곳, `state-distinctness-pairs.ts` JSDoc 2곳)은 §1.A 의 측정 시점 기록이라 건드리지 않았다 — 다만 §8.5 테스트 표의 "번들 38종 전수 게이트" 는 커버리지 **설명**이라 47 이 맞다. A1 또는 메인이 판단. (e) GitHub HC 2종은 계약대로 `--type` 스키마 밖이라 여전히 제외(backlog).
- **§1.E 완료 (2026-09-15)** — 변경 파일: `e2e/specs/26-theme-surface-matrix.e2e.ts`(신규 — 번들 테마 전수 × 표면 13종 스크린샷 생성기), `docs/quality-assurance/2026-08-18-e2e-harness.md`(§2 실행법 2행·§5 제목·스펙 표 26행·§5.2 신설·§8 디렉토리). 표면 13종: settings-toggle-on/off · tooltip · sidebar-selected-hover-row · tab-bar-active-inactive · editor-current-line · editor-drag-selection · editor-find-match · command-palette · context-menu · dialog-overlay · git-panel · terminal-selection. 산출 경로 `e2e/artifacts/theme-matrix/<theme id>/<surface>.png`(영역형=요소 스크린샷, 오버레이=뷰포트). 테마 목록은 `theme_list` 의 `builtin` 전수라 §1.D 로 9종이 늘어도 스펙 무변경. 테마 적용 확인 3단(버튼 `aria-pressed` → `--taide-editor-background` = `theme_get` 의 `editor.background` → `<html data-theme-type>`), 전환 실패 테마는 오기입 방지를 위해 통째로 건너뜀. 시간 상한: 테마당 90초·매트릭스 40분(초과분은 기록 후 스킵), 표면 실패는 기록 후 계속하고 전 캡처 시도 뒤 실패 목록을 한 번에 단언. 전제 단언: `settings.followSystemTheme` 이 켜져 있으면 `theme_get_current` 가 시스템 테마를 돌려줘 매트릭스가 무의미해지므로 즉시 실패. / 테스트: `bun run typecheck:e2e` 통과, `bunx eslint e2e/specs/26-theme-surface-matrix.e2e.ts` 0건, `bunx prettier --check` 통과, 루트 `bun run typecheck`·`bun run lint`·`bun test` 통과(스펙 파일은 `.e2e.ts` 라 `bun test` 대상 아님). / 이탈: (1) `Meta+F`·`Meta+Home` 등 스펙 전용 키 상수와 `e2e/artifacts` 경로를 `lib/constants.ts`·`lib/paths.ts` 가 아닌 스펙 파일 안에 뒀다 — 배정이 "다른 파일은 만지지 않는다" 였다(후속으로 lib 승격 가능). (2) 대상 파일 외 수정 없음. / 미확인·후속: **스펙 미실행**(하네스가 앱을 기동하지 않음 — 사용자가 `TAIDE_E2E_NO_HMR=1 bun run tauri dev` 후 `bun run e2e --grep theme-surface-matrix`). 실행 전 **`.gitignore` 에 `e2e/artifacts/` 추가 필요**(스크린샷에 사용자 실제 프로젝트 이름이 찍힘 — 범위 밖이라 미수정, §5.2 체크박스로 남김). 실측 미확인 가정: 헤드리스 WebKit 에서 xterm 선택 드래그가 렌더되는지(파일럿 리스크 1 WebGL), Settings 한 화면에 `data-state=checked`/`unchecked` 스위치가 각각 최소 1개 존재하는지(없으면 그 표면만 실패로 기록), 에디터/터미널 드래그 오프셋이 기본 뷰포트(1280×720)에서 텍스트 위에 떨어지는지.


- **메인 1차 검증 (2026-09-15, worktree taide-w4)** — `bun run verify` exit 0(bun test 2478/0 · cargo lib 1557 + 통합 30 · clippy 0 · prettier) · `bunx vite build` 0 · `bun run typecheck:e2e` 0.
- **렌즈 검토 (2026-09-15, wf `wf_dc785603`, sonnet·xhigh 3렌즈 + major 반박 2표)** — major 5 · minor 6 · info 1. 메인 판정: **전건 수용**.

| id | 심각도 | 내용 | 수정 방향 |
|----|------|------|------|
| A-1 | major | 형제 쌍(선택 vs hover, 켜짐 vs 꺼짐 트랙 등 5쌍)의 ΔE 를 "state 를 container 위에 합성" 으로 재어 알파 상태색 동일값(ayu 3종·solarized-dark)을 통과시킴 | 형제 쌍은 **양쪽을 surfaceKey 위에 독립 합성 후 비교**(`sibling` 플래그). Rust 미러 동일. 수리 스크립트 재실행 |
| A-2 | major | explorer/sidebar item 토큰이 다른 컨테이너(panel.background·appSidebar.background·editor.background·tabBar.background)에서도 재사용되는데 표에 없음 | 실사용 조합을 쌍 표에 추가 후 재실행 |
| G-1 | major | destructive 버튼 `text-white` → `text-primary-foreground` 교체로 21종에서 3:1 미달 | **`text-white` 복원**(의도적 raw 색 예외로 theme-system §3 에 명시) + advisory 대비 쌍(고정 흰색 vs destructive 배경 토큰)으로 계측 |
| G-2 | major | `app.shadow` 알파 0 인 tokyo-night·vitesse-dark 에서 스크림·다이얼로그 그림자 소실 | 수리 규칙에 **`app.shadow` 최소 알파 하한**(상수) 추가 → 2종 데이터 정정 + Rust 린트 미러 |
| G-3 | major | 메뉴 hover 배경을 `menu.itemHover`(menu.selectionBackground 우선)로 바꾸면서 글자색은 고정 → 일부 테마 1.78:1 | 대비 쌍(`app.foreground` vs `menu.itemHover`) 추가 + 미달 시 `menu.itemHover` 를 `list.hoverBackground` 값으로 수리(변환기 체인도 list.hoverBackground 우선으로) |
| A-3 | minor | listHover 쌍 JSDoc 유래 오기(command.tsx → Monaco 위젯) | JSDoc 정정 |
| G-4 | minor | `dark:` 6곳 제거 전제 오류(live 분기) — 다크 테마 시각 변화 미검증 | 코드 유지, **§1.E 스크린샷 검수 게이트 항목**으로 기록(destructive·outline/ghost hover·aria-invalid 링) + 실기 목록 |
| G-5 | minor | `stateDistinctnessErrors` 를 VSIX 임포트가 경고로 안 씀 | `warningCount` 에 포함 |
| G-6 | minor | 토큰 소비자 테스트가 브리지 줄 존재만 검사 | 유틸리티 클래스 실사용도 검사 |
| G-7 | minor | `.gitignore` 에 `e2e/artifacts/` 없음 | 추가 |
| B-1 | minor | e2e-harness.md 카탈로그 수 40 → 49 | 정정 |
| G-8 | info | convert.ts 순서 JSDoc 부정확 | 정정 |

- **검토 수정 T1 완료 (2026-09-15)** — **A-1**: `StateDistinctnessPair` 에 `sibling?: boolean` 추가. 형제 5쌍(`listActiveVsHover`·`explorerSelectedVsHover`·`sidebarActiveVsHover`·`tabActiveVsInactive`·`switchCheckedVsUncheckedTrack`)은 state·container 를 각각 `surfaceKey` 위에 **독립 합성한 뒤** ΔE 비교한다(`resolvePairSurfaces` 가 `stateBaseHex` 를 돌려주고 `measureDistance`·`pickPaletteReplacement` 가 그걸 쓴다 — 검증·수리 양쪽 반영). 적발: ayu 3종 `explorer.itemSelected`==`itemHover`(ΔE 0.00) · `sidebarActiveVsHover` 동 3종 · `rose-pine-dawn` 3축(1.47) · `vscode-solarized-dark` 토글 트랙(0.00). `tabActiveVsInactive` 는 15종이 걸리지만 전부 `tabBar.tabActiveIndicator` 대안으로 통과(정정 0). **A-2**: 실사용 조합 14행 추가 — `explorerHoverOnPanel`/`explorerSelectedOnPanel`/`explorerFocusedOnPanel`(문제·검색·개요 패널), `explorerHoverOnStatusBar`/`explorerSelectedOnStatusBar`(상태바 스트립), `explorerHoverOnEditorWidget`(스프레드시트 시트탭), `sidebarHoverOnEditor`(브레드크럼·마크다운 프리뷰)·`sidebarHoverOnApp`/`sidebarActiveOnApp`(설정 TOC·테마 편집기)·`sidebarHoverOnTabBar`(탭바 새탭 메뉴), `listHoverOnActiveTab`/`listHoverOnInactiveTab`(`ICON_BUTTON_CLASS` 탭 닫기 버튼), `modalItemHover`(§1.C 가 배선한 다이얼로그 닫기 버튼). 각 행 JSDoc 에 유래 파일·줄 명시, grep 으로 실사용 확인. **A-3**: `listHover` JSDoc 을 `command.tsx` → Monaco `list.hoverBackground`/`dropdown.listBackground`(`monaco/theme.ts:99,155`)로 정정. **G-3**: `COMPONENT_CONTRAST_PAIRS` 에 `menuItemHoverText`(`app.foreground` vs `menu.itemHover`, surface `menu.background`) 1행 추가 + `contrast.ts` 에 배경 치환 수리(`COMPONENT_CONTRAST_BACKGROUND_SUBSTITUTES` = `{menu.itemHover: [list.hoverBackground]}`, 전경 패스보다 먼저, 후보가 3:1 을 넘고 `menu.background` 와 ΔE 2.3 이상일 때만 채택) + `mapping-tables.ts` 체인을 `['list.hoverBackground','menu.selectionBackground']` 순으로 정정. `vscode-light-modern` 1.78→9.92 · `vscode-dark-modern` 2.82→8.64. **G-2**: `APP_SHADOW_MIN_ALPHA = 0.149` 신설(= VS Code 라이트 `widget.shadow` 기본값 `#00000026` = 38/255, 이 변환기의 라이트 `SAFE_DEFAULT_COLORS.shadow` 와 같은 값 — 카탈로그 fitting 이 아님). 47종 실측 0.000×4 / 0.071 / 0.125×2 / 0.149 이상 40종이라 앞 7종만 올린다. 수리는 선언 RGB 를 유지하지 않고 **검정 `#00000026` 으로 교체**(알파 0 인 토큰의 RGB 는 정보가 아니고, `tokyo-night` `#ffffff00` 의 알파만 올리면 다크 테마에 흰 스크림이 깔린다 — §1.C 가 기각한 것과 같은 함정). `tokyo-night`·`tokyo-night-storm`·`tokyo-night-light`·`vitesse-dark`(알파 0) + `ayu-light`·`everforest-light`·`intellij-islands-light`(하한 미만) 7종 정정. **G-8**: `convert.ts` 순서 JSDoc 정정 — 대비 수리가 쓰는 토큰 중 구별성 쌍의 *컨테이너·표면*은 없지만 *상태* 토큰은 둘(`menu.itemHover`·`appSidebar.badge`) 있고, 그래서 치환에 ΔE 제약을 걸고 스크립트·게이트가 사후 재검증한다는 사실로 고쳤다(기존 문구 "the reverse cannot happen" 은 거짓). **G-1**: `button.tsx` destructive 를 `text-white` 로 복원 + `docs/theme-system.md` §3.2 신설(의도적 raw 색 예외 정본) + `component-contrast-pairs.ts` 에 자문 표 `FIXED_FOREGROUND_CONTRAST_PAIRS`(고정 전경 + 배경 토큰, 최소 스키마 확장 대신 **별도 export** — `COMPONENT_CONTRAST_PAIRS` 에 넣으면 게이트·수리·Rust 미러 세 곳의 계약이 바뀐다) + `validateFixedForegroundContrast`(게이트·수리 없음, ANSI 자문 선례) + `theme-token-consumer.test.ts` 에 raw 색 게이트 신설(`RAW_COLOR_EXEMPTIONS` 에 `shared/ui/button.tsx: text-white` 1건만 등재, 나머지 전부 차단 + stale 검사). 흰색 미달 10/47 vs 토큰(`button.primaryForeground`) 미달 19/47. / **데이터 재정정**: `themes:repair-state-distinctness` → `themes:repair-contrast` 재실행으로 **47종 중 22종·46토큰** 갱신(둘 다 2회차 0건 확인). `docs/theme-system.md` §8.5 에 판정 3항(형제)·6항(그림자) · 그림자 축 절 · 검토 재측정 표(19축) · 검토 정정 전후 표(22종) 추가, §8.6 에 수리 규칙 6항(배경 치환) · 예외 등재 2 표 · 고정 전경 자문 표(10종 실측) · 검토 재정정 3토큰 표 추가, 제외 축 표에서 `menu.itemHover`·destructive 축 갱신. / **R 이 미러해야 할 쌍 표 변경(전량 열거)**: ① `STATE_DISTINCTNESS_PAIRS` 37행 → **51행** — 신규 14행은 `listHoverOnActiveTab`·`listHoverOnInactiveTab`(`listActiveVsHover` 뒤), `explorerHoverOnPanel`·`explorerSelectedOnPanel`·`explorerFocusedOnPanel`·`explorerHoverOnStatusBar`·`explorerSelectedOnStatusBar`·`explorerHoverOnEditorWidget`(`explorerSelectedVsHover` 뒤), `sidebarHoverOnEditor`·`sidebarHoverOnApp`·`sidebarActiveOnApp`·`sidebarHoverOnTabBar`(`sidebarHoverOnCard` 뒤), `modalItemHover`(`menuItemHover` 뒤). ② 위 5쌍에 `sibling: true` — **드리프트 키에 `sibling` 이 없으면 미러가 규칙을 안 옮겨도 통과한다. 키 포맷(`label|state|container|surface|alt|minDeltaE`)에 필드를 추가해야 한다.** ③ `APP_SHADOW_MIN_ALPHA = 0.149` 와 그림자 축(컨테이너 없이 자기 알파만 검사)을 `카탈로그_테마는_상태색이_바탕색과_구별된다` 에 추가. ④ `COMPONENT_CONTRAST_PAIRS` 35행 → **36행**(`menuItemHoverText` 를 `editorWidgetLink` 뒤에). ⑤ `COMPONENT_CONTRAST_EXEMPTIONS`(`ayu-dark:menuItemHoverText` 2.72 · `ayu-light:menuItemHoverText` 2.88) 미러 필수 — 안 넣으면 컴포넌트 대비 린트가 두 테마에서 실패한다. ⑥ Rust `SELECTION_FOREGROUND_CONTRAST_EXEMPTIONS` 의 `rose-pine-dawn` **삭제** — 형제 규칙이 `list.activeBackground` 를 `#6e6a8614`→`#ece6e3` 로 옮겨 실제 대비가 3.56 이 됐다(TS 쪽 레지스트리는 이미 비웠다). ⑦ `light_colors()` `app.shadow` `#00000022`(알파 0.133) → `#00000026` 1토큰 — 내가 TS 검증기로 Rust 리터럴 2종을 직접 돌려 확인했고, 이 1건 외에는 `taide-light`·`taide-dark` 모두 신규 51축·36축을 전부 통과한다. ⑧ §8.3 의 "예외 등재는 두 린트 모두 0이다"·행 수(37/35) 문구 갱신 — 나는 §8.3 을 건드리지 않았다. / **테스트**: `bun run typecheck` 통과 · `bun run lint` 0 error(기존 경고 11 = `useVirtualizer`) · `bunx prettier --check` 변경 파일 + 테마 JSON 47종 전량 통과 · `bun test` **2498 pass 0 fail**(theme-convert 99, 신규 12건: 구별성 6 — 형제 적발(ayu-dark 실값)·경계값 통과(everforest-light 2.42, 같은 값으로 접으면 적발되는지 역검증 포함)·형제 surfaceKey 불변식·그림자 하한·형제 수리·그림자 수리, 대비 4 — 배경 치환·후보 기각(ΔE)·후보 기각(임계 미달)·예외 필터, 고정 전경 자문 2) + 게이트 신규 2(컴포넌트 예외 등재 검증·고정 전경 비게이트) + raw 색 게이트 3. `cargo fmt --all -- --check` 통과 · `cargo clippy --workspace --all-targets -- -D warnings` 0건. **`cargo test` 는 theme 도메인 3건 실패 — 전부 R 몫의 미러 미갱신이며 다른 실패는 없다**(`상태색_구별성_쌍_표는_ts_정본과_일치한다` · `컴포넌트_대비_쌍_표는_ts_정본과_일치한다` · `선택_행_전경색_대비_예외_등재분은_실제로_최소_대비에_미달한다`. 카탈로그 린트 2종은 현재 상태로도 통과 — 단 R 이 ④를 넣으면 ⑤ 없이는 실패한다). / **이탈**: ① `sibling` 은 boolean 플래그다(배정의 "또는 동등"). `containerKey` 가 상태 토큰인지 자동 유도하는 방식은 `switchCheckedVsUncheckedTrack`(`input.border` 는 상태이자 경계 토큰)에서 오판한다. surfaceKey 필수 조건은 타입이 아니라 테스트로 고정했다(plain literal 표라 union 타입을 쓰면 Rust 미러가 복제 못 한다). ② G-3 의 ayu 2종은 **수리 불가**라 예외 레지스트리를 신설했다 — `component-contrast-pairs.ts` 에 두어 게이트·수리 스크립트·Rust 린트가 하나만 읽게 했고(테스트·스크립트에 사본을 두면 어긋난다), §1.B 가 "예외 등재 0" 이라 적은 게이트 doc 도 함께 정정했다. 근본 원인은 ayu 의 dim 한 `app.foreground` 가 틴트 없는 메뉴 배경에서 이미 3.09/3.32 라는 것이라, 배경을 어떻게 골라도 3:1 을 못 넘는다(흰색보다 밝은 배경은 없다). ③ 배정 문구는 "appSidebar.itemHover/itemActive vs editor.background" 였으나 `itemActive` 의 실제 컨테이너는 `app.background`(설정 뷰·테마 편집기)라 `sidebarActiveOnApp` 으로 넣었다. `sidebarHoverOnEditor`/`sidebarHoverOnApp` 은 번들 테마에서 항상 같은 값이지만(변환기가 `app.background` 를 `editor.background` 에서 파생) 소비처가 다른 별개 토큰이라 둘 다 둔다 — §8.5 에 명시. ④ `rose-pine-dawn` 의 `selectionForeground` 예외를 TS 에서 **제거**했다(레지스트리는 빈 채로 유지). 형제 규칙이 그 예외의 전제("게이트가 반투명 배경을 raw RGB 로 읽는다")를 없앴으므로 통과하는 예외를 남기는 것보다 지우는 쪽이 맞다. ⑤ 임계 `2.3` 의 "빈 구간" 근거를 47종·51축으로 재측정해 JSDoc·§8.5 를 갱신했다(임계 미만 최댓값 2.26 `tokyo-night-light listHoverOnActiveTab`, 임계 이상 최솟값 2.31 `palenight switchCheckedVsUncheckedTrack` — 구간은 좁아졌지만 여전히 빈다). 기존 38종 분포표는 측정 시점을 명시해 남기고 재측정 표를 따로 붙였다. ⑥ `darcula explorer.itemHover`(#2A2D2E→#404040)·`one-monokai appSidebar.itemHover`(#292d35→#353942)·`tokyo-night-light list.hoverBackground`(#e1e2e8→#cfd1d9)는 한 토큰이 두 축에 연달아 걸려 **2패스에 걸쳐** 이동했다 — 축별 최소 변화의 합이라 단일 축 기준보다 크다. §8.5 표에 `→ a → b` 로 표기. ⑦ `theme-token-consumer.test.ts` 에는 raw 색 검사가 없었다(§1.C 가 raw 색을 전부 제거했으므로). 배정의 "raw 색 검사에 예외 등재" 를 위해 검사 자체를 신설하고 `text-white` 1건만 등재했다 — G-6 를 작업 중인 T2 와 충돌하지 않도록 파일 끝에 별도 describe 로 붙였다. / **미확인·후속**: (a) **실제 렌더 미확인** — 특히 `rose-pine-dawn`·`ayu` 3종의 `explorer.itemSelected`/`list.activeBackground` 가 8% 알파 오버레이에서 **불투명 색**으로 바뀌었고(가장 큰 외관 변화), `app.shadow` 7종은 스크림·그림자 세기가 달라진다(알파 0 이던 4종은 없던 스크림이 생긴다). §1.E 스크린샷 매트릭스의 1순위 검수 대상이다. (b) `menuItemHoverText` 로 승격했지만 `app.foreground` vs `menu.background`(최소 3.09)·`panel.inputBackground`(최소 3.07)는 여전히 수리 불가 제외 축이다 — 여유 0.1 미만이라 새 테마 추가 시 함께 본다. (c) 파괴적 버튼은 `statusIndicator.error` 를 배경으로 쓰는데 그 토큰은 이 표에서 전경으로도 수리된다 — 올바른 해법은 전용 `button.destructiveBackground` 토큰이고 §1.F 가 신규 토큰을 금지하므로 후속이다(§8.6 에 기록). (d) `bun run e2e`·`tauri dev` 는 배정상 미실행.
- **검토 수정 G-5·G-6·G-7·B-1·G-4 완료 (2026-09-15)** — **G-5**: `src/shared/lib/vsix-theme-import.ts` 의 `warningCount` 를 `countWarnings()` 로 분리하고 `stateDistinctnessErrors.length` 를 합산했다(safeDefault + ansiFallback + repairs + stateDistinctness). 실패 사유(`VsixThemeImportFailureReason`)로 올리지 않은 이유는 `convertVscodeTheme` 이 **수리를 돌린 뒤** 남은 것만 보고하기 때문 — 임포트 거부 신설 금지(d-40 §1-a)와 같은 선이다. `vsix-theme-import.test.ts` +1건: `terminal.background` 를 본문 전경색과 같게 두어 수리가 이동할 구간 자체를 없앤 픽스처에서, `terminal.selection`/`terminal.cursor` 를 붕괴시킨 변형과 안 시킨 변형의 `warningCount` 차이가 **`stateDistinctnessErrors` 증가분과 정확히 일치**함을 본다(공식 미러가 아닌 차분 검증이라 §1.A 쌍 표가 늘어도 깨지지 않는다). 합산을 되돌리면 `2 != 0` 으로 실패함을 확인. **G-6**: `src/shared/styles/theme-token-consumer.test.ts` 의 `isConsumed` 를 3축으로 재작성했다 — ① `global.css` 를 **줄 모양**(`--x: var(--y)`)으로 브릿지/칠하는 규칙으로 가르고 칠하는 쪽(`@layer base`·`@utility`)의 `var()` 만 인정, ② `--color-*` 키 ↔ 테마 변수 대응을 shadcn 별칭 2홉(`--color-primary` → `--primary` → `--taide-button-primary-background`)까지 풀어 **생성 유틸리티 클래스 실사용**(`bg-`·`text-`·`border-b-` 등 24종, 꼬리 경계 `(?![a-zA-Z0-9-])` 로 `bg-graph-lane1`⊄`bg-graph-lane12`)을 검사, ③ 런타임 조립 변수(`commit-graph.tsx` 의 `var(--taide-graph-lane${n})`)를 접두사로 인정. 테스트 4 → 6건(유틸리티 단독 소비 인정 1 + 브릿지만 있는 토큰 비소비 1 추가). **예외 등재 3 → 7**: 브릿지 줄만 보던 이전 검사가 소비로 세던 `graph.refTag`·`graph.refHead`(ref 배지를 종류 무관하게 `graph.refBranch` 로만 그림)·`popover.itemHover`(팝오버에 항목 목록 없음)·`input.focusBorder`(포커스 테두리가 전부 `app.focusBorder`)를 사유와 함께 추가하고 `docs/theme-system.md` §3.1 의 소비 정의 문단·예외 표를 같은 내용으로 갱신했다. **G-7**: `.gitignore` 에 `e2e/artifacts/`(`e2e/test-results/` 다음 줄). **B-1**: `docs/quality-assurance/2026-08-18-e2e-harness.md` §5.2 대상 테마 수 `40 = 38 + 2` → **`49 = 47 + 2`** + §1.D 반영 문구 정정, G-7 로 해소된 `.gitignore` 체크박스를 `[x]` 로. **G-4**: `docs/theme-system.md` §3.1 끝에 **§1.E 스크린샷 검수 항목** 블록 신설 — `dark:` 6곳은 죽은 분기가 아니라 `@custom-variant dark` + `theme-provider` 의 `data-appearance` 로 동작하던 **live 분기**였으므로 코드는 유지하되, **다크 테마 스크린샷에서 destructive 버튼 배경·테두리·hover / outline·ghost hover 배경 / `aria-invalid` 포커스 링 3항목을 육안 검수**한다고 명시했다(판정 기준: 라이트와 같아지는 방향이 맞는가). / **테스트**: `bun test` 전체 **2492 pass 0 fail**(247 파일 — 공유 worktree 라 총수는 T1/R 이 테스트를 더할수록 올라간다), `bun run typecheck` exit 0, `bun run lint` 0 error(기존 경고 11 = `useVirtualizer`), `bunx eslint` 만진 3 TS 0건, `bunx prettier --check` 만진 5파일(TS 3 + md 2) 통과. 만진 2 테스트 파일 단독 14 pass 0 fail. 첫 전체 실행에서 난 5건(번들 테마 구별성·대비 게이트)은 동시 작업 중이던 T1 의 §1.A/§1.B 데이터 정정분이라 범위 밖이었고 재시도에서 0 으로 해소됐다. Rust·테마 JSON·`convert.ts`·`state-distinctness.ts`·`contrast.ts` 미변경이라 cargo 계열 미실행. / **이탈**: ① G-6 이 지시한 "유틸리티 클래스 실사용" 축만 넣으면 `graph.lane2`~`lane12` 12토큰이 **거짓 미배선**으로 잡힌다(변수명이 런타임 조립이라 리터럴이 없다) → 조립 접두사 축을 함께 넣었다. ② 유틸리티 대응표를 만들려면 shadcn 별칭 2홉을 풀어야 한다 — 안 풀면 `input.*`·`button.*`·`list.*` 가 전부 유틸리티 없음으로 보인다. ③ 새로 드러난 4토큰은 **배선 대신 예외 등재**다. 배선은 §1.C 소관이고, ref 종류별 배지·팝오버 항목 목록·입력 전용 포커스 색은 전부 **없는 UI 를 발명**하는 일이라 `tooltip.*` 예외와 같은 판단을 적용했다. ④ 공유 파일은 `docs/theme-system.md` §3.1 만, `e2e-harness.md` 는 §5.2 의 2곳만 Edit 했다(타 에이전트 구역 미접촉, 양쪽 prettier 통과). / **미확인·후속**: (a) G-4 의 3항목은 **기록만** 했다 — 실제 판정은 §1.E 스크린샷 실행 후다. G-1 이 `button.tsx` 의 `text-white` 를 되돌리면 destructive 항목의 검수 대상 클래스가 바뀐다. (b) `graph.refTag`/`refHead` 는 그래프가 ref 종류를 구분하게 되는 순간, `popover.itemHover`·`input.focusBorder` 는 해당 UI 가 생기는 순간 예외 테스트가 **실패로** 알려준다(조용한 방치 불가). (c) 소비 판정의 "토큰 id 문자열" 축은 그대로 뒀다 — `xterm-theme.ts` 가 `terminal.foreground` 를 속성 접근으로 쓰기 때문에 따옴표 요구로 좁히면 4토큰이 거짓 미배선이 된다. 대신 JSDoc 산문이 우연히 매치하는 약한 축임은 남아 있다(`graph.lane1`).
- **검토 수정 R 완료 (2026-09-15)** — Rust 미러(`src-tauri/src/domain/theme/service.rs`)와 `docs/theme-system.md` §8.3 만 고쳤고 TS 는 한 줄도 건드리지 않았다. **A-1(형제 쌍)**: `StateDistinctnessPair` 에 `sibling: bool` 추가, `resolve_state_container` 를 TS `resolvePairSurfaces` 의 포트인 **`resolve_pair_surfaces`(→ `(container_hex, state_base_hex)`)** 로 교체, `state_distance(state, container_hex, state_base_hex)` 로 시그니처 확장 — 형제 5행(`listActiveVsHover`·`explorerSelectedVsHover`·`sidebarActiveVsHover`·`tabActiveVsInactive`·`switchCheckedVsUncheckedTrack`)은 state·container 를 각각 `surfaceKey` 위에 독립 합성한 뒤 비교한다. 비형제 경로는 `state_base_hex == container_hex` 라 기존 의미가 그대로다. **A-2(추가 쌍)**: `STATE_DISTINCTNESS_PAIRS` **37행 → 50행**(T1 기록의 "51행·신규 14" 는 오기 — 실제 신규는 13행. `comm` 으로 라벨 차집합을 전수 대조해 TS 단독 13 · Rust 단독 0 을 확인했다). 행 literal 은 TS 정본을 bun 으로 import 해 생성했으므로 순서·키·임계가 정본과 문자 단위로 같다. **G-2(그림자)**: `APP_SHADOW_MIN_ALPHA = 0.149` · `APP_SHADOW_KEY` 신설 + TS `shadowAlpha` 포트(`shadow_alpha` — 6자리는 불투명, 비hex 는 미측정) + `카탈로그_테마는_상태색이_바탕색과_구별된다` 에 컨테이너 없는 알파 축 추가, `light_colors()` `app.shadow` `#00000022`(0.133) → **`#00000026`** 1토큰 정정(`taide-dark` `#00000066` 은 통과). **G-3(④⑤)**: `COMPONENT_CONTRAST_PAIRS` **35행 → 36행**(`menuItemHoverText` 를 `editorWidgetLink` 뒤에) + `COMPONENT_CONTRAST_EXEMPTIONS` 미러 신설(`ayu-dark`·`ayu-light` 의 `menuItemHoverText`)과 컴포넌트 대비 린트의 예외 스킵. **⑥**: `SELECTION_FOREGROUND_CONTRAST_EXEMPTIONS` 에서 `rose-pine-dawn` 삭제 — 형제 규칙이 그 예외의 전제를 없앴다(빈 레지스트리로 유지해 역검증 테스트 배선은 남긴다). **②(드리프트)**: 키 포맷을 `label|state|container|surface|alt|sibling|minDeltaE` 로 확장 + `ts_entry_flag` 파서 신설, 상수 대조에 `APP_SHADOW_MIN_ALPHA` 추가(기존 2종 + `MIN_CONTRAST_RATIO` 포함 4종). **⑧(문서)**: §8.3 만 Edit — 미러 표의 행 수(50/36)·드리프트 키·신규 테스트 2행, "예외 등재는 두 린트 모두 0" 문구를 실제(구별성 0 / 컴포넌트 대비 2 / 선택 행 전경 빈 레지스트리)로 정정, 형제 규칙·그림자 축 절과 `taide-light` 그림자 1토큰 정정 절 추가. / **검증**: `cargo fmt --all -- --check` 통과 · `cargo clippy --workspace --all-targets -- -D warnings` 0건 · `cargo test --workspace` **1589 pass 0 fail**(1559+4+3+6+17 — T1 이 남긴 실패 3건 `상태색_구별성_쌍_표는_ts_정본과_일치한다`·`컴포넌트_대비_쌍_표는_ts_정본과_일치한다`·`선택_행_전경색_대비_예외_등재분은_실제로_최소_대비에_미달한다` 전부 해소, 카탈로그 49종 = 번들 47 + 빌트인 2 전수 통과) · `bun run typecheck` exit 0 · `bun run lint` 0 error(기존 경고 11 = `useVirtualizer`) · `bun test` **2498 pass 0 fail** · `bun test src/shared/lib/theme-convert` 99 pass · `bunx prettier --check docs/theme-system.md` 통과. **미러가 inert 하지 않음을 실행으로 확인**: ① 임시 프로브로 5종(ayu-dark·rose-pine-dawn·dracula·tokyo-night-light·palenight) × 전 축 **245개 ΔE 를 TS 계산치와 소수 4자리까지 대조 → 전건 일치**(프로브 제거 완료), ② `light_colors()` 를 `#00000022` 로 되돌리면 그림자 축이 실제로 실패함을 확인 후 복구. / **이탈**: ① 신규 Rust 테스트 2건 — `형제_쌍은_양쪽을_표면_위에_독립_합성해_비교한다`(쌍 표 대조는 "플래그를 읽고 무시하는" 미러를 통과시킨다. 같은 오버레이를 쓰는 두 상태로 형제 = ΔE 0 / 비형제 = 임계 통과를 양방향으로 고정), `컴포넌트_대비_예외_등재분은_실제로_최소_대비에_미달한다`(이 파일의 기존 예외 레지스트리 2종이 모두 역검증 테스트를 갖는 관례. 없으면 stale 예외가 조용히 린트를 끈다). ② `SELECTION_FOREGROUND_CONTRAST_EXEMPTIONS` 는 지우지 않고 **빈 슬라이스로 유지**했다(TS 쪽도 빈 `{}` 유지). ③ 그림자 축의 카탈로그 최소 여유는 +0.00002 로 다른 축보다 훨씬 좁지만 38/255 정확값이라 결정적이다(§8.3 에 명시). / **미확인·후속**: (a) 실제 렌더 미확인 — `taide-light` 그림자가 0.133 → 0.149 로 진해져 팝오버·다이얼로그 그림자와 모달 스크림이 §1.E 검수 대상이다. (b) `bun run e2e`·`tauri dev` 는 배정상 미실행. (c) T1 문장의 "51행/신규 14" 는 그의 구역이라 고치지 않았고 §8.3 에만 실측치 50 을 적었다.


- **메인 2차 검증 최종 (2026-09-15, 검토 수정 반영 후, taide-w4)** — `bun run verify` exit 0(bun test 2498/0 · cargo lib 1559 + 통합 30 · clippy 0 · prettier) · `bunx vite build` 0 · `bun run typecheck:e2e` 0. 커밋 6분할(data(theme)·feat(theme) 9종·feat(theme) 린트·feat(ui) 배선·test(e2e)·docs) → dev 위 rebase → dev ff → main ff. **§1.E 스크린샷 검수 1순위**: rose-pine-dawn·ayu 3종의 선택 행(8% 알파 → 불투명), app.shadow 알파 하한으로 새로 생긴 스크림 4종, `dark:` 제거로 바뀐 다크 테마 destructive/outline/ghost hover/aria-invalid 링, 밝은 테마 JND 경계값(github-light bracketMatch·vitesse-light focusBorder). 실행은 사용자 dev 기동 후.

## 4. 후속

- 스크린샷 매트릭스 실행·비전 검토 결과 환류. 고대비 타입. Tier B 라이선스 검증 후 추가.
