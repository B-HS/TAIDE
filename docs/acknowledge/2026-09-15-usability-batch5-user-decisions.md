# 사용성 배치 5 — 사용자 결정 (2026-09-15)

> 사용자 지시 13항목(항목 1~10 최초 + 11~13 추가)에 대한 결정 질문 16건의 회신. 계약(d-58~)은 이 문서를 전제로 작성한다.
> 조사 정본: `research/2026-09-15-batch5-research.md`(wf `wf_e9242362`·`wf_5e4b8e2c`).

## 0. 작업 방식 (이 배치부터 상시)

- **항상 Workflow + opus/sonnet.** 메인(Fable)은 오케스트레이팅·계약·2차 검증·소통 전담.
- **Agent(서브에이전트) 도구 절대 금지** — 세션 모델(Fable)이 상속되어 사용량 과다. 리서치·구현·검토·테스트 작성 전부 Workflow.
- 역할표 갱신(결정 16): 테스트 작성 = **sonnet·high**(이전 fable·medium). `docs/agent-operations.md` §1 반영.
- 커밋·푸시 **자동**(결정 15): `git config llm-rules.auto-commit true` / `llm-rules.auto-push true` 기록. 웨이브별 분할 커밋 → dev 푸시 → main ff. 릴리스는 별도 지시 시만.

## 1. 항목별 결정

| # | 항목 | 결정 |
|---|------|------|
| 1 | 단축키 범위 | ⌥⌘←/→ 탭 이동 + VS Code macOS 기본 파리티 감사로 부재 항목 추가. 확정 목록은 조사 결과로 계약에 명시 |
| 2 | ⌥⌘↑/↓ | 분할(pane) 간 포커스 이동 |
| 3 | 파일트리 키 세트 | VS Code macOS 파리티(Enter 이름 바꾸기·⌘↓ 열기·⌘⌫ 삭제·⌘C/⌘X/⌘V·⌘N 새 파일·⌘⇧N 새 폴더·⌥⌘R Finder 표시·⌥⌘C 경로 복사), 탐색기 포커스 한정. **Enter=열기 → 이름 바꾸기로 변경 승인.** 빈 공간 더블클릭 = 루트에 새 파일 초안 |
| 4 | 프로젝트 split(항목 3·12) | 사이드바 프로젝트 아이콘 드래그 → 상하좌우 분할, 각 영역 = 탐색기·에디터·터미널을 갖춘 완전한 프로젝트 셸(규모 XL, 설계 리서치 선행). **"프로젝트 묶음" = 이름 있는 그룹**(사이드바 접기/펼치기·그룹 열기 시 전부 열림·영속) |
| 5 | 알림 | 제목 `<프로젝트 표시명>`, 본문 `<에이전트> · <이벤트>: <세부>`. 클릭 시 해당 프로젝트 활성화+창 포커스(플러그인 지원 확인 후). 이벤트 범위 = 완료 + 차단(권한 요청·질문 대기) |
| 6 | 에이전트 정체 | opencode = sst/opencode, codex = OpenAI Codex CLI, pi = badlogic/pi-mono `pi`(npm `@mariozechner/pi-coding-agent`) **확정**. 감지 깊이 = Claude 와 같은 4신호(신원·인밴드 이벤트·다이얼로그 시그니처·실질 출력), 에이전트별 expect 실측 후 채택. CLI 미설치 시 **에이전트가 설치**(npm -g/brew), 로그인·키는 사용자 |
| 7 | Dock Recent | 최근 10개 + Clear Recent. 메뉴바 File > Open Recent 도 같은 데이터 |
| 8 | 퀵오픈 버그 | 발생 시 **토스트("찾을 수 없음")** 가 보임. 상황은 미상 → 조사 계측 계획대로 |
| 9 | Welcome | **Open Terminal 버튼**을 Welcome 에 추가해 터미널을 열 수 있게(사용자 정정: 단순 버튼 추가가 의도). 활성 프로젝트에 새 터미널, 프로젝트 없으면 비활성+힌트(메인 제안, 이의 없음) |
| 10 | Find | 사이드바 검색 패널 실시간(이전 실행 취소·이력은 Enter 시만·IME 조합 중 제외) + 결과 파일행 아이콘을 탐색기와 같은 registry. **디바운스는 설정에서 조정 가능**(기본 300ms) |
| 11 | + 버튼 | 드롭다운: 경로로 열기(입력 다이얼로그, ~ 확장·존재 검증) · Finder 로 열기 · 구분선 · 최근 프로젝트 |
| 12 | git 패널 | VS Code SCM 방식: 섹션별 드래그 리사이즈·접힘 시 헤더만·펼침 시 남은 공간 분배·크기 영속 |
| 13 | 테마 | ① 전 UI raw 색·폴백 토큰 전수 감사로 기존 31종 보정 + ② 미보유 인기 테마 10종 내외(MIT/Apache 계열, 라이트 비율 확보) 변환기로 추가 + 대비 가드 테스트 |
| 14 | 순서 | 웨이브 1 = 7·9·8·10·6·4·11(소형·독립, 계약 1건) → 2 = 1·2(키맵) → 3 = 5(에이전트) → 4 = 13(테마) → 5 = 3·12(프로젝트 split) |
| 15 | 커밋·푸시 | 자동(§0) |
| 16 | 모델 운용 | 테스트 작성 sonnet·high 로 갱신(§0) |

## 2. 조사 후 추가 결정 (2026-09-15, 회신 4건)

| # | 지점 | 결정 |
|---|------|------|
| 1 | 항목 6 Dock Recent | **불가 확정** — tauri 2.11.5·tao 0.35.3·muda 0.19.3 소스에 Dock 메뉴 진입점 없음(`applicationDockMenu:` 델리게이트 셀렉터 미등록·확장 지점 미노출, 메인이 cargo 레지스트리에서 재확인). **메뉴바 `File > Open Recent`(10개 + Clear Recent)로 대체**, Dock 은 알려진 제약으로 기록. 사용자: "지원을 안하면 별 수 없지" |
| 2 | 항목 4 알림 클릭 라우팅 | **제외** — tauri-plugin-notification 2.4.0 데스크톱은 title/body 만 전달하고 클릭·액션 리스너 커맨드 미등록. 문구 강화(프로젝트명·에이전트·경과·탭)만 + "입력 대기"를 "완료"와 다른 제목으로 분리. 권한 요청 vs 질문 구분(Rust 타입 확장)은 웨이브 3 |
| 3 | 항목 11 git 패널 | **C 안** — 그래프를 하단 별도 Panel 로 분리(드래그 리사이즈·접힘 시 헤더만·펼침 시 남은 공간 채움), 변경/스태시 섹션은 접힘만. 근본 원인 = `commit-graph.tsx:45` 하드코딩 320px 상한. **영속화 = Settings 승격**(접힘 + 그래프 높이, 재시작 유지) |
| 4 | 항목 7 퀵오픈 | 코드로 확정된 결함 전부 수정(랭킹 비결정성·NFC 정규화·pane 소실·심링크·무반응·진단 로그). 하드 제외 디렉토리 완화 = **A**(`.git`·`node_modules` 만 하드 제외) — 단 §2.1 참조. 보조 창 ⌘P = 안내 토스트, 창 스코프 팔레트는 웨이브 5 |

### 2.1 보류 — 하드 제외 디렉토리 완화 (계약 작성 중 발견)

`IGNORED_DIR_NAMES`(`src-tauri/src/constants.rs`)는 퀵오픈·내용 검색 walk 뿐 아니라 **파일 트리(`tree::service`, gitignore 미참조)·워처 `ScopedIdCache` 가지치기**에도 공유된다. 퀵오픈만 완화하면 트리가 숨기는 `dist/`·`target/` 파일이 퀵오픈에만 나타나고(autoReveal 불가), 전역 완화하면 gitignore 를 안 보는 트리·워처가 `target/`(Rust, 10만+ 파일)을 그대로 보이고 감시한다. 메인 재질문 후 확정(d-58 §1.G G5).

## 3. 웨이브 2~5 사전 결정 (2026-09-15, 회신 3건 + 미회신 5건은 추천안 적용)

| # | 지점 | 결정 |
|---|------|------|
| 1 | 에이전트(웨이브 3) | pi·gemini 는 이 Mac 에 미설치 — **"설치되어 있다고 가정하고 구현"**(1차 출처 문서·바이너리 strings 기반, 실측은 [미확인] 표기). opencode·codex 는 설치돼 있어 실측 탐침(expect pty 캡처) 가능. 나머지는 추천안: codex 를 HTTP 훅 → 인밴드 OSC 777 로 통일, `parse_title_glyph` 에이전트 파라미터화, 다이얼로그 시그니처는 영어 로케일 우선 |
| 2 | 테마(웨이브 4) | 사용자 질문 "토큰이 있어도 색이 배경과 겹치는 건 확인했나" → 정적 집계만 했고(app.background 와 동일값 접힘 수·SAFE_DEFAULT 동일 수·대비 7쌍), **실제 렌더는 미확인**. 웨이브 4 에 ① 전 fg/bg 쌍·인접 표면 구별성 정적 린트 확장 ② e2e 스크린샷 매트릭스(테마 × 주요 표면) + 비전 검토 ③ 데이터 정정 을 포함한다. 순서는 추천안(UI 배선 갭 → Tier A 형제 variant 9종) |
| 3 | 프로젝트 split(웨이브 5) | 추천안: 보조 창 완성(S) → 한 창 안 셸 슬롯 분할(L) 2단계, 그룹은 별도 `ProjectGroup` 엔티티, `active_project` 유지 + `shell_slots` 추가(무마이그레이션), 그룹 열기는 첫 멤버 즉시 + 나머지 백그라운드 큐 |
| 4 | 웨이브 2 5건(미회신 → 추천안) | G5 하드 제외 디렉토리 **현행 유지**(결정 §2 #4-A 철회) / ⌥⌘↑↓ 미배정(그룹 포커스는 ⌘K ⌘←→↑↓) / ⌘1~9 = N번째 그룹 포커스(키맵 엔트리 9개) / 탐색기 단축키 = 파일트리 로컬 핸들러 + 컨텍스트 메뉴 단축키 라벨 / 키바인딩 에디터 when 누락 결함 수정 포함. **사용자가 웨이브 2 착수 전 번복 가능** |

### 3.1 테마 "상태색 겹침" — 사용자 정정 + 메인 직접 집계 (2026-09-15)

사용자 의도 정정: "겹침" = **상태 표시 색이 바탕색과 같아 상태가 안 보이는 것**(에디터 드래그 선택 vs 에디터 배경, 설정 토글 켜짐 vs 카드 배경 등). 조사 T9 는 이 종류를 검사하지 않았다(선택 행·매치 강조 2쌍만). 메인이 번들 38종 JSON 을 직접 집계(`$palette` 해석, 동일 값 비교):

| 상태 쌍 | 동일값 테마 수 | 테마 |
|---------|:--:|------|
| 에디터 드래그 선택(`editor.selection`) = 에디터 배경 | 6 | vscode-dark-plus·light-plus·dark-modern·light-modern, visual-studio-cpp-dark·light |
| 토글 켜짐 트랙(`button.primaryBackground` = `--primary`) = 설정 카드 배경(`panel.background` = `--card`) | 5 | darcula, vscode-dark-plus·light-plus, visual-studio-cpp-dark·light |
| 토글 손잡이(`app.background`) = 켜짐 트랙 | 6 | 위 5 + vscode-tomorrow-night-blue |
| 터미널 선택 = 터미널 배경 | 21 | — |
| 비활성 선택 = 에디터 배경 | 17 | — |
| 활성 탭 = 탭바 배경 | 10 | — |
| 현재 줄 강조 = 에디터 배경 | 7 | — |
| 버튼 hover = 버튼 배경 | 8 | — |

→ d-61 은 **상태 vs 바탕 구별성 린트**(위 쌍 + hover 행·포커스 링·체크박스·배지·스크롤바 썸 등 컴포넌트 실사용 쌍 전수)를 최우선으로 신설하고 미달 테마 데이터를 원본 팔레트 안에서 정정한다. 토글 트랙은 `--input`(=`input.border`)·`--primary`·손잡이 `--background` 3자 구별이 조건.

## 4. 웨이브 병렬화 (2026-09-15, 회신 A)

- **웨이브 3(d-60) ∥ 웨이브 4(d-61) 를 별도 git worktree·브랜치에서 병렬** 구현·검토(파일 집합이 거의 겹치지 않음). 각각 완료 후 dev 에 순서대로 머지(로케일 JSON·MESSAGE_NAMESPACES·lib.rs 등록·bindings 재생성은 가산 충돌 → 머지 시 재생성으로 해소).
- **웨이브 5(d-62)** 는 설계 계약만 지금 작성, 구현은 3·4 머지 후(사이드바·셸 대수술이 4 의 UI 배선과 겹칠 여지).
- 웨이브 2(d-59)는 dev 워킹트리에서 진행 중 — 커밋 직후 worktree 2개 생성.

## 5. 릴리스 (2026-09-15, 사용자 지시)

- 웨이브 5(d-62) 완료·dev/main 반영 후 **v0.2.0 릴리스**: 버전 동기 3파일 + Cargo.lock · `release-notes/v0.2.0.md`(d-58~d-62 요약) · verify + vite build exit 0 → `chore(release)` 커밋 → dev 푸시 · main ff → 태그 `v0.2.0` → Release 런 완주 → draft(dmg + SHA256SUMS). 태그 숫자 4 금지 규칙(`2026-08-28-release-tag-no-digit-4.md`)에 저촉 없음. 이력은 `deployment.md` §9.
