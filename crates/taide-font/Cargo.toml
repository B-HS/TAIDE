[package]
name = "taide-font"
version = "0.1.0"
license = "MIT"
edition = "2021"
rust-version = "1.89"

[dependencies]
fontdb = { version = "0.24.0", default-features = false, features = ["fontconfig"] }
taide-model = { path = "../taide-model" }
