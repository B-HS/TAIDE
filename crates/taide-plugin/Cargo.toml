[package]
name = "taide-plugin"
version = "0.1.0"
license = "MIT"
edition = "2021"
rust-version = "1.89"

[dependencies]
parking_lot = "0.12"
serde_json = "1"
taide-infra = { path = "../taide-infra" }
taide-model = { path = "../taide-model" }
uuid = { version = "1", features = ["v4"] }
