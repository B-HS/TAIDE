[package]
name = "taide-layout"
version = "0.1.0"
license = "MIT"
edition = "2021"
rust-version = "1.89"

[dependencies]
log = "0.4"
taide-infra = { path = "../taide-infra" }
taide-model = { path = "../taide-model" }

[dev-dependencies]
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
