[package]
name = "taide-terminal"
version = "0.1.0"
license = "MIT"
edition = "2021"
rust-version = "1.89"

[dependencies]
taide-infra = { path = "../taide-infra" }
taide-model = { path = "../taide-model" }

[dev-dependencies]
uuid = { version = "1", features = ["v4"] }
