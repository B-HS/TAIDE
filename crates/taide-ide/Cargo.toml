[package]
name = "taide-ide"
version = "0.1.0"
license = "MIT"
edition = "2021"
rust-version = "1.89"

[dependencies]
taide-infra = { path = "../taide-infra" }
taide-layout = { path = "../taide-layout" }
taide-model = { path = "../taide-model" }
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
serde_json = "1"
