[package]
name = "taide-sync"
version = "0.1.0"
license = "MIT"
edition = "2021"
rust-version = "1.89"

[dependencies]
log = "0.4"
serde_json = "1"
taide-locale = { path = "../taide-locale" }
taide-model = { path = "../taide-model" }
taide-settings = { path = "../taide-settings" }
taide-theme = { path = "../taide-theme" }

[dev-dependencies]
uuid = { version = "1", features = ["v4"] }
